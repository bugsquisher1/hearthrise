# Hearthrise — Living Priority Board

**The single source of truth for what's done, in flight, queued, and decided.**
Maintained by the Coordinator: backlog items are added here consistently as they
arise (player reports, feature ideas, discoveries) so nothing is lost. The HTML
board artifact renders this file. Keep status, priority, order, and notes current.

Legend — **Status:** ✅ shipped · 🔧 in progress · 📋 spec'd (approved, not built) ·
🧊 backlog · ⛔ blocked · ⏸️ back-burner (deprioritized, not cancelled).
**Priority:** P0 (now) · P1 (soon) · P2 (this cycle) · P3 (nice-to-have).

_Last updated: 2026-09-03 (**b501 ASSEMBLING — the disappearing-rewards build**: forge/room build fix + server-side combat keys + hr_import_apply owned-slot entitlement, all 3 server surfaces LIVE+verified; blocked only on the combat-style check-B guard fix, test-only, in flight). **🔒 GOLD+GEMS ARMED — server-authority program COMPLETE.**_

> **✅ b501 SHIPPED LIVE (2026-09-03): the disappearing-rewards build — fixes 2 of the 3 player-reported reload-loss bugs.** Live at ?v=501 (index + build-info verified), assembled build boots clean (0 console errors). **Gates: full suite GREEN 1118/1118 in-page + 0 runtime errors; combat-style check-B guard fixed same build (engine verified correct; the red was introduced by increment 1's own RNG-desync, caught pre-ship — NOT a pre-existing red, see the corrected row below); import-apply applied+verified on prod; live-hash re-pinned.** ✅ **LIVE PLAY-GATE RUN (2026-09-04, prod, QA account, b501):** Forge/rooms fix VERIFIED — `upgradeProperty()` at tier 0 with 0/30 logs + 0/20 ore: server refused, tier stayed 0, gold stayed 1200, one refusal toast naming the material, no "built!" toast, and tier/gold persisted across reload (no phantom advance, no re-charge). Progression persisted exactly across reload: kills 72, attack xp 1900, hitpoints xp 1604, gold 1255 (cadence RPCs). Keys: no seal dropped in ~15 goblin kills (expected ~0.5) — the client-mint half is fixed by construction (server drop rows + guard + security), NOT witnessed as a live drop. **Visual gate at desktop READ: Home / Inventory / House / Combat all intact** (portraits sized in their cards — no b361 class); 922×423 pass could not be driven through the connected Chrome (window collapsed, outer 160×28) → running `tests/visual-qa.mjs` (headless, both sizes). Discord posted with HONEST wording (204). **The gate also MEASURED the beta-2 killer class — see the 🔴 P0 row below.** Minor: "Hero slot 4 — Checking…" afford display did not resolve on Home (possible stuck state; audit to confirm). **① Forge/room build** (player report #1): rooms took resources then stayed unbuilt on reload — the "optimistic-apply, swallowed-rejection" class (tier++ before the server confirmed, `.catch(()=>{})` that couldn't fire). Killed across 4 sites (upgradeProperty/upgradeRoom/buildPlot/buyBankSpaceGold); advance only on a server OK, speak the refusal, keep the resources. MERGED 10de43fd; tests UNLOCK-VERDICT-1/PROP-REFUSE-1/PROP-OK-1. **② Combat keys vanishing** (player report #2 — bone keys/goblin seals): the client-authored key wrapper minted keys the reload rebuild (BLOB_RETIRED) then dropped. Folded all 6 keys into the SERVER drop table (`monsters.js` → `combat-sim.rollDropTable`) at the frozen rates — now granted+recorded by the server on live AND away kills (also fixes the offline-key P2). MERGED 3e9aabab; **edge redeployed FIRST (payload ac7481dd, GET-verified live)**; guard `dungeon-key-drops`. **③ hr_import_apply owned-slot entitlement** (the §8/ceremony interaction): the sealed cutover ceremony impersonates the player and calls the §8-gated creator, so importing an owned slot>0 hero failed — now grants `character_slot:N` for catalogued slots 1..4 before the impersonated create. **APPLIED to prod** (splice@10458, seal@8835<splice<create@13074, ACL owner-only); live-hash re-pinned (5dca0a61→eae7ce38); guard `cutover-import`. Both server surfaces **security GO, no conditions**. **③ dungeon scrip** (player report #3) is a MULTI-INCREMENT program, not in b501 — see the dedicated row below.

> **🔴 P0 PROCESS — CI ON MAIN HAS BEEN RED SINCE 2026-08-29 (proven 2026-09-04 from GitHub Actions: 40 runs, ZERO green; every completed run `failure`, the rest cancelled/superseded). The in-page "Smoke suite (headless)" step PASSES; the CI-ONLY steps fail — so every release since b488 was gated on the weaker local `node tests/run-smoke.mjs` while CI failed unwatched.** Failing steps (reproduced locally on main): **live-hash-drift** (unexplained live≠replay on `hr_record_rejection` 7b62…/469b… and `hr_state_of` — the baseline `why` is the `--write` REVIEW placeholder dated 08-31, never filled; I carried it forward in the b501 re-pin) · **restore-census** (2 problems — a new table unclassified, likely `hr_hero_slots`) · **conservation-fuzz** (harness: `hr_create_character` slot 1 `slot_not_owned` — the hero-slot §8 create-gate ripple; market-v2 was fixed for this, this CI-only guard was not) · **claim-intent** (same ripple, likely) · **rpc-resolution** (6 RPCs changed resolution; baseline never re-pinned after the hero-slot/C6 migrations) · **raid-card-copy** (5 copy assertions; feature fenced behind the clan-launch gate). **This is the true answer to "how is this still happening": the board's row 213 named "local run-smoke strictly weaker than CI (12 guards CI-only)" and nobody looked at CI.** Lane IN FLIGHT (reliability-engineer, `fix/ci-green-lane`): fix each red CORRECTLY (never loosen), resolve the live≠replay drift with a real body diff, re-baseline rpc-resolution only with reviewed justification, and add `run-smoke --ci` (derived from smoke.yml with a parity guard) so the local ship gate EQUALS CI. **New rule (CLAUDE.md to be amended): a release is green only when BOTH the local `--ci` run AND the GitHub run on the release commit are green.**

> **🔴 P1 LIVE — "ROOMS STILL NOT BUILT" (Paione, post-b501, 2026-09-04) = RESIDUE-AHEAD PROPERTY DEADLOCK. Fix IN FLIGHT (systems-engineer, `fix/property-rung-server-truth`).** Server truth: `property:homestead=1` only (no farmstead) → rung 1. Residue `client_state.homestead={tier:2}` → his client thinks he owns the Fieldworth Farmstead. `hr_rejections`: `unlock_buy:room.forge.1` → `prereq_property_tier {have:1,need:2}` ×10 today (also 09-01, 08-31; `worker_hire.2` the same since 08-27; another player on `room.workshop.1` ×11). Mechanism: `src/net/property-record.js` heals `G.homestead.tier` RAISE-ONLY (max(residue, server) — main.js ~L280), built in b466 for residue-BEHIND; the pre-b501 optimistic `tier++` left residue AHEAD and the heal preserves it forever → House shows the Farmstead, Forge/worker look buildable, "Upgrade Property" offers the Manor, every click refused; the refusal sentence (legacy.js:11060 "build the Fieldworth Farmstead first") contradicts the screen. **Class: a residue field gating a server-owned capability** (flagged 08-31). b501 stopped new optimistic advances but cannot repair residue already wrong. **Fix = server rung is truth BOTH directions** (set, not max, when the record is loaded; residue = display cache written back; every capability gate — rooms/workers/plots/the upgrade offer — reads the record; gate closed while unloaded; kill the homestead.js:302 ratchet; re-rule the raise-only "grandfathered" tests). Tests PROP-TRUTH-1/2, PROP-REFUSE-2 + a derived residue-gates-capability census guard. Client-only, no data patch — Paione self-heals on next load and is offered the Farmstead. **Gate lesson:** my b501 play-gate ran on the QA account (residue = server) and the refusal branch only — it could not see residue-ahead; record-backed play-gates need a residue-AHEAD fixture.

> **🔴 P0 — ATTENDED-FIGHT LOOT/FOOD UNDER-CREDITING (the beta-2 killer class, MEASURED LIVE 2026-09-04).** A 3-minute attended goblin fight on prod (QA account, b501) produced this ledger: `set_activity:combat:goblin` → 4× `combat/xp_credit` (498 xp) → ONE `combat/accrue` on stop with gold_in 55, **qty_in 16** → `set_activity:idle`. The client predicted +11 goblin_ear +15 bones (26 drops) and consumed 7 cooked_shrimp; after reload: ears +7, bones +9 (=16, exactly the accrue row) and the 7 shrimp RETURNED. Kills/XP/gold persisted exactly because they ride the cadence credit RPCs (`hr_credit_kills`, `hr_credit_combat_xp`, b484–b486); loot + food are settled ONLY by `computeAccrual` (hr-accrue accrual.js), which re-simulates the attended window as an UNATTENDED span → realizes ~62% of the shown drops and ate nothing → the MERGE reconcile retires the prediction → the bag snaps DOWN. The settle loop is visibility-gated (`decideSettle` → 'hidden'), so an alt-tabbed player settles once at stop/unload (`settleBeforeIntent`) instead of every 90s — same under-realization per span. **This is the root of player report #2 ("items gained in combat disappear") and of every food-return report; b501 removed only the client-minted-key half, and any client-SHOWN drop — keys included — can still be retired by the settle.** It also bounds dungeon loot/scrip (incr 2/3 ride settlement). Fix is SERVER-SIDE attended-loot crediting (per-kill loot credit RPC capped by physical-max, or an attended flag the settle prices as attended, or span-sim fidelity) — never a client Math.max; economy surface → security-gated. Missing test: an attended-fight fixture asserting server-credited drops == client-predicted within RNG tolerance and consumed food debited. Root-cause + options in flight: phase-1 audit workflow `wf_35814a2e-eee` dimension `attended-settle-gap`. Probe gotcha (do not report as a bug): `decideSettle(getSettleState())` answers 'switch-off' because the raw state lacks the derived `enabled`; the loop's own snapshot has `enabled: isServerAccrualEnabled()` = true.

> **🔧 DUNGEON SCRIP (player report #3) — server-of-record program, in progress (2026-09-03).** Scrip was client-authored (`awardDungeonScrip`→bag item), so BLOB_RETIRED's reload-rebuild dropped it — same class as forge/keys. Fix = a multi-increment program because arming the earn/read is COUPLED to the quartermaster SPEND (arming the read without fixing the spend leaves the shop debiting a bag entry that no longer holds the balance). **Increment 2 (scrip→`player_state.dungeon_scrip` column projected by `hr_state_of` + `hr_dungeon_settle` RPC): BUILT + verified (guard green, 7/7 mutations) + SECURITY GO-WITH-CONDITIONS for a DARK ship** (branch `worktree-agent-a5887d3f5a7897ba4` @ 5235d76d). Ships DORMANT (`DUNGEON_SETTLE_ARM_ENABLED=false`, byte-identical to today until armed). Security conditions: **(1 op, mine at apply)** re-pin live-hash for `hr_state_of` + `hr_assert_grant_hygiene`; **(2 design, gates the ARM — game-designer ruling IN FLIGHT)** BoP-vs-tradeable on 8 progression-unlock loot ids (blueprints + `farm_deed` — "buy past a content gate on the market") + whether key drop rate is a sufficient loot throttle; **(3 increment 3 must fix)** the QM spend journals negative scrip `kind='dungeon'`, so the earn daily-cap sum must exclude spends (else earn >5000/day by spending first); **(4 non-blocking)** add guard cases (q=0 cap-bypass, intent_mismatch, concurrency). **Increment 3 (`quartermaster_buy` spend + client arm coordination: migrate `G.inventory.dungeon_scrip`→`G.dungeonScrip`, wire `configureDungeonSettle` at boot, flip the flag): NOT built.** Report #3 is FIXED only when 2+3 ship armed together (→ b502), after b501's play-gate + the designer ruling + increment-3 security review. Apply order (at ship): catalogue → scrip → settle, edge redeploy REQUIRED.

> **✅ b499 SHIPPED LIVE (2026-08-31): the consumption build.** Ammo consumed per swing (the mechanic was ruled+built in b357 and never called — Paione's arrow report closed; edge deployed FIRST at c17cd6bd per security A1-A4; opt-out flag coupled to the hr-ammo-dry UI marker BY GUARD) · server-confirmed hatches + honest refusals + ask-once (load-bearing under C6: one refused repeating trigger = one alarm, ever) · auto-eat settings sync (toggle dormant → the armed-conditions build is queued) · C6 severity applied · streak vocabulary ruling (LIVE-verified: "Played · 1 day running" on the welcome card) · activity-intent contract resurrected (3 layers of rot, one real engine literal) · the 5 in-page reds attributed+fixed. **Gates: suite 1103/1103 ALL GREEN incl. edge-payload; visual gate read; live play-gate (reload-settle through the new engine, zero console errors, nothing confiscated); Discord posted.**
> **🎯 THE BETA-3 PLAN (2026-08-31, from four measured audits — full visual: artifact e2da9f60 "The War Table").** Funnel measured live: 49 signed up → 38 confirmed (−11 at the EMAIL WALL, 9 of them post-delivery-fix = the confirm-then-sign-in-again dead end) → 34 characters (−4 to the since-fixed silent boot bug) → 16 active week 1 → 3 now. Invite codes failed ~3:1 (13 refused_unknown vs 5 redeemed — hand-retyped from DMs). And the biggest week-1 bounce-causer measured through the real engine: **a new player's first overnight fight banks 2.0% of the night, then dies** (lvl-40+iron+500 food is the first config that survives; a gathering night banks 100%). Targets: keys >60% (was 17%) · signup→character >90% (was 69%) · D1→D2 >60% · zero c_incident week 1 · same-day report replies.
> **PHASES:** ① Week-one-worth-returning-to: first-night gathering-rescue prompt (critical, no engine change) · auto-eat completion (in flight) · the armor overhaul BOTH ends (bronze does nothing at lvl 1 + saturation kills week 2 — one engine change, two play-gates) · bounty-strip honesty (hours) · weekly capability filter (hours — the 2026-08-23 daily fix never reached weeklies) · sequence the week (renown/collection as the spine) · runecrafting (soft). ② The door: confirm-link lands you IN signed-in + resend button (73% of the loss) · invite LINKS with attribution + founder cosmetic · standing funnel view · stranger play-gate · CAPTCHA dormant. ③ Ceremony: F3/F4 one-liners → wipe migration built+rehearsed (census-derived, live-hash before/after) → restore drill (Tyler ~90s) → PITR on (Tyler's own trigger fires here) → wave-scale load verdict (connections fail TOTAL at 60) → fresh-account playthrough (test account, Tyler) → secrets+MCP+Sentry armed. ④ Launch week: T-48h announce → personal staggered keys (10+10, 72h re-mint) → receipts changelog (bug→build that killed it) → day-1 presence + 4-surface monitoring on the severity SLAs → pre-decided rollback ladder (two nevers: no second wipe, no ad-hoc SQL) → 48h retro vs targets. **No date until every gate is green; the date slips, not the bar.**
> **NEXT WAVE (from the designer's rulings, in order):** ① auto-eat cheapest-sufficient fallback + ARM the toggle with its 3 conditions (systems, dual-runtime, edge redeploy) ② **the defence-saturation P1** (leather obsoletes plate from tier 4; monster accuracy floors at 10% vs 67 def while ladders deliver 281 — its own engine change + security pass + play-gate; NO defence item ships before it) ③ the ward line (8 icons — art freeze needs a Tyler spend ruling OR emoji-free procedural placeholders) ④ ammo_carry migration on its A3 trigger.

> **✅ b498 SHIPPED LIVE (2026-08-31): the honesty build.** Daily-reward sheet reads the CLAIM streak the server pays on (two server streaks existed; b475 wired the display to the wrong one — the b497 gate watched it promise 2,000+5 and pay 500) · companion catalogue reseed APPLIED (82/21, the 23514 landmine disarmed) + grant hardening trio C1-C3 APPLIED (narrow-by-message subtransaction with the egg-consume inside; whelp req_item enforced — measured free; claimed_source) · live-hash drift guard extended to the prosrc pin idiom, **tracked set 37→40** (hr_companion_grant/clan_upkeep_pay/hr_unlock_buy were pinned by migrations and watched by NOTHING), 40/40 green vs replay AND prod. Gates: suite 1095/1095 · visual gate both states/sizes · live gate on prod (sheet shows the true 1-day streak) · edge 2fc78db7 byte-exact · Discord posted. **Queued from the reviews:** C6 severity file (unknown_unlock→c_incident, missing_req_item→c_escalating, patterned on intent-mismatch-escalates — own anchored patch on hr_record_rejection) · client surfacing of a refused hatch (Systems) · dragon_egg must become server-held IN THE SAME BUILD it becomes obtainable (recorded in the hardening header).

> **✅ b497 SHIPPED LIVE (2026-08-30→31 UTC): the balance build.** Auto-Eat I free at creation + granted to all 35 existing characters (entitlement-only, verified live: trait present, switch stays the player's) · bounty difficulty pricing un-inverted (forged residual 1.53×→1.147×; accept envelope = the client contract, `_serverContract` live-proven) · worker output anchored to the paced rate (−37.5% passive, edge redeployed byte-exact 9a0c1567) · goal-pool gold retuned to ruled values + the two save-reach heals · cloth tier inputs · cron de-collision. **Every gate ran:** assembled suite 1094/1094; headless visual gate READ both sizes; 4 migrations dry-run→committed at 23:45 UTC (15 min before day-boundary, C4 satisfied); LIVE play-gate on prod: reload-survival (contract 14/17 + trait came back), 17 kills credited at honest cadence, turn-in paid the scaled 270g+5marks exactly, daily claimed+stamped. Discord posted. **Found by the gate:** daily-reward modal overpromises after a missed streak day (P2, §8 below — display-only, server paid correctly).

> **🔧 2026-08-30 — b497 RULED-BALANCE WAVE (assembling).** Lanes: **worker-anchor MERGED** (f49dd121 — worker output anchors to the PACED player rate, crew-of-six 1.651→1.032 equivalents; edge redeploy at ship; player-facing note: passive worker income drops ~37.5% to intended rate) · **lane A REVIEWED** (Auto-Eat I free at creation + bounty difficulty pricing {easy .90/normal 1.00/hard 1.20/elite 1.50}; branch cb97ae17, suite 1091/1091; **security verdict:** bounty pair GO-WITH-CONDITIONS [F2: client must adopt the accept envelope's `required`+tier+rewards — dead-bounty class otherwise, ~39% of pre-deploy tier-1 hard slots]; auto-eat §1 **F1 WITHDRAWN → GO-WITH-CONDITIONS** [security verified the systems disproof independently and retracted: the server debits attended meals at every visible settle — no free food, the gate was right, the demanded "fix" would have been double-debit item destruction]; conditions: C1 merge guard branch 2087eba3 same-build ✅cleared · C2 on 66709733: F2/F5/F6 ✅CLEARED, one new F7 (refusal path dead code — guard-order + real-shape fixture, routed to builder) · C3 rewrite the migration's false-premise header lines (routed, same round); §grant-existing **GO — same txn as §1**; security signs the lane after running the auto-eat guard against the final tree; measured upside: forged-difficulty residual 1.53×→**1.147×**, abusable ceiling −16.7% every tier) · **eat-intent gate: FIX REFUSED, PREMISE DISPROVEN** (systems @ 2087eba3: the server DOES debit attended meals — the 90s settle loop is visible-only (`decideSettle` returns hidden⇒no-settle), measured 2 meals/60s with negative delta.items on the shipped engine; dropping the gate = double debit = item loss, mutation-proven 1090/1091. Branch ships comment-only + 2 new guards incl. hold-preservation block 2b. **F1 re-ruling with security IN FLIGHT** — both the lane-A header and the original F1 chain inherited the same false "server eats only away" premise) · **lane C MERGED** (8cd4cbf8 — ruled retune across all three goal systems [hr_goal_rewards rows + hr_claim_daily CASE + hr_claim_quest CASE — the "one migration" assumption was wrong and lane C caught it]; plant DOWN 3@150; cloth gains tier-plank input; PLUS two save-blob heals the retune exposed: frozen quest defs + frozen daily slate = spent-unpaid dailies, RETUNE-1/2 mutation-proven; **security ratification of the 2,200→2,800/char-day bound move IN FLIGHT**; edge redeploy payload 4301435c)
> **ASSEMBLED SUITE RUNNING** on the quiet machine (all lanes in: worker anchor + census + slot-shift + lane A + guards + drift re-baseline + lane C). Cloth-bolt ladder question queued for the designer post-wave (lane C mirrored plate/leather with tier plank at half slot weight — no new ids, doesn't strand current crafters). · **restore-census MERGED** (2ce0d03d — 90/90 tables DR-classified, new `operator_tunable` class, census in CI credential-free with `!cancelled()`, mutate 6/6 + selftest 9/9 verified post-merge on main).
> **Durability, MEASURED (2026-08-30):** Pro live, 8 daily backups, walg on, **PITR OFF** (24h loss window; enabling = paid add-on → **Tyler ruling, budget freeze**); restore drill still never run (free). 20,832 restore-only rows exist nowhere but the DB + backups. Handed off: `hr_unlocks` rebuild=82 vs prod=65 (17 companion unlocks prod never got — Backend to rule apply-vs-repo-fix); migration self-checks leave 3 pinned ledger orphans (wrap in subtransactions, Backend, non-blocking); `market_listings` DR class revisit under market v2.
> **Schema-drift audit CLOSED (2026-08-30):** 456/456 classified, zero missing migrations; prod↔repo byte-identical on 8/9 categories after the two closures I applied (hr_crops not-null + xp-prune scheduled, both independently verified by the auditor); CI masking (551 commits) fixed with `!cancelled()`. Residuals queued §8: cron 04:35 slot collision (fold into next migration batch) + baseline-diff prose/schema split. **Restore drill still never run — Tyler-only, the one cutover-blocker.**
> **Ship plan when lanes land:** integrate serially → quiet assembled suite → apply lane migrations (dry-run→commit; fold cron-slot shift in) → edge redeploy (accrual/workers vendored) → bump 497 → push → live play-gates → changelog + Discord.

> **✅ b493 SHIPPED (same day): the trust build.** 7 fixes + ftue guard, all gated: definitive suite 1082/1082 (after QA class-killed the 19-failure assembly break: stale ?v= specifiers loading six modules TWICE — bump script hardened + 200ms cache-buster guard added), headless visual gate PASS (tally verified both sizes), TWO prod migrations applied (kill-daily credit + kill-goal-XP→hitpoints, both dry-run+§gate-proven on prod, C6 checks green), security sign-offs on every new surface incl. the K10 re-rule (kill-goal XP bound accepted: forged counter is a GATE never a MULTIPLIER; catalogue pinned BY VALUE). LIVE play-gates: tier heal verified (Paione's exact repair: Tier 2/6, 4 plots, Workers 1/1, Aldric visible), bounty-free daily stamping + trailing drain (4→6 with zero kills) + settle-absorbed no-double-count receipt all watched on prod. Discord posted w/ Paione CTA.
> **~~STILL STAGED~~ → RENOWN FAUCET IS LIVE + POST-HOC SIGNED (2026-08-31):** the full security pass found it applied to prod on/before 2026-08-30 06:56Z while the repo still said staged (stale deployment record — the pass's own P1). Deployed bytes PROVEN byte-identical to the reviewed file; property measured on prod (forged credit = exactly 0 renown, 11/11 rows reconcile); forensic window captured before the log pruned (~25 renown total residue, 0 boss credits ever, 0 gems — wipe removes it). Conditions routed: live-hash drift check (the class-kill), F2 mutation `expect`s, F7 wording fixed in the design doc. Routed P3s: hr_claim_goal idem intent_mismatch (Backend); rewardSummaryHTML raw skill id (Art, visual-gated pass); smoke-test.js:1930 tier-ratchet order-dependency (QA). Class-sweeps recommended: residue-fields-that-gate-capability audit + idle-boot-hydration census (three occurrences now).
> **🎯 2026-08-29 STUDIO-QUALITY PUSH (post-restart; Tyler: "you know what needs done").** Six parallel workstreams dispatched off the b491 live playthrough:
> - ~~NEW P1 — silent boot-hydration failure~~ **CLOSED (b493, closure recorded 2026-08-31 launch-readiness review):** the boot veil + await-auth + fail-LOUD shipped in b493 and its live play-gate verified a clean hydrated boot; the b492 dead-token race + stalled-ensure latch are both guarded. RE-VERIFY on wipe day (Phase 3 fresh-account pass) — a default-render on wipe day is indistinguishable from a botched wipe.
> - ~~NEW P1 — combat-XP claim-vs-prediction leak~~ **CLOSED (b493, closure recorded 2026-08-31):** the prod journal FALSIFIED the loss hypothesis — no XP was ever lost; the display double-counted then snapped down. b487's fold-back + b493's display-retire fix closed it; the b499 display-prediction guard pins the monotone display across every envelope shape.
> - **Kill-daily credit**: prior staging RESCUED from an uncommitted stale worktree → branch `staged/kill-daily-credit` (a83e131b; 884-line migration + 696-line test, REVIEW-ONLY header). Backend agent reconciling it against b491 + completing. Security review required before ship.
> - **Load-test (scalable-state exit criterion #4)**: reliability-engineer measuring hr-accrue capacity + beta-wave verdict (zero-spend constraints).
> - **Session Tally visual defect** (P2, fight screen): bottom strip self-overlapping/unstyled at short viewports (the flagged `.fs-sess-best` P3 is worse live) — art-director fixing with both-size screenshots.
> - **Worktree audit COMPLETE**: 69 worktrees + 191 stale branches classified; ~11 GB reclaimable; kill-daily was the ONE dirty-valuable (rescued); `debt/css-tokens-pass-1`'s zero-visual-change commit cherry-picked to `hold/css-tokens-pass-1` (ships with next integration + visual gate). Deletion sweep HELD until the current wave lands (live agents own worktrees).
> - **b491 live-verified by playthrough**: food-stays-eaten ✓ (real settle), anti-cheat cap ✓ (clamps 30× drive, never honest play), daily claim ✓, style persistence ✓, maxHP=HP-level ✓, level_up daily "Confirming…" resolves ✓.

> **⚡ 2026-08-29 BUG-WAVE DAY (Tyler: "do whatever it takes to fix the bugs today"). Shipped LIVE: b487 → b489 + 2 prod hotfixes, all gated (true-exit smoke + headless visual gate) + Discord-announced with test CTAs.**
> - **b487:** mid-fight XP-revert CLASS-KILL (pending fold-back over the absolute envelope — closes ALL credit-lag windows, the bug reported ~10×) · Session Tally (§10 #2) · Buy Back render extraction · 3 integrity fixes. **Clan P1 exploit CLOSED IN PROD** (clan_contribute gold-debit + feast inventory-debit + buy_listing dropped; security GO; migration applied + verified).
> - **🚑 PROD HOTFIX (the big one):** `hr_rpc_gate` had lost FIVE buckets to a stale-template replacement (2026-08-30-bounty-kill-credit) — `hr_trait_buy`, `hr_set_style`, `hr_claim_goal`, `hr_goal_state`, `client_state_put` all refused 100% as "rate_limited" with ZERO telemetry. This ONE bug was: Auto-Eat unbuyable · styles snapping to default · quest claims dead · ALL residue saves frozen ("Reconnecting…", reload resets, homestead builds refused via stale prereq tier). Restored live + `tests/rpc-gate-bucket-guard.mjs` (negative-proven) kills the drift class.
> - **b488:** cross-skill XP labels (quarry tiles say "Mining XP" — Paione's 260-rubble/0-Stonemason confusion; XP was paying Mining by design, the tile never said) · grant-hygiene baseline reconciled (honest note: b487's gate run was EXIT=1 on this baseline lag, masked by a wrapper echo — process fixed, true-exit now read from inside the log).
> - **b489:** BOUNTY BOARD fix wave (6 defects, 7 mutation-proven tests): unsettleable Proof/Weapon/Streak contracts bricked the board from BH5 (boot-heal withdraws free) · 0-marks turn-in (server paid, client never refetched) · dead shop buttons now honest "Unavailable" · daily free reroll restored + price-drift fixed + prepaid token respected · phantom bonus toast removed.
> - **Routed decisions from the wave (owners):** bounty types Proof/Weapon/Streak need server verbs before re-enable (Systems; board is 100%-cull until then — content reduction, deliberate) · `BOUNTY_SHOP` needs an `hr_bounty_spend` sibling and `goldBoost`/`cosmeticCloak` are read by NOTHING even if bought (do not re-enable that shelf first) · Easy-vs-Normal bounty pricing inversion (Easy can need more kills for less pay) + Unlocks strip lighting gated types (game-designer) · clan RPC idempotency keys before clans un-gate (Backend).
> - Still in flight: food-restock-in-combat fix (4 reports) · kill-counter/pickaxe/tool-tab batch (+ verify style/claims fixed by the hotfix). Land as b490.

> **🌙 OVERNIGHT AUTONOMOUS RUN (2026-08-27), for Tyler on wake:**
> Ran the team on a gate-independent batch. **3 changes assembled onto local main (b487 candidate), smoke 1050/1050 green — NOT pushed.** The push is held on the ONE thing I can't do without your signed-in QA tab: the **release visual gate + play gate** (the Browser pane won't composite in this session and the game is online-only, so no screenshots and no live signed-in run were possible). One gate run in the connected Chrome ships it.
> - **Session Tally + Watch Report** (board §10 #2) — Fight-screen + away welcome-card hunt summary (XP/h, gold/h, drops/h, net, per-monster kills, session bests). **Settled server receipts ONLY**, fails closed on any non-`serverAuthoritative` number — no projection. Read-only, no server/migration. New `src/features/session-tally.js`. *(art-director flagged one P3 polish: `.fs-sess-best`/`.fs-sess-foes` have no dedicated CSS → render identical to body text. Address during the gate run.)*
> - **Integrity hardening (3 P3s from the QA sweep)** — (1) combat-XP forced-flush in-flight race that could under-credit ATTENDED XP at settle (the b485/b486 class — forced flush now awaits the in-flight promise instead of early-returning null); (2) `state_too_large` residue-overflow now surfaced (telemetry + one-time warn) instead of silently stopping ALL residue persistence forever; (3) arm-homing guard hardened to catch `Object.assign(G,…)`/alias writes (the blind spot that stranded `bestiary`). Each with a regression test.
> - **Render extraction #10** — vendor Buy Back modal → `src/render/buyback.js`, pure refactor, byte-identical DOM (git-confirmed).
> - **QA sweep:** no P0/P1/P2 found; persistence sweep clean (83 fields homed), style→skill XP routing sound, save/load invariants intact. The three P3s above were the only findings — all fixed.
> - **Bounty-shop depth proposal** (board §3, you asked 2026-08-20) — game-designer delivered a full spec: repeatable Hunter's Cache chest ladder + rare-drop moment + flat catalogue, priced against earn rate. Mostly needs the marks server-column armed + a server-rolled grant verb (NOT client-authored); only the flat one-time unlocks are pure-data. **One decision for you: chests = efficiency-discount vs. lottery-at-par.**
> - **Housekeeping flag:** ~50 stale agent worktrees are littering `.claude/worktrees/` + `R:\the game\` from prior sessions; several hold unmerged branches. I left them ALL untouched (won't risk losing work unattended) — worth a cleanup pass when you're awake.

_Prior overnight note (2026-08-18 → 08-19):_

> **🌙 OVERNIGHT AUTONOMOUS RUN (2026-08-18 night → 2026-08-19), for Tyler on wake:**
> Ran the team on the structure-first program while you slept. **16 builds shipped (b388–b403), all gated (deterministic smoke + visual gate), all live.**
> - **Bug batch (b388–b393):** farming/cooking-XP root fix, worker rebalance, Stable tab + drop tables, offline-clarity UX, enchant discoverability + fire→ember wording.
> - **b394 gather-XP rebalance** (your Mithril report) — engine deployed via your paste + client synced, edge-guard green.
> - **Render-layer extraction #3–7** (b395/b397/b398/b400/b401): Achievements, Bestiary, Equipment Bonuses, level-up toast, **equipment paper-doll (164 lines)** → `src/render/*`, all pure-refactor + byte-identical.
> - **b399 test-determinism:** killed the b227 flake — **suite is now deterministic, 937/937**. The ratchet is trustworthy.
> - **b402 dead-code retirement** (~55 lines, airtight-proven) · **b403** negative gear-bonus display fix · **b404** objectives popout (final leaf).
> - **Capstone QA PASS:** the assembled b404 build (all 17 builds stacked) verified sound — 8 render modules load+coexist, zero overflow at 1440×900 + 922×423, no collisions on the Profile screen where they converge, 224 rapid tab-switches 0 errors, 938/938. One PRE-EXISTING finding (not a regression, designer call): the Active Effects card is force-hidden on Home by the `#panel-profile:has(#hd-root) > .card` rule — retire the dormant card or resurface it.
>
> **⛔ NEEDS YOUR DECISION / IN-LOOP (not done autonomously — on purpose):**
> 1. **Gold-arming: NO-GO** (§2). Verification proved arming breaks the economy (fail-closes every unstamped balance → 885/934). Real blocker = a **direction call**: build a stamped-envelope test harness (test-only, must be reviewed) OR a Security ruling on `balanceOf` fallback. Verified diff on branch `fix/gold-arm-staged`.
> 2. **Render controllers + showTab tap-registry (Tier-2):** the next render phase, b361-class core-nav-wiring — **execution-ready plan scoped** (registry-first, then buildTibiaDoll✓→shop→farm→inventory→profile→combat, one-per-commit). Held for your in-loop session.
> 3. **CSS→tokens:** the big theme debt — **execution-ready plan scoped.** Key finding: the token *architecture* is already mature; the debt is un-converted component *literals*, and the easy "Class A" (literal already = a token's cozy value) ones are nearly exhausted. Most remaining are "Class B" — foreign colors (teal remnants, status reds) whose conversion **mints a token + shifts Hearthlight = a design decision no test can judge.** So it's structurally in-loop: ~9 screen-batches (order: homestead/clan shakedown → inventory → combat → buttons → …), <20 new tokens, each with a cozy-identical guard test + your per-screen coherence review. Not unattended-safe.
> 4. **Engine (hr-accrue) deploys:** still your one-command paste (harness blocks me). Nothing engine-side is pending right now.

> **LOCKED DIRECTION (Tyler, 2026-08-18): STRUCTURE-FIRST. PAUSE ALL NEW FEATURES
> until the game is in a SCALABLE STATE. Beta pushed to NEXT WEEK (~2026-08-25).**
> Supersedes the 2026-08-17 "full list before beta." Build for the end-state
> ([[build-for-the-end-state]]: large-scale multiplayer semi-idle), clean code is a
> must. Only server-authority + the structural/architecture work (§9) proceed;
> bug-fixes allowed; NO new content/features until the exit criteria below hold.
>
> **SCALABLE-STATE EXIT CRITERIA (all must hold to resume features):**
> 1. Server-authority record flips ARMED + security-signed (gold + inventory un-forgeable). §2. — ✅ **GOLD+GEMS ARMED LIVE b421 (2026-08-20).** Inventory flip is REAL-LAUNCH (not beta) by expert verdict; b376 merge-mitigation is the correct beta posture. Currency authority = DONE.
> 2. Render layer extracted from `legacy.js` → `src/render/*` far enough that UI changes are safe. §9.
> 3. Core screens on CSS tokens (no hardcoded colors on shipped surfaces). §9.
> 4. `hr-accrue` load-tested at target concurrency. §9.

## 1a · WHAT SHIPPED (the 2026-08-17 push, now closed) + WHAT'S PAUSED

| Item | Status | Pri | Notes |
|---|---|---|---|
| Sell fix (b377) · Clan gate (b378) · Elements/enchanting (b379) | ✅ | — | All shipped live. Enchant's full logged-in round-trip still to confirm on QA account. |
| Server-auth safety machinery (b380 inventory · b381 gold · b382 baseline) | ✅ dormant | — | The flips' safety layers, shipped dormant. See §2 for arm runway. |
| Beta bug batch — farming-66 / cooking-XP-reset (b388) · workers OP (b389) · Stable tab + monster drops (b390) | ✅ | — | b388: client-only skills (farming/cooking) no longer stomped downward on reconcile (skill-authority.js carve-out) — the real "Farming locked at 66 / cooked food vanished" root cause. b389: worker crew was 3.12× an active player 24/7 free — eff 25%→10%, steeper hire costs. b390: Stable tab re-added to nav + legacy block-32 dup deleted; monster cards show full drop table. Remaining from batch: offline-XP *clarity* (b391, in progress) + Mithril report (awaiting Tyler resend). |
| Offline-clarity UX (b391) | ✅ | — | The communication half of the offline-confusion complaints (bug half fixed in b388): never-return-to-silence card, proactive 12h-cap row, attributed return payoff. Visual-gate PASS, live. |
| Enchant clarity (b392) | ✅ | — | Tyler's "super confusing / I don't see an enchant option." Root cause: enchant lived only on the Combat loadout, not the Inventory tab the game directs players to. Now mounted under the Inventory paper-doll + empty-state picker teaches the essence→rune path. Visual gate caught a mobile clip (withdrawn as a harness false-positive), PASS, live. |
| Mithril mining report | ⛔ awaiting Tyler | — | Report got cut off ("Mithril requires 60 mining, gives 37 exp, takes 10.1s… Build:"). Need the resend to know the actual complaint (yield? time? XP?). |
| Quest-board rework | ⏸️ PAUSED | — | Design DONE, build PAUSED until scalable-state. Consolidates 4 quest systems → one board; unblocks DAILY_COUNTERS. Resume after §9. |
| Kindling / Beacon events (Muster) | ⏸️ PAUSED | — | Approved design; build PAUSED until scalable-state. |

## 1 · LAUNCH READINESS (beta → next week; after scalable-state)

| Item | Status | Pri | Notes |
|---|---|---|---|
| Dedicated test account | ⛔ | **P0** | Tyler action (Coordinator **cannot create accounts**). Unblocks the team playthrough + enchant round-trip verify. |
| Full-systems team playthrough + BALANCE audit | 🧊 | **P0** | After scalable-state: team runs every system on the test account, finds bugs + tunes balance. Then open keys. |
| Wire rooms + banner tiers | 🔧 | P2 | Generated & on disk; visible-seam cleanup (fits the render/CSS track). |
| Backups / restore DRILL | 🧊 | **P0** | HARD blocker for arming BOTH record flips. Needs a rehearsed run (runbook exists: docs/design/restore-runbook.md). |
| **Beta-morning bug wave (b461)** | 🔧 | **P0** | 2026-08-23, live players: (1) quest-modal claims silently dead under arm → `hr_claim_goal` server credit + honest toasts; (2) worker_hire/farm_land/bank offers wiped by the b459 unlock regen → **re-applied live**; (3) stale impossible daily slates → heal-on-load; (4) residue-save listener stacking per token refresh → 60/min rate gate → once-latch; (5) mobile "stats reset to Lv1" — server rows intact, client display; boot-probe telemetry added, needs device screenshot. |
| **Runecrafting coherence** | 🔧 | **P1** | Tyler 2026-08-23: "doesn't make sense." Verified: enchant runes are CRAFTING recipes, Runecrafting's runes are ammo nothing spends (E1 unbuilt), no level-1 on-ramp (needs Stonemason 8), three rune vocabularies. Game-designer ruling+build in worktree: Runecrafting owns rune binding, level-1 on-ramp, honest purpose for the ammo ladder, unified naming. |
| Ammo consumption in the fight (E1) | 🧊 | P2 | The designed consumable loop's gate (consumable-economy.md §E1): arrows/runes/whetstones spent per swing, fail-soft ×0.25 when dry. Touches the SHA-pinned combat-sim (repack + redeploy + AWAY-1 parity). Not a drive-by; schedule after beta stabilises. |

**Decision pending Tyler:** wipe timing (confirmed: wipe before new players). See §7.

---

## 2 · SERVER-AUTHORITY PROGRAM (the integrity foundation — post-launch)

_Backend-architect plan delivered 2026-08-17 (review-ready, in security review). Key rulings folded in below._

| Item | Status | Pri | Notes |
|---|---|---|---|
| **Gold + gems → SERVER_OF_RECORD** | ✅ **ARMED LIVE (b421, 2026-08-20)** | — | **DONE — the server-authority program is complete.** Tyler pressed the trigger 2026-08-20 after the full deps program (path B: build all 6 reward deps + farming, then flip clean = zero regression). Shipped b412–b420 (muster/raid in-RPC credit · bestiary/collection/streak/renown tracking · daily/quest/collection/renown/bounty claim RPCs · bounty board+marks · companion pet-perk model · server-owned farming · companion un-gate). The flip itself = 2-line client-only uncomment (record.js SERVER_OF_RECORD gold/gems), NO migration/edge (server has owned gold since cutover). Gated GO on all three: **armed suite 960/960 deterministic** (income-regression gone — every earned-gold source server-paid), **security GO** (no forged value crosses to another player's economy/ranking), **release visual gate PASS** (boot fail-closed em-dash + disabled spends, no NaN/crash, both viewports). Read-path harness `stampBalanceLikeLoad` drives the real applyRecord path. Non-blocking residuals: unwired gem sinks (unlockSlot) show em-dash-pending until next hr_load (honest, beta-wipe moots); companion non-gold procs (refundIngredients/doubleDrop) mint INVENTORY ungated — safe until INVENTORY arms (separate flip), then extend the rollProc defer. |
| Inventory-absolute flip — **REAL-LAUNCH, not beta** | 🧊 | P1 | Expert verdict: CANNOT be made safe pre-beta. Needs every live bag-writer server-side (Model A: gather/craft/combat-drop via the accrual window — multi-day) + a rehearsed restore drill + (under amnesty) a wipe first, or absolute-apply irreversibly deletes freshly-crafted items. **b376 merge-mitigation IS the correct beta posture** (no data loss, dupe remains). Do NOT rush-arm before beta. |
| **Inventory flip → arm "server owns the bag"** — THE SINGLE ACTIVE TRACK | 🔧 | **P0** | Sole priority (Tyler, 2026-08-17). Step 1 (audit) + **Step 2 (ownership predicate + carve-out) SHIPPED DORMANT b380** — `serverOwnedItem` partition + guards, `isInventoryAbsolute()` false in prod so behaviour is unchanged. **Security review: GO-WITH-CONDITIONS.** **KEY REFRAME (verified):** the cross-player forgery boundary for ITEMS is ALREADY held today by the server market/apply RPCs reading `player_inventory` under lock — a forged item quantity can't be listed/sold/traded to another player *now*. The three residuals (forged excluded items, overlap items magic_essence/wheat, companion doubleDrop) are LOCAL-display/data-loss only, NONE cross-player. **Arm conditions:** (1) complete server inventory baseline [the real blocker — live craft/drop chains settle completely+in-order], (2) rebuild the partition against a loaded `window.DUNGEONS` at the arm site + guard test, (3) completeness guards in release gate, (4) equip flip armed, (5) drift soak. Then restore drill → wipe → arm. |
| Clan pass 2 | 🔧 **fix staged 2026-08-27** | **P1** | **CONFIRMED LIVE (security sweep 2026-08-27):** `clan_contribute` = free 10M/day mint into shared treasury + own `contributed` ranking (no gold debit); `clan_feast_deposit` = free feast meter (no inventory debit); legacy `buy_listing` RPC = latent griefing landmine (moves no value; inert only by an incidental dropped-column mismatch — one migration re-adds it → "destroy any seller's escrow for free"). Edge engine (hr_market_buy, combat-XP, kills) verified SOUND + conserving. **Fix package staged + SECURITY GO** (branch `fix/clan-economy-sinks`, commit 6cf7a374): gold-debit clan_contribute, inventory-debit feast (server-derived heal from new `hr_feast_foods` catalogue), revoke+drop buy_listing + remove client fallback. Conservation guards 15/15 green (real PGlite). Security re-review = GO, no new hole. **RECONCILED 2026-08-31:** the migration WAS applied (b487 wave — 'Clan P1 exploit CLOSED IN PROD… migration applied + verified'; census counts hr_feast_foods=28 live). Remaining: Tyler's design confirm that clan contribution is a GOLD SINK, before clans un-gate. Non-blocking follow-up: add idempotency keys to the clan RPCs before clans un-gate (self-harm-bounded today, clans hard-gated off). |
| Phase 3 ledger accumulator | 🧊 | P2 | Before real player growth (per-write journalling at scale). |
| Leaderboards off server tables | ✅ | — | Shipped (b374). Last cross-player ranking forgery closed. |
| Equip / market / clan-deposit authority | ✅ | — | Shipped. Forged values can't cross to another player's economy. |

---

## 3 · FEATURE ROADMAP (the original ~15-point list; superseded mid-week by the integrity program)

| # | Item | Status | Pri | Notes |
|---|---|---|---|---|
| 7 | Monster rework (Tibia 11 classes) | ✅ | — | Folded 111→108, painted portraits. |
| — | Combat rework (War Table + Fight) | ✅ | — | Shipped b365. |
| — | Items / itemization + Review Book | ✅ | — | Review book shipped; item waves wired. |
| — | Hunt band fairness | ✅ | — | Applied to prod. |
| 13 | Auto-eat in bounty shop | ✅ | — | 100-mark unlock; honesty fixed b373. |
| 14 | Build-button-to-top + QoL | ✅ | — | + level-up celebration, requirement inspector, etc. |
| 10 | Muster → Kindling/Beacon events + donation pools + weekly blessing vote | 📋 | P2 | Approved, NOT built (only name echoes exist in code). Weekly reset Sunday night; vote closes 1 day prior. |
| — | Bestiary Charms (PROG-01) | 📋 | P2 | Approved, NOT built. |
| — | Diaries (PROG-02), PROG-03, PROG-08 | 📋 | P2 | Approved, NOT built. |
| 12 | Elements / enchanting | 🧊 | P2 | Gated on monster rework (done) → now next-eligible. Steal from RS/Melvor. |
| — | Completion Log · Quest Board rework · supply-projection UI · world-boss blessing | 🧊 | P3 | From the batch-2 build sequence; mostly unbuilt. |
| 6 | Practice / familiarity tiers | ⏸️ | — | Back-burner. Spec in progression-depth.md Phase 2. |
| 11 | Buy-offers return | ⏸️ | — | Back-burner. |
| — | **Bulk / quantity buy from shop** | 🧊 | P2 | Paione (2026-08-20): "could we buy stuff in quantity? 300 seeds, 50k iron arrows." Add a quantity param to the buy flow. CHEAPER to do correctly now that every buy goes through a server verb — add the qty arg server-side rather than bolting on a client loop. Slot after the arm. |
| — | **Marks/bounty-shop chest contents + pricing** | 📋 **PROPOSAL DELIVERED (2026-08-27)** — awaits Tyler decision | P2 | Fingerguns (2026-08-20): wants ideas for what mark-purchasable chests contain (ammo/materials/gold/deeds) + per-item cost, once everything else is unlocked. Bounty shop is intentionally CHEAP for beta (wipes between releases). Delegated to game-designer for a proposal. Paione hit everything at bounty lvl 20 — shop needs more depth/sinks past the current ceiling. |
| — | Workers gather Stonemason rocks | ⏸️ paused | P2 | Paione + Tyler (2026-08-18): workers already gather wood/ore/fish; stone is a raw material too, so not gathering it is a gap from when Stonemason shipped. Tyler: "yes we should make sure the worker can gather rocks." Small gap-fill — good EARLY pickup once the feature-pause lifts. Files: src/features/workers.js, src/data/stonecraft.js. |
| — | Worker passive-XP trickle (design Q) | 🧊 discuss | P3 | Paione: workers give ~0.1% of normal XP. Tyler: "we need to discuss the exp portion." Nice retention hook BUT passive XP changes progression pacing — game-designer sizes how much / which skills / does it stack (100 workers × % balloons). Not a gut number. Discuss when the pause lifts. |

---

## 4 · ART PIPELINE

| Item | Status | Pri | Notes |
|---|---|---|---|
| Combat/skills/bounty backgrounds (12) | ✅ | — | Shipped b375, painterly, per monster class. |
| Home banner repaint | ✅ | — | Shipped b374 (dawn splash). |
| Rooms (8) + banner tiers (6) | 🔧 | P1 | Generated via API, on disk, queued to wire. |
| Re-roll 2 rejected backgrounds (demon, board_planks) | 🧊 | P3 | Fall back cleanly meanwhile. |
| Re-roll room interiors to matched painterly style | 🧊 | P3 | Interiors came out more storybook-outlined than landscapes. Tyler's call after seeing them wired. |
| **Full icon re-do to quality bar (512 items + 111 monsters)** | 🧊 | P2 | The big future art investment. NOT a quick swap (transparent, 32–64px, ~$25–40, multi-day). Post-ship. |

---

## 5 · GAME MODES (after base game proven)

| Item | Status | Pri | Notes |
|---|---|---|---|
| Ironman · Group Ironman · Hardcore Ironman · Hardcore (normal) · Hardcore Group Ironman | 🧊 | future | Needs server-authority to enforce (Ironman = server trade restriction; Hardcore = server-attested death). Per-account server flag at creation. Strong retention lever. |

---

## 6 · DEFERRED DESIGN SPECS (ready to build when slotted)

| Item | Status | Pri | Notes |
|---|---|---|---|
| Super-rare "fabled" drops (reveal + clan broadcast + shareable card) | 📋 | P2 | ~2-day Phase 1, plumbing 80% there. Behind integrity work. `docs/design/super-rare-drops.md`. |
| Layered-additive homestead art | 🧊 | P3 | The clan-castle-style build-up; richer than tier-swap. |
| Enchant v2 — permanent per-weapon binding + expensive wipe rune | 🧊 | P2 | Tyler direction (2026-08-17): v1 enchant is ephemeral (clears on any weapon swap, re-applied for 1 rune). Make it PERMANENT per-weapon (each weapon remembers its element, survives swaps) and require an EXPENSIVE "wipe" rune to change it — for commitment/weight + a rune/gold economy sink. Needs per-weapon-instance tracking (v1 deferred it). Wipe price is the key tuning knob (meaningful vs. punishing). Decide permanence + wipe cost in the enchant balance pass at the launch gate; build post server-authority. |
| Enchant v2 — magic as the FLEXIBLE style (dual-element) | 🧊 | P2 | Tyler direction (2026-08-17): today magic enchants identically to melee (one element, +15% vs weak). Give the three styles distinct identities: melee/ranged commit to their one bound element; MAGIC can channel a *different* element per fight, covering two weaknesses = versatility as its identity. NOTE: the current build has no per-cast element selection (magic is just a combat style), so this is a NEW magic subsystem, not a tune. Decide alongside enchant permanence in the enchant/magic-identity design pass; post server-authority. |

---

## 6b · FULL CODEBASE AUDIT (2026-09-02, triggered by 3 dungeon/rooms reload-loss reports)

_Six read-only audit dimensions + a systems root-cause lane. THE ROOT CAUSE of all three reports (forge unbuilds, dungeon keys vanish, scrip zeroes): **dungeon & homestead-room rewards are client-authored with NO server-settlement path** — a reload loads the server's authoritative inventory, which never received them → gone. The forge is "armed read, unwired write"; keys/scrip are "client-minted, never written."_

**⚠ CORRECTION recorded (Coordinator, live-verified 2026-09-02):** the audit's headline "inventory-absolute is LIVE in prod, deleting items" is **NOT true on real devices** — measured on the live signed-in QA tab: `isInventoryAbsolute()`=false, `isEnvelopeAbsolute()`=false, `maybeAutoArm()`=false, drift 0, merge kill-switch unset. Inventory is MERGE on production; absolute-delete is a node-theoretical arm (the agent forced `baselineCompleteSeen` + a synthetic `inventory_complete` envelope), NOT a live behavior. No panic-disarm needed. The loss is reload-not-persisted, not in-session deletion.

| Finding | Sev | Class? | Disposition |
|---|---|---|---|
| **Dungeon reward economy is client-authored, no server settlement** (scrip, all 6 keys, loot, signature weapons, farm_deeds, blueprints — lost on reload AND inert in-session: sinks validate vs server inventory that never receives them) | **P1** | CLASS | **THE FIX the reports need: a server dungeon-reward settlement intent** (dungeon_run/quartermaster_buy → hr_apply → player_inventory). Backend + security. Systems lane root-causing now; backend dispatch on its return. |
| **Forge/rooms: armed read, unwired write** — `ROOMS_RECORD_ARM_ENABLED=true` reads server `room:<id>` rows, but building a room never writes them; resources debited server-side, room state has no server home | **P1** | instance (+ cooking-kitchen sibling on same path) | Systems lane; needs the room-build server write (spec for backend). Fix must cover ALL room ids via `upgradeRoom`, not just Forge. |
| **Value/comment drift on ~10 inventory comments + 7 arm flags** (value `=true`, comment says DORMANT/REVERTED/false-in-prod) — the exact class that caused the b498 marks misdiagnosis AND made the Coordinator give Tyler a wrong answer on this very bug | **P1** | CLASS | Pure-comment sweep + a guard that fails the build if an `_ARM_ENABLED=true`/`_SERVER_BACKED=true`/`BLOB_RETIRED=true` const's trailing comment contains DORMANT/REVERTED/false-in-prod. Safe, do soon. |
| **arm-homing-guard is blind to write-persistence** — verifies READ-homing only; a homed-read/unwired-write field (rooms) passes green while the earn never reaches the server | P2 | CLASS (the guard's own hole) | Add a complementary WRITE-path assertion for every armed SERVER_OF_RECORD field + server-owned inventory lane. |
| **clientMayWriteRecordField('inventory') returns TRUE under arm** — every inventory-mint gate relying on it is a no-op; absolute-delete guarded ONLY by the 2-entry flipArmBlockers registry | P1 | CLASS (mis-modeled protection) | Make `clientMayMintOwnable(id)` real (consult serverOwnedItem + isInventoryAbsolute), or make flipArmBlockers the sole contract + a build census. Latent today (merge), lands when absolute arms. |
| **reconcileWorkers wipes the crew on a lean/empty `workers:[]`** — the KD420 farm race, unguarded for workers AND codified by a smoke test | P2 | instance of lean-envelope wholesale-replace | Mirror reconcileFarm's `opts.authoritative`: a non-authoritative empty/shrinking array must not clear a non-empty crew. |
| reconcileFarm still wholesale-replaces on a NON-empty lean envelope (KD420 only closed the all-empty subcase) | P3 | same class | Reconcile per-plot-index; don't drop a plot the envelope merely omits. |
| Dungeon keys don't drop from away/offline combat (gate item for a whole pillar only accrues while the tab is open — breaks offline==online) | P2 | away-parity gap | Fold the key-drop table into the server accrual reward path. Same server dungeon workstream. |
| Dungeon cooldown decorative (manual/scavenger ignore it AND pay MORE than auto; not server-enforced — b288 only nominally closed) | P2 | ordering hazard | Server-enforce per-instance cooldown on BOTH run modes when the dungeon verb lands. |
| Hearth Token bond has two divergent homes (server column client never reads + client item server never validates) | P2 | latent (0 tokens live) | Pick ONE home: `player_state.hearth_tokens` as SERVER_OF_RECORD, spend via a verb, delete the G.inventory item. Blocks monetization. |
| **Hero-slot merge rippled into 4 red tests/guards — suite red, NO new live bug** | 🔧 **P1 (suite red, staged fixes blocked)** | The §8 create-gate (correct — closed the free-mint) rippled into tests/guards/ceremony not updated after that migration (only caught now on a full-suite run — the process lesson below). PRECISE breakdown, each verified: **(1) cutover-import (R0/R1/R2/R5) = REAL:** §8 blocks `hr_import_apply` (the sealed ceremony CALLS the gated `hr_create_character`) from recreating a slot>0 char. Fix (backend + security): hr_import_apply grants the `character_slot:N` entitlement before recreating an OWNED slot. Latent — real-wipe is fresh slot-0, no import — but holds the suite. **(2) rpc-gate-bucket-guard = GUARD FALSE-POSITIVE (live correct, verified `has_bucket:true`):** the guard reads only the "last full definition" and can't see hero-slot's programmatically-SPLICED bucket. Teach the guard the splice idiom. **(3) C6 severity = GUARD FALSE-POSITIVE:** last-patcher analysis tripped by hero-slot's §0 signature reference + rpc-gate splice; C6 is applied+correct live. Teach the guard. **(4) VOICE-2b (in-page) = TEST needs update:** hero-slot client rewiring changed `slotRows()` afford/shortBy/canBuy shape; the live b465 shortfall display is CORRECT (verified "Needs 373 more gems" on the live account) — update the test to the new semantics. Do NOT revert §8. All 4 land through reviewed passes as the API stabilizes (it's 529-overloaded, killing worker agents). |
| Combat-style check B — GUARD BUG (engine correct), FIXED in b501 | ✅ | **TWO corrections, stated honestly. (1)** An earlier board note said check B was standalone-only/green — false: it IS in run-smoke's `combatStyleGuard()` and sets exitCode. **(2)** I then over-corrected to "shipped RED through b497–b500 / process gap" — also FALSE, and I'm the one who wrote it before verifying. Ground truth (qa-engineer execution + my file diff): check B was **GREEN through b500**; b501's increment 1 added drop rows to `monsters.js`, and **each drop row consumes one RNG draw per kill**, desyncing the seeded fight enough to break check B's fragile 2% tolerance. So the red appeared **today when I merged increment 1**, run-smoke caught it **pre-ship** (CI runs `node tests/run-smoke.mjs` on every push; the guard did its job), and it's fixed in the SAME build. No red ever shipped; no process gap. **Root cause:** check B asserted styled totals equal within 2%, assuming speedMod is the only throughput lever — but accuracy/damage/defense mods also change kills-per-window, and the compared styles differ in damageMod/defenseMod. Engine verified CORRECT (per-event route invariance Σ(style.xp)=1; full-engine equality over 200 saturated-fixture combos; defenseMod read by nothing). **Fix (test-only, `50f0ebdd`, in b501):** throughput-invariant checks (catalogue/per-event route magnitude + saturated-fixture equality), both mutation-proven, all 6 --selftest mutations still RED. |
| PROCESS: run the FULL node-guard suite after any SQL/migration merge, not just the touched guard | 🧊 | P2 | The hero-slot §8 create-gate (correct) silently reddened `market-v2` for ~3 days because after applying it I re-ran only live-hash, not run-smoke — market-v2's wash-trade leg minted a 2nd character the gate now blocks. A migration that changes an RPC's semantics can break any guard that replays the chain. Rule: after an SQL merge, run-smoke (or at least the PGlite-replay guards) before declaring green. Reinforces the audit's "run-smoke weaker than CI" finding. |
| **Test health:** gold-intents.mjs RED + hidden; worker-accrual/character-bootstrap/enchant-intent guards run NOWHERE; leaderboard-lockdown rotted; local run-smoke strictly weaker than CI (12 guards CI-only); no client→server→reload round-trip test for ANY record field; no dungeon-reward persistence test; 4 vacuous mobile-gated in-page tests; gold-intent-shape + raid-claim-exploit dead | P1-P3 | CLASS (wired-into-nothing + coverage gaps) | Test-cleanup + creation lane: wire the orphans, delete the dead, add the round-trip + dungeon-persistence guards. This is Tyler's "prune + add smoke tests" ask. |
| Client renown diverges from server (forgeable self-only residue); rested-charge spend is armed-read/unwired-write (latent) | P3 | instances | Board; renown = the hr_renown_state projection follow-up; rested = move spend to a verb before enabling potency. |

## 7 · DECISIONS PENDING TYLER

| Decision | Default acting-on | Notes |
|---|---|---|
| Wipe vs. amnesty at cutover | amnesty | Repo says amnesty (2026-08-15). Recommend ONE wipe right before widening invite, gated on record-flip + currency authority + a restore drill. |
| Stand up the test account | — | Highest-leverage launch-prep enabler. |
| Room-interior style re-roll | — | After seeing rooms wired. |
| **Enable PITR** (paid add-on) | **DEFERRED (Tyler, 2026-08-31)** | Ruled: not until the game is in a better spot — active players went quiet after the beta-2 launch, so the 24h loss window currently protects ~nothing, and the spend buys ~nothing. **Re-open trigger: before the next beta wave invites go out** (protecting a fresh cohort's first week is exactly what PITR is for). Daily backups continue meanwhile. |
| **Run the restore drill** (free) | recommend NOW | 8 daily backups exist; none ever opened. Runbook: docs/design/restore-runbook.md. At 34 MB it will never be cheaper. The census banner nags every run until `restore_drill.last_executed` is stamped. |
| Add 2 repo secrets for edge-quota-watch | — | `SUPABASE_ACCESS_TOKEN` (from `~/.supabase-token`) + `DISCORD_ALERT_WEBHOOK` (from `~/.hearthrise/changelog-webhook`) in GitHub → Settings → Secrets → Actions. Workflow is committed and INERT until then. |
| Re-auth the Supabase MCP connector | — | Dropped 2026-08-30; specialists lose `execute_sql` until re-authorized from an interactive session (`/mcp`). Coordinator's curl path unaffected. |

---

## 8 · KNOWN RESIDUALS / SMALL BUGS

| Item | Status | Pri | Notes |
|---|---|---|---|
| Craft material dupe | documented | P2 | Accepted pre-wipe trade (dupe > data loss). Real fix = inventory authority (§2). |
| 32px requirement chips below 44px tap target | 🧊 | P3 | Art-director handoff. |
| P3 polish backlog (various from audits) | 🧊 | P3 | In `.claude/coordination/DISCOVERIES.md`. |
| **Crafted arrows/ammo not consumed in combat** | ✅ **SHIPPED LIVE b499** (2026-08-31, security GO A1-A4) | — | Root: the whole mechanic was ruled (consumable-economy.md) and built (b357 src/core/ammo.js) but `simulateTick` never called it — deliberately deferred as design item E1 and forgotten. Now wired per-swing (ammoPerShot float = any rate is a data edit), AWAY-1 preserved ctx-blind, pending-consume hold client-side, dry = fail-soft ×0.25 never a stall. Wiring surfaced 3 defects (melee never spent; latent fail-soft restatement; the opt-out cliff — `AMMO_EMPTY_SLOT_IS_DRY=false` until an empty-quiver indicator ships, Art). SHIP ORDER: edge deploys BEFORE the client (809dfeb9) or holds restock. Deferred owed migration: `player_state.ammo_carry` (whetstones under-charge 0.02/swing attended-only until then; spec in HANDOFFS.md — needs the hr_apply carry-validation block). |
| ~~`tests/activity-intent.mjs` RED + orphaned~~ | ✅ fixed + wired (1796ba58, b499) | — | Resurrected: 3 layers of fixture rot fixed per each control's own prescription + one real engine literal corrected (SKIP.NOTHING); spawned by run-smoke so it can never orphan again. 22 groups green. |
| `tools/pack-edge.mjs` specifier scan is comment-blind | 🧊 | P3 | Prose containing an import-shaped phrase in a vendored file fails the deploy (caught the ammo lane). Teach the scan to skip comments. |
| A farm test leaks an async toast into the suite | 🧊 | P3 | Reds lane (2026-08-31): "Could not plant — try again" arrived on the `kill` notice channel during two unrelated tests — some farm test fires a fire-and-forget that toasts after its own teardown. Notice-counting tests are now subject-scoped (hardened), but the leaker should be found and awaited. |
| Renown board: prod returns `available:false` → Throne chip withdrawn | documented | P3 | Measured live by the reds lane: `{ok:true, board:'renown', available:false}` is prod's genuine answer (renown was removed from leaderboard_ranked as no_server_source, 2026-08-18). The module-global UNAVAILABLE latch now has a test seam; the PLAYER-facing question — should the Throne chip exist while the board is server-sourced-nothing — is a designer call when renown boards return. |
| Shields exist but unobtainable | 🧊 | P2 | Paione (2026-08-20): shield items are defined but have no drop/craft/shop source. Fingerguns confirmed: "created; but no way to obtain at the moment." Pure data-row fix (add a source) — content pass. |
| Cron slot collision `35 4 * * *` | 🧊 | P3 | Drift audit (2026-08-30): `hr-combat-xp-credit-prune` shares 04:35 with `hr-rejections-prune` — two DELETEs at once on Micro. Pre-existing practice (`*/5` and `7 * * * *` also collide). Fold a slot shift (e.g. 04:33) into the next migration batch; not worth its own apply. |
| Drift-baseline diff unusable as review surface | 🧊 | P3 | Drift audit (2026-08-30): a commentary-only change re-renders `tests/schema-drift.baseline.json` as a 3,316-line whole-file diff — a real 2-line inventory change would hide in it. Fix: split `known_production_delta` (prose) into its own file so schema diffs and prose diffs can't mask each other. |
| **WIPE-DAY TRIPWIRES** (security F3+F4, accepted residuals with re-open triggers) | 🧊 | **P1 at ceremony** | The moment `hearthrise.import_reopen` is set or the wipe-day ceremony is scheduled, BOTH become blocking: (F3) restore real `player_progress` import verification via the `insert…returning (xmax=0)` counting shape — §1b left the ceremony with ZERO progress verification; (F4) add the derived clamp in `hr_import_apply` §(l): `v_ae_pct := least(v_ae_pct, hr_auto_eat_max_pct(hr_auto_eat_tier(p_user, v_slot)))` — else a forged blob imports tier-II auto-eat (pct 100) without the 100-Mark sink. Both one-liners-ish, both from the 2026-08-30 lane-A review. |
| `hr_unlocks` seed divergence → **companion catalogue DESTROYED by wholesale re-apply** | 🔧 **fixed-staged** (7da7bfa6; security pass running) | **P1** (upgraded — live landmine) | Forensics (xmin heap, 2026-08-31): the generated owner's unscoped `delete from hr_unlocks` took 20 rows on its 2026-08-23 re-apply; two victims were re-applied, the 17 companion rows never were. **LIVE consequence: every honest non-shop companion acquisition raises uncaught 23514 and the companion drops on reload** — unhit only because zero acquisitions since 08-23. Reseed migration staged (derived-from-allowlist, by-value pins both directions, upsert-scoped); new `unlock-catalogue-ownership` guard measures the blast radius + cross-checks the repair list vs the manifest. Apply after security GO → prod 82/21 closes the census divergence. |
| Class-kill: owner column on `hr_unlocks` + scoped generator deletes | 🧊 | P2 | The real fix for the wholesale-delete class (the `hr_unlock_offers.source` precedent). Needs gen-unlocks.mjs change + editing the applied generated file + ONE deliberate wholesale re-apply — the exact dangerous op, so it's its own reviewed change, not a rider. Also fix the repo-side shape disagreement on the 4 shop companions (flag/flag vs rung — correct only by apply order today). Until then the guard makes recurrence measured, not silent. |
| ~~`hr_companion_grant` 23514 → machine code~~ | ✅ shipped b498 (hardening C1) | — | Superseded by the trio. |
| Auto-eat picker gates on the wrong predicate | 🧊 | P3 | Found by the b499 surfacing lane: `setAutoEatFood`/`openAutoEatPicker` gate on `hasTrait('auto_eat')` (entry-tier id) while every other reader uses `autoEatTier() > 0` — a tier-II-only owner is told the feature they upgraded is locked. One-line fix but it turns the b341 regression test red; wants its own commit with the test re-pinned. |
| Companion grant lost past the retry ladder | documented | P3 | b499 gives a refused/failed hatch ~103s of retries + a 5-min cooldown, but no persistent pending-grant queue across reload — a transport-dead hatch is lost (the trigger condition re-fires for repeating quests; one-shots don't). Acceptable for beta; a durable intent queue is the real fix if reports appear. |
| ~~SYNC_ENABLED_TOGGLE dormant~~ | ✅ **ARMED + live-verified b500** | — | Ruled (2b, three conditions) + shipped; live gate proved the round-trip on prod (`hr_set_auto_eat` 200, server row moves, pct tier-clamped, restored as found). |
| ~~LIVE FREE-MINT: `hr_create_character(p_slot)`~~ | ✅ **CLOSED ON PROD 2026-09-01** | — | Found by the fresh-character play-gate; security P1 (bypassed the 3,100-gem ladder + leaked tradeable gold/items). `2026-09-08-hero-slot-buy.sql` §8 applied (dry-run→commit 201). LIVE-VERIFIED: `hr_create_character(4)` → `slot_not_owned`, `hr_create_character(1)` → grandfathers `created:false`, 35 chars untouched. live-hash re-pinned (hr_create_character/hr_rpc_gate/hr_state_of moved, 40/40 green). CLIENT (buy button + server-owned ownership) ships next bump. |
| **Visual P1s from the b501 gate (pre-existing, harness `tests/visual-qa.mjs` + read PNGs, 2026-09-04)** | 🧊 | P2 (art-director) | (1) **Combat destination strip `#wt-dests`** truncates card titles at BOTH desktop and 922×423: "The Crownless Wyrm" (185>134px), "Crypt of Bones", "Today's blessing", "Clan Raid"/"World Event" kickers and the "Opens in Open Beta 1"/"Events ▸" buttons are cut by the `.panel` edge — the strip is a horizontal scroller whose right-hand cards fall off-screen with no affordance. (2) **Landscape Inventory**: "· 2,719 items" clipped behind Buy space/Multi-select/Manage (`.invc-space-sub`); 19 controls <44px, worst the 30px BAG/EQUIP/SAVED `#inv-mob-tabs` buttons. (3) The bottom-left ⚙ chip overlaps the first content row at 922×423 (Slime's stat line on Combat, the Gold pill on House). (4) Home "Hero slot 4 — Checking…" afford text did not resolve on the live account (possible stuck pending state on the b465 owned-slot afford shape — confirm before fixing). None caused by b501 (no CSS/structure change); none block. `tests/visual-qa.mjs` itself is an ORPHAN (zero refs) that WORKS — wire it into the release gate. |
| **Bounty accept must clamp tier+difficulty by SERVER BH level (security F2, ledger SA-048)** | 🧊 | **P1 — within ONE BUILD of the BH-XP apply** | `hr_accept_bounty__ungated` gates tier by combat level only; the board-depth/difficulty ladders are client-side, so a direct API caller gets tier-6 hard at 23.8 XP/kill vs the 0.61 the UI offers = ~39× per-kill on a now-RANKED board, no forgery. The BH-XP fix is the prerequisite that makes the clamp possible (server can finally read a BH level): `least(hr_bounty_unlocked_tier(v_cl), boardTierForBountyLevel(hr_level_from_xp(bh_xp)))` + difficulty clamped to the BH-unlocked set (also closes the tracked 'elite' residual from 2026-08-23). |
| **Per-UTC-day Bounty-Hunter XP ceiling (security F3, ledger SA-050)** | 🧊 | P1 — before `skill:bountyHunter` is promoted/rewarded/seasoned | The kill-credit physics cap was signed off as bounding a *self-only* forgery; feeding a ranked skill from it falsifies that premise (~2.23M BH XP/day sustained, BH 99 in ~6 days). Ship the ledger-read ceiling in the exact shape `c_combat_xp_day_budget = 5,000,000` already established, pinned by a drift test. Sibling finding while sizing it, LARGER and pre-existing: **bounty gold sets `gold_in=0` and sits entirely outside `hr_day_budget_limits()`** (~26M/day physics-possible vs the 25M shared ceiling) — ledger SA-049, own lane. |
| Hero-slot `hr_apply` ns-key belt-and-suspenders (G2b) | 🧊 | P2 | Security-queued (2026-09-01): the principled fix behind the character_slot closure — `hr_apply`'s generic progress block should refuse keys matching `^(character_slot|entitlement|trait):` (namespaces only a SECURITY DEFINER RPC may write). Its OWN reviewed change against the pinned hr_apply body + guard — NOT smuggled into the slot migration. Surface is closed today (REWARD_KEY_RE rejects colons both sides; claimableFor allowlist), so this is defence-in-depth, not urgent. |
| No first-time tutorial for a newly-CREATED character | 🧊 | P3 | Live playthrough (2026-09-01): a fresh character on a 2nd/3rd slot lands on the home dashboard with zero onboarding — FTUE fires only for new ACCOUNTS. Moot for the beta-3 wipe (everyone's a fresh account), but a returning player's new character gets no guidance. Board so it's known; revisit if account-modes/alts ship. |
| Daily-reward modal auto-pops on every slot switch/reload | 🧊 | P3 | Live playthrough: the Day-N modal re-appears on each reload/slot-switch and swallows the first click. Mild nag; consider once-per-day-per-device suppression. |
| Live playthrough VALIDATIONS (no action — recorded) | ✅ | — | 2026-09-01, fresh slot-2 character on prod: creation kit + food bridge correct (shrimp 10/cooked 20), free Auto-Eat granted through the NEW gated-create path, daily claim pays 500, attended combat + auto-eat fire, and the **b500 death sheet is perfect live** — "Slain by Goblin · 3 kills first" (cause + count, not blank), owner-aware copy ("you already have Auto-Eat — switched off, not missing", NO store price), and the "Turn Auto-Eat back on" tap flips the switch. The whole b497→b500 arc holds on production. |
| Armor saturation — CONFIRMED LIVE at the low end | 🧊→ Phase 1 | P1 | 2026-09-01 measured on prod: fresh L1 vs goblin hit 51.8% bare → ~47.6% with a full ~250g starter armor set (~4pt swing) while the tutorial promises "armor is what stops you dying." The armor-overhaul P1 (Phase 1 #2) is confirmed, not theoretical. |
| Bounty Unlocks strip — CONFIRMED LIVE | 🧊→ Phase 1 | P2 | 2026-09-01: fresh board offers 3 Cull contracts; the strip advertises Proof/Weapon/Streak/Boss/Chain/Hard/Elite as earnable unlocks the board never posts. Phase 1 "bounty board honesty" confirmed. |
| Gem wallet: per-character vs account-pooled | 🧊 | P3 | Designer currency-scope question (surfaced by the hero-slot build): gems are per-(user,slot); a buy from Hero 1 can't spend Hero 2's gems though both are the same account. Self-only, not an exploit. The slot buy "charges the wallet on screen" as the least-surprising rule; whether gems should pool account-wide is the ruling. |
| Auto-eat sync drops the choice on `network` refusals | 🧊 | P3 | Found by the b500 live gate: `flushServerSync` retries only collect_first/rate_limited; any other refusal (incl. transient network) console.warns and DISCARDS the player's toggle/food/threshold choice. A flaky connection silently loses a settings change. Harden: stay-dirty + bounded retry for 'network' (the transport-ladder precedent from the hatch path), keep definitive refusals dropping. |
| **Live-hash drift check for hash-pinned function bodies** | ✅ **SHIPPED** (c21e823a, 2026-08-31) | — | `tests/live-hash-drift.mjs` + 37-entry baseline: 30 byte-identical live-vs-replay, 7 comment-only (each proven code-identical by --codediff); selftests 16/16 by named assertion; CI-wired credential-free, live half out. **First measure caught the SECOND stale NOT-APPLIED pair** (intent-mismatch headers — corrected 4b7ae793) and one prod body with mojibake comments. Ritual: run `--live` before any migration/restore/cutover step. |
| Prod mojibake in `hr_put_client_state__ungated` comments | documented | P3 | 1 of 264 bodies: box-drawing chars stored as CP1252-of-UTF-8 — an apply path once mangled UTF-8 (code identical, cosmetic today; the hazard is the same path mangling a string literal silently). Baselined `live.mojibake:true` — a SECOND occurrence goes red. **Coordinator decision (2026-08-31): NOT re-applying the body just to fix comments** — a prod DDL write for zero behavior change is worse than the blemish; fix rides the next legitimate re-apply of client-state, using a UTF-8-clean path. |
| `.gitattributes` doesn't pin `tests/**` or `src/**` to LF | 🧊 | **P2** (upgraded 2026-08-31) | Two independent hits in one day: (1) CRLF anchors in fresh checkouts break template-anchor mutations; (2) **worktree agents measure FALSE edge-pack hashes** — the streak lane reported main's pack "out of sync" (80c480d9) when the true main-tree pack was 9a0c1567 = deployed exactly; a wrong "in sync" call in the other direction would skip a needed redeploy. Fix: pin `src/** tests/** supabase/** text eol=lf` in one quiet renormalisation commit between waves, then re-verify pack hash unchanged from the main tree. |
| Faucet-pass residuals F2/F3/F4/F5/F6 | 🧊 | P2-P4 | F2: 8 of 11 renown-faucet mutations count ANY throw as caught — add per-mutation `expect` regex (the bounty-drift convention). F3: settle/credit advisory-lock asymmetry can over-discount (self-only, errs safe). F4: `hr_bounty_monsters.monster_id` needs `CHECK length ≤ 47` (credited-key ceiling; headroom 30 today). F5: `first_blood` quest reads the undiscounted counter (150g once — accepted, named). F6: `hr_import_apply` writes no credited rows — if ever wired to an endpoint it re-opens the boss half (threat-register entry; postgres-only today). |
| Daily-reward modal overpromises after a missed day | 🔧 **fixed-staged** (8582f9b9, ships b498) | P2 | Root cause deeper than the gate read: TWO server streaks (days settled vs days claimed); b475 wired the sheet to the wrong one. `deriveLoginStreak` now a single vendored authority (b349 pattern), sheet + Home card both fixed, mutation-proven against the exact live numbers (Day3/2000/5 vs Day1/500). Edge redeploy at b498 (2fc78db7). **Designer handoff:** topbar chip + welcome-back modal show the PLAY streak labelled "Daily streak" — two true numbers, one confusing label; naming decision needed. |
| TTL-expired-hold counter → bug-report payload | 🧊 | P3 | Security (2026-08-30 F1 re-rule): nothing journals "client held a consumption the server never made" — a future policy split surfaces only as "my food comes back" reports (how b467→b479 was found). Cheap detector: count TTL-expired pending-consume holds per session, attach to the existing bug-report payload. Makes the next occurrence a measurement instead of a report. |
| Auto-eat food-choice divergence: server `bestHealingFood` vs client `G.foodSlot` | 🧊 | P2 | Found in the 2026-08-30 gate investigation: `hr_set_auto_eat` has ZERO client call sites, so post-b497 the server eats the most valuable healer in the bag (`auto_eat_food` NULL) while the client honours the equipped food slot — unit counts converge, but different STACKS drain and the server picks the pricier one. Self-only value destruction; bites when a player carries a premium healer. Fix: debounced settings sync (Systems — `hr_set_auto_eat` bumps version+journals so it can't fire per drag) + Designer ruling: "may a player disable a mechanic their payout depends on?" Pending security severity rating in the F1 re-rule. |

---

## 9 · CODE HEALTH / ARCHITECTURE (the clean-code track — Tyler: "clean code is a must")

_Code-health audit 2026-08-18. Headline: **on a real trajectory to the large-scale-multiplayer end-state — the hard bet (server-authority) is won and proven live, the logic core (`src/core`) is clean/pure/dual-runtime. The liability is the RENDER layer.** Four-layer scorecard: Data 85% · Logic 80% · **Render 20%** · Platform 40% — "built the load-bearing half first."_

| Item | Status | Pri | Notes |
|---|---|---|---|
| `legacy.js` render-layer split → `src/render/*` | 🔧 **active — rolling** | **P1** | Monolith 18.8k lines. Strangler-fig, one domain at a time, behind smoke + visual gate. Pattern: `docs/design/render-extraction-pattern.md`. **Shipped:** #1 **b383** Lifetime Stats · #2 **b384** Active Effects · #3 **b395** Achievements · #4 **b397** Bestiary · #5 **b398** Equipment Bonuses — all pure-refactor, both-theme visual gate PASS. Companions/Stable dup resolved b390. **Clean leaf helpers ~exhausted (1-2 left); next phase = tab/screen CONTROLLERS** (renderCombat/Inventory/Profile/Farm/Shop/buildTibiaDoll) — bigger, need per-controller mapping not the mechanical pattern; scope deliberately (higher blast radius). Serialized: one agent on legacy.js at a time. |
| Test-determinism (kill the b227/b334 flakes) | 🔧 | **P1** | Root cause (investigated 2026-08-19): flakes are ASYNC wrapper installation (setTimeout 600ms sets `__viewedSkillId`; setInterval 200ms wrapAll), NOT static load-order. **Tier 1** (make `__viewedSkillId` synchronous in base openSkillDetail → kills b227) = safe unattended, IN PROGRESS. **Tier 2** (adopt the unused `safe.js` tap registry, replace ~30 `window.showTab` reassignments with deterministic taps) = the real fix but ~40 sites of core nav wiring, silent failure, b361-class → **HELD for Tyler-in-the-loop, fold into the controller extraction**. A deterministic suite is the foundation the riskier debt work rests on. |
| CSS → tokens | 🧊 **held for Tyler** | P1 | 1,756 hardcoded hex. HIGH value (themes/skins backlog → one token set). BUT tokenizing deliberately changes Hearthlight (default theme) component-by-component; piecemeal overnight = players see a half-converted theme with no one eyeballing cumulative coherence. Do as a coordinated pass WITH Tyler reviewing the whole-theme result, not unattended. |
| CSS → tokens, screen-by-screen (convert-as-you-go) | 🔧 | P1 | 1,756 hardcoded hex, 1,343 `!important` across 9 sheets (theme-cozy.css alone: 947 !important). Guard tests catch each conversion. Start with combat + inventory (densest, break most). Unblocks the render layer + themes/skins. |
| Adopt `src/platform/storage.js` across 37 direct-localStorage files | 🧊 | P2 | Mechanical, one file at a time. Prereq for the Steam/mobile platform seam. |
| Load-test `hr-accrue` at a few hundred concurrent intents | 🧊 | P2 | The one "fine at 5, unknown at 500" — verify pooler connlimit 20 + rate-bucket throughput before real launch. |
| Correct doc drift as the split proceeds | 🔧 | P3 | CLAUDE.md line-count corrected 2026-08-18; keep SYSTEMS_MAP in sync when render extraction starts. |
| Do NOT | — | — | Rewrite the monolith wholesale (rejected); widen bump script into supabase/tests (trap); reintroduce a 2nd away-combat path or a data double-copy (both guard-tested). |

---

## 10 · HUNTERA-DERIVED ADOPTIONS (option B — Tyler, 2026-08-23)

_Discovery 2026-08-23: **Huntera** (huntera.com.br, BR, launched 2026-08-21) is a browser Tibia clone — Angular + Phaser real-time tile world, custom authoritative WS server (123 intents / 184 events), automation-first combat, escrowed market, PIX + Solana coin store, "RMT liberado." Tyler's call: **do NOT clone it** (CipSoft IP, original tile art, a second real-time engine); **adopt its best ideas into Hearthrise.** Game-designer ruled (final authority), systems-engineer costed against live server surfaces. Both agree on the order below. **END GOAL (Tyler, 2026-08-23): the 4–7 month WALKING-WORLD program** — a Huntera-class walking world done legally: our content/systems (built) + original tileset/walk-cycle art + Phaser world layer + Realtime presence. Current track unchanged (beta → scalable-state); the Living Town spike (item 0) is the program's gate, then a scoped plan with an art budget. **All items gated behind the scalable-state exit criteria** (top of board) — nothing here starts before render extraction / CSS tokens / `hr-accrue` load test, and none of it precedes the inventory flip's real-launch schedule (items 1–4 are flip-independent, which is why they sort first)._

**Effort classes:** ① data-row+UI · ② client feature on an existing RPC · ③ new server verb · ④ new table · ⑤ touches SHA-pinned `combat-sim.js` (repack + Tyler-paste redeploy + AWAY-1 parity).

| # | Item | Status | Pri | Class / effort | Notes |
|---|---|---|---|---|---|
| 0 | **Living Town — shared presence plaza** (the hook Tyler named) | 📋 **DECIDED (Tyler 2026-08-23): 3–5 day throwaway FEEL SPIKE is the FIRST POST-BETA item** — gray-box Phaser scene in a worktree, CC0 placeholder tiles, click-to-walk, Realtime presence, camp fight replayed from real combat-sim events; zero art spend, nothing ships. Spike verdict decides whether the real (art-gated) build gets a scoped program. | P2 (spike) → art-gated (real build) | ②/④ M–L (3–5 wk eng + ART) | Tyler (2026-08-23, playing Huntera): "the coolest part is having a living world in the town and seeing other players on the screen." NOT the second-engine problem — the veto is on shared *authoritative combat*; a town is **cosmetic presence**: one Phaser scene from a Tiled map, click-to-walk, players broadcast position a few×/s over **Supabase Realtime** (already used for chat), name labels + chat bubbles, sharded ~50–80/plaza instance. Hunts stay the server-resolved sim (walk to the gate, tap a camp) — zero change to `hr_apply`/away parity/security. Free hooks once bodies are on screen: emotes, click-a-player → gear/level (server data), clan colours, market as a stall, world-boss crowd at the gate. **Blocker = art** (coherent tileset + walk cycles + outfit/gear variants under the art freeze). Designer to rule on identity (cozy hearth-town, Idle Clans mobile density) before any prototype. |
| 1 | **Sell-lock + loot filter** | 📋 | P2 | ① S (1–2 d) | Flag an item "never auto-sell / never vendor"; loot-class filter for what the bag keeps. Cheapest real win; no new verb. |
| 2 | **Session Tally + Watch Report** (hunt-analyzer) | 🔧 **BUILT — staged b487, awaits gate** | P2 | ② S–M (2–4 d) · **BUILT 2026-08-27** (src/features/session-tally.js; settled-only; smoke green; on local main unpushed, needs visual+play gate). | Surfaces XP/h, gold/h, drops/h, supplies/h, net, per-monster kills, **session bests** on the Fight screen; the away side writes the SAME shape into the welcome-back card so live and offline read identically. Data already exists server-side: `player_ledger` (own-read RLS, one row per apply window — per-kill granularity comes from `hr_bestiary_of` deltas) + `windowEnvelope()` in accrual.js (`paidMs` not `awayMs`). Copy the `src/net/market-history.js` read-only-cache precedent; extends the existing `Ledger` in combat-screens.js. **Rule: reports settled server credit only — never a projection.** No migration, no repack, no security review. |
| 3 | **Bestiary Charms (PROG-01)** | 📋 | P2 | ① probably no new verb | Already approved §3; Huntera's bestiary-completion bonus = this. Server bestiary counters exist (`2026-08-20-bestiary.sql`). |
| 4 | **Standing Orders — B1 "Withdraw when"** | 📋 | P2 | ③+⑤ M (3–5 d) · do ALONE so AWAY-1 has one variable | War Table card, three clauses: *Provisions* (= today's auto-eat, unchanged) · **Withdraw when** HP danger / food out / bag full / after N hours · *Steward* (see #6). Rules are **columns on `player_state`** written by one narrow SECURITY DEFINER RPC (`hr_set_battle_rules`, copy `hr_set_auto_eat` exactly — callable by `authenticated`, not `hr_engine`, clamped on write, read identically by live tick + `computeAccrual`; bind SQL↔JS with a test like `tests/auto-eat-authority.mjs`). Evaluated in `simulateSpan` (which already terminates early on death). **Rules are a TABLE like `AWAY_SCOPE`** — unknown rule = inert, never "stop". Withdraw ends the span early = self-nerf, safe, mints nothing. Biggest away-quality lever we lack ("you died at hour two"). |
| 5 | **Loadout presets ("Marching Orders")** | 🧊 | P3 | ② S | Gear + food + orders saved as one named preset. QoL, no new sim. |
| 6 | **Standing Orders — Steward auto-sell on return** | 📋 | P3 | ③ M | Auto-sell chosen loot classes on return, **server-priced NPC vendor verb only** (`vendor-sell.js` exists) — never locked/bind-on-pickup items, never above vendor, never a client-computed credit. |
| 7 | **Quarry** (prey-style focus pick) | 📋 design-deconflict first | P3 | ③+⑤ M (4–6 d) | A LAYER, not a duplicate: bounties = contracts for Marks; **Quarry = one bestiary-studied monster you're focused on, chosen free once/day, +drop/+XP vs that species only.** Bonus applies inside `simulateSpan` per UTC segment like `botd`. Sibling columns on `active_bounty` / reroll = marks via `hr_bounty_spend`. **Designer must deconflict bounty / BotD / Quarry (three "kill this for more" layers) before build.** If it ever grants Marks or a second reroll currency → kill it. Reject Huntera's wildcard currency. |
| 8 | **Depot → Bank client UI** | 📋 | P2 (post-wipe) | ② S (1–2 d) | Server half is BUILT and dormant: `player_bank`, `hr_bank_move(p_slot,p_item,p_qty,p_dir,p_idem)` (locked, idempotent, rate-gated, journalled), `hr_state_of` projects `res.bank`. `G.bank` is client-authored today. Also the fix for the dead Cellar "+500 storage" perk. Flip couples to `INVENTORY_ARM_ENABLED`. |
| 9 | **Standing Orders — B2 targeting priority + ammo/rune selection** | 🧊 | P3 | ③+⑤ M | Changes fight outcomes/drop table → separate parity run. **Blocked on E1** (ammo consumption; `src/core/ammo.js` "NOT YET WIRED"), already scheduled post-beta. |
| 10 | **Enchant v2 + Tempers** (imbuements MERGED) | 📋 | P2 | ④ M–L (6–10 d) | Designer: one enchanting system, two axes — permanent element binding (board §6) + **Tempers** = charge-limited slots fed by monster materials (the home for ~25 vendor-trash tier-3–6 drops). NOT a separate shrine (would split the rune economy). Engineer: `player_inventory` PK `(user_id,slot,item_id)` has no instance dimension and adding one means rewriting `hr_apply` (forbidden) → use a **sibling `player_item_instances` table, the `bank-store.sql` precedent**. Timestamp-expiry tempers are cheaper than the wipe-rune economy. |
| 11 | **Buy-offer order book (escrowed, two-sided, partial fills)** | 📋 | P2 — **last; not pre-launch** | ③④ XL (12–20 d) | Designer: highest-value economy item ("nobody online to sell to me" dead moment). Engineer: buy-offers were **deliberately demolished** (`2026-08-17-market-buy-offers-lockdown.sql`: no server model/RPC/escrow — client escrowed its own gold). Genuine server rebuild: gold-escrow ledger, matcher under the existing market lock discipline, partial fills, cancel refunds, history. Hardest concurrency item on the board. **Prereqs: Phase 3 ledger accumulator (§2) + `hr-accrue` load test.** Then unhide the UI. |
| 12 | Guild log / directory / ranks | 🧊 | P3 | ②/③ S–M | Cheap retention; gated behind the clan-contribute mint fix (§2 Clan pass 2). |
| 13 | Friends list | 🧊 | P3 | ④ S–M | Mutual-consent table; `display_names` is the name authority. |
| 14 | Arena → PvE ladder · Discord/push alerts on the platform seam | 🧊 | P3 | — | Later. (Telegram-style per-user webhook secrets declined — new abuse surface for what Discord serves.) |

**Explicitly REJECTED (do not re-propose):** stamina with paid refills / VIP tiers (our daily cap is the honest version and must never be purchasable) · coin transfer by character name · face-to-face trade (RMT/scam rail, defeats market authority) · PIX / USDC / USDT coin store / any RMT (Bond stays IAP-only, non-mintable) · death-protection "blessings" (no item loss on death → fake stakes) · chase/keep-distance + spell rotation bar + follow-member (no tile world) · **instanced party hunts — TECHNICAL VETO** (`hr_apply` is single-character by construction; shared authoritative tick across N characters = a second engine; co-op stays on shared objectives: muster/raid contribution) · hunt-analyzer profit shown as a projection · setting sprawl (every rule must be server-evaluable or away-play diverges from live — the one failure the away architecture exists to prevent).

**Already HAVE (do not rebuild):** auto-eat (potion rules) · offline training · hunt catalog by level · cyclopedia (Review Book + item index) · quick-sell/NPC sell · daily reward · highscores (b374) · polls (`clan_vote_*`) · support tickets (`bug_report_submit`) · arena/raids (`raid_strike/claim`, `hr_hunt_band`, muster).
