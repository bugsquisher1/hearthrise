# Security review — M6 hunts, the Analyzer and Vigour, as a MONEY SURFACE

**Reviewer:** security-engineer (veto) · **Branch reviewed:** `lane/m6-hunts-backend` @ `76af0943`
(base `set/b551` `1addc2db`) · **Review branch:** `sec/m6-hunts` · **Date:** 2026-09-22
**Design authority:** `docs/design/HUNTS_AND_ANALYZER.md`, `docs/design/HUNT_ANALYZER_UI.md`

| migration | verdict |
|---|---|
| `2026-09-22-hunt-stance-stop.sql` | **GO-WITH-CHANGES** (H-1, H-2 — the SQL is sound; both are honesty defects, neither moves value) |
| `2026-09-22-vigour-daily.sql` | **BLOCK** (S-1 — the meter it creates can be driven to zero by a poll cadence; S-2) |
| `2026-09-22-vigour-refill.sql` | **BLOCK** (S-1 destroys the scarcity this sink charges gold for; S-3, S-4; Tyler's §4.6 numbers unrecorded) |
| `2026-09-22-hunt-analyzer.sql` | **GO-WITH-CHANGES** (S-2, A-1 — it leaks nothing and authors nothing; the cost is the change) |
| **CLIENT SHIP** (`hunt-panel.js`, `accrue.js`, `activity.js`) | **GO-WITH-CHANGES** (C-1 — ship the panel, hold the Vigour bar until S-1 lands) |

**No production reads and no production writes were made.** Every measurement below ran against the
repo's own engine in this container. The three conclusions that matter:

**The stance/stop half is the best-built thing in this lane and I could not break it.** A stance
carries no multiplier, the catalogue re-validates server-side in `hr_apply`, out-of-bounds refuses
instead of clamping, `__proto__` fails safe, and the stop predicate has exactly one implementation.
**The Vigour half does not measure what it claims to measure**: `vigourChargeMin` floors per settle
window and discards the remainder, so the daily limiter is a function of how often a player settles
— 40 minutes charged for an hour at an ordinary 90-second poll, **zero** for an hour of sub-minute
`set_activity` collects. **And applying two of the four files turns the house's own nightly
grant-hygiene detector red**, which is the same defect the 2026-09-21 world-tick review raised as
M-2 and which nothing in this lane checked for.

---

## 0a. BACKEND LANE RESPONSE — 2026-09-23, `lane/m6-hunts-backend`

Written by the backend-architect lane. Every "Required change" paragraph above
was taken as the spec; where the review offered alternatives, **this section is
where the lane says which it took**. Nothing below is a verdict — Security
re-verifies.

| # | answer taken | where |
|---|---|---|
| **S-1** | **(b), adapted: charge in the ledger's own unit and divide at read time** — NOT (a)'s carry column. `vigourCharge(windowMs)` returns the whole minutes AND the sub-minute remainder; both are proposed as daily counters (`ev:vigour_min`, `ev:vigour_rem_ms`) and `hr_vigour_of` computes `spent_min = minutes + floor(remainder_ms / 60000)`. **Raw ms in ONE row is not available**: `hr_apply`'s `c_max_progress_add` is 1,000,000 and a capped 24 h window is 86,400,000 ms, so the delta would be REFUSED and the player would lose their night — hence the quotient/remainder pair, which is exact rather than merely finer (`floor(Σw/60000) = Σq + floor(Σr/60000)`). (a) was declined because it is a new column, a new `hr_apply` delta key and new engine state the attended, away and tick paths would each have to thread identically; this is a pure function of `grantMs` in the one engine all three share. **A floor in ANY unit has the same defect** — windows of just under 2U charge U — which is why the remainder is kept, not made smaller | `src/core/hunt.js`, `accrual.js:2943`, `vigour-daily.sql` §1(4) + GATE(c7) |
| **S-2** | Was already an additional migration at the lane head (`2026-09-22-engine-allowlist-hunt-reads.sql`, commit `f16f7be5`, post-dating the `76af0943` you reviewed) with both justifications. This lane added what the condition asked for and the file did not have: **the STRICT detector, EXECUTED**, plus an unlisted-grant mutation arm and a probe-gone section, mirroring `2026-09-21-engine-allowlist-tick-settle.sql` §4 | `engine-allowlist-hunt-reads.sql` §3(a2)(b2)(e), §4 |
| **S-3** | **REFUSE the partial**, not price it. Matches the file's own `already_owned` reasoning, keeps the catalogue a flat ladder (a pro-rated price is a second pricing rule in code), and computes the delivered minutes BEFORE the debit. The refusal NAMES `would_deliver` so the panel can say what it would have bought | `vigour-refill.sql` §(6), §7 GATE(e7) |
| **S-4** | Refused by name, **before the advisory lock**, journalled via `hr_record_rejection`; the `default null` stays in the signature for the PostgREST reason. The §(9) intent cache is now unconditional. `missing_idem` is **not** added to `STATELESS_REFUSALS`: slice 1 ships no refill control, the client never calls this verb, and none of `vigour_ceiling` / `vigour_daily_cap` / `insufficient_gold` is registered either. **That list is owed the day a refill button ships** | `vigour-refill.sql` §(0), §7 GATE(e1b) |
| **A-1** | Steps 1 and 2 of your ordered list. Passes 1–3 are ONE scan with two `left join lateral`s; deaths (different `intent`) and the `order by … limit 1` stay separate as you asked. Floored at 24 h. **The denominator moved with the numerator** — flooring the rows alone would divide one day of gold by five days of elapsed, so every rate divides by the new `window_ms` and the envelope carries `window_from` / `window_capped`; `elapsed_ms` and `started_at` still describe the whole hunt. Step 3 (`explain analyze` on a real character) is **yours and the Coordinator's**, post-apply | `hunt-analyzer.sql` §1, `tests/hunt-analyzer.mjs` A6 |
| **H-1** | **(a)**, as you required. Header and table comment both state that `hr_hunt_stances` is an **ID allowlist** and that the knob values are not read at runtime. Made checkable rather than aspirational: GATE(b3) refuses the apply if any routine reads `hr_hunt_stances` and names `eat_at` / `ammo_dry` / `falls` | `hunt-stance-stop.sql` §1, §6 GATE(b3) |
| **H-2** | Cast to `numeric`. All four rules, because the cast is written out four times | `hunt-stance-stop.sql` §2, §6 GATE(b2) |
| **C-1** | **The Vigour bar is HELD — not rendered at all**, rather than rendered with the honesty line. `state.vigour` is absent from every envelope until `vigour-daily.sql` applies, so there is no honest meter to draw, and slice 1 has no gate, intent or purchase behind it. `vigourRow` is deleted; a dated block comment carries the restoration recipe. The in-page arm **passes a full meter object on purpose** and fails if a bar appears. Also added, because A-1 made it necessary: a capped readout prints "totals cover the last 24h", from the server's `window_capped` and never from a clock read in the browser | `src/render/hunt-panel.js`, `smoke/hunt-raids-and-screens.js` |
| **I-3** | **No number was invented.** A dated TYLER line was added to `docs/design/HUNTS_AND_ANALYZER.md` §4.6 stating the four figures are placeholders pending his ruling, and why the catalogue shape makes his answer an `UPDATE` rather than a code change | `HUNTS_AND_ANALYZER.md` §4.6 |

**The guard is now the regression test for its own fix**, as you wrote it to be:
`tests/vigour-charge-conservation.mjs` exits 0, its `guards-unregistered.json`
entry is deleted, both arms are registered in `smoke.yml`, and its `--selftest`
runs in the ORDINARY direction — each mutation plants the defect BACK and must
be caught. `slack` was **tightened** from one minute to 1e-6, because the charge
is now exact; `chargeOf` sums both counters, which a reader should check first
if they suspect the measurement was moved rather than the code.

**Not done, and named rather than left to be found:** A-1 step 3 (measure with
`explain analyze` on a real character post-apply); I-3 itself (Tyler's four
figures); the `STATELESS_REFUSALS` entry, owed the day a refill button ships;
and the joint `hr_apply` / `hr_state_of` restatement you called owed before the
next patch on either — this lane added none.

---

## 1. Findings

| # | Sev | Title | Status | Where |
|---|---|---|---|---|
| **S-1** | **P0 — BLOCKS both vigour files** | The Vigour charge is not conserved under window subdivision. A 90 s poll cadence charges 40 min for an hour; sub-minute `set_activity` collects charge **0** | **CONFIRMED by execution** | `src/core/hunt.js:375` · `accrual.js:2926` |
| **S-2** | **P1 — BLOCKS on apply** | `hr_vigour_of` and `hr_hunt_analyzer` are granted to `hr_engine` and are in no allowlist, so `hr_assert_grant_hygiene(true)` RAISES — nightly, on production, into `maintenance_alerts` | **CONFIRMED (code read; the detector's own predicate)** | `vigour-daily.sql:145` · `hunt-analyzer.sql:239` |
| **S-3** | P1 | A refill clamped by the 22 h ceiling still charges the full rung and still reports `minutes: 120`. The receipt and the meter disagree by up to 119 minutes | **CONFIRMED (code read + arithmetic)** | `vigour-refill.sql:202`, `:254` |
| **S-4** | P2 | `p_idem` is `default null` on a money verb, and a null key skips idempotency entirely — a double-tap double-charges | **CONFIRMED (code read)** | `vigour-refill.sql:261`, `:278` |
| **A-1** | P2 | `hr_hunt_analyzer` makes five unbounded passes over `player_ledger` **inside `hr_state_of`**, i.e. on every envelope, with no LIMIT and no time bound below the 90-day retention | **CONFIRMED (code read)** | `hunt-analyzer.sql:96-190`, `:262` |
| **H-1** | P2 | `hr_hunt_stances`' three knob columns are **decorative** — nothing at runtime reads them; the engine reads the JS table. Tuning the catalogue changes nothing and says so to nobody | **CONFIRMED (repo-wide absence)** | `hunt-stance-stop.sql:72-85` |
| **H-2** | P3 | `hr_hunt_stop_valid` casts to `bigint` **after** the `\d+` test, so a huge JSON number raises 22003 from inside a CHECK instead of returning false | **PLAUSIBLE (unreachable today; defence-in-depth)** | `hunt-stance-stop.sql:113-127` |
| **C-1** | P2 | The panel renders a Vigour bar from `spent_min`/`remaining_min`, which S-1 makes wrong by up to 100% | **CONFIRMED (follows from S-1)** | `src/render/hunt-panel.js` |
| I-1 | Info | `hr_hunt_stop_valid` is not revoked from `authenticated`. Harmless **only** because the global default ACL already revokes it | Accepted | `hunt-stance-stop.sql:133-134` |
| I-2 | Info | `hr_record_rejection` is called on four refusal paths but is not in the file's PRECONDITIONS block | Accepted (§7(e1) executes it) | `vigour-refill.sql:50-62`, `:185` |
| I-3 | Info | Design §4.6 flags four figures as **TYLER's**. Nothing in the repo records his answer; the file ships them as catalogue placeholders, which is the right shape | Named as a gate | `vigour-refill.sql:105-112` |
| I-4 | Info | `hours` stops on **elapsed**, not paid, time. The engine names the deviation itself | Designer's call | `accrual.js:2757-2762` |

---

### S-1 — the Vigour charge is a function of how often you settle **[P0, BLOCK]**

Design §5 states the property this whole feature rests on, in italics of its own:

> "Vigour is charged from the same `ms` the payout is computed from, in the same transaction. A
> window cannot pay and not charge, **because there is one number**."

`accrual.js` already states the payout half of that as a conservation law, one line above the
`ACCRUE_MIN_MS` floor it was written to justify:

> "WHY THIS CANNOT BE GAMED. Paying it grants exactly what the simulation computes for the elapsed
> span and advances `accrued_to` to `now()`, so **time is conserved: switching twice pays the same
> total as switching once**."  — `accrual.js:1467-1472`

The charge does not obey that law. `src/core/hunt.js:375`:

```js
export function vigourChargeMin(windowMs) {
  const ms = Math.floor(Number(windowMs) || 0);
  return ms > 0 ? Math.floor(ms / 60000) : 0;
}
```

`floor`, **per window**, and the remainder is discarded. There is no carry column, no remainder on
the ledger row, and nothing anywhere that re-reads the lost milliseconds — I searched the repo for
one. `accrual.js:2926` proposes the floored value and nothing else:

```js
const charge = vigourChargeMin(grantMs);
if (charge > 0) progress.push({ kind:'daily', key: VIGOUR_PROGRESS_KEY, period: …, add: charge, … });
```

The file's own comment calls this "the same direction every other rounding in this engine takes —
toward the player". Every other rounding is a **payout** rounding. This one is a **limiter**, and
rounding a limiter toward the player is not generosity, it is the limiter not existing.

**Measured, against the real `computeAccrual`, on this branch** (`tests/vigour-charge-conservation.mjs`,
five arms, exit 1):

| how one hour of combat is settled | caller | paid | **Vigour charged** | gold |
|---|---|---|---|---|
| one 3,600,000 ms window | `accrue` | 60.0 min | **60** | 4,174 |
| 40 × 90,000 ms | `accrue` — **no privilege, any client may poll at 90 s** | 60.0 min | **40** | 3,840 |
| 61 × 59,000 ms | `collect` — what **every `set_activity`** passes | 60.0 min | **0** | **4,209** |
| 40 × 90,000 ms | `tick` — the shipped 90 s flush | 60.0 min | **40** | — |
| 80 × 45,000 ms | `tick` — `flush_seconds` tuned to 45 | 60.0 min | **0** | — |

Three separate ways in, in rising order of how much an attacker has to know:

1. **Nobody does anything wrong.** A 90 s poll cadence is above `ACCRUE_MIN_MS` and is an ordinary
   `accrue`. It is a flat **33% discount on the daily limiter**, systematically, for every player
   whose client happens to poll there.
2. **Switch-spam, and it is unbounded.** `runSetActivity` passes `caller:'collect'`
   (`set-activity.js:1111`), which is exempt from `ACCRUE_MIN_MS` by design (b531) and pays any
   sub-minute window that produced value (`set-activity.js:1154` only discards windows with *no*
   value). A client looping `set_activity` at 59 s **hunts at full rate forever**: the meter never
   moves, `spent_min` stays 0, `remaining_min` stays at the grant, the 22-hour ceiling is never
   approached and the refill ladder sells something nobody needs. Note the third row also pays
   **more gold** than the single window — 4,209 vs 4,174 — so this is not even a trade.
3. **An operator knob decides whether the limiter exists.** `caller:'tick'` is exempt from the floor
   too (`tick-shadow.js:149`), and `hr_tick_config.flush_seconds` is classified `operator_tunable`
   in `tests/restore-census.baseline.json`. Below 60 s the world tick charges every ticked character
   nothing, forever — no migration, no review, no guard. Today the tick's only channel is `gather`,
   so this is latent; it arms itself the day `combat` joins `channels`.

**Blast radius: the economy, not the character.** Vigour is the scarcity the 242,000-gold-a-day sink
is sold against (§4.4) and the 22-hour ceiling is the stated defence against "richest player hunts
most" becoming "richest player hunts always". Gold and loot from the uncharged windows are tradeable
on the market, so this is a faucet that crosses to other players' economies — the Target Property in
`CLAUDE.md` §1 — not a self-only cheat.

**Would an existing test have caught it?** No. `tests/vigour.mjs` drives the *SQL* (grant, ceiling,
refill ladder, replay) and the engine arms it runs feed one window at a time. Nothing anywhere asserts
that the charge is additive over a partition of a span. That guard now exists.

**Required change.** Make the charge conserve. Either:

- **(a) carry the remainder** — the `tool_carry` / `ammo_carry` shape `player_state` already uses for
  exactly this problem, as a `vigour_carry_ms int` written in the same delta; or
- **(b) charge in the ledger's own unit** — write `ev:vigour_ms` (or seconds) and let `hr_vigour_of`
  do the division, which removes the rounding from the hot path entirely.

(b) is cleaner and needs no new absolute key in `hr_apply`'s contract; (a) is a smaller diff. Either
way `vigourChargeMin` stops being the unit of account. **A floor on the window is NOT a fix** — the
`collect` exemption exists for a good reason (b531) and re-imposing a floor there would destroy real
paid windows, which is the defect the exemption was written to remove.

**Verification:** `node tests/vigour-charge-conservation.mjs` must exit 0, and the entry for it in
`tests/guards-unregistered.json` must be deleted and the plain run registered in
`.github/workflows/smoke.yml`. The guard is written to be the regression test for its own fix: its
`--selftest` plants a conserving charge in the engine and requires every arm to go green while the
shipped engine stays red, so the arms are proven to measure the law and not an implementation.

---

### S-2 — applying two of these files turns the nightly grant-hygiene detector red **[P1, BLOCKS the apply]**

`hr_assert_grant_hygiene(p_strict boolean default true)` collects, among nine checks:

```sql
-- 2026-09-21-engine-allowlist-tick-settle.sql:546-551
select … into v_engine_extra
  from pg_proc p …
 where n.nspname = 'public' and p.prokind in ('f','p')
   and has_function_privilege('hr_engine', p.oid, 'execute')
   and p.oid::regprocedure::text <> all (c_engine_allow);
…
if p_strict and (… or jsonb_array_length(v_engine_extra) > 0 …) then
  raise exception 'GRANT HYGIENE FAILED: %', v_report::text;
```

This lane grants `hr_engine` EXECUTE on two new functions:

- `vigour-daily.sql:145` — `grant execute on function public.hr_vigour_of(uuid, int) to hr_engine;`
- `hunt-analyzer.sql:239` — `grant execute on function public.hr_hunt_analyzer(uuid, int) to hr_engine;`

Neither appears in `c_engine_allow` anywhere in the repo. I checked both by name across every
migration and ran the derivation tool: `node tools/derive-grant-hygiene.mjs --check` → **exit 0**,
"derivation in sync (10 links, 11 patches)" — i.e. the allowlist as committed at this head does not
contain them and is not pending an edit that would.

**This is the posture both files claim to mirror, and the mirror is the proof.** `hr_bestiary_of`
is cited by name in both headers as the model. `hr_bestiary_of(uuid,integer)` needed its own
dedicated migration — `2026-08-20-live-progress-engine-allow.sql` — to be added to `c_engine_allow`,
and that file's §653-663 even asserts the entry took effect. Mirroring the grant without mirroring
the allowlist entry is half the pattern.

The detector runs at every apply **and nightly via `pg_cron`**, with failures surfacing as
`maintenance_alerts`. After these two applies it raises every night until someone notices.

**Required change.** One additional migration (or one anchored `do $$` block per file, following the
`2026-08-20-live-progress-engine-allow.sql` idiom) that re-derives `hr_assert_grant_hygiene` with two
new `c_engine_allow` entries, each with the one-line justification the array's own contract demands:

```
'hr_vigour_of(uuid,integer)',
-- read-only (STABLE, writes nothing, cannot be replayed into a gain). Two indexed point
-- reads on player_progress for ONE character plus hr_offline_cap_ms, which the engine
-- already holds. Its period keys are the server's own hr_utc_day_key(now()), never an
-- argument, so no call can widen the row set. NO NEW TARGET: p_user is the parameter the
-- engine already passes to hr_apply and hr_state_of.
'hr_hunt_analyzer(uuid,integer)',
-- read-only (STABLE) arithmetic over ONE character's own player_ledger rows since its own
-- active_since, priced from the sealed hr_items catalogue. Writes nothing, calls nothing
-- that writes. Same target argument: p_user is the parameter the engine already passes to
-- hr_state_of, whose envelope this is a narrower read of.
```

Do **not** fix this with `hr_grant_baseline_sync()` or by relaxing `p_strict`. Re-derive with
`tools/derive-grant-hygiene.mjs` and re-run `--check`.

**Pre-apply gate:** after the last of the four applies, run
`select public.hr_assert_grant_hygiene(true);` and require it to return without raising, **before**
the client half ships. That single statement would have caught this, and it is in the runbook in §4.

---

### S-3 — a ceiling-clamped refill charges full price and reports the full 120 minutes **[P1]**

`vigour-refill.sql:202` refuses only the case where the refill would buy **nothing**:

```sql
if coalesce((v_vig->>'budget_min')::int, 0) >= coalesce((v_vig->>'ceiling_min')::int, 0) then
  … return 'vigour_ceiling' …
```

The file's comment is explicit that this exists so "a player at a 22-hour grant would [not] pay 2,000
gold for air". It does not cover **partial** air. `hr_vigour_of` clamps with
`least(c_ceiling_min, v_grant + v_bought)` (`vigour-daily.sql:110`), so a refill that crosses the
ceiling delivers less than the 120 minutes it charges for — and `:254` reports the catalogue's
figure regardless:

```sql
v_result := jsonb_build_object('ok', true, 'nth', v_nth, 'cost', v_cost, 'minutes', v_min, …);
```

`v_min` is `hr_vigour_prices.minutes` = 120, unconditionally.

**Reachable, with whole-hour grants.** `offlineCapHours()` is 12 base plus renown perks plus property
tier (up to +4 at castle), so a 15-hour character has a 900-minute grant. After three refills the
budget is 1,260; the fourth is sold at **54,000 gold** and raises the budget to
`least(1320, 1380) = 1320` — **60 minutes delivered, 120 reported**. At a 16-hour grant (960) the
third refill delivers 120 and the ladder saturates; at 15 hours the fourth is half-price-for-full-fee
and the fifth would be pure air but is correctly refused by the same check *after* the fourth landed.

This is the `CLAUDE.md` §6 class inside a gold verb: the server takes real gold, tells the client
"120 minutes", and the meter on the same envelope says 60. The panel prints the receipt.

**Required change.** Compute the delivered minutes before the debit and either refuse or price
accordingly — refusing is the better answer and matches the file's own `already_owned` reasoning:

```sql
-- after v_cost/v_min are read, before the debit
if coalesce((v_vig->>'budget_min')::int,0) + v_min
     > coalesce((v_vig->>'ceiling_min')::int,0) then
  perform public.hr_record_rejection(v_uid, v_slot, 'vigour_refill', 'vigour_ceiling', …, 1);
  return jsonb_build_object('ok', false, 'error', 'vigour_ceiling', …);
end if;
```

If the Designer prefers to sell the partial, then `v_result` and the ledger `meta` must carry the
**delivered** minutes, not the catalogue's — a receipt that overstates what was bought is the defect
whatever the policy. Add a §7 arm on a perked probe character (the file already builds one for the
ceiling arms) that drives the ladder to the clamp and asserts the reported minutes equal the budget
delta.

---

### S-4 — `p_idem` is optional on a money verb **[P2]**

`vigour-refill.sql:278` — `create or replace function public.hr_vigour_refill(p_slot int default 0,
p_idem uuid default null)`. The defaults are justified in the header (PostgREST resolves by named
arguments; a two-argument function with no defaults answers PGRST202), and that reasoning is right
for `p_slot`. It is not right for `p_idem`, because the body treats a null key as "no idempotency at
all", on **both** ends:

- `:151` `hr_intent_replay` returns null immediately when `p_idem is null`, so nothing is looked up;
- `:261` `if p_idem is not null then insert into player_intents …`, so nothing is recorded.

A client that posts `{"p_slot":0}` therefore gets a verb that debits on every call. The per-day cap
bounds the loss at five rungs, so this is not a dupe and not a faucet — but it is a **real player
losing real gold to a double-tap or a retry on a flaky connection**, and the fix is one line. The
header's own §(9) comment ("the client generates a fresh key per gesture") is an assumption about a
client, which is exactly the kind of assumption a server verb must not hold.

**Required change.** Refuse a null key by name, before the advisory lock:

```sql
if p_idem is null then
  return jsonb_build_object('ok', false, 'error', 'missing_idem');
end if;
```

Keep the `default null` in the signature for the PostgREST reason; refuse it in the body. Add a §7
arm asserting a null-key call is refused and moves no gold, and add `missing_idem` to
`STATELESS_REFUSALS` if the client ever calls this verb.

---

### A-1 — the Analyzer runs five unbounded ledger passes on every envelope **[P2]**

`hunt-analyzer.sql:262` splices `'hunt_analyzer', public.hr_hunt_analyzer(p_user, v_st.slot),` into
`hr_state_of`, which is the envelope builder — it runs on every accrue poll, every intent, every
reload and (once combat is ticked) every world-tick settle for every rostered character.

The header's bound is not the bound that applies:

> "player_ledger_user_idx (user_id, slot, at desc) already exists, and a hunt is bounded below by
> `active_since` and above by `ACCRUE_MAX_SPAN_MS` (24 h) **per window**."

`ACCRUE_MAX_SPAN_MS` bounds **one window's length**, not the **number of windows** since
`active_since`. `active_since` moves only on `restart: true`. A character left on one monster and
settled at the ordinary cadence writes ~1,440 accrue rows a day, and `player_ledger`'s retention is
**90 days** — so the honest upper bound on the scan is ~130,000 rows, and there is no `LIMIT`, no
time floor and no early exit.

Over those rows the function makes **five separate passes** (`:146`, `:157`, `:171`, `:180`, `:186`),
two of which expand `jsonb_each_text` over `meta->'delta'->'i'` and `->'x'` and join `hr_items` /
`hr_skills`. That is five index scans plus two lateral expansions **per envelope**, for a block most
players will not have open.

It is correctly scoped — the `active_kind is distinct from 'combat'` early return at `:126` means
non-hunting characters pay nothing, and I confirm this is a **cost** finding, not a leak: `p_user` is
`hr_state_of`'s own parameter, every predicate is `user_id = p_user and slot = …`, the function is
`hr_engine`-only, and a forged call returns a different shape of the caller's own rows.

**Required change**, in the order I would do them:

1. **Fold passes 1–3 into one scan.** Passes 2 and 3 select the same rows as pass 1 with a different
   lateral; a single `select … from player_ledger l left join lateral (…) ` computes all of them in
   one pass. Pass 4 (deaths) has a different `intent` and pass 5 is `order by … limit 1`, which the
   index serves.
2. **Floor the lower bound.** `l.at > greatest(v_st.active_since, v_now - interval '24 hours')` is
   honest for a readout whose every rate is already per-hour, and it makes the scan bounded by the
   accrual cap the header already claims it is bounded by. If the Designer wants lifetime-of-hunt
   totals, they need a rollup, not a wider scan.
3. **Measure it.** `explain (analyze, buffers)` on a character with 50,000 accrue rows since
   `active_since`, before and after, in the post-apply verification of §4. A readout that costs more
   than the settle it describes is a readout that will be turned off under load.

There is no separate rate gate on this path and I do not think one is wanted: it inherits
`hr_state_of`'s callers, which are already gated. Making the query cheap is the right control.

---

### H-1 — `hr_hunt_stances`' knob columns are decorative **[P2]**

`hunt-stance-stop.sql:72-85` creates a three-column catalogue and seeds it, and the header says the
table exists because "'validated against a server catalogue' is the property the design asks for and
a CHECK list is a second copy of `src/core/hunt.js` STANCES that no query can compare against".

The **validation** genuinely uses it — `hr_apply`'s patched arm does
`not exists (select 1 from hr_hunt_stances where stance_id = v_act->>'stance')`, which is the right
authority and I verified it is the only stance check that cannot be bypassed by a compromised edge.

The **values** are read by nothing. The engine resolves a stance through `stanceOf(inp.huntStance)`
(`accrual.js:1789`), which reads the frozen JS `STANCES` table. `eat_at`, `ammo_dry` and `falls` in
SQL are never selected by any function, any RPC or any client — I searched the repo and
`src/render/hunt-panel.js` does not read the table either (it renders `STANCE_WORDS`, a local label
map, and the server-projected `hunt_stance` string).

So: an operator who tunes `hr_hunt_stances.eat_at` changes nothing and is told nothing. That is the
"two places that can disagree" shape the design spent §9 rejecting, arriving by the other door — and
it is worse than a plain duplicate because one copy is silently inert. `tests/hunt-stance-stop.mjs`
S2 compares the two by regex over the **migration text**, which keeps the seed honest but does not
make the table load-bearing.

**Required change** — pick one and say which in the file:

- **(a)** Say so in the table comment and in the header: the catalogue is an **id allowlist**; the
  knob columns are documentation of what the engine does, are not read at runtime, and a fourth
  column is still a finding. Keep S2. Cheapest, and honest.
- **(b)** Make the engine read them — pass the row through the envelope beside `hunt_stance` and let
  `stanceOf` take values rather than look them up. This is the version that makes the table mean what
  the header says, and it is a bigger change than this lane should take on today.

I do not block on this, but (a) must land: a catalogue that claims to be authority and is not will be
tuned by somebody eventually.

---

### H-2 — the stop-shape CHECK can raise instead of returning false **[P3, defence-in-depth]**

`hr_hunt_stop_valid` (`hunt-stance-stop.sql:113-127`) tests the digits and then casts:

```sql
when (p_stop ? 'hours') and not (jsonb_typeof(p_stop->'hours') = 'number'
      and (p_stop->>'hours') ~ '^\d+$'
      and (p_stop->>'hours')::bigint between 1 and 24) then false
```

`jsonb` numbers are `numeric` and hold far more than a `bigint`. `{"hours": 1e30}` passes
`jsonb_typeof = 'number'`, renders as 31 digits, matches `^\d+$`, and then overflows the cast —
raising `22003` from inside a CHECK constraint rather than returning `false`.

**Unreachable today**, and I want to be precise about why rather than inflate it: `hr_apply` is
engine-only, the edge's `validateStop` refuses anything that is not `Number.isInteger` and in range
(`src/core/hunt.js:validateStop`), and `hr_apply`'s own patched arm calls this function and turns a
`false` into a named `bad_stop` refusal first. So the only way in is a caller that reaches `hr_apply`
without the edge — which is the case a re-validation layer exists for.

**Required change.** Cast to `numeric`, which cannot overflow here, or bound the length first:

```sql
and (p_stop->>'hours')::numeric between 1 and 24
```

Same for `food_floor`, `ammo_floor`, `falls`. One arm in §6 that feeds `{"hours": 1e30}` and requires
`false` rather than a raise.

---

### C-1 — the client is clean, and the one thing it renders is wrong **[P2]**

I reviewed `src/render/hunt-panel.js`, `src/net/accrue.js` (`hydrateHunt`) and `src/net/activity.js`
against `CLAUDE.md` §6 and found **nothing client-authored**:

- every value lands in `_`-prefixed scratch (`G._hunt`, `G._vigour`, `G._huntAnalyzer`), which is
  never persisted and absent on a cold boot;
- nothing is in `RESIDUE_FIELDS`, nothing is merged upward, and `hunt_analyzer: null` **replaces**
  the previous reading rather than being coalesced, which is the right answer to the phantom-seed
  class;
- `hydrateHunt` reads by `hasOwnProperty`, so "this build has no hunts" and "no stance chosen" stay
  distinguishable and an older deployment renders no panel rather than a fabricated one;
- there is no refill button (design §6 ships Vigour read-only) and no client-side kill counter;
- the one wall-clock value is the elapsed pill, which nothing gates on and nothing can spend.

The naming call — `hydrateHunt`, not `reconcileHunt`, because there is nothing to reconcile — is
correct and I would have raised it the other way round.

**The one change:** the panel renders a Vigour bar from `spent_min` / `remaining_min`, and S-1 makes
those numbers wrong by up to 100%. Ship the Analyzer block and the stance/stop sentence; hold the
Vigour meter (or render it with the "settled HH:MM UTC" honesty line and no bar) until S-1 lands.
Shipping a limiter bar that reads "11h 20m remaining" all night for a player who has hunted twelve
hours is the exact class Tyler ruled on 2026-09-14, in the feature that quotes that ruling most.

---

## 2. What I checked and could not break

Stated as closed / bounded / remaining, per the role's contract.

**CLOSED.**

| question from the brief | answer | how |
|---|---|---|
| Can a client pay less than the server price? | **No.** The wire carries a slot and a uuid — no amount, no price, no currency. `v_cost` is read from `hr_vigour_prices` inside the same transaction, after the advisory lock (`:180`). §7 GATE(a) refuses any 3+ digit literal in the verb body, so the price cannot migrate into code | code read + the file's own executed gate |
| Is the ledger row unconditional? | **Yes.** `:240` is unguarded, after the debit, carrying the signed movement `-v_cost` with `gold_in/xp_in/qty_in/gems_in` explicit zeros. §7(e2) asserts exactly one row and its sign | code read |
| Can `p_idem` be replayed across days? | **No.** `v_intent := 'vigour_refill:' || v_day` (`:158`) and `hr_intent_replay` compares the stored intent — a next-day reuse answers `intent_mismatch` and moves nothing | code read of `2026-09-03-intent-mismatch-class.sql:266-286` |
| …across slots? | **No.** `hr_intent_replay` compares `v_row.slot is distinct from p_slot` as well as the intent. I checked this specifically because the cache insert at `:262` conflicts on `(user_id, intent_id)` only, so the slot half had to come from the reader — it does | code read |
| Double-spend under concurrency, two refills in flight? | **No.** `pg_advisory_xact_lock(hashtextextended(v_uid || ':' || v_slot, 0))` at `:153`, taken **before** the replay read, and I verified the key is byte-identical to `hr_apply`'s (`2026-09-14-hr-apply-restatement.sql:764`) — so a refill also serialises against the settle that spends the minutes it adds. `select … for update` on `player_state` beneath it. Gold cannot go negative: the check and the update are one statement apart under that row lock | code read, both keys compared |
| Is the wrapper rate-gated? | **Yes.** `hr_rpc_gate('hr_vigour_refill')` at 6/min, patched into the gate's `case` at an exactly-once anchor that raises otherwise. An unknown bucket fails **closed**, so a failed patch ships the verb dead rather than ungated | code read; §7 GATE(c) executes it |
| Is `__ungated` unreachable? | **Yes.** Revoked from `public, anon, authenticated, service_role`; §7 GATE(c2) asserts `has_function_privilege('authenticated', …)` is false for the ungated body **and** true for the wrapper, so it cannot ship dead either. `hr_client_rpc_baseline` carries a targeted row for the wrapper only — not a `hr_grant_baseline_sync()` re-approval of the whole surface, which would have been the shortcut | code read |
| Does a stance carry a multiplier? | **No, and the one place it nearly did is closed.** `STANCE_KEYS` is a four-key allowlist enforced by `assertNoStanceMultiplier` at module load; §6 of the migration asserts `hr_hunt_stances`' column set from `information_schema`. The real find is theirs: `careful` asks 0.75 while Auto-Eat I's ceiling is 25%, so `accrual.js:1817` clamps with `Math.min(stance.eatAt, eatCeiling)` where the ceiling is derived from the character's **traits** and defaults to **tier 0** (the lower ceiling) when absent — fail-closed in the right direction. `tests/hunt-stance-stop.mjs --mutate=stance_bypasses_tier` patches the clamp out of the engine and the arm goes red | code read + their mutation |
| Server-side stance validation, attended **and** away? | **Yes, one engine.** `hr_apply`'s patched arm is the authority (`:210-218`), the edge asks the same question a round trip earlier, and `stanceOf` uses `hasOwnProperty` so `__proto__` and `constructor` fail safe to `steady`. `tests/hunt-stance-stop.mjs --mutate=stop_away_only` makes the stop predicate away-only and the AWAY-1 arm goes red | code read + their mutation |
| Does the world tick inherit the stop predicate? | **Not applicable today, and correctly so.** There are exactly **two** callers of `computeAccrual` repo-wide — `index.ts:857` and `set-activity.js:908` — and both pass the four new fields identically (the A14 mirror). The tick's only channel is `gather` (`tick-gather.js:74`) and `tick-gather.js` is not a combat path, so there is no third implementation to disagree. **This becomes a gate the day `combat` joins `hr_tick_config.channels`** — see S-1(3) | repo-wide grep for `computeAccrual` |
| Can the budget be exceeded to earn **more** than a player without Vigour? | **No — the ceiling is hard and I measured it.** `hr_vigour_of` applies `least(c_ceiling_min, grant + bought)` at `:110`, once, and every reader sees the capped number; the refill clamps `refills` to 5 on **read** so a corrupted counter cannot widen it (§3(c4) writes 99 and requires 5 back). Past the budget `vigourMult` blends at `VIGOUR_DRY_MULT`, which is **imported from `AMMO_DRY_MULT`** rather than retyped, so the two cannot drift. **The measured ceiling is 1,320 paid minutes (22 h) per UTC day**, reachable only via `grant + 5×120` clamped down — and gold buys at most 600 of them. Loot rides the drop **chance** (`ctx.weakness` folding into `dropMult`) rather than the quantity, which I checked is a reduction the 0.95 cap cannot mask (it would need a pre-cap probability above 3.8) and which avoids `floor(1 × 0.25) = 0` making rare drops deterministically unobtainable while tired. That is the right call and better than the design asked for | code read + arithmetic |
| Can the UTC day boundary be gamed by a client clock? | **No.** The day key is `hr_utc_day_key(now())` computed **in the database** — in the refill after the advisory lock (`:157`), and in the engine handed over from the envelope, never derived from `nowMs`. `accrual.js:2919` is explicit that the boundary is "a database spelling" and charges nothing when `vigIn.day_key` is absent | code read |
| Does the Analyzer read any client number? | **No.** Every input is a `player_ledger` row `hr_apply` wrote out of a settled window, `player_state`, `hr_items` or `hr_skills`. No `meta` field the client supplies reaches it | code read, field by field |
| Does it leak another user's rows? | **No.** `p_user` is `hr_state_of`'s own parameter, every predicate is `user_id = p_user`, and the function is `hr_engine`-only with `authenticated`/`anon`/`service_role` revoked. It writes no row, so it cannot be replayed into a gain | code read |
| Is loot priced from the server catalogue? | **Yes, vendor value.** `hr_items.value`, joined `left` so a retired row prices 0 rather than aborting the readout. §3 GATE(b) refuses the function if it names a market surface at all | code read + their gate |
| Are the `hr_state_of` patches programmatic? | **Yes, all three, and each raises rather than no-ops.** Each asserts its anchor matches **exactly once** and `raise`s otherwise; each is a no-op on re-apply via a marker check; the spliced text is 4 lines each. `hr_apply`'s two patches are the same idiom. No restatement of either body | code read |
| Byte-identical second apply? | **Yes.** `node tests/schema-drift.mjs` → **exit 0**, "repo rebuilds to the committed fingerprint (960788fbfffb…)" | executed |

**BOUNDED AND JOURNALLED.** Every refill writes one `player_ledger` row with `kind='vigour'`, the
signed movement, the rung, the day key and the idem key, so a support request about a missing 18,000
gold is answerable from the append-only record. Every refusal writes an `hr_rejections` aggregate by
name (`vigour_daily_cap`, `vigour_ceiling`, `insufficient_gold`). The `player_ledger_kind_check`
widen is an **insertion at an asserted-once `'accrue'::text` anchor**, never a restatement — so it
cannot silently drop a kind another file added.

**REMAINING, ACCEPTED.** (i) A player who forges a stance or a stop rule buys themselves a different
auto-eat threshold and a different bedtime — the design chose a field not worth forging and I agree
with the choice. (ii) Nothing in this feature is tradeable, rankable or contributable, so the Target
Property is satisfied by construction **until a "top XP/h this week" board exists**; that board
re-opens this review. (iii) The `bag_full` rule is accepted, validated and stored but can never fire
(`bagFree` is passed `null` because the game has no bag capacity); `evaluateStop` fails safe on it
and the panel does not print it, which is the honest handling — but it is a stop rule a player can
set that will never stop anything, and it is the Designer's to rule on.

---

## 3. Guards — real exit codes

Run in this container on `sec/m6-hunts` @ the lane head, after `npm install --no-audit --no-fund`.

| command | exit | last line |
|---|---|---|
| `node tests/selfcheck-no-global-dml.mjs` | **0** | `OK — 211 migrations, 9 global statement(s), all 9 acknowledged with a written reason` |
| `node tests/apply-order-honesty.mjs` | **0** | `30 file(s) carry a measured verdict (30 evidenced-live, 0 evidenced-absent)` |
| `node tests/schema-drift.mjs` | **0** | `OK — repo rebuilds to the committed fingerprint (960788fbfffb…)` |
| `node tests/guard-hygiene.mjs` | **0** | `PASSED — no orphans, no ghosts, no stale entries, no vacuous proofs` (13 allowlisted, incl. mine) |
| `node tools/derive-grant-hygiene.mjs --check` | **0** | `derivation in sync (10 links, 11 patches)` — **and that is S-2: the two new engine grants are not in it** |
| `node tests/vigour-charge-conservation.mjs` | **1** | **RED BY DESIGN** — five arms, finding S-1 |
| `node tests/vigour-charge-conservation.mjs --selftest` | **0** | `the fix makes every arm green and the shipped engine is red` |
| `node tools/lane-done.mjs` | **0** | `lane-done: all green.` — every ratchet, `no-client-xp-mint`, `property-gate-census`, `dead-exports`, `window-globals-exist`, `no-duplicate-toplevel-fns`, `token-single-source`, `css-literal-ratchet`, `ci-shape`, `guard-hygiene`, `bump-version.sh --check`. Run twice: on the lane head at `76af0943`, and again on `sec/m6-hunts` with my two files added. **Both exit 0**, 24 steps, no red |
| `node tests/grant-hygiene*.mjs` | **n/a** | **no such file.** The only match under `tests/` is `companion-grant-hardening.mjs`. Grant hygiene is enforced by the **database** function `hr_assert_grant_hygiene`, which is why S-2 is invisible to CI and must be a post-apply step |

**Baselines — are the regenerations honest?** Yes, both, and I checked by set comparison rather than
by reading the diff.

- `tests/restore-census.baseline.json` — 111 → 113 tables. **Removed: none.** Added: exactly
  `hr_hunt_stances` and `hr_vigour_prices`, this lane's own. The only changed existing entry is
  `hr_client_rpc_baseline` (`replay` 80 → 81, the one new RPC row). The diff *looks* like it touches
  six unrelated tables (`hearthfind_log`, `hearthfind_ordinal`, the four `hr_tick_*`) — those are
  **re-sorted into alphabetical position**, not re-classified; their JSON is byte-identical. Both new
  entries carry real notes: `hr_vigour_prices` is `operator_tunable` with a `live_value` block that
  says in so many words it is the **authored** ladder and not a measurement because the file is
  unapplied, and that the Coordinator must re-measure after the apply and after any of Tyler's
  rulings. That is exactly right.
- `tests/schema-drift.baseline.json` — every count delta is accounted for by this lane's objects:
  relations +2, policies +2, rls +2 (the two tables), indexes +2 (their pkeys), constraints +10
  (4 + 4 + the two on `player_state`), columns +9 (4 + 3 + 2), functions +5
  (`hr_hunt_analyzer`, `hr_hunt_stop_valid`, `hr_vigour_of`, `hr_vigour_refill`,
  `hr_vigour_refill__ungated`). **Nothing hides in it.** One limitation the Coordinator should know
  rather than infer: this baseline records function **signatures**, not bodies — so the two `hr_apply`
  patches, the three `hr_state_of` patches and the `hr_rpc_gate` patch are **invisible** to
  `schema-drift`. `tests/live-hash-drift.baseline.json` is the guard that sees those, and re-measuring
  it after the apply is in the runbook below for that reason.

**`tests/schema-apply-order.json`** gained the four files in the order §4 uses, each marked staged.
`apply-order-honesty` agrees with `live-hash-drift.baseline.json` about all 30 measured files.

**CI registration** is honest: `.github/workflows/smoke.yml` gains six steps — a `--selftest` **and**
a plain base run for each of the three new guards — which satisfies the proof-with-no-floor rule
(`guard-hygiene` rule 2) without needing a `proof_baseline` exemption.

---

## 4. Coordinator runbook

**Do not start until S-1 and S-2 are fixed and re-reviewed.** The order below is what to run once
they are, and nothing in it substitutes for the verdicts above.

`CLAUDE.md` §2: one file per `node tools/apply-migration.mjs` call, never inside `begin/commit`,
never during 00:00–00:10 UTC, Coordinator only. The order is forced by the PRECONDITIONS blocks —
each file refuses to apply if its predecessor has not.

### 4.1 Apply order

```bash
node tools/apply-migration.mjs supabase/migrations/2026-09-22-hunt-stance-stop.sql
node tools/apply-migration.mjs supabase/migrations/2026-09-22-vigour-daily.sql
node tools/apply-migration.mjs supabase/migrations/2026-09-22-hunt-analyzer.sql
node tools/apply-migration.mjs supabase/migrations/2026-09-22-vigour-refill.sql
# then, and it is NOT optional — see S-2:
node tools/apply-migration.mjs supabase/migrations/2026-09-22-engine-allowlist-hunts.sql
```

Why this order and not the file names' alphabetical one: `vigour-daily` declares
`hr_offline_cap_ms` / `hr_utc_day_key` / `hr_state_of`; `hunt-analyzer` declares
`player_state.hunt_stance`, so **stance-stop must precede it**; `vigour-refill` declares
`hr_vigour_of`, so **vigour-daily must precede it**. Each raises by name if you get it wrong, which
is the right failure. The fifth file is S-2's fix and must exist before the first four are considered
applied.

Watch each apply's `raise notice` lines. Each file ends with one long "all green, net zero" notice
from its §-gate; **an apply that prints no such line did not run its gate** and must be investigated,
not retried.

### 4.2 Read-only post-apply verification

Hand these to the verification agent as **read-only** SQL. Nothing below writes.

**(1) S-2 — the detector must not raise.** Run this FIRST; it is the one that fails today.

```sql
select public.hr_assert_grant_hygiene(true);
```

Expect a JSON report and **no exception**. If it raises, read
`engine_execute_outside_allowlist` in the message: `hr_vigour_of(uuid,integer)` and/or
`hr_hunt_analyzer(uuid,integer)` there means the fifth migration did not land. Do **not** call it
with `false` to make it quiet and do **not** call `hr_grant_baseline_sync()`.

**(2) The client surface is exactly what was approved — one new verb, nothing else.**

```sql
select p.oid::regprocedure::text as fn,
       has_function_privilege('authenticated', p.oid, 'execute') as auth_exec,
       p.prosecdef as secdef,
       (p.prosrc ~ 'hr_rpc_gate|hr_rate_gate|hr_rate_ok') as mentions_gate
  from pg_proc p join pg_namespace n on n.oid = p.pronamespace
 where n.nspname = 'public'
   and p.proname in ('hr_vigour_refill','hr_vigour_refill__ungated',
                     'hr_vigour_of','hr_hunt_analyzer','hr_hunt_stop_valid')
 order by 1;
```

Expect `hr_vigour_refill(integer,uuid)` → `auth_exec = true`, `secdef = true`,
`mentions_gate = true`. **Every other row must be `auth_exec = false`.** A `true` anywhere else is a
stop-the-line.

**(3) The engine holds execute on the two reads and no table privileges.**

```sql
select p.oid::regprocedure::text
  from pg_proc p join pg_namespace n on n.oid = p.pronamespace
 where n.nspname='public' and has_function_privilege('hr_engine', p.oid, 'execute')
   and p.proname in ('hr_vigour_of','hr_hunt_analyzer')
 order by 1;

select count(*) as engine_table_privs
  from information_schema.role_table_grants
 where table_schema='public' and grantee='hr_engine';
```

Expect both functions listed, and `engine_table_privs = 0`.

**(4) The price ladder is the authored one, and it is the cap.**

```sql
select nth, cost_gold, minutes from public.hr_vigour_prices order by nth;
select count(*) as rungs from public.hr_vigour_prices;
```

Expect 1/2000, 2/6000, 3/18000, 4/54000, 5/162000, all 120 minutes, `rungs = 5`. **The row count IS
the per-day cap** — `hr_vigour_refill` refuses an `nth` with no row — so a sixth row is a balance
change and needs this review again. **Re-measure the `live_value` block in
`tests/restore-census.baseline.json` from this output**, and again after any of Tyler's rulings land,
or a rebuild silently reverts a tuned economy to the placeholders.

**(5) The stance catalogue has three rows and exactly four columns.**

```sql
select stance_id, eat_at, ammo_dry, falls from public.hr_hunt_stances order by stance_id;
select string_agg(column_name, ',' order by ordinal_position) as cols
  from information_schema.columns
 where table_schema='public' and table_name='hr_hunt_stances';
```

Expect careful/0.75/stop/2, reckless/0.25/swing/null, steady/0.50/swing/null, and
`cols = 'stance_id,eat_at,ammo_dry,falls'`. **A fifth column is a multiplier wearing a name.**

**(6) The two columns, their FK and their CHECK are live.**

```sql
select column_name, data_type, is_nullable
  from information_schema.columns
 where table_schema='public' and table_name='player_state'
   and column_name in ('hunt_stance','hunt_stop');

select conname, pg_get_constraintdef(oid)
  from pg_constraint
 where conrelid='public.player_state'::regclass
   and conname in ('player_state_hunt_stance_fk','player_state_hunt_stop_shape');
```

Both nullable; the FK references `hr_hunt_stances(stance_id)`; the CHECK is
`hr_hunt_stop_valid(hunt_stop)`.

**(7) The three envelope patches all landed, and `hr_apply` accepts exactly five activity keys.**

```sql
select strpos(pg_get_functiondef('public.hr_state_of(uuid,int)'::regprocedure),
              '''hunt_stance'', v_st.hunt_stance') > 0 as projects_stance,
       strpos(pg_get_functiondef('public.hr_state_of(uuid,int)'::regprocedure),
              '''vigour'', public.hr_vigour_of') > 0 as projects_vigour,
       strpos(pg_get_functiondef('public.hr_state_of(uuid,int)'::regprocedure),
              '''hunt_analyzer'', public.hr_hunt_analyzer') > 0 as projects_analyzer,
       strpos(pg_get_functiondef('public.hr_apply(uuid,integer,bigint,uuid,jsonb)'::regprocedure),
              'HUNT STANCE AND STOP (2026-09-22)') > 0 as apply_patched,
       strpos(pg_get_functiondef('public.hr_apply(uuid,integer,bigint,uuid,jsonb)'::regprocedure),
              '''kind'',''id'',''restart'',''stance'',''stop''') > 0 as allowlist_is_five;
```

All five `true`. `allowlist_is_five = false` with `apply_patched = true` would mean the key allowlist
patch was lost — a stop-the-line, because it is the line that has been refusing these fields.

**(8) The rate bucket exists, and the ledger kind widened by insertion.**

```sql
select strpos(pg_get_functiondef('public.hr_rpc_gate(text)'::regprocedure),
              '''hr_vigour_refill''') > 0 as bucket_present;

select pg_get_constraintdef(oid) as kind_check
  from pg_constraint
 where conrelid='public.player_ledger'::regclass and conname='player_ledger_kind_check';
```

`bucket_present = true` — an unknown bucket fails **closed**, so a missing one ships the verb
permanently `rate_limited`. The `kind_check` must still list **every** kind it listed before, plus
`'vigour'`.

**(9) Nothing was fabricated.** `CLAUDE.md` §2 — the self-checks roll their probes back.

```sql
select count(*) as probe_rows from public.player_state
 where user_id in ('00000000-0000-4000-8000-0000b5510001',
                   '00000000-0000-4000-8000-0000b5510002',
                   '00000000-0000-4000-8000-0000b5510004');
select count(*) as probe_users from auth.users
 where id in ('00000000-0000-4000-8000-0000b5510001',
              '00000000-0000-4000-8000-0000b5510002',
              '00000000-0000-4000-8000-0000b5510004');
```

Both **0**.

**(10) The Analyzer's cost, measured rather than assumed** (A-1). On a real character on a combat
pointer with the most accrue rows since `active_since`:

```sql
explain (analyze, buffers)
select public.hr_hunt_analyzer(user_id, slot) from public.player_state
 where active_kind = 'combat' and active_since is not null
 order by active_since asc limit 1;
```

Record the timing next to the row count from:

```sql
select count(*) from public.player_ledger l
  join public.player_state s on s.user_id = l.user_id and s.slot = l.slot
 where s.active_kind='combat' and l.kind='combat' and l.intent='accrue'
   and l.at > s.active_since;
```

If the readout costs more than the settle it describes, A-1's changes are not optional.

### 4.3 After the applies

1. `node tests/live-hash-drift.mjs --live --write`, then write the whys from `--codediff`.
   **This is the guard that sees the two `hr_apply` patches, the three `hr_state_of` patches and the
   `hr_rpc_gate` patch** — `schema-drift` does not (§3).
2. Flip the four apply-order notes to APPLIED.
3. `node tools/restore-census.mjs` and re-pin — classifying nothing new, but **re-measuring
   `hr_vigour_prices.live_value` from step (4) above**.
4. Deploy the edge (`supabase/functions/**` moved) and verify `payload_sha256` equals
   `pack-edge --hash`. **The engine half and the DB half must land together**: an edge that sends
   `vigour` to a database with no `hr_vigour_of` is safe (the projection is absent and the engine
   proposes no charge), but a database projecting `vigour` to an edge that ignores it means the panel
   shows a budget nothing charges against.
5. Only then does the client half ride a cut, per C-1.

---

## 5. Verdict

**BLOCKED** on `2026-09-22-vigour-daily.sql` and `2026-09-22-vigour-refill.sql`.

The unmet conditions, in full:

1. **S-1** — the Vigour charge must be conserved under window subdivision.
   `node tests/vigour-charge-conservation.mjs` exits 0, its `guards-unregistered.json` entry is
   deleted and its plain run is registered in `smoke.yml`.
2. **S-2** — `hr_vigour_of` and `hr_hunt_analyzer` are added to `c_engine_allow` with their
   justifications, `node tools/derive-grant-hygiene.mjs --check` exits 0, and
   `select public.hr_assert_grant_hygiene(true);` returns without raising after the applies.
3. **S-3** — a ceiling-clamped refill is refused, or the receipt and the ledger carry the
   **delivered** minutes; with a §7 arm on a perked probe.
4. **S-4** — a null `p_idem` is refused by name, with a §7 arm.
5. **I-3** — Tyler's four §4.6 figures (2,000 opening, ×3, 5-refill cap, `VIGOUR_DRY_MULT` 0.25 or
   0.00) are answered and the answer is recorded. They are catalogue rows, so his answer is a
   reviewed `UPDATE`, not a re-review — but the refill verb must not reach production with four
   unanswered money numbers in it. Design §6 ships Vigour read-only with **no refill button**, so
   nothing about the playable slice waits on this.

`2026-09-22-hunt-stance-stop.sql` and `2026-09-22-hunt-analyzer.sql` are **GO-WITH-CHANGES** and can
apply ahead of the vigour pair once **S-2** and **A-1** land; H-1(a) and H-2 may ride the same file.
The client half is **GO-WITH-CHANGES** per C-1 and is otherwise the cleanest half of this lane.

**Residual risks I am accepting**, with their triggers: the hunt authors nothing tradeable, rankable
or contributable **until a top-XP/h board exists** — that board re-opens this review; `bag_full` is a
stop rule that can never fire and is the Designer's to rule on; the world tick inherits none of this
**until `combat` joins `hr_tick_config.channels`**, at which point S-1(3) and the A14 mirror both
become gates.

**Debt this lane names and I confirm:** three anchored patches on `hr_state_of` in one lane (chain
depth 5) and two on `hr_apply` (depth 3). The anchors are asserted exactly-once and raise, so nothing
can no-op in silence, and I agree a wholesale restatement inside a feature migration would be a diff
no reviewer could read. **A joint restatement of `hr_apply` and `hr_state_of` is owed before the next
patch on either**, and it is the Coordinator's to schedule.

---

# RE-VERIFY — 2026-09-23

**Reviewer:** security-engineer (veto) · **Re-verified:** `lane/m6-hunts-backend` @ `4778553f`
· **Branch:** `sec/m6-hunts-2`, cut from that head · `npm install --no-audit --no-fund`
**No production read and no production write.** Every proof below ran against the repo's own
engine and a PGlite replay of the repo's own chain, in this container.

| migration | verdict |
|---|---|
| `2026-09-22-hunt-stance-stop.sql` | **GO** (H-1, H-2 closed; it grants `hr_engine` nothing, so S-5 below does not touch it) |
| `2026-09-22-vigour-daily.sql` | **GO-WITH-CHANGES** (S-1 closed by execution; **S-5** — its engine grant has no carrier that can apply) |
| `2026-09-22-vigour-refill.sql` | **GO-WITH-CHANGES** (S-1/S-3/S-4 closed by execution; **I-3 still unmet**, and S-5 orders it) |
| `2026-09-22-hunt-analyzer.sql` | **GO-WITH-CHANGES** (A-1's bound closed; its cost is still unmeasured; **S-5**) |
| **CLIENT SHIP** | **GO** (C-1 closed — the bar is held in the RENDERED panel, proven here) |

**Seven of the eight findings are closed and I could not re-open any of them.** The eighth, I-3,
is open and the lane says so itself rather than inventing a number, which is the right answer to a
question that is not the lane's to answer.

**The one thing this re-verify found that the first review could not: the S-2 fix cannot apply.**
`2026-09-22-engine-allowlist-hunt-reads.sql` and M7's `2026-09-22-trophy-claim.sql` were **both cut
as link 11 off the same base**, and M7's went live at 00:41 UTC this morning. M6's file therefore
restates `hr_assert_grant_hygiene` from a body that predates the two trophy entries. Its own
`GATE(b)` catches that and **refuses the apply** — which is the file failing closed exactly as
designed, and also means the four M6 files have no way to leave the nightly detector green. That is
**S-5**, and it is the only thing between this lane and an apply.

---

## R1. Per finding — closed or not, with the proof that executed

| # | first verdict | now | the proof that ran |
|---|---|---|---|
| **S-1** | P0, BLOCK | **CLOSED** | `tests/vigour-charge-conservation.mjs` exit **0**; both mutation arms exit **1**; `tests/vigour.mjs` V3b partitions exit **0**; one engine, no second copy (below) |
| **S-2** | P1, BLOCKS the apply | **CLOSED in the file, BLOCKED in the chain — see S-5** | strict `hr_assert_grant_hygiene(true)` returns at chain end in the replay; a planted unlisted grant is named AND fatal |
| **S-3** | P1 | **CLOSED** | `vigour.mjs` V10 drives the partial on a clan-perked probe and both anti-vacuity preconditions held; `--mutate=refill_sells_partial` caught |
| **S-4** | P2 | **CLOSED** | V11: a null key is answered `missing_idem`, moves no gold, counts no rung; refused **before** the advisory lock; `--mutate=refill_idem_optional` caught |
| **A-1** | P2 | **CLOSED for the bound; the COST is still unmeasured** | five passes → **three bounded reads**, all `l.at > greatest(active_since, now()-24h)`; the denominator moved with it |
| **H-1** | P2 | **CLOSED**, answer (a) | GATE(b3) executes on every apply; the replay applied the file, so it ran |
| **H-2** | P3 | **CLOSED** | all four bounds cast `numeric`; GATE(b2) drives `1e30`/`1e40` on each of the four and treats a raise as a failure |
| **C-1** | P2 | **CLOSED** | I rendered the real `huntPanelHtml` with a FULL meter object: no `hunt-vigour`, no meter number, and `window_capped` prints the span |
| **I-3** | Info, condition 5 | **NOT CLOSED, and honestly so** | the four §4.6 figures are dated as placeholders; no number was invented |

### S-1 — the charge conserves, and it is ONE function on all three paths

`vigourCharge(windowMs) → { addMin, remMs }` (`src/core/hunt.js:428`) has exactly **one** call site in
the engine, `accrual.js:2945`, which proposes both counters; `hr_vigour_of` sums them and divides
**once** (`vigour-daily.sql` §1(4)). `floor(Σw/60000) = Σq + floor(Σr/60000)` is exact, so the total
is a function of elapsed time alone.

Measured against the real `computeAccrual` on this branch — the table that was the finding, re-run:

| how one hour of combat is settled | caller | paid | **Vigour charged** |
|---|---|---|---|
| one 3,600,000 ms window | `accrue` | 60.0 min | **60.0** |
| 40 × 90,000 ms | `accrue` | 60.0 min | **60.0** |
| 61 × 59,000 ms | `collect` (every `set_activity`) | 60.0 min | **60.0** |
| 40 × 90,000 ms | `tick` (the shipped flush) | 60.0 min | **60.0** |
| 80 × 45,000 ms | `tick` (`flush_seconds` at 45) | 60.0 min | **60.0** |

`tests/vigour.mjs` V3b partitions one hour at 1 s / 10 s / 90 s / 17 min and requires the sums equal
the whole to 1e-9 — exit **0**. Both mutation arms plant the defect back and both go **red** (exit 1):
`floor_per_window` (the original) and `remainder_refloored` (the half-fix a diff reader would miss).

**ONE ENGINE, and `services/world-tick` carries no second copy.** `shadow.js`, `contract.js` and
`gather.js` under `services/world-tick/` are `export *` re-exports of
`supabase/functions/hr-accrue/tick-{shadow,contract,gather}.js` — the production half moved there
because `pack-edge` can only vendor `src/core` and `src/data`. Attended (`index.ts:962`), away and
collect (`set-activity.js:971`) and the tick (`tick-shadow.js:171`) all reach the same
`computeAccrual`, and the charge is a pure function of `grantMs` with no carried state, so there is
nothing for the three to thread differently. `tests/world-tick-parity.mjs` exit **0**.

**Residual, and it is the shape S-1(3) left behind rather than a new defect:** `tick-shadow.js`
builds the engine input key by key and **does not include `vigour`**, so a ticked window proposes no
charge at all. `hr_tick_config.channels` defaults to `array['gather']`
(`2026-09-21-world-tick-settle-fence.sql:191`) and gather never charges Vigour by design, so this is
inert today. **It arms the day an operator adds `combat` to `channels`** — an `operator_tunable` row
still decides whether the limiter exists on that path, which is what S-1(3) said, now for a different
reason. The guard's C3 arm drives `caller:'tick'` through `computeAccrual` directly, so it proves the
engine and **not** the thread. When `combat` joins `channels`, `tick-shadow.js` must pass `vigour`
from the roster row and C3 must be re-pointed at the production input builder. Named, not blocking.

### S-2 — the detector is green at chain end, and still fatal

Run by me, not by the migration, in a PGlite replay of the repo's own chain with all five files
applied:

```
applied 2026-09-22-hunt-stance-stop.sql / vigour-daily / hunt-analyzer / vigour-refill /
        engine-allowlist-hunt-reads:                                            all true
hr_engine present in replay:                                                    true
select public.hr_assert_grant_hygiene(true)                RETURNED, no raise
engine_execute_outside_allowlist                           []
engine holds execute on   hr_hunt_analyzer(uuid,integer), hr_vigour_of(uuid,integer)
-- then I planted an unlisted engine grant of my own:
non-strict NAMES it                                        ["hr__sec_reverify_probe()"]
strict RAISES on it                                        GRANT HYGIENE FAILED: …
probe dropped, strict green again                          true
```

The condition as written is met. `node tools/derive-grant-hygiene.mjs --check` exit **0**,
"derivation in sync (11 links, 12 patches)". **And that `--check` is exactly why S-5 was invisible:
it grades the chain that is in the branch.**

### S-5 — the allowlist link was cut off a base that production has already moved past **[P1, NEW, BLOCKS the apply]**

`tools/derive-grant-hygiene.mjs` link 11 on this lane:

```
base:   2026-09-21-engine-allowlist-tick-settle.sql
target: 2026-09-22-engine-allowlist-hunt-reads.sql
```

`origin/next` link 11, from the M7 lane:

```
base:   2026-09-21-engine-allowlist-tick-settle.sql
target: 2026-09-22-trophy-claim.sql
```

Same base, two targets, and only one can be the last toucher of `hr_assert_grant_hygiene`. M7 won:
`2026-09-22-trophy-claim.sql` was **APPLIED to production 2026-09-23 00:41:24 UTC** and its live body
carries 23 entries including `hr_trophy_of` and `hr_trophy_claim`. M6's file restates the detector
from the body that predates them.

**Executed, in production's real order** — chain to `pg-net-queue-lockdown`, then the two APPLIED
trophy files, then M6's four, then M6's allowlist file:

```
applied  M7 state-of-trophy-prefix (LIVE 2026-09-23 00:40 UTC)
applied  M7 trophy-claim           (LIVE 2026-09-23 00:41 UTC)
  after M7, engine_execute_outside_allowlist = []
applied  M6 hunt-stance-stop / vigour-daily / hunt-analyzer / vigour-refill
  before the allowlist file        = ["hr_hunt_analyzer(uuid,integer)","hr_vigour_of(uuid,integer)"]
REFUSED  M6 engine-allowlist-hunt-reads.sql:
         GATE(b): hr_engine still holds EXECUTE outside its allowlist:
         ["hr_trophy_claim(uuid,integer,text,integer,text)", "hr_trophy_of(uuid,integer)"]
  FINAL outside allowlist          = ["hr_hunt_analyzer(uuid,integer)","hr_vigour_of(uuid,integer)"]
  FINAL strict hr_assert_grant_hygiene(true): RAISED
```

So the end state of following §4.1 today is **production with the nightly detector raising and no
committed file able to clear it** — the precise outcome S-2 was written to prevent, arriving from the
other side. `GATE(a2)` does not catch it: it names `hr_tick_settle` and `hr_quartermaster_buy`,
which are link 10 and earlier. `GATE(b)` does catch it, and rolls the apply back, which is the file
behaving correctly.

**Blast radius: none to a player, all to the control.** Nothing is granted that should not be and no
value moves; what breaks is the detector that would tell us if something were. A detector expected to
be red hides the next genuine regression, which is the whole of why S-2 was a block.

**Required change.**
1. Re-cut the link as **link 12**, `base: '2026-09-22-trophy-claim.sql'`,
   `target: '2026-09-22-engine-allowlist-hunt-reads.sql'`, and regenerate §2 with
   `node tools/derive-grant-hygiene.mjs`; `--check` exits 0 on the **merged** tree, not on the lane.
2. Extend `GATE(a2)` to also require `hr_trophy_of(uuid,integer)` and
   `hr_trophy_claim(uuid,integer,text,integer,text)` in the installed body, so the next link off this
   one cannot drop them either. The gate is only as good as the names in it.
3. Re-run the chain-end strict proof above on the merged tree. I will re-verify **S-5 only**; nothing
   else in this section needs re-doing.

**Would an existing guard have caught it?** No, and that is the structural finding underneath:
`derive-grant-hygiene --check`, `schema-drift` and `lane-done` all grade the chain **in the branch**,
so two lanes cutting a link off the same base is invisible on both branches and appears only at the
merge — or, without this probe, at the apply. **A guard that reads `origin/next`'s link list and
fails when two links share a base is owed**, and it is cheap; I am not making it a condition of this
lane, but it is the second time this month a lane-C file has been correct alone and wrong in the set.

### S-3 / S-4 — both branches, driven

S-3: `v_delivers := least(ceiling_min - budget_min, v_min)` is computed **before the debit**, and
`v_delivers < v_min` refuses — so the zero case and the partial case are one test. The refusal names
`would_deliver` alongside `minutes`. `vigour.mjs` V10 drives it on a clan-level-7 probe (900-minute
grant → 1,260 after three rungs, the fourth would deliver 60 of 120) and asserts the refusal, the
named shortfall and that the meter did not move; its **two anti-vacuity preconditions** both held on
the unmutated run, so the partial branch was genuinely reached. V10b re-adds the ceiling-on-READ arm
that S-3's refusal took away from V9, driving the counter through `hr_apply`.

One note on the arm rather than the fix: under `--mutate=refill_sells_partial` the mutation is
caught by V10's precondition ("the perked probe saturated the ceiling exactly") rather than by V10's
own assertion, because the mutated verb sells the fourth rung and saturates the setup. It is caught,
and the shipped path is proven by the unmutated run and by GATE(e7); a tighter arm would drive the
partial on a probe whose grant leaves a fraction **before** the loop saturates it. Info, not a
condition.

S-4: `p_idem is null` is refused by name at §(0) — **before** `pg_advisory_xact_lock`, so a call that
cannot be made safe does not first serialise every other call on that character — journalled through
`hr_record_rejection`, with `auth.uid()` and the slot already validated above it. §(9)'s intent cache
is now unconditional. The `default null` stays in the signature for the PostgREST reason, which was
always the right half of that argument.

### A-1 — bounded; the cost is still a post-apply measurement

Five passes are now three reads of `player_ledger`, each carrying
`l.at > greatest(v_st.active_since, v_now - interval '24 hours')`: one folded scan (windows + the
signed item map + combat XP, via two `left join lateral`s over a row-selection subquery), one for
deaths, one indexed `order by … limit 1`. `elapsed_ms` and `started_at` still describe the whole
hunt; every sum and rate divides by `window_ms`, and `window_from` / `window_capped` name the span on
the envelope — so a capped readout cannot divide one day of gold by five days of elapsed. The
row-selection subquery exists so the `at` bound stays inside the twelve lines `tests/ledger-rollup.mjs`
reads after each `from public.player_ledger`; pinning a read that **is** bounded would have been the
wrong fix and the lane says so.

**Step 3 of A-1 is not done and cannot be done from here.** `explain (analyze, buffers)` on a real
character is post-apply and is in the runbook at R4(10). The bound makes the worst case ~1,440 rows
instead of ~130,000, which is the structural answer; the measurement is still owed.

### C-1 — the bar is held, proven on the rendered panel

I loaded `src/render/hunt-panel.js` and called the real `huntPanelHtml` with a **full** meter object
(`spent_min: 512, budget_min: 720, remaining_min: 208, ceiling_min: 1320, dry_mult: 0.25`):

```
panel rendered with a FULL meter object, length 1914
any vigour markup or meter number present:   false
capped readout names its span:               true   ("totals cover the last 24h")
uncapped readout does NOT:                   true
```

`vigourRow` is gone; a dated block comment carries the restoration recipe and the markup. The in-page
arm in `src/features/smoke/hunt-raids-and-screens.js` passes a full meter on purpose and fails if
`hunt-vigour` appears, and the capped line comes from the server's `window_capped`, never from a clock
read in the browser. The client half is the cleanest part of this lane and was the cleanest part of
the last one.

### I-3 — still open, and the file is honest about it

`HUNTS_AND_ANALYZER.md` §4.6 now carries a dated line saying the four figures are **placeholders**,
that nothing in the repo records a ruling, and that `2026-09-22-vigour-refill.sql` waits on it.
Condition 5 said the figures must be **answered**, and they are not. That is not a defect in the
lane — inventing a number would have been — but it is the reason the refill file is
GO-WITH-CHANGES rather than GO. **What satisfies it:** either the ruling is recorded and the
`hr_vigour_prices` seed matches it, **or** the file applies with the catalogue seeded **empty** —
`hr_vigour_refill` refuses an `nth` with no row, so the verb ships live and refuses every call
without moving gold, and the ruling becomes a reviewed `INSERT` under this same review. Slice 1
ships no refill control either way, so nothing playable waits on this.

---

## R2. What I re-checked and still could not break

Everything in §2 of the first review holds; I re-read the diffs rather than re-asserting the
conclusions. Three things the fixes newly touch, checked because they are where a fix goes wrong:

- **The remainder row cannot overflow anything.** `player_progress.value` is `bigint`; `hr_apply`'s
  `c_max_progress_add` is 1,000,000 and `remMs < 60,000` per window by construction; and the sum of
  all window ms in a day is bounded by the day, so the counter is bounded by 86,400,000. There is no
  progress-key allowlist in `hr_apply`, which is pre-existing and unchanged — `hr_apply` is
  engine-only, so the engine remains the only writer of either key.
- **The M6 patches compose with M7's live bodies.** On the replay with trophy applied first,
  `hr_state_of` projects `hunt_stance`, `vigour`, `hunt_analyzer` **and** trophy, `hr_apply` carries
  the M6 marker and its activity key allowlist is the five keys. The anchors did not collide; only
  the allowlist link did.
- **`hunt-stance-stop.sql` grants `hr_engine` nothing.** After all four M6 files, the detector named
  only `hr_hunt_analyzer` and `hr_vigour_of` — so the stance/stop file is not part of S-5 and can
  apply on its own.

**REMAINING, ACCEPTED**, unchanged from the first review, plus one: the tick threads no `vigour`
(above), armed by `combat` joining `hr_tick_config.channels`; no top-XP/h board exists, and one would
re-open this review; `bag_full` is a stop rule that can never fire and is the Designer's to rule on.

---

## R3. Guards — real exit codes, run in this container on `sec/m6-hunts-2`

| command | exit | last line |
|---|---|---|
| `node tests/vigour-charge-conservation.mjs` | **0** | `OK — the Vigour charge is conserved under window subdivision.` |
| `node tests/vigour-charge-conservation.mjs --selftest` | **0** | `every planted defect is caught and the shipped engine is green` |
| `node tests/vigour-charge-conservation.mjs --mutate=floor_per_window` | **1** | the original defect, planted back — arms RED (caught) |
| `node tests/vigour-charge-conservation.mjs --mutate=remainder_refloored` | **1** | the half-fix — arms RED (caught) |
| `node tests/vigour.mjs` | **0** | `OK — … the real RPC charges the catalogue price once, caps the day and refuses without taking gold.` |
| `node tests/vigour.mjs --selftest` | **0** | `every mutation caught, unmutated run green` |
| `node tests/vigour.mjs --mutate=spent_ignores_remainder` | **0** | caught (V6b) — `--mutate` exits 0 when the mutation IS caught |
| `node tests/vigour.mjs --mutate=refill_sells_partial` | **0** | caught (V10 precondition; see S-3) |
| `node tests/vigour.mjs --mutate=refill_idem_optional` | **0** | caught (V11) |
| `node tests/hunt-stance-stop.mjs` | **0** | `OK — … the hunt ends at the same moment attended and away.` |
| `node tests/hunt-stance-stop.mjs --selftest` | **0** | `every mutation caught, unmutated run green` |
| `node tests/hunt-analyzer.mjs` | **0** | `OK — every field equals a hand-computed sum over the fixture ledger rows …` |
| `node tests/accrual-engine.mjs` | **0** | `goblin · 12h · tick 2328ms · 10532 ticks · 7934 kills · 39452g` |
| `node tests/world-tick-parity.mjs` | **0** | `ranged glass cannon vs rat … gold 550 vs 576 (-4.5%)` |
| `node tests/schema-drift.mjs` | **0** | `OK — repo rebuilds to the committed fingerprint (960788fbfffb…)` |
| `node tests/apply-order-honesty.mjs` | **0** | `30 file(s) carry a measured verdict (30 evidenced-live, 0 evidenced-absent)` |
| `node tests/selfcheck-no-global-dml.mjs` | **0** | `OK — 212 migrations, 9 global statement(s), all 9 acknowledged` |
| `node tests/guard-hygiene.mjs` | **0** | `PASSED — no orphans, no ghosts, no stale entries, no vacuous proofs.` |
| `node tools/pack-edge.mjs hr-accrue --check` | **0** | `76 files (46 vendored), 1762.9 KB, payload 335090840d6e81fd649b2dd48bab1ffea07a6eb04e1b04c2b5c325b2ab84ae9b` |
| `node tools/derive-grant-hygiene.mjs --check` | **0** | `derivation in sync (11 links, 12 patches)` — **and S-5 is why that 0 is not the whole answer** |
| `node tools/lane-done.mjs` | **0** | `lane-done: all green.` (all steps ok, incl. `ledger-rollup --mutate`, `ci-shape`, `guard-hygiene`, `bump-version.sh --check`) |

My own three proofs, none of which is a repo guard: the chain-end strict detector run (S-2), the
production-order collision replay (S-5) and the rendered-panel render (C-1). Scripts are in this
session's scratchpad, not committed — they are one-shot probes, and a probe kept as a guard without
a mutation arm is the thing `guard-hygiene` exists to refuse.

**CI registration** is honest: `.github/workflows/smoke.yml` carries a `--selftest` and a plain run
for each of `hunt-stance-stop`, `hunt-analyzer`, `vigour` and `vigour-charge-conservation`, and the
`guards-unregistered.json` entry for the conservation guard is gone.

---

## R4. Coordinator runbook — REVISED

**Nothing applies until S-5 lands.** The order below replaces §4.1; §4.2's reads still stand and the
two new ones are R4(11) and R4(12).

### R4.0 The merge chore comes FIRST, and it is the lane's, not yours

This lane is based on `next` **before** the b551 cut (`e1b2b6f1`). `git merge origin/next` into it
today conflicts in **six** files — `accrual.js`, `index.ts`, `set-activity.js`,
`tools/derive-grant-hygiene.mjs`, `tests/schema-apply-order.json`,
`tests/schema-drift.baseline.json`, plus `index.html` and `src/core/index.js`. `CLAUDE.md` §3.3: the
Coordinator never resolves a conflict by hand. **Send it back to `lane/m6-hunts-backend`**, which
merges `next` into itself, re-cuts the allowlist link per S-5, regenerates the two JSON baselines
with their own tools (never hand-merged), re-runs its own tests and reports. The merged
`schema-apply-order.json` must put the two **already-applied** trophy files **before** the M6 five,
because that is the order production is in.

### R4.1 Apply order, after the merge and after S-5

```bash
node tools/apply-migration.mjs supabase/migrations/2026-09-22-hunt-stance-stop.sql
node tools/apply-migration.mjs supabase/migrations/2026-09-22-vigour-daily.sql
node tools/apply-migration.mjs supabase/migrations/2026-09-22-hunt-analyzer.sql
node tools/apply-migration.mjs supabase/migrations/2026-09-22-engine-allowlist-hunt-reads.sql
# and this one only once I-3 is answered or the catalogue is seeded empty:
node tools/apply-migration.mjs supabase/migrations/2026-09-22-vigour-refill.sql
```

**The allowlist file moves UP, to fourth.** It has no precondition on the refill (I read its §1), and
between `vigour-daily`/`hunt-analyzer` and it the nightly `hr-grant-hygiene` cron RAISES — so the gap
must be minutes, not a day, and must not be widened by a file that is waiting on Tyler. One file per
call, never inside `begin/commit`, never 00:00–00:10 UTC, Coordinator only. Each file ends with one
long "all green, net zero" notice from its §-gate; **an apply that prints no such line did not run
its gate** and must be investigated, not retried.

### R4.2 Pre-flight, BEFORE the first apply

```sql
-- (p1) Who is the current last toucher of the detector, and does it carry M7?
select strpos(pg_get_functiondef('public.hr_assert_grant_hygiene(boolean)'::regprocedure),
              'hr_trophy_of(uuid,integer)') > 0 as has_trophy_of,
       strpos(pg_get_functiondef('public.hr_assert_grant_hygiene(boolean)'::regprocedure),
              'hr_trophy_claim(uuid,integer,text,integer,text)') > 0 as has_trophy_claim,
       strpos(pg_get_functiondef('public.hr_assert_grant_hygiene(boolean)'::regprocedure),
              'hr_tick_settle(text,uuid,integer,text,bigint,timestamp with time zone,') > 0 as has_tick_settle;
-- (p2) And it is green BEFORE we touch anything.
select public.hr_assert_grant_hygiene(true);
```

Expect `has_trophy_of = has_trophy_claim = has_tick_settle = true` and (p2) to return without
raising. **If (p1) shows a trophy entry false, stop** — the base moved again and S-5 must be re-cut
against whatever is live, not against what this document says. **If (p2) raises, stop**: production
is already red and the M6 applies would bury the reason.

### R4.3 Post-apply, read-only

Run §4.2's reads (1)–(9) unchanged — they are still correct — with (1) run **immediately after the
fourth apply** rather than after the fifth, and with these two added and (5) amended:

**(5, amended)** expect `cols = 'stance_id,eat_at,ammo_dry,falls'` and the three seeded rows **and**
remember the table is now documented as an **ID allowlist**: a change to `eat_at`, `ammo_dry` or
`falls` changes nothing at runtime, and `GATE(b3)` fails the next apply if any routine starts
reading them. A fifth column is still a multiplier wearing a name.

**(11) The detector carries BOTH lanes, not just the last one to apply.**

```sql
select strpos(pg_get_functiondef('public.hr_assert_grant_hygiene(boolean)'::regprocedure), n) > 0 as present,
       n as entry
  from unnest(array['hr_vigour_of(uuid,integer)','hr_hunt_analyzer(uuid,integer)',
                    'hr_trophy_of(uuid,integer)','hr_trophy_claim(uuid,integer,text,integer,text)',
                    'hr_tick_settle(text,uuid,integer,text,bigint,timestamp with time zone,',
                    'hr_quartermaster_buy(uuid,integer,bigint,uuid,text)']) as n;
```

**All six `present = true`.** A false on either trophy row means S-5 regressed and the detector has
just been walked backwards — that is a stop-the-line, and the repair is to re-derive, never to call
`hr_grant_baseline_sync()` and never to pass `false`.

**(12) The charge conserves on the live meter.** Read-only, on any character with settled combat rows
today:

```sql
select key, value from public.player_progress
 where user_id = '<a QA character>' and slot = 0 and kind = 'daily'
   and key in ('ev:vigour_min','ev:vigour_rem_ms')
   and period_key = public.hr_utc_day_key(now());
select (public.hr_vigour_of('<a QA character>', 0))->>'spent_min' as spent_min;
```

Expect **both keys present once a settle has run** (the remainder row is written whenever the window
is not a whole number of minutes, which is nearly always), and
`spent_min = floor((vigour_min*60000 + vigour_rem_ms)/60000)`. **`ev:vigour_min` present with
`ev:vigour_rem_ms` absent after a day of settles means the remainder half did not deploy** — the
engine and the database are then at two different versions of S-1, and the edge is the half to check.

### R4.4 After the applies

1. `node tests/live-hash-drift.mjs --live --write`, then the whys from `--codediff`. **This is the
   guard that sees the two `hr_apply` patches, the four `hr_state_of` patches and the `hr_rpc_gate`
   patch**; `schema-drift` records signatures, not bodies, and does not.
2. Flip the apply-order notes to APPLIED — including the S-3 wording, which still reads "a refill at
   the 22h ceiling is REFUSED rather than sold for zero minutes" and should now say the **partial**
   is refused too.
3. `node tools/restore-census.mjs` and re-pin, re-measuring `hr_vigour_prices.live_value` from
   §4.2(4) — and again after Tyler's ruling, or a rebuild reverts a tuned economy to the placeholders.
4. **A-1 step 3**: the `explain (analyze, buffers)` at §4.2(10), before and after, recorded with the
   row count. The bound is structural; the number is not yet measured.
5. Edge deploy, then the client half rides a cut.

### R4.5 The edge payload to deploy

`node tools/pack-edge.mjs hr-accrue --check` on **this lane head** gives
`335090840d6e81fd649b2dd48bab1ffea07a6eb04e1b04c2b5c325b2ab84ae9b`
(76 files, 46 vendored, 1762.9 KB). **Do not deploy that hash.** It is measured on a tree that does
not carry M7, and `origin/next` records `hr-accrue 0d11badd live` after the M7 merge — deploying this
one would walk the live function back. `accrual.js`, `index.ts` and `set-activity.js` are three of the
six merge conflicts, so the payload **will** change: re-measure `--check` on the merged set, deploy
from that tree, and verify the live `payload_sha256` equals it. The in-page payload guard is red until
they match.

**The DB half and the engine half must land together.** An edge that sends `vigour` to a database
with no `hr_vigour_of` is safe (the projection is absent and the engine proposes nothing — the
`⚠ SELF-CONFIGURING` branch). The other way round is not: a database projecting `vigour` to an edge
that ignores it means a budget nothing charges against, which is S-1 with a different cause.

---

## R5. Verdict

**2026-09-22-hunt-stance-stop.sql: GO**
**2026-09-22-vigour-daily.sql: GO-WITH-CHANGES**
**2026-09-22-vigour-refill.sql: GO-WITH-CHANGES**
**2026-09-22-hunt-analyzer.sql: GO-WITH-CHANGES**
**CLIENT SHIP: GO**

The unmet conditions, in full, and nothing else is owed:

1. **S-5** — the allowlist link is re-cut as link 12 on `2026-09-22-trophy-claim.sql`, §2 is
   regenerated by `tools/derive-grant-hygiene.mjs`, `GATE(a2)` names the two trophy entries, and the
   chain-end strict proof re-runs green **on the merged tree**. Blocks the apply of `vigour-daily`
   and `hunt-analyzer`, which grant what it records. I re-verify S-5 only.
2. **I-3** — Tyler's four §4.6 figures are answered and the seed matches, **or**
   `hr_vigour_prices` is seeded empty and the ruling lands as a reviewed `INSERT`. Blocks
   `vigour-refill.sql` alone; slice 1 ships no refill control, so nothing playable waits on it.
3. The merge chore is done **by the lane**, not by hand at integration, and `lane-done` is re-run in
   its worktree on the merged tree.

`hunt-stance-stop.sql` is **GO** as it stands and may apply first; it grants `hr_engine` nothing and
is not part of S-5. The client half is **GO** and may ship on any cut — it renders no Vigour bar and
renders no Analyzer block until the server projects one.

**Residual risks I am accepting**, with their triggers: the world tick threads no `vigour`, armed the
day `combat` joins `hr_tick_config.channels`; the Analyzer's cost is bounded but unmeasured until
R4.4(4); `bag_full` can never fire; nothing here is tradeable, rankable or contributable **until a
top-XP/h board exists**, and that board re-opens this review. **Debt I confirm and do not block on:**
four anchored patches on `hr_state_of` now (chain depth 5) and two on `hr_apply` — the joint
restatement is owed before the next patch on either. **And the new one: no guard compares link bases
across lanes.** S-5 was correct on its branch, correct on M7's branch, and wrong in the set; that is
worth a guard before the next milestone, not after it.

---

# RE-VERIFY 2 — S-5 ONLY — 2026-09-23

**Reviewer:** security-engineer (veto) · **Re-verified:** `lane/m6-hunts-backend` @ `655f44a9`
· **Branch:** `sec/m6-hunts-3`, cut from that head · `npm install --no-audit --no-fund`
**No production read and no production write.** Every proof below ran in this container against the
repo's own engine and a PGlite replay of the repo's own chain, driven in **production's real order**.

| migration | verdict |
|---|---|
| `2026-09-22-hunt-stance-stop.sql` | **GO** (unchanged; it grants `hr_engine` nothing) |
| `2026-09-22-vigour-daily.sql` | **GO** (S-5 closed — the grant it makes now has a carrier that applies) |
| `2026-09-22-hunt-analyzer.sql` | **GO** (S-5 closed; the cost measurement stays a post-apply step) |
| `2026-09-22-engine-allowlist-hunt-reads.sql` | **GO** (S-5 closed by execution; **S-5b**, a P3 comment defect, named below) |
| `2026-09-22-vigour-refill.sql` | **GO with the EMPTY seed** (I-3 answered fail-closed; **I-5 blocks the later INSERT, not this apply**) |
| **CLIENT SHIP** | **GO** (unchanged) |

**S-5 is closed, and I could not re-open it.** The two things this pass found are both downstream of
the answers the lane gave: the file's hand-written §2 header still names the **stale base** it was
re-cut off (**S-5b**, P3), and I-3's "Tyler's ruling is a reviewed `INSERT`, never a code change" is
**true only at 120 minutes a rung** — at any other number the receipt and the meter disagree, by
execution (**I-5**, P2, a condition on that `INSERT` and on nothing else).

---

## R6.1 S-5 — CLOSED, in production's real order

Replayed the repo's chain to `2026-09-22-pg-net-queue-lockdown.sql`, then applied by hand, in the
order production is actually in — including **`2026-09-23-client-state-retired-fields.sql`**
(APPLIED 02:29:30 UTC), which is on `origin/next` and **not yet on this lane**:

```
chain -> 2026-09-22-pg-net-queue-lockdown.sql          206 files, hr_engine present: true
applied  M7 state-of-trophy-prefix   (LIVE 00:40:44Z)
applied  M7 trophy-claim             (LIVE 00:41:24Z)
applied  client-state-retired-fields (LIVE 02:29:30Z)
  after the applied head, engine_execute_outside_allowlist = []
applied  M6 hunt-stance-stop / vigour-daily / hunt-analyzer
  before the allowlist file, outside = ["hr_hunt_analyzer(uuid,integer)","hr_vigour_of(uuid,integer)"]
applied  M6 engine-allowlist-hunt-reads.sql            (R4.1's FOURTH slot)
applied  M6 vigour-refill.sql
── CHAIN END ──
  engine_execute_outside_allowlist            []
  strict hr_assert_grant_hygiene(true)        RETURNED, no raise
  installed detector carries                  hr_hunt_analyzer, hr_vigour_of, hr_trophy_of,
                                              hr_trophy_claim, hr_tick_settle, hr_quartermaster_buy  (6/6)
  hr_engine holds EXECUTE on                  the same four reads and nothing else
  §3(e) mutation probes left behind           []
-- planted, by me, at chain end:
  non-strict NAMES it                         ["hr__sec_m6_3_probe()"]
  strict                                      RAISED: GRANT HYGIENE FAILED: …
  probe dropped, strict again                 RETURNED, no raise
```

`node tools/derive-grant-hygiene.mjs --check` exit **0**, `derivation in sync (12 links, 13 patches)`
— on this lane **and on the merged tree** (R6.6), which is where R5 condition 1 required it.
The same run with the M6 files in the order the earlier RE-VERIFY used (the four, then the allowlist
file **last**) is **also** green, so the allowlist file genuinely carries no precondition on the
refill and R4.1's move to fourth is free.

**One thing the probe surfaced that is worth recording rather than burying:**
`2026-09-23-client-state-retired-fields.sql` **REFUSED** on the first run, from its own §0 body-hash
gate, because the repo's replay applies `2026-09-14-client-state-projection-denylist.sql` and
production never did. Neutralising that one never-applied file made the §0 gate **accept** — which
is a second, independent, executing confirmation that production's `hr_put_client_state__ungated`
is the body that file names, and that its restatement is aimed at the right base. Nothing about it
touches `hr_engine` or the detector (it restates one body and revokes it from four roles), so it is
inert to S-5 either way; the M6 proof above is run on the neutralised chain.

**The arms, and they still bite.** Two mutations of my own on the allowlist file:

| arm | result |
|---|---|
| **`stale_base`** — the §2 body derived from the OLD base, i.e. the two trophy entries absent | **REFUSED at `GATE(a2)`**: *"this restatement DROPPED an allowlist entry an earlier link recorded — it was hand-edited or cut against a stale base"*. The file rolls back whole; chain end is then `outside = [hr_hunt_analyzer, hr_vigour_of]` and strict RAISES — the red state the refusal exists to keep out of production |
| **`stale_base` + `gate_a2_unnamed`** — the same stale body with S-5's `GATE(a2)` extension removed | **REFUSED at `GATE(b)`** with the *exact* error the 2026-09-22 RE-VERIFY measured: `hr_engine still holds EXECUTE outside its allowlist: ["hr_trophy_claim(…)", "hr_trophy_of(uuid,integer)"]` |

That pair is the proof the `GATE(a2)` extension is real work and not a comment: **the same defect
now stops at §3(a2) instead of at §3(b)**, nine sections earlier. (`gate_a2_unnamed` alone applies
clean, as it must — `GATE(a2)`'s names guard the **next** link off this one, not this one.)

---

## R6.2 I-3 — the empty seed is fail-closed, driven on a funded character

The file's own `GATE(d0)/(e0)/(e0b)/(f)` execute on every apply and did so in every run above. My own
arm, at chain end, on a real character holding **100,000,000 gold**:

```
(1) SHIPPED STATE   hr_vigour_prices rows = 0
(2) FUNDED CHARACTER, EMPTY CATALOGUE
    answer             {"ok":false,"slot":0,"error":"refill_unpriced"}
    gold               100000000 -> 100000000   (moved: false)
    ev:vigour_refills  0
    meter unchanged    true
    journalled         [{"code":"refill_unpriced","n":1}]
```

Refused **by name**, **before** the advisory lock and before the gold test, **zero gold**, **no rung
counted**, **the meter untouched**, and journalled so `vitals --refusals` shows who found the control
before the ruling landed. **The client's held bar is unaffected because there is no bar and no
control**: C-1 deleted `vigourRow`, slice 1 ships no refill button, and `state.vigour` reaches the
browser only as a projection it renders nothing from — so an unpriced catalogue is invisible rather
than broken. That is the answer condition 2 asked for, met.

### I-5 — "a later priced row needs only a data row" is true at 120 minutes and **false** at any other **[P2, NEW, blocks the I-3 `INSERT`, not this apply]**

`hr_vigour_refill__ungated` reads **`minutes` from the catalogue** (`:239`) and reports it on the
receipt and on the ledger row. `hr_vigour_of` does **not**: `bought_min := refills * c_refill_min`
with `c_refill_min constant int := 120` (`vigour-daily.sql:87`). Nothing binds the two, and
`hr_vigour_prices` constrains only `minutes > 0`. Executed, at chain end, with the catalogue seeded
two ways and **no code changed between them**:

```
(3) DATA ROW ONLY, minutes=120 (the designer's proposal)
    receipt  ok=true nth=1 cost=2000 minutes=120
    gold     -2000       budget_min 720 -> 840 (delta 120)     receipt vs meter  AGREE
(4) DATA ROW ONLY, a ruling of minutes=180
    receipt  ok=true nth=2 cost=6000 minutes=180
    gold     -6000       budget_min 840 -> 960 (delta 120)     receipt vs meter  DISAGREE
                                       — the player paid for 180 and the meter credited 120
```

**CONFIRMED by execution.** That is S-3's own finding — *"the receipt and the meter on the same
envelope disagreeing"* — and CLAUDE.md §6's *"the browser says one thing and the server says
another"*, re-opened through the exact door I-3 opens. `GATE(e7)` cannot see it: it prices its arms
from a fixture ladder that is 120 everywhere, so it is satisfied vacuously with respect to any other
ruling. Blast radius is **self-only** and the gold is a sink, not a transfer — but a player pays a
real price for minutes they do not receive, and the only control standing between that and
production today is a promise in a note.

**It does not block this apply**, because the table ships empty and the verb refuses every call. It
**blocks the `INSERT`**, and what satisfies it is either:
(a) `hr_vigour_of` reads `minutes` from `hr_vigour_prices` instead of `c_refill_min` — one number,
which is the rule the rest of this lane already follows; or
(b) cheaper, and available while the table is still empty: a `check (minutes = 120)` on
`hr_vigour_prices`, or a `GATE` asserting every row's `minutes` equals the meter's constant, so a
ruling that moves the block size **fails the apply** instead of shipping a disagreement.
I recommend (b) now and (a) whenever the meter is next touched. Either way the ruling stays data.

### I-6 — "the number of rows IS the per-day cap" is not true **[P3]**

The table comment and §(5)'s comment both say the catalogue's row count is the daily cap. It is not:
`v_cap := v_vig->>'refills_max'`, which is `c_refill_max constant int := 5`. Measured — with **six**
priced rungs seeded, `refills_max` still reported **5**. It fails **closed** (a sixth rung is refused,
no gold moves), so this is an honesty defect and not a money one; but it means a ruling of more than
five rungs silently does nothing while the refusal blames a cap the player never set. Fix with I-5's
(a), or correct both comments.

---

## R6.3 R4.1 — the recorded order matches the runbook

`tests/schema-apply-order.json` `order` tail, read from the lane head:

```
… 2026-09-22-pg-net-queue-lockdown, state-of-trophy-prefix, trophy-claim,
  hunt-stance-stop, vigour-daily, hunt-analyzer, engine-allowlist-hunt-reads, vigour-refill
```

Identical to R4.1: the two APPLIED trophy files ahead of all five, the allowlist file **fourth**, the
refill **last**. The notes carry the S-5 re-cut, the R4.1 rationale and the I-3 empty catalogue, and
each of the five still reads `STAGED, NOT APPLIED`. `node tests/apply-order-honesty.mjs` exit **0**
(29 measured verdicts here, **30** on the merged tree — the thirtieth is
`2026-09-23-client-state-retired-fields.sql`, which explains the 30→29 step from the last re-verify;
it is the merge, not a regression). The `schema-drift` re-baseline in `59aabfea` is **additions
only** — two tables, five functions, two policies, two indexes, ten constraints, nine columns, all
M6's own — nothing moved and nothing was dropped.

**The one timing fact the runbook needs:** the nightly detector is `hr-grant-hygiene` on
`50 4 * * *`, i.e. **04:50 UTC**. Files 2–4 must land in one batch well clear of it; on today's
schedule that is trivially satisfied and the next exposure window is 2026-09-24 04:50 UTC.

---

## R6.4 S-5b — the file's own §2 header still names the base it was re-cut OFF **[P3]**

`tools/derive-grant-hygiene.mjs` rewrites only the text **between** `⟦DERIVED …⟧` and `⟦/DERIVED …⟧`.
The six lines immediately above the opening marker are hand-maintained, and after the re-cut all
three facts in them are wrong:

```
-- GENERATED by tools/derive-grant-hygiene.mjs (LINKS[10]) from
-- 2026-09-21-engine-allowlist-tick-settle.sql's committed body, …
-- … PART 1f-ii walks this as the chain's ELEVENTH link.
```

It is `LINKS[11]`, the base is `2026-09-22-trophy-claim.sql`, and it is the **TWELFTH** link — the
`LINKS` array, `--check` and `GATE(a2)` all say so and all three are correct. So nothing executes off
the stale text; what it costs is that **the one human-readable record of which base this body came
from names the stale one**, in the file whose entire finding was "cut against a stale base". The next
author to derive a link off this one reads that header first.

**Not a condition of the apply** — it is a comment, and the machine-checked half is right. Owed in
the next push that touches this file. The durable fix is to move `base`/link-number into the DERIVED
span so `derive-grant-hygiene.mjs` owns them and they cannot go stale again; that is the same
structural answer as R5's "no guard compares link bases across lanes", and both are still owed.

---

## R6.5 Guards — real exit codes, run in this container on `sec/m6-hunts-3`

| command | exit | last line |
|---|---|---|
| `node tests/vigour-charge-conservation.mjs` | **0** | `OK — the Vigour charge is conserved under window subdivision.` |
| `… --selftest` | **0** | `every planted defect is caught and the shipped engine is green` |
| `… --mutate=floor_per_window` | **1** | `4 problem(s)` — the original defect, planted back, caught |
| `… --mutate=remainder_refloored` | **1** | `4 problem(s)` — the half-fix, caught |
| `node tests/vigour.mjs` | **0** | `OK — the grant is derived and floored, the ceiling holds against every refill …` |
| `node tests/vigour.mjs --selftest` | **0** | `every mutation caught, unmutated run green` |
| `node tests/hunt-stance-stop.mjs` | **0** | `OK — a stance carries only the three shipped knobs …` |
| `node tests/hunt-stance-stop.mjs --selftest` | **0** | `every mutation caught, unmutated run green` |
| `node tests/hunt-analyzer.mjs` | **0** | `OK — every field equals a hand-computed sum over the fixture ledger rows …` |
| `node tests/accrual-engine.mjs` | **0** | `goblin · 12h · tick 2328ms · 10532 ticks · 7934 kills · 39452g` |
| `node tests/world-tick-parity.mjs` | **0** | `ranged glass cannon vs rat … gold 550 vs 576 (-4.5%)` |
| `node tests/world-tick-hydration.mjs` | **0** | `green — one envelope, one field list …` |
| `node tests/delta-transport.mjs` | **0** | `delta-transport guard — GREEN` |
| `node tests/schema-drift.mjs` | **0** | `OK — repo rebuilds to the committed fingerprint (8183a968d8a9…)` |
| `node tests/apply-order-honesty.mjs` | **0** | `29 file(s) carry a measured verdict (29 evidenced-live, 0 evidenced-absent)` |
| `node tests/selfcheck-no-global-dml.mjs` | **0** | `OK — 214 migrations, 9 global statement(s), all 9 acknowledged` |
| `node tests/guard-hygiene.mjs` | **0** | `PASSED — no orphans, no ghosts, no stale entries, no vacuous proofs.` |
| `node tests/ci-shape.mjs` | **0** | `run-ci-local --list enumerates the whole matrix.` |
| `node tools/pack-edge.mjs hr-accrue --check` | **0** | `79 files (48 vendored), 1818.6 KB, payload ef545673709a542951d4473193d24e7256e1149428d9a490f85d4051b01af1c7` |
| `node tools/derive-grant-hygiene.mjs --check` | **0** | `derivation in sync (12 links, 13 patches)` |
| `node tools/lane-done.mjs` | **0** | `lane-done: all green.` |

My own four proofs, none of which is a repo guard: the production-order chain-end run and its two
mutation arms (R6.1), the funded-character I-3 drive and the two-seed receipt/meter comparison
(R6.2), the merge measurement (R6.6) and the self-configuring engine run (R6.7). One-shot probes,
kept in this session's scratchpad and not committed — a probe kept as a guard without a mutation arm
is what `guard-hygiene` exists to refuse. **The one that deserves to become a guard is I-5's**, and
it should land with I-5's fix rather than with this review.

---

## R6.6 The merge chore — re-measured, and it is now ONE file

R4.0 described six conflicting files. That was before the lane merged `next` at `3da0870`. Measured
today, `git merge origin/next` into `655f44a9` conflicts in **exactly one**:

```
CONFLICT (content): tests/schema-apply-order.json          (both sides appended to the tail)
auto-merged: .github/workflows/smoke.yml, tests/accrual-engine.mjs, tests/ci-shape.baseline.json
auto-deleted: supabase/migrations/2026-09-14-client-state-projection-denylist.sql   (next removes it)
auto-added:   supabase/migrations/2026-09-23-client-state-retired-fields.sql
```

The resolution is mechanical and is production's order: **`client-state-retired-fields` first**, as
an APPLIED file, then the M6 five in R4.1's order. On that tree, measured by me:
`derive-grant-hygiene --check` exit **0**, `schema-drift` exit **0**
(`8183a968d8a9…` unchanged), `apply-order-honesty` exit **0** (30 verdicts), and
`pack-edge hr-accrue --check` gives the **same** `ef545673…` — **the merge does not move the edge
payload.** It is still the lane's merge to commit, not the Coordinator's (CLAUDE.md §3.3); I measured
it in a throwaway worktree and committed nothing.

---

## R6.7 EDGE DEPLOY ORDER — **before-applies**

**The engine is self-configuring against a database without M6's tables; the database is not
self-configuring against an engine without M6's code.** Executed, on this lane's engine:

```
engineInputsFromEnvelope(<a pre-M6 hr_state_of row>)
  huntStance = null   huntStop = null   vigour = null
stanceOf(null).id = 'steady'  (= DEFAULT_STANCE)
computeAccrual(… vigour: null …), one settled hour of combat:
  accrued true, gold 4174, progress rows [kills, rare_drops, ev:kill_any, …]
  vigour rows       []          ← the ⚠ SELF-CONFIGURING branch: the presence of the input IS the switch
  delta.activity    null        ← 0 stance/stop keys
```

So a new edge on an un-migrated database pays combat exactly as it does today, proposes **no** Vigour
counter (`hr_apply` would take the row and `hr_vigour_of` would not exist to read it), resolves the
default stance, and never injects `stance`/`stop` into an activity statement — every engine-proposed
`delta.activity` in `accrual.js` is `{kind:'idle', id:null}`, and `request.js` forwards `stance`/
`stop` only under `hasOwnProperty`, which slice 1's client never sets. **The reverse is the unsafe
one:** a database projecting `vigour` to an edge that ignores it is a budget nothing charges against
— S-1 with a different cause — and a database validating `hunt_stance` for an edge that never sends
one is merely inert. Deploy the edge first, then apply; the gap is safe in that direction and only
that one.

The set's edge will also carry M1f, M7, the token gate and M3 when they merge, which does not change
the answer: each is additive on the same seam, and the payload to deploy is whatever
`pack-edge --check` measures **on the assembled set**, re-measured at the cut and verified against
the live `payload_sha256` (the in-page payload guard is red until they match). On this lane head, and
unchanged by the pending merge, that is
`ef545673709a542951d4473193d24e7256e1149428d9a490f85d4051b01af1c7` — **do not deploy it from here**:
it is measured on a tree that does not yet carry the rest of the set.

---

## R6.8 Coordinator runbook — FINAL

**Order of operations:** lane merges `next` (R6.6) → edge deploys from the assembled set → five
applies → post-apply reads → nightly-detector check. One file per call, never inside `begin/commit`,
never 00:00–00:10 UTC, Coordinator only, never an agent.

### (A) Pre-flight reads — BEFORE the first apply

```sql
-- (p1) The detector's current last toucher carries BOTH already-applied lanes.
select strpos(pg_get_functiondef('public.hr_assert_grant_hygiene(boolean)'::regprocedure), n) > 0 as present, n
  from unnest(array['hr_trophy_of(uuid,integer)',
                    'hr_trophy_claim(uuid,integer,text,integer,text)',
                    'hr_tick_settle(text,uuid,integer,text,bigint,timestamp with time zone,',
                    'hr_quartermaster_buy(uuid,integer,bigint,uuid,text)']) as n;
-- (p2) And it is GREEN before we touch anything.
select public.hr_assert_grant_hygiene(true);
-- (p3) The M6 objects do not exist yet, and the four reads are not granted yet.
select to_regprocedure('public.hr_vigour_of(uuid,integer)') is null   as vigour_absent,
       to_regprocedure('public.hr_hunt_analyzer(uuid,integer)') is null as analyzer_absent,
       to_regclass('public.hr_vigour_prices') is null                 as prices_absent;
-- (p4) The clock: the nightly detector is 04:50 UTC.
select now() at time zone 'utc' as utc_now;
```

Expect all four `(p1)` rows `present = true`, `(p2)` to **return without raising**, `(p3)` all true.
**If any `(p1)` row is false, STOP** — the base moved again and S-5 must be re-cut against whatever
is live, not against this document. **If `(p2)` raises, STOP**: production is already red and these
applies would bury the reason. **If `(p4)` is within an hour of 04:50 UTC, wait** — the two-file gap
below must not straddle the cron.

### (B) The five applies — one file per call, files 2–4 in ONE sitting

```bash
node tools/apply-migration.mjs supabase/migrations/2026-09-22-hunt-stance-stop.sql
node tools/apply-migration.mjs supabase/migrations/2026-09-22-vigour-daily.sql
node tools/apply-migration.mjs supabase/migrations/2026-09-22-hunt-analyzer.sql
node tools/apply-migration.mjs supabase/migrations/2026-09-22-engine-allowlist-hunt-reads.sql
node tools/apply-migration.mjs supabase/migrations/2026-09-22-vigour-refill.sql
```

**Between the second/third and the fourth, `hr_assert_grant_hygiene(true)` RAISES by design** — that
is the window the allowlist file closes, and it must be **minutes, not a day**, and must never
contain 04:50 UTC. The refill is fifth because it waits on nothing and gates nothing; it ships the
catalogue **empty** and refuses every call, so it may equally be held for a later day without
affecting the other four. Each file ends with one long "all green, net zero" notice from its §-gate;
**an apply that prints no such line did not run its gate** — investigate, do not retry.

### (C) Post-apply reads — read-only

R4.2's `(1)`–`(9)` and R4.3's `(11)`/`(12)` stand unchanged, with `(1)` run **immediately after the
fourth apply**. Two additions:

```sql
-- (13) The detector is green, and carries FOUR lanes, not just the last one to apply.
select public.hr_assert_grant_hygiene(true);
select (public.hr_assert_grant_hygiene(false))->'engine_execute_outside_allowlist';   -- expect []
-- …then re-run (p1)'s four names plus the two hunt reads: all six present = true.

-- (14) I-3: the catalogue shipped EMPTY and the verb says so.
select count(*) as rows_must_be_zero from public.hr_vigour_prices;
```

**A false on either trophy row at `(13)` means S-5 regressed and the detector has been walked
backwards — stop the line.** The repair is to re-derive; never `hr_grant_baseline_sync()`, never
pass `false`. A non-zero `(14)` means the apply seeded a placeholder price and `GATE(d0)/(f)` were
bypassed — that is a money surface live on a number nobody ruled on, and it is a rollback.

### (D) After the applies

1. `node tests/live-hash-drift.mjs --live --write`, whys from `--codediff` — this is the guard that
   sees the `hr_apply` and `hr_state_of` patches; `schema-drift` records signatures, not bodies.
2. Flip the five apply-order notes to APPLIED with their instants, and amend the refill note's S-3
   wording so it says the **partial** is refused too.
3. `node tools/restore-census.mjs` and re-pin; classify `hr_hunt_stances` and `hr_vigour_prices`,
   the latter at `live_value = 0 rows`, and re-pin again after Tyler's ruling.
4. **A-1 step 3**: `explain (analyze, buffers)` on a real character, before and after, with the row
   count. The bound is structural; the number is still unmeasured.
5. Verify the live `payload_sha256` equals `pack-edge --check` on the deployed tree.

### (E) Rollback

Every file is additive and reversible without data loss, in reverse order:

| file | rollback |
|---|---|
| `vigour-refill` | `drop function hr_vigour_refill, hr_vigour_refill__ungated; drop table hr_vigour_prices;` re-apply the previous `hr_rpc_gate` last toucher; remove the `hr_client_rpc_baseline` row. No gold ever moved (empty catalogue), so nothing is owed back |
| `engine-allowlist-hunt-reads` | re-apply `2026-09-22-trophy-claim.sql` — its `⟦DERIVED⟧` body is the previous detector, and it is the link this one was cut off. **Only valid once the two hunt reads are revoked**, or the detector goes red again |
| `hunt-analyzer` | `drop function hr_hunt_analyzer;` re-apply the previous `hr_state_of` last toucher |
| `vigour-daily` | `drop function hr_vigour_of;` re-apply the previous `hr_state_of` last toucher. The `ev:vigour_min` / `ev:vigour_rem_ms` rows are inert without it and expire on their own retention |
| `hunt-stance-stop` | re-apply the previous `hr_apply` and `hr_state_of` last touchers; the two columns and `hr_hunt_stances` may stay (inert) or be dropped |

**Rolling back the allowlist file alone is the one that leaves production red** — revoke the two
engine grants in the same transaction, or do not roll it back at all.

---

## R6.9 Verdict

**S-5: CLOSED**

**2026-09-22-hunt-stance-stop.sql: GO**
**2026-09-22-vigour-daily.sql: GO**
**2026-09-22-hunt-analyzer.sql: GO**
**2026-09-22-engine-allowlist-hunt-reads.sql: GO**
**2026-09-22-vigour-refill.sql: GO**
**CLIENT SHIP: GO**
**EDGE DEPLOY ORDER: before-applies**

— because a new engine against a database without M6's tables is inert by construction (`vigour`
absent proposes no charge, a null stance resolves to `steady`, and no engine-proposed delta ever
carries a `stance`/`stop` key), while a database projecting `vigour` to an old engine is a budget
nothing charges against, which is S-1 with a different cause.

`vigour-refill.sql` is **GO with the catalogue EMPTY** and on that basis only: it ships a live verb
that refuses every call by name, moves no gold and counts no rung. **Tyler's ruling is a separate
reviewed `INSERT` and it is BLOCKED until I-5 is answered** — at any block size other than 120
minutes the receipt and the meter disagree, by execution, and `GATE(e7)` cannot see it.

**Conditions outstanding, in full, and nothing else is owed:**
1. **I-5** — bind `hr_vigour_of`'s block size to the catalogue, or constrain the catalogue to the
   meter's constant, **before** any priced row is inserted. Blocks the `INSERT`, not the apply.
2. **I-6** / **S-5b** — two comment defects (the per-day cap is `c_refill_max`, not the row count;
   the allowlist file's §2 header names the base it was re-cut off). Owed in the next push that
   touches each file.
3. The merge chore is the lane's, not the Coordinator's — now one mechanical conflict (R6.6), with
   `lane-done` re-run in its worktree on the merged tree.

**Residual risks I am accepting**, with their triggers, unchanged plus one: the world tick threads
no `vigour`, armed the day `combat` joins `hr_tick_config.channels`; the Analyzer's cost is bounded
but unmeasured until (D)(4); `bag_full` can never fire; nothing here is tradeable, rankable or
contributable **until a top-XP/h board exists**, and that board re-opens this review. **Debt I
confirm and do not block on:** the joint `hr_apply` / `hr_state_of` restatement, now four anchored
patches deep on `hr_state_of` and two on `hr_apply`; and **no guard compares link bases across
lanes** — S-5 was correct on its branch, correct on M7's, and wrong in the set, and the file's own
header (S-5b) shows the same fact can go stale a second time in the same week.

---

# Coordinator record — R6.8 executed on production (2026-09-23, 06:24–06:35 UTC)

| Step | Result |
|---|---|
| (A) pre-flight p1–p4 | p1 four names present, p2 detector returned clean, p3 all absent, p4 06:24 UTC (04:50 detector long past) |
| edge deploy from the SET, before the applies | `c33bfb86…` deployed 06:24:30, live `payload_sha256` == `pack-edge --hash` on the set; the pack lists `tick-combat.js`, `trophy-claim.js`, `set-activity.js`, `request.js` |
| (B) hunt-stance-stop | APPLIED 06:25:16 (`hr_hunt_stop_valid` landed) |
| (B) vigour-daily | APPLIED 06:25:19 (`hr_vigour_of` landed) |
| (B) hunt-analyzer | APPLIED 06:25:21 (`hr_hunt_analyzer` landed) |
| (B) engine-allowlist-hunt-reads | APPLIED 06:25:52, 31 s after the analyzer (`hr_assert_grant_hygiene` + `hr__hunt_allow_probe` landed) |
| (B) vigour-refill | APPLIED 06:26:46. **A first invocation at 06:25:53 returned without landing anything** — no objects, no `landed bodies` line, and the error text (if any) was lost to a `tail`; the post-apply reads proved nothing had landed (`hr_vigour_prices` absent, no refill functions) before the file was run a second time. The management endpoint returns no NOTICEs, so the §-gate's "all green, net zero" line is not observable through `tools/apply-migration.mjs`; the `landed bodies` line plus the reads below are the evidence |
| (C) (1)/(13) | `hr_assert_grant_hygiene(true)` returned clean; `engine_execute_outside_allowlist = []`; six names present (trophy ×2, tick_settle, quartermaster, vigour_of, hunt_analyzer) |
| (C) (2) | only `hr_vigour_refill(integer,uuid)` has `auth_exec = true` (secdef, mentions_gate); the other four rows false |
| (C) (3) | `hr_engine` executes `hr_hunt_analyzer` and `hr_vigour_of`; `engine_table_privs = 0` |
| (C) (4)/(14) | `hr_vigour_prices` **0 rows** (I-3, shipped empty) |
| (C) (5) | careful/0.75/stop/2, reckless/0.25/swing/null, steady/0.50/swing/null; `cols = stance_id,eat_at,ammo_dry,falls` |
| (C) (6) | `hunt_stance text` / `hunt_stop jsonb`, both nullable; FK → `hr_hunt_stances(stance_id)`; CHECK `hr_hunt_stop_valid(hunt_stop)` |
| (C) (7) | projects_stance, projects_vigour, projects_analyzer, apply_patched, allowlist_is_five — all true |
| (C) (8) | `bucket_present = true`; `player_ledger_kind_check` lists every prior kind plus `'vigour'` |
| (C) (9) | probe rows 0, probe users 0 |
| (C) (10) A-1 step 3 | `explain (analyze, buffers)` on the oldest combat pointer: **6.5 ms, 1,257 shared buffer hits**, against **2** matching accrue rows in scope. The number is small because the ledger is; it is recorded as the "after" with its row count, and must be re-read when a played combat cohort exists (the "before" no longer exists) |
| (C) (12) | not yet readable: no character has settled combat rows today (the combat shadow cohort is not yet played — SEC_WORLD_TICK_M3 RE-VERIFY 4 §8-PLAY) |
| (D) 1 | `live-hash-drift --live --write`: 56 tracked, 54 entries, 51 agree, 2 code-identical, `hr_frame_emit` absent (STAGED, why written); guard exit 0 |
| (D) 2 | five notes flipped to APPLIED with their instants; the refill note already refuses the partial (S-3 wording present) |
| (D) 3 | `restore-census`: `hr_vigour_prices` re-pinned `replay 0`, `live_value [] (2026-09-23)`, note rewritten for I-3; `hr_hunt_stances` was already classified `seeded/3`; guard exit 0 |
| (D) 5 | verified above (live == pack) |

Nightly-detector check: the next 04:50 UTC run is the first with all four lanes; read `hr_grant_hygiene_log` (or the cron log) after it.

---

## VIGOUR PRICE BY LEVEL — 2026-09-26 review

Security (veto) on `lane/vigour-price-by-level` f6946f05 → `sec/vigour-price-by-level`. File:
`supabase/migrations/2026-09-25-vigour-price-by-level.sql`. Everything below was executed in the
PGlite chain replay (`tests/schema-replay.mjs`), no DB access.

**Premise correction.** The brief called `2026-09-22-vigour-refill.sql` never-applied and said
`hr_vigour_prices` never existed in production. The repo record says otherwise: it APPLIED
2026-09-23 06:26:46 (R6.8, (C)(4) measured `hr_vigour_prices` at **0 rows**). The drop is still
safe: §5 refuses if the table holds a row, and nothing else reads it (grep over migrations, edge and
tests: only the restore-census note and a `src/core/hunt.js` comment name it). Pre-check P3 below
re-measures the zero before apply.

| ID | Sev | Finding | Evidence (executed) | Fix | Status |
|---|---|---|---|---|---|
| **VP-1** | P1 (money) | `hr_vigour_of` clamped the day's refill count by the rule's CURRENT `refills_max`. Now that the cap is data, any reviewed UPDATE lowering it confiscates minutes already paid for in gold | 5 refills bought (ledger −28,500 gold); `update … set refills_max=3` → `bought_min` 600→360, `budget_min` 1320→1080 | read clamp is now the rule's hard bound (`c_refills_bound = 11`, the CHECK max), so the cap gates only the next SALE. M6 GATE(c4)'s property (a stuffed counter cannot widen the budget) still holds: with the 720 floor any count ≥5 saturates the 22 h ceiling. New §7 (e7b) + (e7c); new `tests/vigour.mjs` V8b + mutation `cap_cut_confiscates`; V10b re-pointed to ≤11 | **LANDED** |
| **VP-2** | P2 (availability) | `base_gold` / `per_level_gold` unbounded. `hr_vigour_of` prices on every envelope, so an operator typo that overflows bigint raises in `hr_state_of` for every player, not just in the shop | `per_level_gold = 4e18` → `hr_vigour_of`, `hr_state_of` and the verb all raise `bigint out of range` (no gold moved) | CHECK `<= 1,000,000,000` on both; worst case (1e9 + 1e9×126)×101 ≈ 1.3e13. New §7 (b2) | **LANDED** |
| VP-3 | Info | The verb's comment says the day is read "AFTER the lock"; `now()` is transaction start, so a call that starts 23:59:59 and waits on the lock counts on the earlier day | code read | none needed: meter, counter, intent key and price all use the same `now()`, so they cannot disagree. Wording only | accepted |
| VP-4 | Info | Two concurrent refills could not be EXECUTED (PGlite is one connection). The argument: `pg_advisory_xact_lock` on hr_apply's (user, slot) key is taken in its own statement before any read; under READ COMMITTED every later statement (the STABLE `hr_vigour_of` included) snapshots after the lock is granted, i.e. after the other refill committed; `player_state … for update` and `gold >= 0` CHECK back it | code read | none | accepted, unexecuted |

**What I tried and could not break (executed).**
- *Level from the server only:* `hr_vigour_refill_price` reads `hr_party_level` (player_skills, no client write — `authenticated` UPDATE on player_skills: permission denied). `p_level`/`p_price`/`p_cost` do not resolve (42883); forged `request.jwt.claims` ignored (§7 e5). Level 0/NULL/no rows → 1 (1,950 gold); max XP → combat 126 (rung 1 58,200, rung 5 174,600). NULL user / NULL nth → NULL (not for sale).
- *Arithmetic:* step 0.3333, base 1001, per-level 7 → all five rungs equal an independent `floor(numeric)`; bigint throughout; numeric step, never float.
- *Index and replay:* same `p_idem` → `replayed`, no debit; same `p_idem` on slot 1 → `intent_mismatch`; the idem key is required (S-4 kept); the counter is per (user, slot, UTC day) and only the verb increments it.
- *Refusals take nothing:* broke → `insufficient_gold`, counter 0, ledger 0; 6th → `vigour_daily_cap`; ceiling-clamped → `vigour_ceiling` (S-3 kept); no rule → `refill_unpriced`; cap cut → `vigour_daily_cap`. Gold unchanged in each.
- *Ledger:* one append-only `vigour` row per sale with the signed debit and the pricing level; `gold_in`/`gems_in` zero; gems and hearth tokens never move (§7 e8).
- *Reachability:* as `authenticated` and `anon`: `hr_vigour_refill_price`, `hr_vigour_of` (any user), the ungated verb, and SELECT/UPDATE on `hr_vigour_price_rule` all **permission denied**; the gated wrapper sells as the caller. `hr_assert_grant_hygiene(true)`: all three lists `[]`. The file grants hr_engine nothing and adds no client RPC, so no allowlist link is cut and the detector is not restated; §7(g) executes it strict on whatever body is live (M7 trophy-claim included).
- *No leak:* `hr_vigour_of` stays hr_engine-only; the client sees only its own meter on its own envelope.
- *Second apply byte-identical:* functiondef md5s, ACLs, rule row, RLS flags and baseline note unchanged after re-executing the file on the replayed chain.

**§7 bites — mutations planted in the file, gate left armed, replay goes red:**

| Mutation | Red at |
|---|---|
| price ignores level | GATE(e1) |
| cap cut confiscates (VP-1 reverted) | GATE(e7b) |
| replayed key debits again | GATE(e6r) |
| sale journals 0 instead of −cost | GATE(e6) |
| price fn granted to authenticated | GATE(a5) |
| coefficients unbounded (VP-2 reverted) | GATE(b2) |
| a broke refusal still counts | GATE(e6) |

**Gates (real exit codes, on the landed head):** `node tests/schema-drift.mjs` 0 · `node tests/vigour.mjs` 0 ·
`node tests/vigour.mjs --selftest` 0 (13/13 caught, incl. `cap_cut_confiscates`) ·
`node tests/apply-order-honesty.mjs` 0 · `node tools/lane-done.mjs` 0.
`tests/live-hash-drift.baseline.json` not touched.

### Coordinator runbook (one file, never 00:00–00:10 UTC)

Read-only pre-checks (management SQL, no writes):
- P1 `select to_regprocedure('public.hr_vigour_refill__ungated(int,uuid)'), to_regprocedure('public.hr_vigour_of(uuid,int)'), to_regprocedure('public.hr_party_level(uuid,int)');` — all non-null.
- P2 `select public.hr_assert_grant_hygiene(true);` — three lists `[]` (never within the 04:50 cron minute).
- P3 `select count(*) from public.hr_vigour_prices;` — **0**. Non-zero: STOP (§5 would refuse anyway).
- P4 `select to_regclass('public.hr_vigour_price_rule');` — null.
- P5 save the rollback bodies: `pg_get_functiondef` of `hr_vigour_of(uuid,int)` and `hr_vigour_refill__ungated(int,uuid)` to `~/.hearthrise/rollback-vigour-2026-09-26.sql`.

Apply (the one call): `node tools/apply-migration.mjs supabase/migrations/2026-09-25-vigour-price-by-level.sql`
— the §7 NOTICE line must print; any exception means nothing landed.

Post-apply read-only:
- `select * from public.hr_vigour_price_rule;` — one row 1500 / 450 / 0.5 / 5.
- `select to_regclass('public.hr_vigour_prices');` — null.
- `select has_function_privilege(r,'public.hr_vigour_refill_price(uuid,int,int)','execute') from unnest(array['anon','authenticated','service_role','hr_engine']) r;` — all false.
- `select public.hr_assert_grant_hygiene(true);` — three lists `[]`.
- `select count(*) from auth.users where id::text like '00000000-0000-4000-8000-0000b554%';` — 0.
- then per the file header: `live-hash-drift --codediff` (expect `hr_vigour_of`), `--live --write` with the header's why, apply-order note → APPLIED, restore-census re-measure `hr_vigour_price_rule`.
- First real refill on the QA account (play gate): cost equals the envelope's `next_refill_gold`, one `vigour` ledger row.

Rollback: re-execute the P5 file (restores both bodies), then
`drop function public.hr_vigour_refill_price(uuid,int,int); drop table public.hr_vigour_price_rule;`
and recreate the empty `hr_vigour_prices` from `2026-09-22-vigour-refill.sql` §2 (every refill then answers `refill_unpriced` — the pre-apply state). Gold already spent stays spent and journalled; no refund SQL without a separate Security GO.

### Verdict

MIGRATION: GO-WITH-CHANGES (VP-1 and VP-2 are landed on `sec/vigour-price-by-level`; apply that head, not f6946f05)

CLIENT SHIP: GO (for a client that reads next price/refills-left from hr_vigour_of only) — it must render `next_refill_gold` / `refills_left` verbatim, hide the control when either is null/0, send one fresh `p_idem` per click (re-sent unchanged on retry), and never derive a price.

## TIMBERLINE — 2026-09-26 review

Reviewed `origin/lane/content-timberline` @ 7ca326e8 (vs `origin/set/b554`), on `sec/timberline`. Class C: five
`gather`/woodcutting `hr_activities` rows at 22/38/52/68/82. Each is a new XP source and a new source of an
EXISTING tradeable log (oak/willow/maple/yew/runewood). No item, recipe, function body or grant moves.

### Findings

| # | Sev | Finding | Status |
|---|---|---|---|
| TL-1 | P1 | TIMBERLINE-3/4 fail in the FULL in-page suite (31 XP per chop against 30; 49/action against 48, i.e. 10,192 against 9,984). `--only TIMBERLINE` passes 4/4, so the failure depends on test order. Gear `xpB` left equipped by an earlier test reaches `addXp` through `getEquipmentStats()`. The tests stubbed `getBonus`/`restedQuantum` but not the equipment read. This is not a pay divergence: server-side TIMBERLINE-S1 pays exactly 48/action, and the server applies the same projected-equipment `xpB`. | **LANDED** on `sec/timberline`: both tests pin an empty-gear stat block (`C.combat.equipmentStats({}, ITEMS, {})`), restored in `finally`. Full suite: TIMBERLINE green, 1365 → 1367 passed. |
| TL-2 | P3, pre-existing | The world-tick SHADOW gather channel is not value-exact against accrual on variable-yield nodes (`qty [a,b]`, a<b). XP and tick count match exactly; the log count differs only in its random draw (1 h: weeping 484 vs 497, elder 319 vs 317, ancient 272 vs 280, and the SHIPPED `maple_tree` 466 vs 486). `world-tick-parity` P-G1 fixtures cover only fixed-yield nodes (normal/oak/iron), so its "digit for digit" claim is untested for [1,2]. It pays nothing while `hr_tick_config.shadow = true`. | Filed for backend-architect: must close before the gather tick goes live. Not this lane's. |
| TL-3 | P3, pre-existing class | Patch-style catalogue files (deep-seam, reed-and-tide, this one) never update `hr_catalogue_meta.digest`, so the live digest lags the repo (`3d040ae0…` after this branch). Only `hr_create_character`'s ledger `meta.catalogue` reads it, as forensics. No authority depends on it. | Filed. No change here (fixing one file would make the class inconsistent). |
| TL-4 | design | `yew_tree` (Hearthfind `worldroot_seed`) is out-paced from WC 68 instead of 75. | Already filed by the lane for game-designer. No security impact: a node without a hearthfind row draws no RNG. |

### Verified by execution

**(1) Economy** (pure engine: `pacedActionMs`, `PACE.xp`, 20% vendor bid). Every figure in the brief reproduces exactly. Paced xp/s gains over the
rung below: hollow +19.0, weeping +7.8, grove +11.3, elder +11.1, ancient +9.4. The next old rung beats each new
stand by +14.5 / +11.7 / +9.3 / +9.6 / +11.2%. XP/h rises strictly with req across all 12 rungs, and no lower stand
reaches a higher one's xp/h. g/h rises strictly except Hollow Oak (2,143) < Oak (2,250), which is an XP rung by
design and makes nothing obsolete. Max xp/h (duskwood 13,327), max g/h (39,808) and max items/h (normal 750) are
unchanged, so the `hr_apply` per-call clamps and `hr_day_budget` headroom are unchanged. Market: supply of existing
logs rises (willow +21, maple +14, yew +39, runewood +41% logs/h at the stand's level). That supply is bounded by
the same clamps and journalled. There is no new price surface, and the client sends only an activity id.

**(2) Server authority.** A pglite replay of the full chain, then the PRODUCTION shape (the five rows deleted, 503
activities), then the file applied:
- Insert path lands exactly the 5 ruled rows. Each tuple is byte-identical to the regenerated catalogue's; 508 after apply.
- Second apply writes NO tuple (xmin identical on all 508 rows).
- Probe is net-zero: auth.users, player_state, player_skills, player_ledger, player_intents, hr_rejections and hr_items counts are unchanged across 7 applies.
- Mutations, each refused and each reverting §1 to 0 rows:
  - maple_grove at 51 in §1+§2(a) → GATE(d): `ok` at WC 51.
  - elder_yew dropped → GATE(a).
  - NULL req_lv → GATE(a).
  - duplicate level → GATE(b).
- `hr_apply` is not executable by anon/authenticated, and `hr_activities` is not writable by them.
- xp/ms/qty are absent from SQL. Yields come only from the edge catalogue (`GATHER_NODES` ← `src/data/gathering.js`); the client declares an id.

The chain replay alone only exercises the 0-row path, because the regenerated catalogue already carries the rows. The production-shape run above covers the insert path.

**Regenerated `2026-08-11-catalogue.generated.sql`:** `gen-catalogues --check` passes, which proves it is tool output. It contains zero
function bodies. Replaying it would change only catalogue rows, the `hr_catalogue_meta` digest, and the
catalogue RLS policies/grants it re-issues. No live function body moves, so `live-hash-drift` is unaffected
(baseline untouched). `restore-census` stays consistent: replay pin 508, and live reaches 508 after this apply. It
is a chain record and is NOT to be applied.

**(3) Both paths.** Attended settle and away accrual both run `computeAccrual` → `simulateSkillSpan` over the
same `GATHER_NODES`. Results: `accrual-engine` TIMBERLINE-S1 green (208 ticks, 48 XP each, unknown_node control); in-page
TIMBERLINE-3 (attended) and TIMBERLINE-4 (away) green after TL-1. Tick-shadow vs accrual: XP and ticks identical on all five stands (see TL-2 for items).

**(4) Edge payload.** `pack-edge hr-accrue --hash`: `c799d1594e286d3b872d41a2d6970f4ac50e5f8eab61c98ff03586d55a517bad`
(set/b554) → **`27f405b9c58ecadb0733d957376c48a89002112fe087a2e8bae7866ed31a29ed`**. Only `src/data/gathering.js`
moved in the payload. Order: **apply → edge deploy → client push**. Every mis-order fails closed
(`unknown_activity` dead tile / `unknown_node` refusal).

**Gates (real exit codes, on the landed head):**

| Command | Exit | Note |
|---|---|---|
| `node tests/schema-drift.mjs` | 0 | |
| `node tools/gen-catalogues.mjs --check` | 0 | |
| `node tests/catalogue-literal-drift.mjs` (+`--selftest`) | 0 / 0 | |
| `node tests/apply-order-honesty.mjs` (+`--selftest`) | 0 / 0 | |
| `node tests/restore-census.mjs` | 0 | |
| `node tests/accrual-engine.mjs` | 0 | |
| `node tests/worker-accrual.mjs` | 0 | |
| `node tests/world-tick-parity.mjs` | 0 | |
| `node tests/hearthfind-boss-rate.mjs` | 0 | |
| `node tools/lane-done.mjs` | 0 | |
| `node tests/run-smoke.mjs` | 1, ENVIRONMENTAL | 1367/1381 in-page passed; the only in-page ✗ is `errors: clean log` = "Failed to fetch". Also the payload-hash GET 403 and cold-load fetch errors: this sandbox cannot reach Supabase. |

`tests/live-hash-drift.baseline.json` was not touched.

### Coordinator runbook (one file, never 00:00–00:10 UTC)

Read-only pre-checks:
- `select count(*) from public.hr_activities;` → **503**.
- `select count(*) from public.hr_activities where kind='gather' and req_skill='woodcutting';` → **7**.
- `select count(*) from public.hr_activities where activity_id in ('hollow_oak_tree','weeping_willow_tree','maple_grove','elder_yew_tree','ancient_runewood_tree');` → **0**.
- `select count(*) from public.hr_items where item_id in ('oak_log','willow_log','maple_log','yew_log','runewood_log') and tradeable;` → 5. Record `select count(*) from public.hr_items;` (538 expected).
- `select public.hr_assert_grant_hygiene(true);` → three lists `[]` (not in the cron minute).

Apply (the one call): `node tools/apply-migration.mjs supabase/migrations/2026-09-26-timberline.sql`
— NOTICEs `5 of 5 activity rows moved` and `... all green`. Any exception means nothing landed.

Post-apply read-only:
- activities **508**; woodcutting rows **12** at 1/15/22/30/38/45/52/60/68/75/82/90; hr_items unchanged.
- `select count(*) from auth.users where id='00000000-0000-4000-8000-0000b5550002';` → 0.
- `hr_assert_grant_hygiene(true)` → `[]`×3.
- `live-hash-drift --live`: expect NO change (no body moved). If it goes red, STOP and read it.
- Apply-order note → APPLIED; restore-census re-measure if hr_activities carries a live value.

Edge deploy (AFTER the apply): `node tools/pack-edge.mjs hr-accrue --out <dir>/supabase/functions/hr-accrue`, copy
`supabase/config.toml`, `npx --yes supabase@latest functions deploy hr-accrue --workdir <dir> --project-ref nezapsylztqbbwuwembx`.
Verify the live `payload_sha256` = `27f405b9c58ecadb0733d957376c48a89002112fe087a2e8bae7866ed31a29ed`.

Client: rides the 20:00 UTC cut. Visual gate on the woodcutting screen (7 → 12 tiles), desktop and 922×423. Play gate: chop
Hollow Oak on the QA account, reload, confirm the log count matches the envelope.

Rollback (reverse order):
1. Redeploy the set/b554 edge payload (`c799d159…`). Away/attended spans on a stand then refuse `unknown_node`, so nothing pays.
2. Stage a one-file migration
   `delete from public.hr_activities where kind='gather' and activity_id in (…five…);` and apply it with
   `tools/apply-migration.mjs`. It is data only and needs no new Security GO beyond this one.

No clawback: every log cut is an existing, journalled item.

### Verdict

MIGRATION: GO

EDGE DEPLOY: GO (after the apply; verify hash `27f405b9…`)

CLIENT SHIP: GO-WITH-CHANGES (TL-1 landed on `sec/timberline`: ship that head, not 7ca326e8)

## LUCKY FINDS — 2026-09-26 review

Security-engineer (veto), adversarial re-review of `origin/lane/content-lucky-finds` @ e3eb9929 (3 commits on `set/b554` @ 9c70fa98), per the pack-1 brief (`origin/lane-briefs-b555:pack-1.md`). No DB access, no deploy; the sandbox cannot reach Supabase, so every network red below is environmental. Review branch: `sec/lucky-finds`.

### What was executed (not read — run)

| Check | Result |
|---|---|
| `tests/lucky-finds.mjs` / `--selftest` | exit 0 / exit 0 (9/9 mutants RED on their own rule, clean tree GREEN) |
| `tests/world-tick-combat-parity.mjs` / `--mutate --<each of 18>` | exit 0 / all 18 mutants "RED, as required" (run one by one) |
| `tests/hearthfind-boss-rate.mjs` | exit 0 — elk_king 91/h, grim_reaper 113/h, dragon 83/h, all equal their pins |
| accrual-engine, attended-loot-credit, settle-carry-loss, hearthfind-roll, dungeon-key-drops, world-tick-parity, bestiary-trophy, bounty-drift, conservation-fuzz, party-split, party-settle, collection-renown-claim-drift, no-new-prediction, monolith-ratchet, schema-drift, apply-order-honesty, catalogue-literal-drift, items-catalogue, restore-census, inventory-mint-census | all exit 0 |
| Own C6 mutation: strip `steel_helm` (a real .015 row) from the decomposed side | RED at 16 starts — the arm still catches a genuinely starved row |
| Own C6 check: 8 starts on this tree | RED on `steel_helm` (the lane's claim that 8 is now a false positive is true) |
| Own C6 check: remove the lucky exclusion | RED on `warlords_torc` (the exclusion is needed, and it is the only thing it removes) |
| Own rate proof: 26 monsters × 40 h, same seed, WITH vs WITHOUT lucky rows, every non-guaranteed existing row z-tested against its effective chance | 194 row×variant tests, worst \|z\| = 3.45, none > 4 — existing rates unchanged within tolerance |
| Own server coherence: `computeAccrual` small_wolf, 300 seeds × 8 h | 0 nights where a `rare_drop` event and `delta.items.wolfbone_torc` disagree; per-kill 1 in 1,126 (row 1 in 1,087 at weakness bonus; 1 in 1,250 without) |
| `pack-edge hr-accrue --hash` | **c81bc5f2a5e79a043bd3e7c6d8692731f30c110eb81ef262e8d5305ab61176a2** (was c799d159…7bad on both `origin/main` b553 and `set/b554`) |

### Findings

1. **(1) Economy — PASS.** 26 rows, all live gear (weapon/armor/jewelry), tradeable, not BoP/Hearthfind/tier-8/unique, not QM/dungeon/shop/boss/raid loot, not any other drop, unique across rows, item tier ∈ {T, T+1}. Faucet: every row ≤ 5 % of its gp midpoint at base (guard c); per-kill value is bounded regardless of kill speed, so the cap holds at any kills/h. Designer's two extra swaps (`fire_devil → warlock_helmet`, `chained_demon → yew_staff`, art on REJECTED_WRONG_SUBJECT) were not in the pre-review: both are in the repo catalogue chain (`2026-08-11-catalogue.generated.sql`, tradeable=true, slots helmet/weapon) and `catalogue-literal-drift` replays in sync — the live `hr_items` check is pre-check P2 below. No Hearthfind source (elk_king, grim_reaper, dragon) or seeded fixture monster touched. Market: every item already has a crafting source, so a find adds supply at ≤ ~1 per 4–21 h per hunter and cannot set a price floor/ceiling on its own.
2. **(2) Determinism — PASS, C6 is NOT a loosening.** The appended row is the last draw of `rollDropTable`, so within a kill nothing earlier moves; across kills the stream shifts by one draw, which re-rolls specific seeds but not rates (z-test above). Hearthfind pins unchanged (its roll is after the drop table and its three sources carry no lucky row). C6: `HEALTH_STARTS` 8 → 16 plus "lucky ids excluded from the starvation verdict". Proven: `fixedSeed` still starves goblin_totem/goblin_seal at 16; a single starved ordinary row (`steel_helm`) is still RED at 16. **Residual (P3, accepted):** a defect that starves ONLY lucky rows on the decomposed world-tick path would not be caught by C6 (under-mint, never over-mint; `lucky-finds.mjs` holds the rates on the one-call path only).
3. **(3) Client never announces/credits an unrolled drop — GO-WITH-CHANGES, change LANDED.** `lucky-finds.js` wraps `COMBAT_FX.addItem/onDrop/recordKill`; every client kill path (live `combatSimCtx`, `killMonster → resolveKill`, `simulateAwayCombat`'s per-call `Object.assign` copy) reads that one object after the hook binds, and the collection "New discovery" toast sits on `window.addItem`, which the suppressed `fx.addItem` never reaches. **SEC-LF-1 (P2, fixed on this branch):** `core/combat-sim.js:189` bumps `state.stats.rareDrops` BEFORE `fx.onDrop`, and on the client `state` is `G` on both paths — so a client-dice lucky roll still counted into "Rare drops looted" and could unlock the Lucky/Loot Goblin achievement toasts for a roll the server never made (§6). Fix: the hooked `onDrop` takes that count back for a lucky row; LUCKY-1 now asserts `stats.rareDrops` unchanged across the attended AND away client kill, and goes RED with the fix removed. (The server's `wk_rare` claim is judged on its own `ev:rare_drops`, so no money moved.) Reveal: only from `away.events` `rare_drop` on the applied response (degrade rungs replace `out`, the forfeit returns no events — events never outlive the credit), deduped on `version:item`; replaying an envelope says nothing (LUCKY-2). **Residual (P3):** a response without `version` would key as `"undefined:item"` and could suppress a second, genuine announcement — under-announce only, never a false one.
4. **(4) Edge payload — hash changes.** New `c81bc5f2a5e79a043bd3e7c6d8692731f30c110eb81ef262e8d5305ab61176a2` (monsters.js rows + `formatDropOdds` in core/drops.js). The in-page edge payload guard is RED until the Coordinator deploys it (here it is additionally RED on the sandbox 403).
5. **(5) Catalogue/census — PASS, zero-diff.** Drop rows live only in src/data (edge-bundled); no table added or changed; `catalogue-literal-drift`, `items-catalogue`, `restore-census`, `inventory-mint-census` green; `hr_apply` still resolves each id against its own catalogue.
6. **Accepted residuals carried from the pre-review:** forged attended kill claims ≤ 3× away-sim rate now include lucky rows (bounded, journalled in `meta.att`, Watch A); bank-cap degrade can forfeit a window including a find (pre-existing); world-tick settles send no events, so a find there is silent (never false). `G.stats.rareDrops` being a client-dice counter for ORDINARY rare rows is pre-existing and out of this lane.

### Gates on `sec/lucky-finds` head (real exit codes)

| Gate | Exit |
|---|---|
| `node tests/lucky-finds.mjs` | 0 |
| `node tests/lucky-finds.mjs --selftest` | 0 (9/9 RED) |
| `node tests/world-tick-combat-parity.mjs` | 0 |
| `node tests/world-tick-combat-parity.mjs --mutate --<name>` × 18 | 0 each, every mutant "RED, as required" |
| `node tests/hearthfind-boss-rate.mjs` | 0 |
| `node tools/lane-done.mjs` | 0 ("all green") |
| `node tests/run-smoke.mjs` | 1 — 1367/1381 passed, 1 failed, 13 skipped. **All reds are environmental** (sandbox egress to Supabase): "errors: clean log" (`Failed to fetch` ×6), Edge payload guard (GET hr-accrue → 403), Account-wall and Cold-load guards (`Failed to fetch`). LUCKY-1..4 pass. |
| SEC-LF-1 proof: `run-smoke --only LUCKY-` with fix / with the fix line removed | 4/4 / LUCKY-1 RED ("counted in stats.rareDrops (0 -> 2)") |

### COORDINATOR RUNBOOK (edge deploy; no DB apply; never 00:00–00:10 UTC)

Pre-checks:
- P1 Record the LIVE payload hash first (the rollback target): the in-page payload guard's GET, or `node tools/pack-edge.mjs hr-accrue --hash` on `origin/main` — expected **c799d1594e286d3b872d41a2d6970f4ac50e5f8eab61c98ff03586d55a517bad**. If live differs from that, STOP and find out what is deployed.
- P2 Read-only SQL: `select id, tradeable from public.hr_items where id in ('wolfbone_torc','bramble_blade','banded_signet','adept_body','fang_studs','rat_stick','willow_longbow','willow_staff','spidersilk_choker','maple_bow','lazlos_maul','warlock_helmet','wraithglass_drops','warlords_torc','trollhide_cape','void_censer','panthers_eye_pendant','wraithsilk_shroud','yew_staff','yew_bow','shadowsilk_cape','chitinweave_cloak','archmage_gloves','dragonrib_bow','runewood_staff','emberfang_blade');` — **26 rows, all tradeable**. Fewer than 26: STOP (an unknown id makes `hr_apply` refuse the whole settle for that hunting spot).
- P3 On the deploy tree: `node tests/lucky-finds.mjs && node tests/hearthfind-boss-rate.mjs && node tests/world-tick-combat-parity.mjs` all exit 0, and `node tools/pack-edge.mjs hr-accrue --hash` prints the expected hash below.

Deploy (CLAUDE.md §3.3), from a checkout of the reviewed head:
```
node tools/pack-edge.mjs hr-accrue --out <dir>/supabase/functions/hr-accrue
cp supabase/config.toml <dir>/supabase/config.toml
npx --yes supabase@latest functions deploy hr-accrue --workdir <dir> --project-ref nezapsylztqbbwuwembx
```
Expected: live `payload_sha256` == **c81bc5f2a5e79a043bd3e7c6d8692731f30c110eb81ef262e8d5305ab61176a2**.

Post-deploy:
- D1 Live `payload_sha256` equals the hash above; the in-page edge payload guard goes green.
- D2 Live-shaped proof (read-only, 24–48 h after deploy): `select item_id, count(*) from public.player_ledger where item_id in (<the 26 ids>) and created_at > '<deploy ts>' group by 1;` — every row's source is an accrue settle (never another path), counts in the order of (kills at that spot) / (1 in N). Adjust column names to the ledger's schema; no writes.
- D3 `node tools/vitals.mjs` — fights/claims not down vs the prior day, and `--refusals` shows no new `unknown_item`/accrue refusal burst on the 26 spots.
- D4 The client half (lucky-finds.js, formatter, LUCKY-1..4) rides the next 20:00 UTC cut AFTER D1; visual gate on combat, inventory, BotD card, desktop + 922×423.

Rollback: redeploy the previous payload — the same three commands from a checkout of `origin/main` (b553), verifying live `payload_sha256` == **c799d1594e286d3b872d41a2d6970f4ac50e5f8eab61c98ff03586d55a517bad**. Items already minted stay (they are real, journalled, tradeable gear with a crafting source); no removal SQL without a separate Security GO. Hold the client half if the edge is rolled back (it would then name rows the engine cannot roll — harmless, as it only reacts to server events, but the odds text would be advertising a dead drop).

### Verdict

DATA+EDGE DEPLOY: GO

CLIENT SHIP: GO-WITH-CHANGES (SEC-LF-1 is landed on `sec/lucky-finds`; ship that head, not e3eb9929, and only after the edge deploy's D1)

## LEDGER OF FIRSTS — 2026-09-26 review

Scope: the Ledger delta only, `git diff 183abee4 a01a6dde` (lane/content-ledger-of-firsts), reviewed on
`sec/ledger-of-firsts`. Lucky Finds is reviewed separately (sec/lucky-finds). No DB access; nothing applied or deployed.

### What was executed

| # | Property | Evidence | Result |
|---|---|---|---|
| 1 | Eligibility is server-decided | `__ungated` re-derives `count(*)` from `hr_bestiary_of` / `hr_collection_of` (server rows written only by the engine's sim `fx.addItem` / kill seams, accrual.js:2067); the client sends only `{p_milestone_id, p_slot}` (goal-claim.js:300) | PASS |
| 1 | Body is 2026-08-22 §4 verbatim + 7 arms | awk-extracted body diff: only 7 added `when` lines | PASS |
| 1 | Idempotent / no double pay | once-guard `insert … on conflict do nothing` + `row_count` before the credit, same txn. Two tabs: the 2nd insert blocks on the 1st's unique tuple, then conflicts → `already_claimed`. §4(b) replays all 11 | PASS |
| 1 | Client never earns from residue | `serverCounts()` reads `G._bestiaryTrophies.killsByMonster` (replaced whole, roster-filtered) and `G._collectionServer.found`; null → nothing claimable. LEDGER-1..4 + collection-wire-shape selftest | PASS |
| 2 | Gold bounded + journalled | server CASE catalogue; one `player_ledger kind='collection'` row per ok claim (§4(e)) | PASS |
| 3 | Reachability | see below | PASS (after SL-1) |
| 4 | Edge parity | read-only savepoint, verified `${user}`/`${slot}`; additive `collection` key at the 4 bestiary sites; accrual.js untouched | PASS |
| 5 | Grants / drift / order | §4(a)+(f); schema-drift 0; second apply byte-identical; apply-order-honesty 0; live-hash-drift 0 (no body read-back) | PASS |

**§4 mutation proof** (scratch PGlite harness via `bootReplay({patches})`, the file patched, the chain replayed):
hunter40 gold 9000 → RED (b) · once-guard removed → RED (b, collect25 replay re-credited) · `sum(qty)` instead of
`count(*)` → RED (c) · `grant … to authenticated` → RED (a) · hunter25 gems 5 → RED (b) · hunter60 threshold 59 → RED (b) ·
collect75 domain monsters → RED (b) · ledger insert removed → RED (e). 8/8. (Dropping only the `revoke` stays green,
correctly: `create or replace` keeps the 2026-08-22 ACL.) Second apply of the file on the replayed chain: BYTE-IDENTICAL.

**Economy.** Full ladder 165,000 g + 40 gems per slot; **new: +93,000 g, 0 gems per slot, once each** (6 slots max per
account = 558,000 g, each needing its own play). Earliest new gold: collect25 1,000 g (T1 has 28 non-lucky drops) and
hunter25 4,000 g (T1's 14 + 11 of T2): 5,000 g in the first tiers. That's small next to renown's 1.603M. No cheap-monster
shortcut: distinct counts need distinct catalogue ids, and the only such lever is the known bounty mint (R-1).

**Reachability (measured on this head):** 108 monsters; 139 distinct drop ids = 107 plain + 26 lucky + 6 BoP keys
(goblin_seal, bone_key, arcane_tome, void_fragment, obsidian_sigil, dragonsbane_key). Items rungs ≤ 133 non-BoP ids, so
none depends on a BoP or Hearthfind drop (Hearthfind items aren't in MONSTERS drops). collect100 ≤ 107 (no lucky needed);
**collect125 needs ≥ 12 of the 26 Lucky Finds** (≥ 18 if no BoP key ever dropped). Reachable, but only with Lucky Finds
live: without pack 1 the max is 113 < 125 (the brief's fallback).

### Findings

- **SL-1 (Medium, guard gap; FIXED on this branch).** The drift guard's items reachability counted BoP keys, so a rung
  reachable only through a bind-on-pickup key would pass. The pool now excludes `ITEMS[id].bop`, with a 10th `--selftest`
  mutation (9 lucky ids flagged bop → 124 < 125 → RED). smoke.yml comment updated; no step added.
- **R-1 (accepted residual, carried from the pre-review).** The bounty-branch distinct-monster mint
  (`hr_accept_bounty__ungated` + `hr_credit_kills__ungated`, no `active_id` check) opens hunter25/40/60/85 = 52,000 g per
  slot, once, for a forger. It's journalled (hr_kill_credit_log targets; the ledger `meta.have`). Detection:
  `count(distinct target) > 6` per character per day from `hr_kill_credit_log where not free`. Fix it in its own lane C
  BEFORE any further monster-count rung is added.
- **I-1 (Info).** `collection.found` is read before this settle's `hr_apply`, so the display lags one settle. It only
  under-offers (fail-safe), and the claim re-derives at call time.
- **I-2 (Info).** The client counts roster-filtered monster ids; the server counts every `ev:kill_monster:*` key. The
  server can be ≥ the client: an under-offer, never a false Claim.
- **I-3 (Info).** `noteServerClaims` replaces the server-claimed set on each envelope carrying `progress`. The residue
  still backs it, and a re-offer self-heals as `already_claimed`, with no value moved.

### Edge

`node tools/pack-edge.mjs hr-accrue --hash` on the reviewed head: **60370eef754584def521c9fa26ab7cec32003d01028d5d501ad26907ae37ad0f**
(lucky-finds base 183abee4: c81bc5f2…; origin/main: e7b8f9b5…). This payload **contains Lucky Finds' monsters.js drop rows**.
The hash that is deployed is the one packed from the ASSEMBLED set, not this lane's.

### Verdict

MIGRATION: GO
EDGE DEPLOY: GO-WITH-CHANGES — deploy only from a set that also carries Lucky Finds with its own Security GO (this payload includes pack 1's drop rows); if Lucky Finds is BLOCKed, this edge is not deployable as-is.
CLIENT SHIP: GO-WITH-CHANGES — SL-1 (landed on `sec/ledger-of-firsts`; merge that head, not a01a6dde); ships at the cut AFTER the apply and the edge deploy.

### COORDINATOR RUNBOOK (one file, never 00:00–00:10 UTC)

Pre-checks (read-only):
- P1 `select to_regprocedure('public.hr_claim_milestone(text,integer)'), to_regprocedure('public.hr_claim_milestone__ungated(text,integer)'), to_regprocedure('public.hr_bestiary_of(uuid,integer)'), to_regprocedure('public.hr_collection_of(uuid,integer)'), to_regprocedure('public.hr_note_rejection(text,integer,jsonb)');`: all non-null.
- P2 `select public.hr_assert_grant_hygiene(true);`: three lists `[]`.
- P3 `select count(*) from public.player_ledger where kind='collection';` and the same on `player_progress where kind='collection'`: note both (baseline for post-apply).
- P4 save the rollback body: the 4-arm `__ungated` is `2026-08-22-collection-claim.sql` §4 (repo = live, measured 2026-09-26). No read-back is needed.

Apply (the single call): `node tools/apply-migration.mjs supabase/migrations/2026-09-27-ledger-of-firsts.sql`. The
`ledger-of-firsts: 11 arms walked …` NOTICE must print. Any exception means nothing landed.

Post-apply (read-only):
- `select has_function_privilege(r,'public.hr_claim_milestone__ungated(text,integer)','execute') from unnest(array['anon','authenticated','service_role']) r;`: all false.
- `select public.hr_assert_grant_hygiene(true);`: three lists `[]`.
- `select count(*) from auth.users where id = '000000c0-0000-0000-0000-0000000027c1';`: 0 (zero leak).
- The P3 counts are unchanged.
- `live-hash-drift --live`: no new pin expected. Apply-order note → APPLIED, and flip the stale 2026-08-22-collection-claim note to APPLIED. restore-census: no change.

Edge (AFTER the apply, AND after Lucky Finds has a Security GO and is in the same set): pack from the assembled set,
deploy, then check that the live `payload_sha256` equals that set's `pack-edge --hash`. Lucky Finds has no SQL, so its edge
rows and this lane's `collection` read ship in ONE deploy. There's no second deploy and no ordering between them beyond
"both reviewed". Old clients ignore the new key.

Client: at the daily cut after the edge. Visual gate on the Collection log modal and the Home tile, desktop + 922×423.
Play gate: a QA-account settle shows `collection.found`; the next-rung line matches it.

Rollback: re-apply 2026-08-22-collection-claim.sql §4 (the 4-arm body) plus its revoke through apply-migration (the new
ids then answer `unknown_milestone`, which the wrapper journals); redeploy the previous edge payload. Paid claims stay paid
and ledgered. The client must roll back with the SQL, or it offers rungs the server refuses.

### Gates (real exit codes, this head)

`collection-renown-claim-drift` 0 · `--selftest` 0 (10/10) · `collection-wire-shape` 0 · `--selftest` 0 (7/7, control silent) ·
`schema-drift` 0 · `apply-order-honesty` 0 · `live-hash-drift` 0 · `lucky-finds` 0 · accrual-engine / accrue-envelope-away /
away-receipt-journal / world-tick-parity / world-tick-combat-parity / trophy-wire-shape / world-tick-edge-contract 0 ·
`edge-jwt-gate --strict` 1 (ENVIRONMENTAL: host not in the sandbox allowlist).

## PROVISIONER'S COUNTER - 2026-09-26 review

Subject: `lane/content-provisioners-counter` @ de5224e7 (contains origin/set/b555). Reviewed on `sec/provisioners-counter`.
Delta: `seed.cooked_lobster` (5 for 2000 g) added, `equip.iron_arrows` (1 for 150 g) retired, shops.js regenerated,
two `ITEMS[id]` reads in the edge catalogue moved to `catalogueGet`, G-PROV + PROVISION-1/2 + the b526 loop extended.

SERVER/MIGRATION: NOT NEEDED
CLIENT SHIP: GO-WITH-CHANGES (ordering only: the edge deploy below must be live before the client half ships)

### Findings (executed, not read)

1. **Where the server reads the price: the edge bundle, not SQL.** `2026-08-15-gold-intents.sql` L12-20 rules that the
   128 offers are NOT re-expressed in PL/pgSQL. `hr-accrue/catalogue.js` builds `GOLD_OFFERS` from `src/data/shops.js`
   (L248-263); `shop-buy.js` charges `offer.gold * qty`; hr_apply only clamps and checks affordability. No `hr_*offers`
   table carries seed/equip rows (`hr_unlock_offers` = unlocks only; `hr_qm_offers` = dungeon). `cooked_lobster` and
   `iron_arrows` are both already in `hr_items` (catalogue.generated.sql L284/L426), so the grant validates. **No migration
   is missing. The edge deploy IS the apply** and this review is its Security GO.
2. **Browser vs server (§6).** The client's `resolvePurchase`/`shopOfferIndex` read the same shops.js, so after the deploy
   they agree byte for byte. The one window where they disagree is ordering: a client that ships before the edge shows
   Lobster and gets `409 unknown_offer` (no gold moves, but it is a "client shows X, server refuses" P1 by §6). Hence
   GO-WITH-CHANGES = edge first. The reverse window (new edge, old cached client still showing the 150 g arrow) refuses
   with `unknown_offer` and moves nothing — G-PROV proves that path.
3. **Arbitrage — closed.** Buy 2000 vs vendor 5×240 = 1200 (cooked food is not `raw`, full `v`): −800 per loop. Cooking
   it yourself: raw lobster vendors at 20 and cooks at Cooking 30 — the fire stays the cheap path. Feast: lobster 16 g
   per heal point vs shrimp 3.75 g (already sold), so no cheaper clan-contribution path. No recipe consumes
   `cooked_lobster`. Computed over all 32 live `GOLD_OFFERS` with the edge's `vendorPriceOf`: no offer has buyback >
   price; seven sit at exactly 1.00 (equip.steel_platebody, six seeds) — pre-existing, zero-profit, handed to the designer.
   Market: an NPC ceiling of 400 g per cooked lobster only caps sellers above it; buying at 400 to resell at the vendor
   loses 160 each. The per-call clamp has headroom (2000 × MAX_QTY ≪ 50 M).
4. **Retired iron_arrows.** Owners keep them and can still sell: `vendor-sell.js` prices via `vendorPriceOf` (1 g), which
   never reads the offer list. No quest, starter kit, recipe input or tutorial references *buying* them (grep: only the
   craft recipes that PRODUCE them, item art/descriptions, the wield-req pin). Bronze arrows (ammoPerShot 0) remain the
   new-ranger answer.
5. **`catalogueGet` edge change — behaviour identical.** Both loops iterate `Object.keys(ITEMS)`, i.e. own keys only, so
   `hasOwnProperty` is always true and `catalogueGet(ITEMS,id) === ITEMS[id]` for all 538 keys (measured, 0 diffs).
   `__proto__`/`constructor`/`toString`/`hasOwnProperty` are not own keys of ITEMS (plain Object prototype) and cannot
   be reached by these loops before or after. Loaded the b555 catalogue.js and this one side by side: `ITEM_EQUIP_SLOTS`,
   `RUNE_ELEMENT`, `EQUIP_SLOT_INDEX`, `ENCHANT_SLOTS` deep-equal, same (null) prototypes, none holds a prototype name.
   The `catalogueGet` import already existed (L39), so no new module edge / cycle.
6. **Edge hash.** `pack-edge --hash`: set/b555 = `3385241e46c5…` → this head = **`33840ea505b5e6197c61b0a7ae088c2fe99d7dbf5988ce2af4d7ab6f627fa6ac`**.
   The bundle changed (shops.js + catalogue.js): **an edge deploy is required**, and it supersedes b555's pending one.
7. **Guards consistent.** gen-shops/unlock/catalogues `--check`, shop-drift-guard (8 mutations), catalogue-literal-drift,
   clan-feast-catalogue-drift, recipe-yield-guard, restore-census (no new table) all 0. G-PROV mutation-proven here:
   lobster at 1999 in shops.js → `gold-intents` exit 1 (`✗ G-PROV: resolveOffer…`), reverted.

### COORDINATOR RUNBOOK

Pre-checks
- The assembled set contains this lane AND every staged migration whose edge half is in the set is APPLIED first
  (today: `2026-09-27-ledger-of-firsts.sql`, per its own runbook above; Lucky Finds has its GO). Deploying this bundle
  before that apply ships an edge that calls SQL that does not exist.
- `node tools/gen-shops.mjs --check` → 0 on the set; `node tools/pack-edge.mjs hr-accrue --hash` on the set — record it
  (equals `33840ea5…` only if nothing else edge-side landed on top).
- Not 00:00–00:10 UTC.

Deploy (no SQL apply)
1. `node tools/pack-edge.mjs hr-accrue --out <dir>/supabase/functions/hr-accrue` + copy `supabase/config.toml`.
2. `npx --yes supabase@latest functions deploy hr-accrue --workdir <dir> --project-ref nezapsylztqbbwuwembx`.

Post-checks
- Live `payload_sha256` == the set's `pack-edge --hash`; the in-page Edge payload guard goes green.
- Play gate (QA account): Supplies → buy one Lobster bundle: gold −2000, +5 lobster in the envelope, ledger row
  `{offer:'seed.cooked_lobster', unit_gold:2000}`; Equipment tab has no Iron Arrows; vendor-sell 1 lobster → +240.
- `node tools/vitals.mjs --refusals`: no `unknown_offer` burst after the client ships.
- Client half rides the 20:00 UTC cut AFTER the deploy; visual gate Shop/Supplies/Equipment, desktop + 922×423.

Rollback
- Redeploy the previous edge payload (the b555 bundle, `3385241e…`, or whatever is live before step 2) and ship the
  client revert in the same window — the two must roll back together or the Supplies row is offered and refused.
  Lobsters already bought stay bought and ledgered; nothing to reverse in SQL.

### Gates (real exit codes, this head)

`gen-shops --check` 0 · `shop-drift-guard` 0 · `gen-unlock-offers --check` 0 · `gen-catalogues --check` 0 ·
`recipe-yield-guard` 0 · `catalogue-literal-drift` 0 · `clan-feast-catalogue-drift` 0 · `restore-census` 0 ·
`gold-intents` 0 (19 groups, real PG18; mutation 1999 → 1) · `schema-drift` not run (no SQL authored) ·
`run-smoke` 1: in-page 1381/1395, 1 ✗ `errors: clean log` (6× `Failed to fetch`) + edge payload guard 403, account-wall,
cold-load, auth-resilience — all ENVIRONMENTAL (sandbox egress blocks supabase.co) ·
`lane-done` 0 ("all green", 24 checks, run on the lane head before this doc commit).
