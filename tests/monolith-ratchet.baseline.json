{
  "_why": "CEILINGS AND FLOORS, not targets. src/legacy.js may only shrink; src/render/** may only grow. Regenerated by `node tests/monolith-ratchet.mjs --write` when a number moves the RIGHT way — never to make a red build green (CLAUDE.md §2).",
  "_method": "lines = physical lines (split on \\r?\\n). functions = column-0 `function name(` declarations, the same predicate as tests/no-duplicate-toplevel-fns.mjs. functionConsts = column-0 `const|let|var name = (async)? function|(|arg =>`. render = every *.js under src/render, recursively.",
  "measured": "2026-09-08",
  "monolith": {
    "file": "src/legacy.js",
    "lines": 20878,
    "functions": 494,
    "functionConsts": 3
  },
  "render": {
    "dir": "src/render",
    "files": 14,
    "lines": 2780
  }
}
