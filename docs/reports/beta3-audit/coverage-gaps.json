[
 {
  "gap_confirmed": true,
  "where": "Class (1) reload-loss / allowlist stranding of G fields. Allowlist: src/net/client-state.js:105-234 RESIDUE_FIELDS (51 names, hand-maintained), hydrateInto at :347-366. Record: src/net/record.js:168 SERVER_OF_RECORD, settle() at :1646-1778. Capstone: src/net/capstone.js:91 `export const BLOB_RETIRED = true;`. Guards checked: tests/arm-homing-guard.mjs (static/derived homing census), src/features/smoke-test.js:27585 RESIDUE-B466 (in-memory patch→hydrate), tests/client-state.mjs, tests/blob-retire.mjs, tests/rooms-record.mjs, tests/equipment-record.mjs, tests/marks-record.mjs, tests/rested-record.mjs, tests/companions-record.mjs, tests/farm-record.mjs, tests/predict-display.mjs, tests/slot-switch.mjs. Live counter-example the gap allows: src/net/property-record.js:207-211 effectivePropertyTier + :229-239 healPropertyTier.",
  "gap_confirmed_note_unused": "n/a",
  "proposed_test_ok": false,
  "test_spec": "CORRECTED SPEC — tests/reload-truth.mjs (node guard, PGlite, NO live DB), three blocks, replacing the proposed tests/record-roundtrip.mjs.\n\nWHY THE PROPOSAL IS REPLACED (two structural defects, both fatal to the class):\n D1. It iterates `RESIDUE_FIELDS` and `serverOfRecordFields()` — i.e. THE ALLOWLIST ITSELF. The original incident (b466, paione: \"Bestiary achievements keep resetting every time you log out and in\", smoke-test.js:27586-27596) was `G.bestiary` NOT BEING ON that list. A loop over the list can never enumerate a field missing from the list, so the proposed test is green on the exact bug it is written for. The iteration source must be the SCANNED G-write set, not the allowlist.\n D2. It asserts only `G[field] deep-equals the written value` — round-trip fidelity. Today's live P1 (Paione, homestead) is the OPPOSITE failure: `homestead` IS homed (client-state.js:206), DOES round-trip perfectly, and is still broken because the residue copy is AHEAD of the server rung and the heal is raise-only (property-record.js:229-239 \"NEVER LOWERS\"). A sentinel written and read back passes on homestead while the player's Forge is refused ×10. Fidelity is necessary and not sufficient; the missing assertion is AGREEMENT with server truth.\n\nFIXTURE (shared, one per block): `bootReplay()` from tests/schema-replay.mjs — the whole migration chain on PGlite in-process, which is what gives real `hr_put_client_state` (+ its 2026-08-22-client-state-denylist.sql deny-list and size cap), `hr_state_of`, `hr_unlock_buy`, `hr_apply`. One character created via the same helper tests/auto-eat-at-creation.mjs uses (do NOT hand-roll `hr_create_character` slot 1 — that is the §8 create-gate `slot_not_owned` ripple currently reddening conservation-fuzz in CI). Client side: `await import('src/net/client-state.js')`, `record.js?v=<derived>`, `accrue.js?v=<derived>` with the ?v= parsed out of the importer's own source (the tests/client-state.mjs:27-32 pattern — otherwise a version bump silently breaks the import and the guard turns vacuous).\n\nBLOCK RT-0 — EVERY WRITTEN FIELD, DRIVEN (kills the ORIGINAL incident).\n Iteration source: import `scanGFieldWrites()` from tests/arm-homing-guard.mjs (export it; it is the derived scanner that produced \"71 scanned from src/\"). For each scanned field name F:\n   • seed `G[F]` with a type-shaped sentinel (object → {__rt: F}, number → 424242, array → [F], string → 'RT:'+F);\n   • run the REAL save path `buildResiduePatch(G)` (capstone.js) and PUT it through the REAL `hr_put_client_state` RPC on PGlite as the character's user;\n   • `select hr_state_of(uid, 0)` and feed `{ok:true, ...env}` through the SAME chain record.js settle() calls — applyRecord, applyClientState + hydrateInto, reconcileInventory/Bank/Workers/Companions/Farm/Traits — onto a FRESH `{}`;\n   • ASSERT: `Gfresh[F]` is defined AND deep-equals the sentinel, OR F is on NO_SYNC/derived with a stated reason.\n This is the assertion RESIDUE-B466 simulates in memory (smoke-test.js:27641-27651, buildResiduePatch→hydrateInto on a caller-supplied object, no server) but never executes against a server: it puts the deny-list, the jsonb shallow merge, the size cap and the projection into the loop, and it enumerates fields the allowlist has forgotten.\n\nBLOCK RT-1 — DUAL-HOME AGREEMENT (kills TODAY'S LIVE P1; the assertion nothing in the repo makes).\n Registry `DUAL_HOMED` = the residue fields that also have a server statement of the same fact. Seeded from the one known pair — `homestead` ↔ `unlock` namespace `property:*` (property-record.js:99-101, whose max IS the tier per 2026-08-16-unlock-buy.sql §prereq_property_tier) — and MECHANICALLY grown: for every RESIDUE_FIELDS name, fail unless the field is either declared server-silent in the registry or declared dual-homed WITH a stated reconciliation direction. A new residue field that shadows a server fact then cannot ship undeclared.\n EXACT ASSERTIONS, both directions, per dual-homed field:\n   RT-1a (server ahead — the b493 tier-heal case): server rung 2, residue tier 1 → after the apply chain, `effectivePropertyTier(G) === 2` AND `G.homestead.tier === 2` (heal persists). This one passes today.\n   RT-1b (RESIDUE AHEAD — THE LIVE BUG): write `client_state.homestead = {tier:2}` via the real `hr_put_client_state`, grant only `property:homestead` (rung 1) via the real `hr_unlock_buy`, then reload → **ASSERT `G.homestead.tier === 1`** (server truth wins DOWNWARD), AND that the next `buildResiduePatch(G).homestead.tier === 1` (the corrected value is what re-uploads, so the deadlock cannot re-arm), AND that a subsequent real `hr_unlock_buy('room.forge.1')` is REFUSED with `prereq_property_tier` while the client UI gate agrees — i.e. client refusal and server refusal are the same verdict, never \"client offers, server refuses ×10\". On current main RT-1b FAILS (residue 2 survives; property-record.js:210 returns max(2,1)=2), which is exactly the confirmation this guard exists to give.\n   RT-1c: same shape for `worker_hire` via effectiveWorkerSlots (property-record.js:254-257) — same raise-only asymmetry.\n\nBLOCK RT-2 — MUTATION CONTROL (the file is REJECTED without it; 48 of 110 tests/*.mjs currently ship none).\n `node tests/reload-truth.mjs --list | --selftest | --mutate=<id>`; a mutation nothing catches exits 1 as SLIPPED.\n   m1 `residue_field_dropped` — delete 'bestiary' from RESIDUE_FIELDS → RT-0 RED (this is the original incident, replayed).\n   m2 `unhomed_new_field` — plant a `G.__rtProbe = 1` write in a scratch src/ fixture → RT-0 RED (a NEW field, invisible to any allowlist loop).\n   m3 `raise_only_heal` — restore `effectivePropertyTier` to max(server, residue) → RT-1b RED. Because main is red on m3 today, RT-1b is authored as an EXPECTED-FAIL pinned to the open P1 and flipped to hard-fail by branch fix/property-rung-server-truth in the same commit as the fix (CLAUDE.md testing discipline: the test ships with the fix).\n   m4 `denylist_swallow` — add a residue field to the hr_put_client_state deny-list → RT-0 RED (the failure arm-homing-guard.mjs:329-345 checks statically, now driven).\n\nWHERE IT LIVES: node guard, `tests/reload-truth.mjs`. NOT in-page — smoke-test.js has no server and cannot boot PGlite, which is precisely why RESIDUE-B466 had to simulate. Wired BOTH into tests/run-smoke.mjs's preflight AND into .github/workflows/smoke.yml, per the confirmed P0 that local run-smoke is strictly weaker than CI (a guard wired only locally is invisible to the release gate).\nLIVE DB: NOT required. PGlite replays the real chain. One optional `--live` arm (read-only, beside tests/beta-invite-gate.mjs --live) re-runs RT-1b against a real account to confirm the deploy matches the replay; it must never be the primary arm.",
  "evidence": "GAP CONFIRMED by execution and by file:line on main (HEAD 00b927b1).\n\n1. THE TWO SETS ARE DISJOINT (the board claim). `grep -ln \"pglite|bootReplay|PGlite\" tests/*.mjs` → 59 files; `grep -ln \"applyRecord|hydrateInto|applyClientState|decodeRecord|reconcileInventory|reconcileCompanions|reconcileFarm|buildResiduePatch\" tests/*.mjs` → 15 files. `comm -12` of the two sorted lists returns ONLY tests/run-smoke.mjs and tests/run-sql-tests.mjs — the two RUNNERS, not guards. No guard on main does client write → real server → hr_state_of → client apply.\n\n2. THE hr_state_of CALLERS DO NOT APPLY. `grep -ln hr_state_of tests/*.mjs` → 27 files, but the only ones that also touch src/net/record.js or client-state.js are tests/arm-homing-guard.mjs (readFile of record.js source at :230 and :364 — a REGEX over source, not an execution) and tests/client-state.mjs, whose only \"hr_state_of\" is a comment at :7 and which never boots PGlite (verified: no pglite/bootReplay match in the file; its RPC check at :112 is a captured fetch URL regex).\n\n3. THE HOMING GUARD IS STATIC AND GREEN. Ran `node tests/arm-homing-guard.mjs` → `Arm-homing guard — all 75 G fields homed (71 scanned from src/, 9 record, 51 residue, 8 mechanism, 19 no-sync).` It proves a field is LISTED. It executes no write and no read.\n\n4. THE ONLY \"ROUND-TRIP\" TEST IS IN-MEMORY. src/features/smoke-test.js:27585 RESIDUE-B466 drives buildResiduePatch → hydrateInto on a caller-supplied object (:27641-27651) with a hand-typed 15-name list at :27612-27614; only 4 fields carry real values (:27636-27638). No HTTP, no server. All 6 hr_put_client_state hits in smoke-test.js (:11302, :27599, :27665, :32198, :32238, :32253) are prose or transport-shape/retry assertions.\n\n5. STRUCTURE VERIFIED. src/net/client-state.js:105 RESIDUE_FIELDS — counted 51 quoted names in :105-234; hydrateInto exported at :347, iterating RESIDUE_FIELDS at :348 (its own header at :341-346 states it was exported \"so the round-trip is testable as a PURE function\" and that driving it through the envelope path \"would either be vacuous … or would have to reset the session's own bag\" — the repo's own admission that the real path is untested). src/net/record.js:168 SERVER_OF_RECORD, settle() at :1646, reconcile calls at :1681/:1697/:1708/:1731/:1732/:1743. src/net/capstone.js:91 `BLOB_RETIRED = true` (its trailing \"// DORMANT\" comment is stale, per the brief).\n\n6. THE PROPOSED TEST WOULD NOT HAVE CAUGHT THE ORIGINAL INCIDENT. Named incident: b466 / the 2026-08-24 sweep — paione, live open beta, \"Bestiary achievements keep resetting every time you log out and in\" (smoke-test.js:27586-27587), root cause \"G.bestiary was written by the game and homed by nothing\" (:27591-27592), one of 22 stranded reload-reset fields (memory: live-proven-bugs-2026-08-24), same class as the beta-morning failures in CLAUDE.md §Session criteria 1 (\"a flag/payout that used to live in the save blob, forgotten on reload\"). The proposal's iteration source is \"EVERY entry in serverOfRecordFields() and EVERY name in RESIDUE_FIELDS (iterate the exports)\" — bestiary was on neither, so the loop would have skipped it and passed green.\n\n7. IT ALSO MISSES TODAY'S LIVE P1, THE STRONGEST CURRENT INSTANCE. `homestead` IS a residue field (src/net/client-state.js:206) and round-trips correctly, so a sentinel write/read-back passes. The bug is agreement, not fidelity: src/net/property-record.js:207-211 `effectivePropertyTier` returns `observedTier > residue ? observedTier : residue` (a max) and :229-239 `healPropertyTier` documents \"NEVER LOWERS\" (:227) with header rule \":46-48 THE SERVER RUNG WINS UPWARD\" — so residue tier 2 over server rung 1 survives every reload AND is re-uploaded by buildResiduePatch (:220-223 documents that the raised value \"rides the next buildResiduePatch upload\"), which is the observed Paione deadlock (Forge refused prereq_property_tier ×10).\n\n8. SCRIP SUB-CLAIM VERIFIED. src/dungeons.js:207 `window.addItem('dungeon_scrip', amt)` inside awardDungeonScrip; tests/inventory-mint-census.mjs:119 lists the tokens in the baseline and :154-157 declares them in BLOB_RETIRE_UNSAFE_LANES — DECLARED unsafe, not closed. No tests/*.mjs filename contains roundtrip/round-trip/reload (`ls tests/ | grep -i` → empty).",
  "note": "Verdict: gap CONFIRMED, proposed test REFUTED as written (it would have passed on the incident it names, and passes today on the live P1). Corrected spec's two load-bearing changes: (i) iterate the DERIVED G-write scanner, never the allowlist — an allowlist loop is structurally blind to a stranded field; (ii) assert AGREEMENT with server truth in BOTH directions, not just round-trip fidelity — RT-1b (residue ahead → server wins downward) is the assertion no test in the repo makes and is RED on current main, so it should land in the same commit as fix/property-rung-server-truth rather than as a separate lane. Two side findings while verifying, not part of this item: (a) tests/client-state.mjs:47 asserts `CLIENT_STATE_SERVER_BACKED !== false` fails, i.e. it REQUIRES the flag to ship false (\"DORMANT\") — worth a separate check that this guard has not become vacuous or inverted now that capstone.js:91 BLOB_RETIRED is true; (b) arm-homing-guard's scanner self-documents blind spots at :46-70 (opaque `Object.assign(G,x)` and `X = G;` alias roots), so even RT-0's iteration source is a floor, not a ceiling."
 },
 {
  "gap_confirmed": true,
  "proposed_test_ok": false,
  "where": "Class (2) idle-boot hydration omissions (hr_load path). Primary defect: `enchant` is a declared SERVER_MECHANISM field (tests/arm-homing-guard.mjs:270) that src/net/record.js settle() (:1646-1778) never hydrates; the only writer is src/net/accrue.js:1841-1845 inside applyEnvelopeState, reachable only from accrue.js:2709 (accrued:true) and activity.js:674 (activity switch). Guard defect: the MUST_CALL list at tests/arm-homing-guard.mjs:373-374 is hand-typed and does not derive from SERVER_MECHANISM_FIELDS (arm-homing-guard.mjs:267-278).",
  "test_spec": "TWO PIECES. Piece 1 is the class-kill; piece 2 is the behavioural proof. Neither needs a live DB.\n\n=== PIECE 1 (node guard, extends tests/arm-homing-guard.mjs, no DB) ===\nName: MECHANISM_RECONCILE totality check (replaces MUST_CALL at arm-homing-guard.mjs:360-380).\nFixture: none (source scan, same as today).\nChange: declare `const MECHANISM_RECONCILE = { inventory:'reconcileInventory', bank:'reconcileBank', workers:'reconcileWorkers', companions:'reconcileCompanions', farmPlots:'reconcileFarm', plotLevels:'reconcileFarm', traits:'reconcileTraits', enchant:'reconcileEnchant' }` and assert, over SERVER_MECHANISM_FIELDS (the existing Set at :267-278), THREE things:\n  (1) TOTALITY — `for (const f of SERVER_MECHANISM_FIELDS) if (!MECHANISM_RECONCILE[f]) fail(f + ' is declared a SERVER_MECHANISM field but maps to no reconcile function — an idle boot answers accrued:false, so applyEnvelopeState never runs and the field silently resets on reload (b477 invisible-crew class).')`\n  (2) NO ORPHAN MAP KEYS — `for (const f of Object.keys(MECHANISM_RECONCILE)) if (!SERVER_MECHANISM_FIELDS.has(f)) fail(...)` (stops the map rotting into decoration).\n  (3) THE CALL — for each distinct value fn, keep the existing regex `new RegExp(fn + '\\\\s*\\\\(\\\\s*G\\\\b').test(recSrc)` against src/net/record.js source, failing with the field name that motivated it.\n  (4) NON-VACUITY — `if (SERVER_MECHANISM_FIELDS.size < 8) fail('the mechanism census shrank — the totality check may be iterating nothing')`.\nWhere: node guard, already in run-smoke preflight (run-smoke.mjs:2440 region). No new wiring.\nExpected result on current main: RED on `enchant` (no reconcileEnchant exists, and settle() has no enchant step) — which is the correct verdict; it goes green with the fix below.\nPrerequisite fix shipped in the same commit (per CLAUDE.md testing discipline): extract `export function reconcileEnchant(G, res)` from src/net/accrue.js:1841-1845 verbatim (the ember/frost/poison validation and the \"absence is not a claim\" rule), call it from applyEnvelopeState where the inline block was, and add `hydrationStep('enchant', () => reconcileEnchant(G, verdict.body));` to settle() beside the traits step (record.js:1708). hr_state_of already projects the field (supabase/migrations/2026-08-18-enchant.sql:158, self-checked at :1558-1560), so no migration is needed.\nLive DB: no.\n\n=== PIECE 2 (node guard, tests/idle-boot-hydration.mjs, PGlite — NOT live) ===\nFixture: `bootReplay()` from tests/schema-replay.mjs (the hero-slot-buy.mjs:61 pattern); one character via hr_create_character(slot 0); then seed EVERY mechanism field with its REAL server writer, never a hand-built row:\n  inventory/bank — hr_apply items delta (admin grant, the tests/enchant-intent.mjs:72 shape);\n  workers — hr_hire; companions — hr_companion_grant; farmPlots+plotLevels — hr_farm_plant (+ the plot-tier verb);\n  traits — hr_trait_buy; enchant — grant a rune then hr_apply {enchant:{weapon:'<rune_id>'}} (hr_apply resolves it to the element via hr_runes, 2026-08-18-enchant.sql:823).\nThen capture the exact body the client will see: `select hr_state_of(UID, 0)` → BODY.\nHarness: `globalThis.window = { G: {} }` (the tests/blob-retire.mjs:31 shim) plus the capstone arm; `globalThis.fetch = (u) => /hr_load/.test(String(u)) ? Promise.resolve(new Response(JSON.stringify({ ok:true, ...BODY }))) : realFetch(...)` — the identical stub INV-HYDRATE-1 uses at smoke-test.js:38283, but fed the REAL projection instead of a hand-typed `{inventory: FULL}`.\nDrive the REAL path (this is the part the reader's proposal got wrong — do NOT re-implement the apply chain): `import { configureRecord, requestRecord, bootHydrationState, resetRecord } from '../src/net/record.js'` and `const v = await requestRecord()`. record.js is node-driveable: it has exactly one `document.` reference and uses global `fetch` (record.js:1595).\nExact assertions:\n  A1  v.outcome === 'loaded'.\n  A2  TOTAL LOOP over SERVER_MECHANISM_FIELDS (imported from the guard, not retyped): for each field, `window.G[field]` is defined AND deep-equals the projection of BODY for that field. Concretely: G.enchant.weapon === the element hr_runes gave the seeded rune; G.workers.hired contains the hired uid; G.traits includes the bought trait id; G.farmPlots.length === the planted count; G.inventory[id] === BODY.inventory[id] for every id.\n  A3  bootHydrationState().partial is empty (no hydrationStep threw and got swallowed).\n  A4  NON-VACUITY: assert the loop actually ran `SERVER_MECHANISM_FIELDS.size` iterations and that size >= 8, so a field quietly dropped from the census cannot green the test by iterating nothing.\nMutation control (`--mutate`, mandatory — this file must not join the 48 flag-less guards): load src/net/record.js source into a temp module with `reconcileWorkers(G, verdict.body)` (record.js:1743) deleted, import THAT, and assert A2 goes RED naming 'workers'; repeat deleting the new enchant step and assert RED naming 'enchant'.\nWhere: node guard `tests/idle-boot-hydration.mjs`, wired into tests/run-smoke.mjs preflight beside autoEatAtCreationGuard/armHomingGuard AND added to .github/workflows/smoke.yml, so local and CI agree (the confirmed P0: run-smoke is strictly weaker than CI).\nLive DB: no — PGlite replay only.\n\n=== PIECE 3 (in-page, one test, cheap) ===\n`ENCH-HYDRATE-1` in src/features/smoke-test.js, cloned from INV-HYDRATE-1 (:38273): stub hr_load to `{ok:true, version:3, now, state:{slot:0, accrued_to, enchant:{weapon:'ember'}}, skills:{}, inventory:{}}`, set `window.G = { enchant:{}, offlineBudget:{} }`, `await R.requestRecord()`, assert `G.enchant.weapon === 'ember'` with the message \"an IDLE boot must rebuild the paid weapon enchant from the hr_load body — otherwise the affordance (legacy.js:8556) renders 'Enchant weapon' and the player burns a second rune on one they already own\". This covers the browser wiring the node shim fakes.",
  "evidence": "GAP CONFIRMED — verified on current main (HEAD 00b927b1, repo root R:\\the game\\the game):\n1. `enchant` is declared a SERVER_MECHANISM field: tests/arm-homing-guard.mjs:270 `'enchant',     // accrue.js (state.enchant)` inside SERVER_MECHANISM_FIELDS (:267-278, 8 entries).\n2. MUST_CALL is HAND-TYPED and shorter: tests/arm-homing-guard.mjs:373-374 `const MUST_CALL = ['reconcileInventory','reconcileBank','reconcileWorkers','reconcileCompanions','reconcileFarm','reconcileTraits'];` — 6 names for 8 mechanism fields; it is not derived from the Set, so `enchant` is never checked.\n3. settle() has no enchant step. `grep -n \"reconcile[A-Za-z]*\\s*(\" src/net/record.js` → only :1111 reconcileCreditedXp, :1681 reconcileCompanions, :1697 reconcileFarm, :1708 reconcileTraits, :1731 reconcileBank, :1732 reconcileInventory, :1743 reconcileWorkers. settle() is src/net/record.js:1646-1778; I read it in full — the steps are client-state, companions, farm, traits, inventory+bank+workers, daily, activity-resume. No enchant.\n4. The ONLY writer of G.enchant from server state is src/net/accrue.js:1841-1845 (inside applyEnvelopeState, exported at :1797), and applyEnvelopeState has exactly two call sites: accrue.js:2709 (the accrued-settle path) and src/net/activity.js:674 (the activity-SWITCH envelope). Neither runs on an idle boot ({accrued:false, reason:'idle'}). src/legacy.js:1373 then defaults `G.enchant={}`.\n5. The server DOES send it, so the omission is purely client-side: supabase/migrations/2026-08-18-enchant.sql:158 `'enchant', coalesce(v_st.enchant, '{}'::jsonb)` in hr_state_of, with a self-check at :1558-1560.\n6. PLAYER IMPACT is the b477 shape with a consumable cost: src/legacy.js:8556 `const el=(G.enchant&&G.enchant.weapon)||null;` decides whether the affordance renders \"✦ <el> enchant / Change\" or \"✦ Enchant weapon\"; :8576 openEnchantPicker reads the same. hr_apply's enchant block MERGES (2026-08-18-enchant.sql:851-854) with no already-enchanted refusal and debits the rune (insufficient_item path), so a player whose enchant vanished on an idle reload re-enchants and burns a second rune. G.enchant also feeds client combat prediction (src/core/combat.js:134,166 via legacy.js:3262).\n7. NO TEST COVERS IT. `grep -rn enchant tests/*.mjs` → only arm-homing-guard.mjs:190/270 (census strings) and tests/enchant-intent.mjs, whose only \"settle\" hits (:27,:108,:332,:377-378) are the server-side collect-before-enchant ordering — it never imports record.js and never drives a client apply. In-page: ELEM-6 (smoke-test.js:45456-45476) is the only enchant-persistence test and it asserts membership in `Ev.snapshot(G)` — the RETIRED blob (src/net/capstone.js:91 `export const BLOB_RETIRED = true;`) — then calls `A.applyEnvelopeState` directly, i.e. it proves the path that does NOT run on an idle boot. INV-HYDRATE-1 (smoke-test.js:38273-38306) drives the real requestRecord→settle but with a hand-typed body carrying only `inventory`, so it is green today with enchant broken.\n8. The guard reports GREEN with the hole open. `node tests/arm-homing-guard.mjs` → `Arm-homing guard — all 75 G fields homed (71 scanned from src/, 9 record, 51 residue, 8 mechanism, 19 no-sync).` EXIT=0.\n9. No PGlite test exercises the client apply chain: `comm -12` of `grep -rl \"pglite|bootReplay\" tests/*.mjs` (59 files) against `grep -rl \"net/record.js|net/accrue.js|net/client-state.js\" tests/*.mjs` (17 files) = {hero-slot-buy.mjs, run-smoke.mjs}; hero-slot-buy.mjs only READS record.js as text (tests/hero-slot-buy.mjs:758 `/export function reconcileHeroSlots\\(/.test(rec)`), so no test feeds a real hr_state_of body through settle().\n\nPROPOSED TEST — JUDGED AGAINST THE ORIGINAL INCIDENT (b477 \"invisible crew\", QA 0a47ba77: a player with Aldric mining saw G.workers.hired=[] after an idle reload and re-hired a worker he already owned; fixed in commit aaeeb48c \"fix(workers): hydrate crew roster on idle boot (b477) + guard the class\"):\n· Part (a) — derive MUST_CALL from SERVER_MECHANISM_FIELDS — WOULD have caught it, proven against real history. At a5b074ba (the commit immediately BEFORE aaeeb48c), `git show a5b074ba:tests/arm-homing-guard.mjs` already listed 'workers' (and 'inventory','bank','enchant') in SERVER_MECHANISM_FIELDS at lines 123-126, while `git show a5b074ba:src/net/record.js | grep \"reconcile[A-Za-z]*(G\"` returned only :1361 reconcileCompanions, :1372 reconcileFarm, :1383 reconcileTraits. A derived totality check is RED on workers/inventory/bank at that exact commit — before the player report. So (a) is sound and is the highest-value piece; I keep it, but it is UNDER-SPECIFIED as written: 'enchant' has no reconcile function to map to (it must first be extracted from accrue.js:1841-1845), and the mapping is many-to-one (farmPlots and plotLevels both → reconcileFarm), so the map must be declared explicitly and asserted total in BOTH directions plus non-vacuous.\n· Part (b) — as written it would NOT have caught b477. It says \"run the settle-equivalent apply chain onto fresh G\", i.e. the test calls the reconcile* functions itself. b477 was not a broken reconcile — reconcileWorkers existed and was unit-tested; the bug was that settle() never CALLED it. A test that mirrors the wiring re-creates the omission on the correct side and passes green. That is why proposed_test_ok is false. The corrected form (Piece 2 above) stubs fetch and drives record.js's real requestRecord()→settle(), asserting on window.G afterwards, so a missing call inside settle() is exactly what fails.",
  "note": "Two corrections to the phase-1 reader's list for this item, both verified: (1) applyEnvelopeState has TWO call sites, not one — src/net/accrue.js:2709 and src/net/activity.js:674 (the activity-switch envelope). The reader named only :2709. This does not change the verdict — an idle boot with no activity switch still gets no enchant — but it means the field also survives incidentally for a player who changes activity after logging in, which is why the bug reads as intermittent. (2) The reader's cross-class claim that the pglite-importing and client-apply-importing test sets are disjoint is very slightly wrong: tests/hero-slot-buy.mjs is in both, though it only regex-scans record.js as source text (:758), so the substance of the claim holds. Severity call: this is a P1, not a P2 — the failure costs the player a consumed rune (hr_apply merges the enchant with no already-enchanted refusal, 2026-08-18-enchant.sql:851-854), exactly the b477 re-hire shape. Piece 1 goes RED on current main by design; it is meant to ship in the same commit as the reconcileEnchant extraction + the settle() hydrationStep."
 },
 {
  "gap_confirmed": true,
  "where": "Class (3) \"live client actions not server-authored\" — coverage.json index 2. Repo root R:\\the game\\the game, HEAD 00b927b1. Open lanes on current main: src/dungeons.js:206-211 (awardDungeonScrip), :232-247 (buyFromQuartermaster), src/dungeon-scavenger.js (per-node grants), src/legacy.js:6046 (quest reward ITEM), src/legacy.js:8483 (auto-eat sends no intent when the server owns the debit).",
  "proposed_test_ok": false,
  "evidence": "GAP CONFIRMED — I re-derived the guard list rather than trusting the reader (110 files in tests/*.mjs).\n\n1) SCRIP / RUN LOOT / QUARTERMASTER ARE STILL CLIENT-AUTHORED, AND THE GUARD IS GREEN OVER THEM.\n- src/dungeons.js:206-211 `function awardDungeonScrip(id, fraction){ ... window.addItem('dungeon_scrip', amt) ... }` — client mint, no server verb.\n- src/dungeons.js:232-247 `buyFromQuartermaster(id)` adds the item then `removeItem('dungeon_scrip', entry.scrip)` entirely client-side; QM_STOCK (:220-229) is a client catalogue with client prices (19 entries incl. `ashcrown_greatsword` 340, `dragonfang_pike` 800).\n- tests/inventory-mint-census.mjs:175-193 BLOB_RETIRE_UNSAFE_LANES declares 4 lanes (scrip, run loot, quartermaster, scavenger) with `owedIntent` strings. Enforcement block (4) at :297-330 requires only that a token be DECLARED, and it runs over `DUNGEON_FILES = ['src/dungeons.js','src/dungeon-scavenger.js']` (:328) — declaration IS the pass condition.\n- COMMAND RUN: `node tests/inventory-mint-census.mjs` → `Inventory-mint census — OK: every mint site classified; arm-gate fail-closed while un-backed lanes exist.` Exit 0. The build is green with four reload-losing client mints open. The reader's claim holds exactly.\n- No behavioural guard exists: `grep -rln \"quartermaster|dungeon_settle|attended\" tests/*.mjs` → accrual-engine, auto-eat-at-creation, combat-xp-cap-drift, inventory-mint-census, kill-daily-credit, run-smoke — none is a settle/purchase verb test. In-page, smoke-test.js:46247-46293 (B372-SCRIP-1) is an item-ledger synthetic whose own comment says it stays meaningful \"the day a real `quartermaster_buy` verb ships\".\n\n2) NEW FINDING THE READER MISSED — THE QUEST-REWARD ITEM LANE IS BOTH UNGUARDED AND FALSELY PINNED.\n- src/legacy.js:6046 `if(r.item)addItem(r.item,r.qty||1,false);` inside `completeQuest` (:6024). Client mint, no server settlement; the gold half was armed (hr_claim_quest) and the item half was not.\n- tests/inventory-mint-census.mjs:141-152 names this lane in prose (\"at least ONE confirmed sibling of this class … lost on reload under BLOB_RETIRED exactly like scrip/keys\") and explicitly excludes src/legacy.js from enforcement (4).\n- src/features/smoke-test.js:3628-3634 has a comment \"QUEST under arm: proceeds, fires claimQuest, no local gold, item granted\" — but the fixture at :3630 is `reward: { gold: 150 }` with NO `item` key, so the item branch at legacy.js:6046 is never executed by that test. The class's most load-bearing in-page test asserts the item lane in its comment and not in its code.\n\n3) THE AUTO-EAT SUB-CLAIM STANDS, DESPITE A GUARD THE READER DID NOT LIST.\n- src/legacy.js:8483 `if(opts.auto&&!_clientOwnsAutoEatDebit())return true;   // the server eats this one — hold only` (predicate at :8446). Confirmed: no eat intent crosses when server auto_eat_enabled=true.\n- tests/auto-eat-authority.mjs EXISTS and is wired (tests/run-smoke.mjs:20 `import { autoEatAuthorityGuard }`), and the reader did not list it. It does not close the gap: its header (:1-25) scopes it to \"hr_set_auto_eat, EXECUTED\" — the VERB's authority/refusals on PGlite, \"the claim about BEHAVIOUR: what the function does when a player … actually calls it\". Nothing in it compares client-consumed meals to the server's debit. Today's live measurement (7 cooked_shrimp consumed, 0 debited) remains unguarded.\n\n4) THE ORIGINAL INCIDENT IS ALREADY COVERED — WHICH IS WHY THE PROPOSED TEST IS MISAIMED.\nThe beta-2 incident of this class is the combat-style XP routing bug (Paione, proven live 2026-08-24: \"Strength/Defense/HP exp is not saving — only Attack saves\"; memory live-proven-bugs-2026-08-24). tests/combat-style.mjs:17-30 names it verbatim and drives the real computeAccrual; the fix is visible at supabase/functions/hr-accrue/index.ts:662 `combatStyle: st.combat_style ?? null,` and pinned by that guard's `style_from_body` mutation (tests/combat-style.mjs:150-157).\n\nVERDICT ON THE PROPOSED TEST: NO — none of its three parts (census WARN, tests/dungeon-settle.mjs, attended-loot-credit) touches simulation INPUTS, so all three would have run green through the entire style-XP incident. Two further defects: (a) \"assert BLOB_RETIRE_UNSAFE_LANES is EMPTY once hr_dungeon_settle ships\" is self-referential — it can only fire after the fix, so it guards nothing in the window that matters; (b) demoting the open lanes to \"a run-smoke WARN line\" converts a hard gate into a line of prose inside a ~700-assertion run, which is how this repo got to instance #15 of assertions that assert nothing (tests/auto-eat-authority.mjs:17-21).\n\nAlso confirmed: no derived census of engine inputs exists. `grep -rln \"st.combat_style|ENGINE INPUT|inputFrom\" tests/*.mjs` → only combat-style.mjs (one field) and activity-intent.mjs (:221, one incidental mention; its header :3-21 scopes it to the set-activity verb).",
  "test_spec": "TWO specs. The first is the class-kill the proposed test should have been; the second repairs the census the proposal weakens.\n\n════ SPEC 1 — tests/sim-input-authority.mjs (THE CLASS-KILL) ════\nCLASS SHAPE, STATED: every incident in this class is \"a value the player chooses that the SERVER's simulation needs, which has no server column — so the server simulates with a DEFAULT and, because the output field is armed, the server's wrong answer RETIRES the client's correct prediction.\" combat_style was that. auto_eat_food, tool_carry, ammo_carry, active_id and fight are the same shape and are only covered one-incident-at-a-time.\n\nNAME: tests/sim-input-authority.mjs (exports `simInputAuthorityGuard`; `--list` / `--selftest` mutation catalogue, matching tests/combat-style.mjs house style).\n\nWHERE IT LIVES: node guard, wired as a run-smoke PREFLIGHT beside `combatStyleGuard` (tests/run-smoke.mjs:65). It MUST be in run-smoke, not CI-only — the confirmed P0 that local run-smoke is strictly weaker than CI (live-hash-drift is CI-only) is exactly the failure this must not repeat.\n\nLIVE DB: NO. Part A is static parse; part B is PGlite in-process with the real migration chain applied verbatim, the pattern tests/activity-intent.mjs and tests/auto-eat-authority.mjs already use. No credential, runs in CI and locally identically.\n\nFIXTURE:\n- Static: supabase/functions/hr-accrue/index.ts (the engine-input object literal, currently ~:575-745), plus the `player_state` column set parsed out of supabase/migrations/2026-08-11-player-state.sql and every later `alter table public.player_state add column` in supabase/migrations/*.sql.\n- Behavioural: PGlite + tests/sql/pglite-fixture.sql + the real migration chain; one character via hr_create_character; MONSTERS/ITEMS from src/data; a pinned seed and a fixed 600 s span through the REAL `computeAccrual` imported from supabase/functions/hr-accrue/accrual.js (not a port).\n\nEXACT ASSERTIONS:\nA1. Parse the input literal into {inputKey → source expression}. For EVERY key assert the source matches /^st\\.[a-z_]+/ — i.e. it is a player_state column read. FAIL BY NAME on any key whose source reads the request (`body.`, `inp.`, `req.`, `parseIntent` output) or is a bare literal.\nA2. For every `st.<col>` referenced, assert `<col>` exists in the parsed player_state column set. (An input reading a column nobody shipped is the pre-fix state.)\nA3. For every input whose source carries a `?? <default>` with a non-null right-hand side, require the key to be listed in an in-file `DEFAULTED_INPUTS` table with a reason string; an undeclared gameplay default fails. (`combatStyle: st.combat_style ?? null` passes — null is the engine's explicit \"unchosen\"; `?? 'accurate'` would fail.)\nA4. Cross-bind: every input key must appear in either (i) a client intent verb — grep the verb names out of src/net/*.js transport call sites — or (ii) an explicit `SERVER_DERIVED` allowlist with a reason. An input the player can influence but cannot SET is the bug with the fix half-built.\nB1. BEHAVIOURAL DIVERGENCE (the assertion that actually kills the class). For each CHOOSABLE input (combat_style, auto_eat_food, auto_eat_pct, tool_carry, ammo_carry, active_kind/active_id, fight): call the REAL client intent verb as the user to set a NON-DEFAULT value, then run computeAccrual over the pinned span/seed, and assert the outcome DIFFERS from the identical run left at the default. Concretely for the original incident: with combat_style = sword/defensive, assert `out.summary.xp.defence > 0 AND out.summary.xp.attack === 0`; the default (accurate) control must be the mirror image. \"The column exists\" is not the assertion — the day combat_style shipped as a column, an engine still calling `resolveStyle(eq.weaponType, null)` would have passed A1-A4 and failed B1.\n\nMUTATION CONTROLS (`--selftest`) — each must be CAUGHT, and the first one RECONSTRUCTS the beta-2 bug so the guard is proven against the real incident, not a plausible one:\n m1 `style_no_column`: strip `combat_style` from the player-state migration AND revert index.ts:662 to `resolveStyle(eq.weaponType, null)` → A2 + B1 RED. This is literally the tree as it stood on 2026-08-24.\n m2 `style_from_body`: `combatStyle: (body && body.combatStyle) ?? st.combat_style ?? null` → A1 RED.\n m3 `silent_default`: `autoEatFood: st.auto_eat_food ?? 'cooked_shrimp'` → A3 RED.\n m4 `no_verb`: delete the set-style transport call from src/net/goal-claim.js → A4 RED.\n m5 `engine_ignores_input`: engine reads the column but discards it (pass the default into resolveStyle) → A1-A4 GREEN, B1 RED. m5 is the control that proves B1 is load-bearing; if m5 ever goes green the whole file is theatre.\n\n════ SPEC 2 — repair tests/inventory-mint-census.mjs (do NOT demote it to a WARN) ════\nNAME: keep `inventoryMintCensusGuard` (already wired, tests/run-smoke.mjs:2455). Three changes, no new file.\nFIXTURE: same static scan; add src/legacy.js to the enforcement set.\nASSERTIONS:\n S2a. DEBT EXPIRY: each BLOB_RETIRE_UNSAFE_LANES entry gains `owedSince` (build number) and `hardFailAfter`. Past `hardFailAfter` the guard FAILS. A declared debt that never expires is a permanent green over a live loss — the scrip lane has been declared since 2026-09-02 and the player still loses it.\n S2b. WIDEN (4) TO src/legacy.js: classify every mint token there as serverSettled | declaredUnsafe. `addItem(r.item,…)` at src/legacy.js:6046 must land in declaredUnsafe with `owedIntent: 'hr_claim_quest grants the reward ITEM server-side, mirroring the gold half'` — turning the census header's prose (:141-152) into a mechanical fact. Blinded-scanner control in the tests/gold-site-census.mjs pattern.\n S2c. FIX THE FALSE PIN: in src/features/smoke-test.js:3630, change the fixture to `reward: { gold: 150, item: 'oak_log', qty: 2 }` and assert what the comment at :3628 already claims — under arm, `G.inventory.oak_log` is UNCHANGED and the server credit path is what grants it. Today the comment says \"item granted\" and the fixture has no item.\nLOCATION: node guard (S2a/S2b) + in-page (S2c). LIVE DB: NO.\n\n════ AND ONE ROW OF SPEC 1 THAT IS OWED ON SHIP ════\nWhen hr_dungeon_settle / quartermaster_buy land, scrip and QM prices become engine inputs and must be added to Spec 1's A4/B1 sets (price read from a server catalogue, not QM_STOCK), and their lanes deleted from BLOB_RETIRE_UNSAFE_LANES — the census already fails on a stale entry, so removal is forced.",
  "note": "Three things the parent should carry forward. (1) The reader's guard list for this class is stale in one direction — tests/auto-eat-authority.mjs exists and is wired at run-smoke.mjs:20 — but stale in the safe direction: it proves the hr_set_auto_eat verb, not consumption parity, so the eat gap is unchanged. (2) I found one lane the reader did not: the quest-reward ITEM at src/legacy.js:6046, which is client-minted, excluded from census enforcement by design (tests/inventory-mint-census.mjs:141-152), and whose in-page test (smoke-test.js:3628-3634) claims to cover it in a comment while its fixture carries no item — a false pin, the class-11 pattern showing up inside class 3. (3) The proposed test's instinct (close the dungeon lanes) is right as BACKLOG but wrong as this class's guard; the class's root shape is simulation INPUTS with no server home, and the only thing standing between the next combat_style and production is one hand-typed file written after the last incident. Spec 1's m1 mutation reconstructs the 2026-08-24 tree exactly, so its RED is a proof rather than an argument. Read-only session: nothing edited, committed or pushed; one standalone guard run (node tests/inventory-mint-census.mjs), output quoted above."
 },
 {
  "gap_confirmed": true,
  "proposed_test_ok": false,
  "where": "tests/attended-window-conservation.mjs (NEW node guard, wired into tests/run-smoke.mjs preflight beside the existing `import { runAll as accrualGuards } from './accrual-engine.mjs'` at tests/run-smoke.mjs:19 and `sqlGuard as settleSqlReport } from './live-settlement.mjs'` at :74). Blocks A1 = PGlite via tests/schema-replay.mjs bootReplay (same boot tests/live-settlement.mjs and tests/kill-daily-credit.mjs already use); blocks A2/A3 = pure Node driving src/net/{accrue,pending-consume,record}.js with `?v=`+BUILD.cache (the tests/predict-display.mjs:47-56 derived-version pattern). NO in-page half is needed and NO live DB is needed for the gating blocks; an optional `--live` probe block stays OUT of CI preflight for the same reason tests/beta-invite-gate.mjs keeps its --live half out (no credential in CI).",
  "test_spec": "NAME: tests/attended-window-conservation.mjs — \"the two writers must agree about the same window, and nothing the player watched may vanish without a receipt.\" Three blocks, each with a mutation control and an anti-vacuity assertion.\n\nWHY NOT THE READER'S SHAPE: the fix cannot be \"make the settle's drops equal the client's drops\" — it is impossible by construction (supabase/functions/hr-accrue/index.ts:500-506: the PRNG seed is `hr_seed(user,slot,'accrue:'+accrued_to)` mixing a 256-bit secret with RLS on and no client grant, precisely so the client cannot predict rolls; src/core/combat-sim.js:64,130,137 rolls the client's own table with the client's RNG). So every assertion below is RNG-FREE: it compares COUNTS the two writers each already own, or demands a receipt.\n\n── A1  KILL-COUNT PARITY BETWEEN THE TWO WRITERS  (PGlite; the loot half) ──\nFIXTURE: one continuous 180 s attended window on `goblin`, mid-level character that survives it (reuse the SLOW-style honest fixture from tests/live-settlement.mjs:60-70, not a 10-HP fresh char — a death ends the span and reads as a false green). t0 = accrued_to; now = t0+180000. Client-observed kills K (from a client-seed combat-sim run of the same span).\nSTEPS: (1) `select hr_credit_kills(user, slot, 'goblin', K, idem)` on the replayed chain → `credited`. (2) `computeAccrual({accruedToMs:t0, nowMs:t0+180000, activeKind:'combat', activeId:'goblin', seed:<server seed>, ...})` → `settle`.\nEXACT ASSERTION: `assert(settle.summary.kills === credited, 'the settle priced loot for N kills while hr_credit_kills credited M for the SAME window')`. Anti-vacuity: `assert(settle.accrued === true && credited > 0)`.\nRED ON CURRENT MAIN: computeAccrual has no kill-credit input at all (grep `killsCredited|hr_credit_kills|kill_credit` across supabase/functions/hr-accrue → NO MATCHES), so it re-sims the window independently and its kill count is a function of the secret seed, not of what the counter credited. The migration states the split as intended: supabase/migrations/2026-08-30-bounty-kill-credit.sql:44-48 \"1. hr_credit_kills → the kill COUNTER only. NO gold, NO XP, NO drops. … 3. the settle/accrual path → per-kill loot (unchanged; the one loot writer).\"\nGREEN AFTER THE FIX: computeAccrual takes `killsCreditedSinceAccruedTo` (mirroring the existing `combatXpAccruedToMs`, accrual.js:1180-1181) and prices loot for exactly those kills.\n⚠ LOAD-BEARING SIDE EFFECT the parent must plan for: tests/combat-xp-settle-split.mjs:82, :92, :97 currently ASSERT the defective property — \"loot unchanged\", \"only combat XP may be split\", \"loot must still pay away\" — and its verdict string (:104) says so. I ran it: `node tests/combat-xp-settle-split.mjs` → \"settle credits combat XP only past the watermark; loot unchanged; AWAY-1 preserved\" (exit 0). The suite pins the bug. The loot-watermark fix MUST rewrite that guard's contract in the same commit or the build goes red for the right reason and gets \"unblocked\" the wrong way.\nMUTATION M1: remove the credited-kill input from accrual.js → A1 RED.\n\n── A2  A HELD MEAL MUST BE SETTLED OR RE-SENT  (pure Node; the food half) ──\nFIXTURE: G with cooked_shrimp:20; feed reconcileInventory/noteServerAutoEat an envelope carrying auto_eat_enabled=true so `clientOwnsAutoEatDebit()` is false (src/net/accrue.js:2383).\nSTEPS: call the real client seam 7×: `noteItemConsumed('cooked_shrimp',1,{auto:true})` (src/legacy.js:8483 `if(opts.auto&&!_clientOwnsAutoEatDebit())return true;  // the server eats this one — hold only`). Then apply the settle envelope for the SAME window with `delta.items.cooked_shrimp = -M`.\nEXACT ASSERTION: `assert(M === 7 || divergenceReceipt.eat.cooked_shrimp === 7 - M, 'the client held 7 auto-meals; the settle debited M; the difference is neither settled nor re-sent')` — i.e. the union (server debit ∪ client-sent eat intents) must cover every meal the player watched being eaten.\nRED ON CURRENT MAIN: neither branch exists. The hold lives in `_pendingConsume` (src/net/pending-consume.js:105 `export const FIELD = '_pendingConsume'`), which is `_`-prefixed scratch and appears in NO persistence allowlist — `grep -n \"_pendingConsume\\|pendingConsume\" src/net/client-state.js src/net/record.js tests/arm-homing-guard.mjs` returns ZERO hits — and it TTLs out at 10 minutes (pending-consume.js:118). So an unsettled hold dies at reload and reconcileInventory's `Math.max` ratchet (accrue.js:2428-2432, :2441) restocks the food. That is the measured 7 shrimp returning.\nMUTATION M2: delete the `opts.auto && !_clientOwnsAutoEatDebit()` guard at legacy.js:8483 → the double-debit control (server debit AND client intent for the same meal) must go RED, so the fix cannot be \"just always send\".\n\n── A3  NO SILENT RETIREMENT ACROSS RELOAD  (pure Node; ships immediately) ──\nFIXTURE: G holding the attended session's bag (goblin_ear 11, bones 15, cooked_shrimp 13) plus the display record of what was shown. Server body = the hr_state_of projection the settle actually wrote (goblin_ear 7, bones 9, cooked_shrimp 20).\nSTEPS: run the REAL boot chain onto a FRESH `{}` — record.js settle() → applyClientState → reconcileInventory/reconcileBank/etc.\nEXACT ASSERTION: `for each id where fresh[id] < displayed[id]: assert(hydrationReceipt.retired[id] === displayed[id] - fresh[id])` and `assert(sum(retired) === 10)` for this fixture. A drop the player watched land may be retired, but never in silence.\nRED ON CURRENT MAIN: no retirement receipt exists anywhere on the boot path; the merge is a one-way Math.max ratchet against a FRESH bag, so on reload the client's 26 units simply do not exist and the server's 16 is all there is.\nMUTATION M3: delete the receipt → RED.\n\nLIVE DB: not required for A1/A2/A3. Optional `--live` probe (kept out of preflight): drive one 180 s window on the QA account, then compare `player_ledger` combat/accrue `qty_in` for the window against `hr_kill_credit_log` claimed × the window's kill count — this is the exact query that produced the 2026-09-04 measurement and is worth keeping as a reproducible probe, not as a gate.",
  "evidence": "GAP CONFIRMED on current main (HEAD 00b927b1). I searched tests/*.mjs (110 files) and src/features/smoke-test.js myself rather than trusting the reader.\n\nNO SUCH TEST EXISTS: `grep -rn \"attended-settle-parity|attended-loot|attended-window\" tests/ src/ .github/` → no matches. `grep -ril attended tests/*.mjs src/features/smoke-test.js src/net/accrue.js supabase/functions/hr-accrue/*.js` → only accrual-engine.mjs, auto-eat-at-creation.mjs, combat-xp-cap-drift.mjs, combat-xp-settle-split.mjs, kill-daily-credit.mjs, predict-display.mjs, run-smoke.mjs, smoke-test.js, accrual.js, accrue.js — and none of them asserts attended-vs-settle parity of kills, loot or food.\n\nMECHANISM (all verified by reading current main):\n• supabase/functions/hr-accrue/accrual.js:798 `export function computeAccrual(input)` — takes NO presence and NO kill-credit input. `grep \"killsCredited|killsSinceAccrued|hr_credit_kills|kill_credit\" supabase/functions/hr-accrue` → NO MATCHES.\n• accrual.js:1163-1181 — the ONLY watermark is `combatXpAccruedToMs`; accrual.js:1420-1423 states it outright: \"Loot/gold/progress below are UNCHANGED and still pay the full [accrued_to, now] window — only combat XP is split.\"\n• supabase/migrations/2026-08-30-bounty-kill-credit.sql:44-48 — hr_credit_kills pays \"NO gold, NO XP, NO drops\"; the settle is \"the one loot writer\". The client has exactly two credit RPCs: `grep -o \"hr_credit_[a-z_]*\" src/net/*.js src/*.js | sort -u` → hr_credit_combat_xp, hr_credit_kills. There is no loot-credit RPC.\n• supabase/functions/hr-accrue/index.ts:500-506 — the settle seed is `hr_seed(...)` mixing a 256-bit secret with RLS on and no client grant, \"which is exactly why the seed may not be a function of visible values alone\". src/core/combat-sim.js:64 imports rollDropTable, :130 rolls, :137 `call(fx,'addItem',ev.id,1)` — the client rolls its own drops with its own RNG. Attended loot therefore CANNOT match the settle even in principle.\n• src/net/accrue.js:3153 `if (!st.visible) return { settle: false, reason: 'hidden', waitMs: wait };` — an alt-tabbed session settles once at stop/unload, so the whole session is one unattended re-sim.\n• src/net/accrue.js:2428-2432, :2441 reconcileInventory is a Math.max one-way ratchet — which protects a LIVE bag but does nothing on reload, where G is fresh and the server's figure is all there is.\n• src/legacy.js:8483 `if(opts.auto&&!_clientOwnsAutoEatDebit())return true; // the server eats this one — hold only`; src/net/accrue.js:2383 `clientOwnsAutoEatDebit() { return serverAutoEatObserved === false; }`. The hold lives in src/net/pending-consume.js:105 `FIELD = '_pendingConsume'` with a 10-minute TTL (:118) and is in NO allowlist — `grep -n \"_pendingConsume|pendingConsume\" src/net/client-state.js src/net/record.js tests/arm-homing-guard.mjs` → ZERO hits. Unsettled holds die at reload; the food returns.\n\nTHE NEAREST GUARDS, AND WHY EACH MISSES:\n• tests/accrual-engine.mjs:1316-1376 attendedSettleAutoEatGuard — asserts the SERVER's re-sim eats and debits its OWN meals (`on.delta.items[PROVISION] === -on.foodEaten`, :1356). It never compares to what a client ate. Worse, it is the measurement the client's hold-only gate RESTS on (its own failure message at :1352-1355 says so), so it licenses exactly the behaviour that lost the 7 shrimp.\n• tests/combat-xp-settle-split.mjs:82,:92,:97 — actively PINS the defect: \"loot unchanged\", \"only combat XP may be split\", \"loot must still pay away\". COMMAND I RAN: `node tests/combat-xp-settle-split.mjs` → output `settle credits combat XP only past the watermark; loot unchanged; AWAY-1 preserved` (exit 0).\n• tests/goal-counters.mjs:812-827 COLLECTION-1 — asserts `ev:loot:<id>` add == the item delta gained, i.e. the settle is self-consistent with itself. Says nothing about the attended window.\n• tests/predict-display.mjs — prediction retirement for XP and GOLD only; `grep -i \"drop|loot|item\"` finds no item-drop prediction. Item drops have no prediction-retirement guard.\n• tests/kill-daily-credit.mjs (1061 lines) — never mentions items/loot/qty_in.\n• tests/conservation-fuzz.mjs — conserves value across market/craft/vendor ops against an independent ledger model; the accrual grant is an \"intended mint\" of whatever the engine says, so an under-realized attended settle conserves perfectly and passes.\n\nCORRECTION TO THE PHASE-1 READER'S GUARD LIST: it omitted tests/live-settlement.mjs (723 lines, wired at tests/run-smoke.mjs:74), which is the closest existing work — it fixed a REAL window-boundary loss (computeAccrual rebuilt `state={monsterHp:0}` every window, so any target whose time-to-kill exceeded the window paid ZERO forever) with SETTLE-1/1a/1b/1c/1d/1e and a --mutate catalogue. It does NOT close this gap (its SETTLE-1d asserts AWAY-1 parity, i.e. that the carry is the ONLY divergence), but it is the correct file family and fixture style to copy, and its \"SLOW dragon that survives the hour\" fixture note (:20-30) is the trap the new guard must avoid.\n\nVERDICT ON THE PROPOSED TEST: directionally right, not shippable as written, and it would NOT have caught the original incidents. Its block (E) asserts \"the item delta is scaled to K kills of expected loot\" — an expectation over a probabilistic drop table, so it is either flaky or vacuous depending on the tolerance chosen. Its block (F) demands `delta.items.cooked_shrimp === -7`, which is UNSATISFIABLE: the settle's re-sim eats what its own differently-seeded simulation needs, so the assertion can only be met by deleting the seed secret (index.ts:500-506) — a security regression. Its block (C) asserts an \"unsettled badge\" UI that does not exist and would only make the loss visible, not stop it. The parts worth keeping are the --live ledger probe (that IS how the 2026-09-04 measurement was made) and the instinct to add a kill watermark mirroring combatXpAccruedToMs.\n\nTHE ORIGINAL INCIDENTS THIS CLASS PRODUCED (both would go RED under the spec above, neither under the reader's): (1) the FOOD half — \"food eaten in combat gets restocked\" / \"I have 2 moonblood and every time I use 1 it comes back\", reported FOUR times b467→b479, quoted verbatim in src/net/pending-consume.js:1-23 — caught by A2. (2) the LOOT half — player report #2 \"items gained through combat are disappearing\", measured on prod 2026-09-04 (memory attended-settle-gap-2026-09-04: one `combat/accrue` row qty_in 16 against 26 client-shown drops, 7 shrimp returned 13→20, while kills/XP/gold persisted exactly via the cadence RPCs) — caught by A1 (the two writers disagree about the same window) and, failing that, by A3 (the 10 retired units must be named in a receipt).",
  "note": "Item index 3 = class (4) attended-vs-unattended settle gap. Read-only throughout: no edits, no commits, no deploys. One standalone guard run, as permitted: `node tests/combat-xp-settle-split.mjs` (green — and its green verdict is itself the finding). Repo HEAD at time of audit is 00b927b1, three commits ahead of the snapshot in my brief (a0284045 SA-021, 1ffc2c3b SA-009, 00b927b1 SA-001 were added); nothing in those commits touches the code paths cited above — they are docs-only (`docs(ledger)`).\n\nTWO THINGS THE PARENT SHOULD ACT ON BEYOND THE SPEC:\n1. THE SUITE CURRENTLY PINS THE BUG. tests/combat-xp-settle-split.mjs:82/:92/:97 assert \"loot unchanged\" as the contract. Any correct fix makes that guard red. It must be rewritten in the SAME commit as the fix, with its header explaining that the loot window is now split like the XP window — otherwise the next person reads a red guard as a regression and reverts the fix.\n2. THE DESIGN QUESTION A1 FORCES, which the game-designer should rule on before the lane is dispatched: making the settle pay loot for exactly the credited kills means the SERVER rolls loot for kills the CLIENT observed, on the server's own seed. The player still will not see the drops they were shown — they will see a different set of the same COUNT. That is honest and conserves value, but it is a visible behaviour change (\"the log said goblin_ear, the bag has bones\"). The alternative is to keep the client's rolls display-only and make A3's receipt the shipped answer (\"4 predicted drops did not land\"), which is cheaper and non-breaking but concedes the ~38% shortfall. A3 is shippable today regardless of which way that ruling goes, and I would ship A3 first: it converts silent data loss into a stated one, which is the property CLAUDE.md's \"kill the class\" rule actually asks for.\n\nNeither A1's engine input nor A3's receipt exists on main, so both blocks are RED-by-construction today — that is the point, but it means this guard cannot be wired into run-smoke's preflight until the fix lands, or CI (already red since 2026-08-29 per the brief) gains a seventh failure. Land the guard and the fix in one commit."
 },
 {
  "gap_confirmed": true,
  "proposed_test_ok": false,
  "where": "src/features/smoke-test.js:2079 (PROP-OK-1), :2081 (vacuous early-return), :2120 (ROOM-REFUSE-1), :2121 (vacuous early-return), :2165 (FORGE-E2E-1), :2170-2172 (vacuous early-return), :2696 (HATCH-REFUSE-1), :27943 (HIRE-STRANDED-1); tests/gold-site-census.mjs (the only client-side derived census, gold-only); src/features/homestead.js:310-392 (upgradeProperty, b500 fix + the incident narrated at :331-345); src/legacy.js:9214-9233 (upgradeRoom, b500 fix); src/legacy.js:19031-19044 (companion buy — DELIBERATE optimistic grant); src/net/accrue.js:1421-1429 (companionAuthorityArmed), :1431-1457 (reconcileCompanions, absolute rebuild at :1456); src/net/capstone.js:91 (BLOB_RETIRED = true, stale \"DORMANT\" comment); src/net/property-record.js:208 (max(server rung, residue)), :226 (\"NEVER LOWERS\"); src/features/workers.js:199 (swallowed .catch, legacy local path), :276-293 (verdict IS read).",
  "evidence": "GAP CONFIRMED — three independent checks on main @ 00b927b1.\n\n(1) No census exists. `ls tests/` → the only census files are gold-site-census.mjs, inventory-mint-census.mjs, restore-census.mjs. `grep -rniE \"optimistic\" tests/*.mjs` → 12 hits, all incidental prose (activity-intent.mjs:1581, gold-intents.mjs:890, market-intent.mjs:432/496, predict-display.mjs:308/317/326, unlock-buy.mjs:121, conservation-fuzz.mjs:880, auto-eat-authority.mjs:236, bug-triage.mjs:404) — zero census, zero cross-verb refusal-revert guard. Coverage is exactly what the reader said: one hand-typed in-page test per verb after each incident.\n\n(2) The vacuity claim is real and slightly WORSE than reported. smoke-test.js:2081 `if (!H || typeof H.upgradeProperty !== 'function') return;` (PROP-OK-1) and :2170-2172 `if (!H || typeof H.upgradeProperty !== 'function' || typeof window.upgradeRoom !== 'function' || !window.ROOMS || !window.ROOMS.forge) return;` (FORGE-E2E-1). The reader missed a third: ROOM-REFUSE-1 at :2121 `if (typeof window.upgradeRoom !== 'function' || !window.ROOMS || !window.ROOMS.forge || !window.ROOMS.forge.levels || !window.ROOMS.forge.levels[0]) return;`. `grep -c \"if (!H || typeof\" src/features/smoke-test.js` → 9. All three b500 property/room guards pass vacuously if homestead.js or ROOMS fails to load.\n\n(3) The census pattern the proposal copies is live and gold-only. `node tests/gold-site-census.mjs` → \"OK — 68 gold write sites in src/** (control: 32 when blinded); 15 wired, 25 deferred, 28 exempt\". Nothing equivalent scans pre-apply sites.\n\nTHE INCIDENT, named by the source itself: src/features/homestead.js:331-345 — \"THE BUG THIS CLOSES (player report #1, the Forge): the old code did `G.homestead.tier++` BEFORE the server confirmed and fired buyUnlock with a `.catch(function(){})` — which could not even fire, because buyUnlock RESOLVES with the verdict, it never rejects.\" Refused Farmstead (client showed 4 wolf_pelt, server settled 3) → tier advanced anyway → getTier()=max(rung 1, residue 2)=2 → the UI offered the Forge → the server refused THAT → gone on reload with materials looking spent. That is Paione's live P1 (fact c).\n\nWOULD THE PROPOSED TEST HAVE CAUGHT IT? Its part (b) OPTIMISTIC-REFUSE-ALL would: against the pre-b500 code, stubbing buyUnlock to {ok:false} then asserting G equals the pre-action snapshot goes RED on G.homestead.tier (2 vs 1), and the \"one notify fired\" assertion goes RED because the swallowing `.catch` printed nothing. So it catches THIS incident. It fails as a CLASS-KILL for three reasons I verified on main:\n\n(A) ITS CENTRAL ASSERTION IS FALSE FOR LEGITIMATE SITES. src/legacy.js:19042 `window.unlockCompanion(id)` pushes into G.companions.ownedIds BEFORE the server answers, and that is CORRECT: src/net/accrue.js:1456 `G.companions = { ownedIds: Array.from(owned), xp, equipped }` rebuilds the field ABSOLUTELY from the server's owned-set, and the arm gate is live (src/net/capstone.js:91 `export const BLOB_RETIRED = true;` — the trailing \"DORMANT\" comment is stale, exactly as warned). \"G equals the pre-action snapshot\" is RED at this site and at every other predict-and-reconcile site (gold.js:472 reconcilePredictions, farm-sync.js:203, item-ledger.js:226). The only way to green it is a hand-typed exception list — reintroducing the drifting list the census was supposed to delete. The source states the true discriminant at legacy.js:19035-19036: companions heal DOWN, \"unlike the property tier whose reconcile only heals UP (a permanent false floor).\"\n\n(B) THE SCANNER KEY IS WRONG AND WOULD PASS VACUOUSLY. The proposal scans for \"hr_unlock_buy, hr_trait_buy, hr_companion_grant, hr_claim_*, hr_farm_*, hr_equip, hr_enchant, hr_hire*, quartermaster\". The client never types those names. `grep -rn \"hr_unlock_buy\" src/` returns ONLY comments (artisan-sim.js:488/514/534, companion-unlocks.js:6/14/24/122, gold-ladders.js:6/11). The real call sites are the seam: `grep -rn \"buyUnlock(\" src/` → homestead.js:369, legacy.js:4263, :9221, :9351, :19044, workers.js:199, :277. A scanner keyed on SQL verb names finds zero client sites and reports green — which is why gold-site-census.mjs:L5 has a blinded-scanner control (68 vs 32 blinded).\n\n(C) IT MISSES THE DURABILITY HALF OF THE LIVE P1. Even with the mint fixed, Paione stays deadlocked: src/net/property-record.js:208 \"THE ANSWER: max(server rung, residue). Never demotes\" and :226 \"NEVER LOWERS\". A guard that only asserts \"the refusal minted nothing\" is green on a save that was minted before the fix. The class is (optimistic pre-apply) × (non-absolute heal); the proposal tests only the first factor.\n\nNOT A LIVE INSTANCE (checked, so the lane doesn't chase them): workers.js:199's swallowed `.catch` is the flag-off LEGACY LOCAL PATH behind `if (serverBacked()) return hireServer(G);` (:186) and mints locally by design; workers.js:278's `.catch` is a redundant no-op beside a real `.then` at :279 that reads the verdict. legacy.js:9221 (rooms) and homestead.js:369 (property) both classify via hrClassifyUnlock and speak refusals — b500 closed them.",
  "test_spec": "Replace the proposal with a two-part guard that keys on the REAL discriminant: an optimistic pre-apply is safe iff the field it touches is healed ABSOLUTELY from server truth. Raise-only or absent heal + optimistic pre-apply = the b501 class.\n\n=== PART 1 (the class-kill) — tests/optimistic-heal-census.mjs ===\nWHERE: node guard under tests/, wired into tests/run-smoke.mjs preflight beside cacheBusterGuard (~:1386) AND listed in .github/workflows/smoke.yml so local and CI agree.\nLIVE DB: NO. Pure source scan + pure-function calls. No PGlite, no network.\nFIXTURE: (i) a scanner over src/** keyed on the CLIENT SEAM names, not SQL verbs — derive the seam list from the exported surface of src/net/gold.js + src/net/*-net.js (buyUnlock, buyTrait, grantCompanion, claim*, farm*, equip, enchant, hire) and assert the derived list is non-empty; (ii) a ledger src/net/optimistic-sites.js in the gold-sites.js shape, one row per call site: {file, symbol, preApplies:['homestead.tier'], heal:'absolute'|'raise-only'|'none', verdict:'read'|'swallowed', why}; (iii) a heal-direction probe table mapping each field to its reconcile export (companions→reconcileCompanions, farm→reconcileFarm, traits→reconcileTraits, workers→reconcileWorkers, inventory→reconcileInventory, homestead.tier→property-record.healPropertyTier).\nASSERTIONS:\n O1 every seam call site the scanner finds in src/** is in the ledger BY NAME (mirror gold-site-census L1).\n O2 every ledger row still corresponds to a live site (L2).\n O3 THE CLASS ASSERTION — for every row whose preApplies is non-empty, heal MUST be 'absolute'. A row declaring 'raise-only' or 'none' fails naming the field and the site. This is the one assertion that is RED on the pre-b500 homestead.js and RED today on any new verb that copies it.\n O4 heal:'absolute' is PROVEN, not declared: call the named reconcile with a fresh G pre-loaded with a HIGHER value than the envelope carries and assert it comes DOWN. Fixture for companions: G.companions.ownedIds=['fox','raccoon'], res.companions={owned:['fox'],xp:{},equipped:null} → assert ownedIds deep-equals ['fox']. Fixture for property: healPropertyTier on G.homestead={tier:2} with a rung-1 envelope → assert getTier()===1. That second one is RED on main TODAY (property-record.js:208 max()) and is the exact Paione deadlock — it goes green only when fix/property-rung-server-truth lands, so this guard is also that branch's regression test.\n O5 every row whose verdict is 'swallowed' must also have preApplies:[] AND heal:'none' with a `why` (workers.js:199's flag-off local path is the only legitimate row today) — a swallowed verdict beside a pre-apply is the b501 shape and fails.\n O6 THE BLINDED CONTROL (gold-site-census L5): re-run the scanner with its pattern set blinded; if the count is not strictly lower, the scanner sees nothing and every assertion above passed for free. Prevents failure (B) above.\nMUTATION (--selftest, all must go RED): revert homestead.js:376 advanceTierTo out of the .then into a synchronous pre-apply; delete one ledger row; flip a row's heal to 'absolute' while its reconcile is raise-only; add a new buyUnlock( call with no ledger row; blind the scanner.\n\n=== PART 2 (the behavioural half) — in-page OPTIMISTIC-REFUSE-ALL, src/features/smoke-test.js ===\nWHERE: in-page, in the regression suite beside FORGE-E2E-1 (:2165). Must be in-page because it drives the real UI entry points (upgradeProperty, upgradeRoom, buyCompanion, hireWorker) against a stubbed seam.\nLIVE DB: NO. Stubs window.HearthriseGold.buyUnlock, same shape as PROP-OK-1 (:2094-2101).\nFIXTURE: iterate the SAME ledger imported from src/net/optimistic-sites.js — derived, so a new verb cannot be forgotten. For each row: snapshotG(); stub the seam to Promise.resolve({outcome:'refused', reason:'insufficient_item', body:{ok:false, error:'insufficient_item'}}); invoke the row's UI entry point; await 30ms.\nASSERTION: not \"G equals the snapshot\" (wrong — see (A)). Instead, per row: (1) every field named in preApplies whose heal is 'absolute' MAY differ, but feeding the next envelope through its named reconcile MUST bring it back to the snapshot value — i.e. assert reconcile(G, envelopeWithoutIt) restores it; (2) every field NOT in preApplies must be byte-identical to the snapshot; (3) exactly one notify fired with kind 'kill' and its text does not match /built!|it's yours|upgraded to|joins your/ — the swallowed-`.catch` silence and the false success toast both fail here.\nVACUITY FIX (required, same commit): convert smoke-test.js:2081, :2121 and :2170-2172 from `if (!X) return;` to `assert(X, 'module missing — this guard cannot pass vacuously')`. Those three early-outs are why the b500 guards would report green if homestead.js failed to load, which is the cache-buster incident's exact symptom.\n\n=== WHAT THIS BUYS ===\nPart 1 O3+O4 is the actual class-kill: it makes \"a client verb pre-applies a field the server only heals upward\" a build failure rather than a player report, and it is RED on main right now at the property tier, so it lands as the guard for fix/property-rung-server-truth instead of being written after the next incident. Part 2 kills the per-verb hand-typed pattern (five tests, five incidents) by deriving the verb list from the same ledger.",
  "note": "Two corrections to the phase-1 reader's list, both verified: (1) it missed ROOM-REFUSE-1 at smoke-test.js:2120, which carries the same vacuous early-return at :2121 — so three b500 guards are vacuity-prone, not two. (2) Its claim that the b501 exemption at legacy.js:19044 is safe depends on an arm flag it did not check: reconcileCompanions is a pure no-op unless companionAuthorityArmed() (accrue.js:1421-1429) reads BLOB_RETIRED. That flag IS true (capstone.js:91) despite its trailing \"DORMANT\" comment, so the exemption holds on main today — but it is one flag flip away from becoming a live instance of the class, which is precisely why O4 must PROVE the heal direction by calling the reconcile rather than trusting a declared 'absolute'. Separately: this item overlaps the in-flight fix/property-rung-server-truth branch (fact c) — O4's property fixture is that branch's regression test and should be handed to that lane rather than built twice. No overlap with the CI-green lane (fact d)."
 },
 {
  "gap_confirmed": true,
  "proposed_test_ok": false,
  "where": "R:\\the game\\the game\\src\\features\\boot-hydration.js:101-107,112-127,274-290 (shouldVeil/readEnv/installBootHydrationVeil); R:\\the game\\the game\\src\\features\\smoke-test.js:37934-38041 (B492-2), :38042-38143 (B492-3/3b), :38144-38186 (B492-4); R:\\the game\\the game\\tests\\run-smoke.mjs:203-270 (wallGuard), :812-865 (coldLoadGuard), :1064-1080 (SUPABASE_SDK_STUB + saveSlotGuard), :3616,:3679 (wiring); R:\\the game\\the game\\src\\net\\record.js:1328-1329,1345-1352,1428-1436,1585,1637-1648,1794-1798; R:\\the game\\the game\\src\\net\\auth.js:949-951; R:\\the game\\the game\\src\\legacy.js:1255-1270,2435-2436; R:\\the game\\the game\\src\\net\\character.js:134,221,394-395",
  "evidence": "GAP CONFIRMED, and it is structural rather than merely missing. (1) No file under tests/ mentions the veil at all: `grep -rln \"hr-boot-hydration-veil\\|BootVeil\" tests/` → no output. `grep -rn \"hr_load\" tests/run-smoke.mjs` → one hit, run-smoke.mjs:1102, and it is a COMMENT inside saveSlotGuard; no guard intercepts or counts an hr_load request. (2) The veil is unreachable from every existing headless guard by construction: boot-hydration.js:103 `if (e.harness) return false;` and :285 `if (w.__HR_TEST_HARNESS__) return null;` (the poll is refused too), while `grep -n \"addInitScript\" tests/run-smoke.mjs` shows every page-driving guard except wallGuard sets `window.__HR_TEST_HARNESS__ = true` (:819 coldLoadGuard, :885 authResilienceGuard, :1078 saveSlotGuard, :1203, :2021). wallGuard (:203-270) is the only harness-free boot and it is session-less, so shouldVeil returns false at boot-hydration.js:105 (`!e.signedIn`). (3) The existing SDK stub cannot produce a session: run-smoke.mjs:1064-1072 `getSession: async () => ({ data: { session: null } })`, and auth.js:949-951 `isSignedIn()` returns `!!session?.access_token` — so no current fixture can make readEnv (boot-hydration.js:112-127) report signedIn:true. Net: the veil's THREE inputs are only ever driven as literals in a pure-function truth table (smoke-test.js:38150-38158), and B492-4 even asserts the harness must NOT veil (:38158, :38181-38183). Nothing boots the page as a signed-in player and observes the screen. (4) The reader UNDER-credited one guard and over-credited another, both material to the verdict: B492-2 (smoke-test.js:37988-38037) is not synthetic-only — it stubs window.fetch, takes a real 401, waits on the REAL ladder timer, asserts a second call fired and that `recordValue(G,'gold').known === false` in between; but it calls `R.beginRecordLoad()` directly, so it proves the LADDER, never the boot WIRING. Conversely B492-3b (smoke-test.js:38134-38142) is a source-text regex — `assert(/p\\.then\\(_load,\\s*_load\\)/.test(after), 'the boot read must fire whether the ensure resolved OR rejected')` — i.e. the single defect that deleted every hr_load of the session (legacy.js:2435-2436) is guarded by a string match that passes on unreachable code (the class-11 pattern). (5) The gap_detail's third clause is not a test gap but a missing feature: `bootHydrationState().partial` (record.js:1428-1436, populated by hydrationStep at :1637-1648 and stamped at :1794-1796) has NO consumer outside record.js — grep for `partial` across src/legacy.js, src/main.js, src/features/*.js, src/net/*.js returns only unrelated hits (raids.js partial-credit, identity.js:852, accrue.js prose). Asserting \"a visible warning exists\" would be RED for a product hole, not a regression.",
  "test_spec": "WHY THE PROPOSAL FAILS THE ORIGINAL-INCIDENT TEST. The incident is B492 / b493 — the 2026-08-29 silent boot-hydration failure, QA account 0a47ba77 slot 0 on build 491 (narrated at smoke-test.js:37840-37876): a cold boot reached \"ready\" and sat 36+ s showing the fresh-G factory literal (attack 0, hitpoints 1154 xp, 500 gold) on an account holding attack 428 / 7,520 gold, net pill \"Online\", no error. Four defects, and the proposed `bootVeilGuard` fixture (`page.route hr_load -> 503 for 20 s`) reproduces NONE of the two that caused it: B492-1 was a fetch that NEVER ANSWERED (no AbortController, latch released in the caller's frame) — a 503 is an answer, so the abort path at record.js:1585 never runs; B492-3 was `ensureThenAccrue().then(f)` with ONE argument, so a rejected/never-settling ensure meant ZERO hr_load requests were ever issued — a route on hr_load never fires, and the guard as written never counts requests, so its veil half would PASS while the session is dead. The proposal would only go red against the pre-fix build because the veil element did not exist yet; as a REGRESSION guard it is weak. Its \"no text 'Level 1'/'500 gold' painted\" assertion is also the wrong instrument: under the fix that text is still in the DOM, merely covered by the veil, so it is simultaneously brittle and unable to catch B492-4 re-broken. And its `partial` clause asserts a feature that does not exist (see evidence 5).\n\nTHE SPEC THAT WOULD HAVE CAUGHT IT.\n\nNAME: `bootVeilGuard(browser, url)` in tests/run-smoke.mjs, wired in the same block as coldLoadGuard (run-smoke.mjs:3679) so it runs in the LOCAL gate and CI, not CI-only. Companion in-page test B492-6 (below) in smoke-test.js.\n\nWHERE IT LIVES: node guard (Playwright), NOT in-page. The in-page suite structurally cannot host it — boot-hydration.js:103/:285 refuse both the veil and its poll under `__HR_TEST_HARNESS__`, and tryRun's async problem is already documented at run-smoke.mjs:865-880.\n\nLIVE DB: NO. Auth comes from an extended SDK stub, hr_load/hr_create_character from route interception. Works unchanged against a local server and against `--url` production (same property coldLoadGuard's header claims at run-smoke.mjs:806-812).\n\nFIXTURE: fresh context per block. Do NOT set `__HR_TEST_HARNESS__` (this is the whole point). Route cdn.skypack.dev to SUPABASE_SDK_STUB_AUTHED — a copy of run-smoke.mjs:1064 whose `getSession` returns `{ data: { session: { access_token: <b64url JWT, sub 00000000-0000-4000-8000-0000000000ab, exp now+3600, minted the way authResilienceGuard mints one at run-smoke.mjs:~890>, user: { id: same } } } }` and whose `onAuthStateChange(cb)` fires `cb('SIGNED_IN', session)` on the next microtask. VACUITY CONTROLS, asserted before any subject assertion, failing (never skipping) the guard: `HearthriseAuth.isSignedIn() === true` and `HearthriseCapstone.isBlobRetired() === true`. Read timings from the page rather than hardcoding: `RECORD_TIMEOUT_MS` (15000, record.js:1328) and `BOOT_RETRY_DELAYS_MS[0]` (800, record.js:1329).\n\nBLOCK A — THE STALL (B492-1, the live shape). Route `/rest\\/v1\\/rpc\\/hr_load/` to a handler that counts hits and NEVER fulfils/continues. Wait RECORD_TIMEOUT_MS + BOOT_RETRY_DELAYS_MS[0] + 2000 ms.\n  A1 `hits >= 2` — \"the stalled boot read was aborted and a second went on the wire\". This is the assertion a 503 fixture cannot make; it is B492-1 + B492-2 in one.\n  A2 `#hr-boot-hydration-veil` exists, `getComputedStyle(el).visibility === 'visible'`, and its bounding box covers >= 90% of the viewport — the veil must COVER, not merely exist.\n  A3 `HearthriseRecord.bootHydrationState().phase === 'pending'` and `recordValue(G,'gold').known === false`.\n  A4 the B492-4 assertion, at the RECORD layer instead of painted text: no client-authored copy of a server-owned field survives — `G.gold` is not 500 and `recordValue(G,'gold').known === false`, and `G.skills.attack` carries no factory xp (the forgetServerOfRecord contract narrated at legacy.js:1255-1269). Text matching is explicitly rejected: the literal is behind the veil, still in the DOM.\n  A5 zero pageerrors.\n\nBLOCK B — THE SWALLOWED ENSURE (B492-3; the defect the proposal cannot produce). Fresh context. Route `/rest\\/v1\\/rpc\\/hr_create_character/` (character.js:134) to (b1) HTTP 500 and, as a second sub-case, (b2) a handler that never fulfils. Do NOT route hr_load; count its requests.\n  B1 at least one `rpc/hr_load` request is observed within 20 s DESPITE the ensure never resolving — the boot read is not hostage to the ensure. This replaces the source-regex at smoke-test.js:38134-38142 with behaviour.\n  B2 veil up throughout; `recordValue(G,'gold').known === false`.\n\nBLOCK C — RECOVERY AND THE NEGATIVE CONTROL. Continue block A's context; release the route to fulfil `{ok:true, version:1, now:<iso>, state:{slot:0, gold:7520, gems:3, accrued_to:<iso>}}` (the incident's real balance).\n  C1 within 2x BOOT_RETRY_DELAYS_MS[0] + 2 s, `#hr-boot-hydration-veil` is GONE from the DOM.\n  C2 `bootHydrationState().outcome === 'loaded'`, `.partial` deep-equals `[]`, `isCharacterHydrated() === true`.\n  C3 `G.gold === 7520` and `recordValue(G,'gold').known === true` — the veil lifted onto the REAL character. Without C3, \"veil gone\" passes on a wiped render, which is the incident.\n  C4 NEGATIVE CONTROL, fresh context: hr_load answers the ok envelope immediately; poll every 100 ms for 5 s and assert `#hr-boot-hydration-veil` is NEVER observed and no 'Connecting' copy flashes. A build that veils forever must be red.\n\nMUTATION CONTROLS (documented in the guard header, runnable by hand): boot-hydration.js:107 `return !e.hydrated` -> `return false` must red A2; delete the abort timer at record.js:1585 must red A1; restore the one-argument `.then` at legacy.js:2436 must red B1; skip `forgetServerOfRecord(G)` at legacy.js:1269 must red A4.\n\nCOMPANION IN-PAGE TEST B492-6 (smoke-test.js, beside B492-4): drive `syncVeil` through `readEnv` with a stubbed `window.HearthriseAuth.isSignedIn` and a stubbed `bootHydrationState`, asserting the veil DEFERS to `#hr-evicted-gate` / `#hr-auth-expired-gate` (boot-hydration.js:249-253) — currently unasserted anywhere — then `uninstallBootHydrationVeil`.\n\nSPLIT OUT, NOT A TEST: the `partial`-surfacing clause. `bootHydrationState().partial` has no consumer outside record.js on main, so file a P2 to render a partial-hydration notice; until it ships, assert only `partial === []` on the happy path (C2) and emit a run-smoke WARN naming a non-empty partial, so the hole is visible in every run rather than asserted into a permanent red.",
  "note": "Two corrections to the phase-1 reader's guard list, both load-bearing. (a) It under-credits B492-2 (smoke-test.js:37988-38037): that test is NOT a synthetic envelope — it stubs window.fetch, takes a real 401, waits on the real ladder timer, asserts a second call fired and that gold stays UNKNOWN across the failure. Its true limit is different and narrower than stated: it calls beginRecordLoad() directly, so it proves the ladder but never the boot wiring, and it runs under the harness where the veil is refused. (b) It over-credits B492-3b: smoke-test.js:38134-38142 is a regex over legacy.js source text, so the one defect that silently deleted every hr_load of the session is guarded by a string match that survives the code becoming unreachable — the class-11 pattern, on the highest-severity defect of this class. Also worth flagging for the coordinator: this guard needs no live DB and no credential, so unlike live-hash-drift it can sit in the LOCAL gate — it is one of the few CI-parity wins available while the CI-green lane is in flight. And the reason no headless guard has ever seen the veil is a deliberate design choice (boot-hydration.js:277-284 argues the harness must get no timer); the fix is not to weaken that rule but to add the one guard that boots WITHOUT the harness flag, as wallGuard already does."
 },
 {
  "gap_confirmed": true,
  "proposed_test_ok": false,
  "where": "Class (7) \"the funnel door\". Code under test: src/net/account-gate.js:781-794 (signup submit + success copy), src/net/auth.js:888-894 (signUp), src/net/auth.js:205-207 (createClient options), src/net/character.js:284-412 (ensureCharacter/ensureThenAccrue). Guards that exist: tests/beta-invite-gate.mjs (static half, wired at tests/run-smoke.mjs:1653 + :1993), tests/open-beta.mjs, tests/character-bootstrap-guard.mjs (NOT wired into run-smoke.mjs — grep 'bootstrap' in run-smoke.mjs returns nothing), src/features/smoke-test.js:17052/17089/17119/17223 OPEN-1..4, tests/run-smoke.mjs:174 anonSurfaceGuard, :203 wallGuard. Proposed new work lives in: src/features/smoke-test.js (in-page), tests/run-smoke.mjs (new headless confirmHandoffGuard), tests/signup-funnel.mjs (--live, credentialled).",
  "evidence": "GAP CONFIRMED on main @ 00b927b1, verified by my own greps (not the reader's list).\n\n1) EMAIL-CONFIRM IS UNTESTED AND UNASKED-FOR.\n   `grep -rn \"emailRedirectTo\" src/net/ src/features/ tests/ .github/ supabase/` → ZERO hits (only src/vendor/supabase-js-2.103.0.umd.js). `grep -rniE \"site_url|redirect_to|redirect allow\" src/net/ tests/ .github/ supabase/migrations` → ZERO hits.\n   src/net/auth.js:888-894 signUp passes `{ email, password, options: { data: metadata } }` — no `emailRedirectTo`, so the confirm link's destination is a dashboard setting the repo neither sets nor reads. tests/open-beta.mjs:51-53 admits this in its own header: \"The dashboard half (email confirmations, auth rate limits, anonymous sign-ins)… No migration and no test in this repo can read it.\"\n   src/net/auth.js:205-207 createClient passes only `{ autoRefreshToken, persistSession }`; `detectSessionInUrl` / `flowType` / `exchangeCodeForSession` appear nowhere in src/ (vendor default is `detectSessionInUrl:!0`, confirmed by grep of the UMD bundle) — so fragment consumption is an unasserted library default, not a pinned contract.\n   `grep -rniE \"resend\" src/ --include=*.js | grep -v vendor` → only src/legacy.js:4765 (XP flush) and src/observability.js:134. There is NO resend-confirmation affordance anywhere in the client.\n\n2) THE SUITE RATIFIES THE DEAD END RATHER THAN CATCHING IT — the decisive finding.\n   src/net/account-gate.js:789-790 hard-codes: 'Account created. Confirm the link in your email, then sign in.' (and the coded variant at :789). smoke-test.js:17119 OPEN-3 stubs `signUp → {user:{id:'u-test'}, session:null}` and calls it \"the real production shape with email confirmation on\" (:17135-17136), then asserts at :17190-17194 that the note matches `/account created/i`. So the one test that reaches this branch PINS the re-sign-in instruction as correct behaviour — the same failure shape as SETTLE-5 in class (4). Nothing anywhere asserts a NEGATIVE about it: `grep -rn \"then sign in|Confirm the link\" tests/*.mjs src/features/smoke-test.js` → ZERO hits.\n\n3) SIGNUP→CHARACTER CONTINUITY HAS NO END-TO-END TEST.\n   `grep -rn \"SIGNED_IN\" src/net/*.js tests/*.mjs src/features/smoke-test.js` → ONE hit, smoke-test.js:17625, inside the b226 `decideSessionEvent` pure truth table. No test drives the real onAuthStateChange listener (src/net/auth.js:289-313) into ensureCharacter→hr_load→gate-open.\n   No headless guard ever boots WITH a session: SUPABASE_SDK_STUB (tests/run-smoke.mjs:1065-1073) returns `getSession: async () => ({ data: { session: null } })`, and tests/run-smoke.mjs:203 wallGuard is explicitly the session-LESS boot. `grep -n \"Guard(\" tests/run-smoke.mjs` lists 14 browser/node guards; none is a signup or confirm-handoff guard.\n   tests/character-bootstrap-guard.mjs proves the RPC on PGlite but is NOT wired into run-smoke.mjs or .github/workflows/smoke.yml (grep 'bootstrap|character-bootstrap' → no hits in either).\n\n4) GUARD RUN I PERFORMED (read-only): `node tests/beta-invite-gate.mjs` → \"49 passed, 0 failed (static only; run with --live for the server half)\". I read all 49 assertion lines: every one is invite-code presentation (metadata shape, disclosure UI, no client-side claim_beta_invite) or the server switch (hr_settings RLS, fail-closed default, per-IP codeless brake). ZERO assertions touch email confirmation, redirect landing, resend, or the signup→character handoff. The live half is not in the preflight by design (its header: \"It needs a credential CI does not have\").\n\nTHE ORIGINAL BETA-2 INCIDENT, named: docs/planning/PRIORITY_BOARD.md:25 — \"Funnel measured live: 49 signed up → 38 confirmed (−11 at the EMAIL WALL, 9 of them post-delivery-fix = the confirm-then-sign-in-again dead end) → 34 characters (−4 to the since-fixed silent boot bug) → 16 active week 1 → 3 now. Invite codes failed ~3:1 (13 refused_unknown vs 5 redeemed — hand-retyped from DMs).\" PRIORITY_BOARD.md:26 Phase ② prices the fix: \"confirm-link lands you IN signed-in + resend button (73% of the loss)\". Target: signup→character >90% (was 69%).\n\nWHY THE PROPOSED TEST WOULD NOT HAVE CAUGHT IT.\n (a) tests/signup-funnel.mjs --live asserts the redirect allowlist and that \"a GET of the confirm landing URL serves the game (not a 404/blank)\". The incident was NOT a broken link — the link landed on the site fine. The loss was that landing did not carry the player IN: the client asks for no session-bearing redirect and the wall's own copy (account-gate.js:789-790) sends them back to sign in a second time. A GET that \"serves the game\" passes green with the bug fully live. Worse, it is proposed for the --live set, which run-smoke.mjs:1993 does not run (only betaInviteGatePreflight, the static half) — so it would never fire in any gate.\n (b) signupHandoffGuard stubs `onAuthStateChange('SIGNED_IN', session)` — it HANDS the client a session, which is exactly the state the bug denies. It tests the segment after the dead end. It is a good guard for the −4 silent-boot loss; it is blind by construction to the −11 email-wall loss.\n Neither half asserts the copy, the resend button, or `emailRedirectTo` — the three things the beta-3 plan says account for 73% of the loss. Neither touches the invite-code half (13 refused vs 5 redeemed).",
  "test_spec": "Four pieces. Pieces 1-3 need NO live DB; piece 4 needs a credential (Management API token at ~/.supabase-token), not a game DB.\n\n── 1. CONFIRM-DOOR-1 (in-page, src/features/smoke-test.js, no DB) ──\nNAME: 'CONFIRM-DOOR-1: the confirm link must land the player IN, and the wall must never send them back to sign in'\nFIXTURE: window.HearthriseGate._buildGate({}) + _wire, with window.HearthriseAuth stubbed exactly as OPEN-3 does at smoke-test.js:17130-17140 — signUp resolves {user:{id:'u-test'}, session:null} (the real email-confirm shape). Plus a spy on the metadata/options object handed to auth.signUp.\nASSERTIONS (exact):\n  A. `signUp` is called with `options.emailRedirectTo === location.origin + '/'` — i.e. src/net/auth.js:888-894 must forward an emailRedirectTo (drive the real exported auth.signUp with a stubbed `supabase.auth.signUp` capture, since the redirect is the ONLY lever this repo owns over where GoTrue sends the player).\n  B. `assert(!/then sign ?in/i.test(ui.note.textContent))` — the success copy must not instruct a second sign-in. Directly inverts the current text at src/net/account-gate.js:789-790.\n  C. `assert(ui.resend && ui.resend.type === 'button')` and clicking it calls `supabase.auth.resend({type:'signup', email, options:{emailRedirectTo}})` exactly once, is disabled for a cooldown, and speaks a confirmation — the missing 'resend button' half of PRIORITY_BOARD.md:26.\n  D. AMEND OPEN-3 at smoke-test.js:17190-17194: keep `/account created/i`, ADD the negative from B so the two tests cannot disagree. (Today OPEN-3 ratifies the dead end; leaving it as-is makes CONFIRM-DOOR-1 fight it.)\nMUTATION CONTROL: restore the literal 'Confirm the link in your email, then sign in.' → B RED; drop emailRedirectTo → A RED; remove ui.resend → C RED.\n\n── 2. confirmHandoffGuard (node, tests/run-smoke.mjs, headless Playwright, no DB) ── THE ONE THAT WALKS THE FUNNEL\nNAME: confirmHandoffGuard(browser, url), wired unconditionally in the browser-guard block beside wallGuard (tests/run-smoke.mjs:203) — must not be skippable.\nFIXTURE: new context; page.route the skypack/vendor SDK to an extended SUPABASE_SDK_STUB (pattern at tests/run-smoke.mjs:1065-1073) whose getSession() returns null on the FIRST call and, when the document URL carries an auth fragment, resolves a fake authenticated session — mirroring the vendored `detectSessionInUrl:!0` default — and then fires the registered onAuthStateChange('SIGNED_IN', session). Boot at `index.html#access_token=FAKE&refresh_token=r&expires_in=3600&token_type=bearer&type=signup`. Do NOT set __HR_TEST_HARNESS__ (the veil/harness suppressions must be live). Capture requests as wallGuard does at :218-227.\nASSERTIONS (exact, ordered):\n  E. exactly ONE POST to /rest/v1/rpc/hr_create_character, carrying a Bearer whose role is 'authenticated' (not the anon key — the b349 check).\n  F. exactly ONE /rest/v1/rpc/hr_load after it, and hr_load is not issued before hr_create_character resolves.\n  G. `HearthriseGate.isOpen() === true` and `document.getElementById('hr-account-gate') === null` within 20 s — the player is IN, with no second sign-in.\n  H. `location.hash` no longer contains 'access_token' (the token is not left in the URL/history).\n  I. zero pageerrors, and no factory-default character painted (no 'Level 1' + '500 gold' pair) before hr_load answers.\nMUTATION CONTROL: stub the ensure to never settle (the src/net/character.js:224 stall class) → the guard must FAIL with a named stall, not hang or pass; and stub hr_load → 503 → guard must report the veil, not a default character.\n\n── 3. INVITE-NORM-1 (node, extend tests/beta-invite-gate.mjs static half, no DB) ──\nThe measured 3:1 code failure (13 refused_unknown vs 5 redeemed, hand-retyped from DMs) is untouched by any of the 49 assertions I ran.\nFIXTURE: the `hr_beta_code_norm` body parsed out of supabase/migrations/2026-08-23-beta-invite-gate.sql (the file is already read by this guard) + the client's normalisation at src/net/account-gate.js:781 (`code.toUpperCase()`), driven over a corpus of realistic human retypings of one canonical code: lower case, leading/trailing space, internal space, en-dash for hyphen, missing hyphen, O↔0, I↔1↔l.\nASSERTION: for every corpus entry, client-normalise(x) === server hr_beta_code_norm(x) === the canonical code, OR the entry is on an explicit REJECTED list with a comment saying why a human typing it must be refused. A silent divergence between the two normalisers is the failure.\nMUTATION: change the client to trim-only → RED.\n\n── 4. tests/signup-funnel.mjs --live (needs the Management API credential; NOT a live game DB) ──\nKeep the reader's config read but make the assertions load-bearing:\n  J. auth config: `site_url` and the redirect allowlist both contain https://hearthrise.net (and only origins we own).\n  K. mailer autoconfirm state equals the intended one, and the confirmation email template's action link resolves to an allowlisted origin (not the Supabase-hosted default) — this is the actual mechanism of the −11.\n  L. a real unauthenticated POST /auth/v1/signup to a disposable address WITH options.email_redirect_to returns 200 with `confirmation_sent_at` set, then the account is deleted; account count unchanged on both sides otherwise (same discipline as beta-invite-gate --live).\nWIRING + THE PROCESS FIX: run it in the same by-hand --live set as beta-invite-gate --live, AND add one line to run-smoke.mjs's summary that NAMES every guard whose --live half did not run this pass. Today a fully green local gate reads as \"the funnel is guarded\" when the only funnel guard that can see the email wall never executed — the same 'local run-smoke is strictly weaker than CI' finding, one level down.\n\nCOVERAGE VERDICT: piece 2 is the one that would have caught the beta-2 incident end to end (fragment → session → character → gate, no second sign-in); piece 1 catches its root cause statically in every run at zero cost and un-ratifies OPEN-3; piece 3 covers the invite half the proposal ignores; piece 4 covers the dashboard half the repo currently admits it cannot see.",
  "note": "Two things worth escalating beyond my item.\n\n(1) OPEN-3 (src/features/smoke-test.js:17119, assertions at :17190-17194) does not merely miss the bug — it ASSERTS the dead end is production-correct, describing `session:null` as \"the real production shape\". That is class (11) false-passing (a test pinning a defect as intended) intersecting class (7), and it is the same pattern the phase-1 reader flagged for SETTLE-5 at smoke-test.js:48394. Any fix to the confirm door must amend OPEN-3 in the same commit or the two tests will contradict each other.\n\n(2) tests/character-bootstrap-guard.mjs is an ORPHAN: `grep -n \"bootstrap\" tests/run-smoke.mjs` and the same over .github/workflows/smoke.yml both return nothing, and the file carries a full --selftest mutation catalogue (:55, :680). It is the second working-but-unwired guard found today (tests/visual-qa.mjs is the first, PRIORITY_BOARD.md:259). Recommend a derived check — enumerate tests/*.mjs and fail on any file exporting a *Guard that no runner imports — which is a cheap addition to the class-(11) proposal (tests/guard-controls-census.mjs) and would have caught both orphans mechanically.\n\nRead-only throughout: no edits, no commits, no deploys. The only command that executed anything was `node tests/beta-invite-gate.mjs` (static half, no network, no DB) — output quoted in evidence."
 },
 {
  "gap_confirmed": true,
  "proposed_test_ok": false,
  "where": "Item 7 = class (8) \"first-night death (fresh Lv1 overnight banks ~2% then dies)\". Repo root R:\\the game\\the game, current main HEAD 00b927b1. Guard the reader MISSED: src/features/smoke-test.js:35861 (B495-3). Gaps that survive: supabase/functions/hr-accrue/accrual.js computeAccrual never run on the fresh config (fixtures at tests/accrual-engine.mjs:389-391, :891, :1333-1341); floor at smoke-test.js:35917; incident of record at docs/planning/PRIORITY_BOARD.md:25.",
  "evidence": "READER'S GAP IS FALSE AS WRITTEN. It claims \"No test runs the SERVER engine for a genuinely fresh character with the REAL start kit ... over an 8-12 h night and asserts the outcome\". src/features/smoke-test.js:35861 `B495-3: a fresh character survives a first away night instead of dying in seconds` does almost exactly that: :35876 `const KIT = await import('../data/start-kit.js?v=501')`, :35878-35891 builds state from KIT.START_EQUIPMENT / KIT.START_SKILL_XP / KIT.START_INVENTORY, :35894-35899 `CS.simulateSpan(..., fromMs: from, toMs: from + 12 * 3600000, rng: RNGM.createRng(0x5eed1234))`, auto-eat ON at :35903-35910, and asserts :35917 `assert(mins >= 10, ...)` and :35922 `assert(summary.kills >= 24, ...)`. The reader's guards[] for class 8 lists its sibling B495-1 (:35752) but omits B495-3 entirely, so its proposed tests/first-night.mjs would be a near-duplicate at the client layer.\n\nWHAT SURVIVES — THREE VERIFIED GAPS.\n\n(a) WRONG ENGINE. B495-3 runs the CLIENT core: :35867 `const CS = window.HearthriseCore && window.HearthriseCore.combatSim`, :35894 `CS.simulateSpan`. Under server authority the night is paid by `computeAccrual` in supabase/functions/hr-accrue/accrual.js. Every computeAccrual COMBAT fixture is a veteran or a synthetic: tests/accrual-engine.mjs:389 `hp: 60, maxHp: 60`, :390-391 `hp: 99, maxHp: 99` (MAXED, :119-122 attack 13034431), :891 `hp: 40, maxHp: 40, skills: baseSkills()` against an invented `probe_feeder` monster, :1333 the only fresh 10-HP run — `inventory: { [PROVISION]: 5000 }` at :1341 `SPANS = [ACCRUE_MIN_MS, 90000, 5 * 60000]`, i.e. <=5 minutes. AWAY-1 parity would bridge client->server but its fixtures are the same veterans with auto-eat OFF (:114-116 \"Every one of them ends in a death... auto-eat is OFF by default\"), and the auto-eat-ON parity pass at :779-800 reuses those veterans with `{cooked_shark:5000, cooked_moonfish:5000, ...}`. So the fresh-10HP + 20-meal + tier-I-clamp config is never compared across the two engines.\n\n(b) FOOD EXHAUSTION IS NEVER THE ASSERTED CAUSE. `grep -rn \"cooked_shrimp\" tests/*.mjs` -> every quantity is 5000 / STACK / 9 (accrual-engine.mjs:781, :1203, :1208, :1093-1145); nothing anywhere seeds the kit's actual 20 (src/data/start-kit.js:102-108 `shrimp: 10, cooked_shrimp: 20`). The small-stack fixtures are conservation guards, not outcome guards: :898 `computeAccrual({...common, inventory: { [FOOD]: 3 }})` uses a synthetic monster with a guaranteed food drop (:885 `drops: [{ id: FOOD, ch: 1 }]`) to prove the bag refills; :2035-2047 sweeps `stock of [1,2,7,40,5000]` on a 99-HP veteran to prove `stock + net >= 0` for hr_apply. Neither asserts that the death FOLLOWS exhaustion.\n\n(c) THE EXISTING FLOOR CODIFIES THE LIVE INCIDENT. B495-3's floor is 10 minutes of a 720-minute night = 1.4%. docs/planning/PRIORITY_BOARD.md:25 records the live beta-3 measurement: \"a new player's first overnight fight banks 2.0% of the night, then dies (lvl-40+iron+500 food is the first config that survives)\". 2.0% of 12 h = 14.4 min — which is the test's own cited figure, :35915 \"deliberately far below the ~14 the kit measures\". B495-3 therefore PASSES on the shipped state it is named after; it is a cliff detector for the pre-b495 0.5-min regression, not a guard on the outcome now costing D1->D2.\n\n(d) TWO SECONDARY DEFECTS IN B495-3. Single seed :35899 `RNGM.createRng(0x5eed1234)` for an RNG-sensitive floor. And :35905 `threshold: 0.25` hardcoded — verified `node -e` on src/core/auto-eat.js: `maxPctForTier(1)= 25`, so the literal is a MIRROR of a server constant, the exact fixture disease the b497 F1 arc named and that tests/auto-eat-at-creation.mjs:22-23 explicitly refuses (\"asserted against the FUNCTION, never the literal 25\"). Plus the class-11 vacuity early-out at :35873 `if (!CS || !C || !AE || !RNGM || !ST) { assert(true, 'core sim unavailable'); return; }`.\n\nREADER'S SIDE-CLAIM CORRECTED: \"character-bootstrap-guard.mjs imports START_KIT (only test that does)\" — `grep -rn START_KIT tests/ src/ supabase/` returns NO tests/ hit; the guard imports the MODULE (tests/character-bootstrap-guard.mjs:81 `await import(... 'src','data','start-kit.js')`) and B495-3 imports it in-page. The named-export claim is wrong; the substance (it is the only node guard reading the kit) holds.\n\nCOMMAND RUN (the one permitted standalone guard): `node tests/accrual-engine.mjs` -> exit=0, green. Its only 12 h combat report line is `fixture: goblin · 12h · tick 2328ms · 1830 ticks · 1324 kills · 249 crits · 6640g` — the 60-HP veteran, and 1830 of 18556 ticks means even THAT fixture dies at ~10% of the night. No first-night line exists in the output.",
  "test_spec": "WOULD THE PROPOSED TEST HAVE CAUGHT THE ORIGINAL INCIDENT? The original beta-2 incident of this class is the b495 FIRST-HOUR P0 (smoke-test.js:35753-35762): the shipped kit was `shrimp: 8` — raw, heals 3 = 24 HP of buffer on a 10 HP character against a Goblin costing 4.94 HP/kill; 36 measured deaths in the first thirty minutes, and because simulateSpan breaks on the first death a first overnight paid \"30 SECONDS of twelve hours (0.1%)\" = 23 XP and no gold (:35918-35921). The reader's proposed tests/first-night.mjs WOULD have caught that — but so does B495-3, which is why B495-3 was written, so it buys nothing. It would NOT catch the live successor (PRIORITY_BOARD.md:25, \"banks 2.0% then dies\"), because any floor set at today's measurement passes on it; and it would NOT catch a client/server divergence, because its spec says \"engine-only, no DB\" while naming an `hr_auto_eat_max_pct(1) mirror` — a mirror of a server constant is the fixture disease, and \"the engine\" unqualified lands on the same client core B495-3 already drives. Hence proposed_test_ok=false.\n\nTHE TEST THAT WOULD.\n\nNAME: tests/first-night-parity.mjs — a node guard, wired into tests/run-smoke.mjs beside autoEatAtCreationGuard (run-smoke.mjs:63 import / :3315 call) AND added to .github/workflows/smoke.yml (per the confirmed P0 that local run-smoke is strictly weaker than CI).\n\nLIVE DB: NO, and no PGlite. computeAccrual is pure and already imported in Node by tests/accrual-engine.mjs — reuse that import path. The one server-side fact needed (the tier-I auto-eat ceiling) is read as TEXT out of supabase/migrations/2026-08-29-auto-eat-tiers.sql:108-117 and compared to `maxPctForTier(1)` from src/core/auto-eat.js, which is a stronger construction than a DB call and costs nothing. The entitlement half (a fresh character actually HAS the flag on) is already proven on PGlite by tests/auto-eat-at-creation.mjs A1/A2 — this guard depends on it, it does not repeat it.\n\nFIXTURE (derived, never assembled from a base object or a literal):\n  hp = maxHp = levelFromXp(START_SKILL_XP.hitpoints)   // not the literal 10\n  skills = START_SKILL_XP; equipment = START_EQUIPMENT; inventory = {...START_INVENTORY}  // the frozen objects from src/data/start-kit.js\n  activeId = the first tier-1 monster the War Table offers, derived from the catalogue ordering, not the string 'goblin'\n  autoEatEnabled: true; autoEatFood: null  // the live NULL-divergence case from b497/b499, not a pinned food id\n  autoEatPct: maxPctForTier(1), asserted equal to the SQL ceiling before use\n  span 12 h; SEEDS = 20 draws.\n\nASSERTIONS.\n  P0 (pre-assert, kills the mirror): parse hr_auto_eat_max_pct's ceiling table out of 2026-08-29-auto-eat-tiers.sql and assert it equals maxPctForTier(1). A hardcoded 25 on either side fails here.\n  A1 CAUSE — the assertion nothing has today: for every seed where `summary.died === true`, `delta.items[chosenFood] === -START_INVENTORY[chosenFood]` — the ENTIRE kit was eaten before the death. A death with food still in the bag is the auto-eat gate failing (the b497 class) and goes red. Derived from the kit's own quantity, never the number 20.\n  A2 BANKED — report, then floor: compute `banked = survivedMs / 12h` per seed; PRINT the median and the min/max on every run so the live figure (2.0% today) is visible in CI instead of hiding under a 1.4% floor; assert `min(banked) >= FIRST_NIGHT_BANKED_FLOOR`, a NEW named export in src/data/start-kit.js, so moving the bar is a data edit reviewed as a design change rather than an edit to a test literal.\n  A3 SEED HONESTY: assert B495-3's single draw (0x5eed1234) falls inside [p10, p90] of the 20-seed sweep. This retires the one-lucky-draw risk without deleting B495-3.\n  B PARITY — the actual hole: run the same seed through src/core/combat-sim.js simulateSpan the way accrual-engine.mjs's clientAwaySpan already does (:400-410 pattern), and assert the full AWAY-1 property set is identical: ticks, kills, crits, died, survivedMs, foodEaten, items, xp, gold. AWAY-1 asserts parity ONLY for 60/99-HP veterans with auto-eat off (accrual-engine.mjs:389-391), so if the client shows 14 minutes and the server pays 3 that divergence is invisible today and this block is the only thing that sees it.\n\nMUTATION CONTROLS (--mutate, since accrual-engine.mjs itself carries none):\n  M1 autoEatEnabled:false -> A1/A2 RED (the b497 entitlement).\n  M2 START_INVENTORY.cooked_shrimp = 8, raw shrimp only -> A2 RED at ~0.1%. This is the block that answers \"would it have caught the original\": it reproduces the b495 incident exactly.\n  M3 add any away/grantMs condition to fx.autoEat in accrual.js -> block B RED (the mutant attendedSettleAutoEatGuard names at accrual-engine.mjs:1312-1315, which its own <=5-min spans cannot see over a night).\n  M4 replace the derived ceiling with the literal 25 -> P0 RED.\n\nALSO REPAIR B495-3 IN THE SAME COMMIT (do not delete it — it is the in-page half): smoke-test.js:35905 derive `threshold` from maxPctForTier(1) instead of the literal 0.25; smoke-test.js:35873 convert `assert(true, 'core sim unavailable'); return;` into a real assertion so it cannot pass vacuously when the core fails to load.",
  "note": "Net: the reader's gap is directionally right but factually wrong, and its proposed test would duplicate an existing guard while missing the live defect. The phase-1 list for this class omitted src/features/smoke-test.js:35861 (B495-3) — worth checking whether the same reader omitted siblings in other classes, since it listed B495-1 from the same contiguous block. The sharpest single finding is (c): B495-3's 10-minute floor on a 720-minute night is 1.4%, and the currently-shipping, board-recorded first-night outcome is 2.0% — so the guard named after this incident passes on the incident. That is a green test standing over a live P1, which is a worse state than no test, because it reads as coverage. Second sharpest is (a): the class-8 outcome is only ever measured on the CLIENT core, while server authority means computeAccrual pays the night; no parity fixture covers fresh + auto-eat-on + a 20-meal bag, and the reader's own class-4 evidence (hr_seed is not a function of visible values, index.ts:500-506) means the two engines cannot be assumed equal by construction. Both proposed blocks are node-side and need no live DB, so this is cheap and CI-safe. One caveat I could not close under read-only rules: I did not execute computeAccrual on the fresh fixture, so the actual server-side banked % for the start kit is unmeasured — that measurement is the first thing the implementing lane should produce, and it is the number A2 should print."
 },
 {
  "gap_confirmed": true,
  "proposed_test_ok": false,
  "where": "Class index 8 = \"(9) deploy split-brain (stale ?v= double modules, edge payload drift, live-hash drift)\". Repo root R:\\the game\\the game @ main 00b927b1. Gap lives in: tests/run-smoke.mjs (no live-hash-drift call anywhere; no served-site fetch), .github/workflows/smoke.yml:130-135 (live-hash-drift is CI-ONLY), tests/cache-buster-guard.mjs:121/212 (repo-side only), tests/run-smoke.mjs:323 deployedPayloadGuard (edge payload only), tests/run-smoke.mjs:558 avatarAssetGuard (repo-side heuristic for a deploy-side defect).",
  "evidence": "GAP HALF 1 — the local gate never runs live-hash-drift, and that guard is RED on current main. `grep -n \"live-hash-drift\\|liveHashDrift\" tests/run-smoke.mjs` → NO MATCH (the only run-smoke hits for build guards are :82 `import { cacheBusterGuard, cacheBusterMutationGuard }`, :1393 cacheBusterPreflight, :1984 invocation, :323 deployedPayloadGuard, :3595 its live-section call). It runs ONLY in .github/workflows/smoke.yml:130-135 (`node tests/live-hash-drift.mjs` / `--selftest` / `--mutate`). I RAN the credential-free half on current main:\n    $ node tests/live-hash-drift.mjs\n    live-hash-drift: 41 tracked names, 41 baselined bodies, replayed in 9.1s\n      RED  unexplained hr_record_rejection(p_user uuid, p_slot integer, p_intent text, p_code text, p_detail jsonb, p_count bigint)\n      RED  unexplained hr_state_of(p_user uuid, p_slot integer)\n      2 problem(s). The repo no longer matches its own record of what is deployed.\nSo the reader's claim is confirmed AND live: a Coordinator running the gate everyone actually runs sees green while a red guard exists in the tree. This is the mechanical proof of fact (d)'s \"local run-smoke is strictly weaker than CI\" (docs/STABILIZATION_AUDIT.md:20, SA-003) for this class specifically. Both REDs are the `--write` REVIEW placeholder left unfilled — the guard's own --selftest plants that exact defect and requires it caught, so nothing is broken in the guard; the finding is unresolved.\n\nGAP HALF 2 — nothing compares the SERVED site to the repo. `grep -rn \"hearthrise.net\" tests/*.mjs tools/*.mjs .github/workflows/*.yml` → only tests/cors-preflight.mjs:60-61 (an Origin header string, not a fetch of the site), tests/run-smoke.mjs:604 + :3592 (prose/CORS note), tests/run-sql-tests.mjs:70, tools/pack-edge.mjs:25, smoke.yml:22. `grep -rn \"build-info\" tests/*.mjs` → tests/cache-buster-guard.mjs:124 (readFile of the REPO copy) and tests/predict-display.mjs:47 (local import). The only network guards in run-smoke are deployedPayloadGuard (:323-390, GETs the hr-accrue payload_sha256 — EDGE only), rpc-resolution, cors-preflight, edge-jwt-gate. run-smoke serves the repo folder itself (serve() at :118-138, `Cache-Control: no-store`), and `--url` (:11) merely points Playwright elsewhere — it asserts nothing about served-vs-repo. Confirmed: no guard fetches the deployed index.html / src/build-info.js / sw.js.\n\nI measured the live site read-only today: repo src/build-info.js:17 `cache: 501`; `curl https://hearthrise.net/src/build-info.js` → `cache:     501`; served index.html carries 106 `?v=501` tags and zero off-version tags (the 3 bare `?v=` hits are the b124 kill-switch's own querySelectorAll/regex strings, which cache-buster-guard.mjs:78 HTML_RE already excludes by requiring an extension + digits). So the static deploy is NOT drifted right now — the gap is an unguarded invariant, not an open incident.\n\nWHY THE PROPOSED TEST IS NOT RIGHT. The original incident of this class is the b493 assembly split-brain, 2026-08-29 (.claude/coordination/DISCOVERIES.md:79-131; .claude/coordination/agents/qa-engineer.md:14-22; tests/cache-buster-guard.mjs:1-40): fourteen `?v=491` import specifiers survived the 492→493 bump, six modules (record, accrue, capstone, predict, auth, styles) evaluated TWICE with separate module state, the suite drove one instance while the game ran on the other, 19 tests red. The proposed `liveBuildGuard` would NOT have caught it, for two independent reasons: (1) the stale specifiers were IN THE REPO, so served == repo exactly — an equality assertion between the two is satisfied by a tree that is internally split-brained; (2) the proposed guard fetches only index.html and src/build-info.js, and index.html's tags were all correctly 493 — every stale specifier was inside src/**/*.js, which it never fetches. b493 is already class-killed repo-side by cache-buster-guard.mjs (derived scan, duplicate-version check, mutation control, preflight at run-smoke.mjs:1393/1984). Third defect: as specified the guard has no lag grading, so on any tree that is bumped-but-unpushed (i.e. every tree at the moment the release gate runs) served != repo is the NORMAL state; it would fire on every run and be switched off — the \"a guard that cries wolf gets deleted\" failure the repo names in run-smoke.mjs:~630. The deploy-side incident this class actually owns is b371 F2 (run-smoke.mjs:541-556): assets/avatars/_placeholder.webp was committed, correctly pathed, served by every local server, and 404 on GitHub Pages for its whole life because Jekyll drops leading-underscore paths — a served != repo defect whose guard (avatarAssetGuard, :558) is a repo-side heuristic for the one known Jekyll rule, not a fetch of what is served.",
  "test_spec": "TWO deliverables. Both are node guards. NEITHER needs a live DB (no credential, no PGlite); one needs the live SITE over plain HTTP, the other needs nothing.\n\n=== 1. tests/served-build-parity.mjs — \"what the player downloads is one coherent build\" ===\nLIVES: standalone node guard, exported `servedBuildParityGuard({ base, repoRoot })`, invoked from tests/run-smoke.mjs's LIVE section beside deployedPayloadGuard (:3595). Must never skip: derive `base` from src/net/supabase-bootstrap.js-style discipline — a hardcoded https://hearthrise.net with `HR_SITE_URL` allowed to OVERRIDE but never to disable (the exact rule deployedPayloadGuard's ⚠ block at :330-346 already establishes after b343/b344).\nNEEDS LIVE DB: no. Needs an unauthenticated HTTPS GET of the public site.\nFIXTURE (for the RED control, no production writes): tests/fixtures/served-split-brain/ — index.html carrying `<script src=\"src/main.js?v=501\">`, src/main.js importing `'./net/record.js?v=501'` and `'./features/boot-hydration.js?v=499'`, src/features/boot-hydration.js importing `'../net/record.js?v=499'`, src/build-info.js with `cache: 501`, plus one tag pointing at a file the fixture server does not have. Served by run-smoke's existing serve() (:118-138) pointed at the fixture dir, so the guard's failure path is proven every run against a local port.\nASSERTIONS (exact):\n A1 DUPLICATE MODULE IN THE SERVED GRAPH — walk the deployed graph, not the repo: GET `${base}/index.html?cb=<nonce>` with `cache:'no-store'`, extract tags with the SAME `HTML_RE` exported from tests/cache-buster-guard.mjs:78 (import it — one spelling, not a copy), then transitively GET every served `.js` and extract specifiers with the SAME `SPEC_RE` (:76). Resolve each specifier against its importer's served path. ASSERT: no resolved path appears with two distinct `?v=` values. Failure text names path + both versions + both importing served-file:line. This is the b493 property asserted on what the player actually downloads, which is the one thing the repo-side preflight structurally cannot say (a hotfix pushed without running run-smoke, or a Pages build that merged something else, lands here and nowhere else).\n A2 ONE VERSION — every served tag version, every served specifier version, and `BUILD.cache` parsed from `${base}/src/build-info.js?cb=<nonce>` are all the SAME integer N. (Repo copy is NOT consulted here; this arm is about internal coherence of the deploy.)\n A3 EVERY SERVED SPECIFIER RESOLVES — each URL in the graph returns HTTP 200 and a JS content-type. This is the b371 `_placeholder.webp` class made general: a path that exists in the repo and does not exist on the deploy fails here regardless of WHY (Jekyll underscore, case-sensitivity, .gitignore, missing commit).\n A4 SERVICE WORKER — GET `${base}/sw.js?cb=<nonce>`, assert its derived cache name is exactly `hearthrise-${N}` (CLAUDE.md build-workflow rule) and that no fixed cache name string is present.\n A5 REPO↔DEPLOY, GRADED THREE WAYS (this is the false-positive fix the proposal lacks): R = repo src/build-info.js BUILD.cache; S = served N; H = `git rev-parse HEAD`; pushed = whether H is an ancestor of origin/main; dirty = `git status --porcelain` non-empty.\n   • S === R → PASS.\n   • S < R AND (dirty OR !pushed) → print \"DEPLOY LAG: this tree is b{R}, production serves b{S} (not pushed yet)\" as a NOTE, not a problem.\n   • S < R AND pushed AND the HEAD commit is older than 15 min → FAIL \"the deploy did not take\".\n   • S > R → FAIL \"production is AHEAD of this tree — every local result you are about to read is about a build no player is running.\" (This is the b343/b344 shape, and it is the one direction that silently invalidates a whole release gate.)\n A6 VACUITY FLOOR — `servedModules.length >= 20` and `tags.length >= 50`, else fail \"the guard fetched nothing\" (measured today: 106 versioned tags on the served index).\nMUTATION CONTROL (`--selftest`, wired into run-smoke like cacheBusterMutationGuard at :1397): run A1-A4 against the fixture server and require, by NAMED assertion, RED on each of — the 499/501 duplicate (A1), the off-version tag (A2), the missing file (A3), a fixed sw cache name (A4) — plus a NEGATIVE control (the clean fixture must read green) so the harness cannot pass by throwing.\n\n=== 2. tests/run-smoke.mjs — stop being strictly weaker than CI (the half my run proved) ===\nLIVES: run-smoke.mjs preflight, right after cacheBusterPreflight (:1984). Call `node tests/live-hash-drift.mjs`'s credential-free entry (it is already importable; the CI step at smoke.yml:133 runs exactly this) and surface its problems as run-smoke problems. On current main this turns the local gate RED with the 2 unexplained bodies — which is CORRECT and is the point: the release gate must not be greener than CI. Do NOT re-implement it and do NOT duplicate the SA-003 fix/ci-green-lane work; the only change here is the CALL plus the parity assertion that every `node tests/*.mjs` line in .github/workflows/smoke.yml has a corresponding invocation in run-smoke's `--ci` set (derive the list by parsing smoke.yml; fail naming any CI-only guard). Needs no DB and no network.",
  "note": "Verdict summary: GAP CONFIRMED on both halves, but the reader's proposed test is aimed at the wrong incident. b493 (the class's original, DISCOVERIES.md:79) is already killed repo-side by cache-buster-guard.mjs; the proposed served-vs-repo equality check could not have seen it (served == repo, and it never fetches src/**). It also has no deploy-lag grading, so it would go red on every pre-push run and be disabled. The two things worth building are (1) the duplicate-version + reachability check applied to the SERVED module graph (catches b493's shape on a deploy that bypassed the preflight, and generalises the b371 Jekyll 404), with the repo↔deploy comparison graded LAG/BEHIND/AHEAD rather than equal/not-equal; and (2) wiring live-hash-drift into run-smoke, which I proved is red right now and invisible to the local gate. Item (2) is one line and pays for itself immediately; item (1) is ~150 lines of node with a local fixture for its RED control. Neither needs a live database. One caution for whoever builds (2): it will make `node tests/run-smoke.mjs` red on main until the two `--write` REVIEW placeholders in tests/live-hash-drift.baseline.json are filled — that is the SA-003 lane's work, so land (2) after or with it, not before, or it will be reverted as noise."
 },
 {
  "gap_confirmed": true,
  "where": "coverage.json index 9 — class (10) \"streak/daily display promising more than the server pays\". Readers/writers on current main: client sheet src/features/daily-reward.js:123-152 (noteServerStreak), :163-196 (serverClaimStreak), :216-224 (localStreak fallback), :234-252 (priced/cycleDay/rewardFor), export surface :574-585; fed by src/net/record.js:1748-1760 (hr_load body) and src/net/accrue.js:1816-1826. Server pricer supabase/functions/hr-accrue/claim-reward.js:337-362 (priceLoginClaim → deriveLoginStreak(lookup) → priceDailyLogin), lookup from supabase/migrations/2026-08-16-claim-reward.sql:253-283 (hr_claim_lookup). Shared rule src/data/rewards.js:151-163 (deriveLoginStreak). Client projection source supabase/migrations/2026-08-26-marks-record.sql:173-187 (hr_state_of 'progress' + 'progress_truncated').",
  "evidence": "GAP CONFIRMED, but narrower and sharper than the phase-1 reader stated.\n\n(1) The reader MISSED a guard: DAILY-SHEET-4 at src/features/smoke-test.js:27289 (\"b497: a MISSED day resets the advertised day\") already drives both doors with five cases and degeneracy controls (A live case, B unbroken control, C 'done' is not a claim, D pre-envelope fallback, E stale capture). So \"the missed day is untested\" is NOT the gap. Verified by reading :27190-27430.\n\n(2) What is genuinely unguarded — verified by search, not by the reader's list:\n  · `grep -rln \"HearthriseDaily\" tests/*.mjs` → ZERO hits. No node guard touches the sheet at all.\n  · `grep -rn \"daily-reward\" tests/*.mjs` → only claim-intent.mjs:261-262 (a source-string mutation on the registry `site` field) and reachability/visual-qa overlay selectors. Nothing drives the pricing path.\n  · `grep -rn \"progress_truncated\" tests/*.mjs src/features/smoke-test.js` → auto-eat-authority.mjs:279 (key-presence list), goal-counters.mjs:509 (asserts it is FALSE on a small character), smoke-test.js:50784 (a comment). NOTHING asserts the sheet's decline-to-capture branch at daily-reward.js:131-137.\n  · Every DAILY-SHEET test feeds a HAND-BUILT envelope literal (`D.noteServerStreak({now:…, progress:[…]})` at :27246-27253, :27334-27340). No test ever feeds a real hr_state_of body.\n\n(3) The two projections are genuinely different code, so \"same function, so they agree\" is false at the INPUT: hr_claim_lookup (2026-08-16-claim-reward.sql:266-283) is bounded to period_key in ('', today, prev) for one kind/key and cannot truncate; hr_state_of (2026-08-26-marks-record.sql:173-187) is `(period_key='' or updated_at >= now()-31 days)`, excludes ev:kill_monster:%/ev:loot:%, and is `order by period_key, kind, key limit 1000`. Because '' sorts first, PERMANENT rows are safe and DATED rows (daily/login) are exactly the ones the cap can cut — record.js:281-284 says as much for unlock rows. The client mitigates with `if (env.progress_truncated === true) return;` (daily-reward.js:137); nothing tests it.\n\n(4) A LIVE OVER-PROMISE STILL REACHABLE ON MAIN, unguarded and in fact enshrined as correct: daily-reward.js:216-224 localStreak returns `G.streak.count` (the PLAY streak) whenever lastClaimDay is today or yesterday. Claim history 1,2,(gap, played),4 → claim streak today = 2, play streak = 5 → with no/truncated/stale envelope the sheet advertises Day 5 (with week multiplier) against a Day 2 payout. daily-reward.js:210-215 asserts this \"can now only ever UNDER-state the day\"; that is false for a mid-history gap. smoke-test.js:27382-27390 (DAILY-SHEET-4 case D control, \"claimed yesterday must still advertise Day 3\") locks the wrong behaviour in.\n\n(5) Server-side coverage is real but disjoint: `node tests/claim-intent.mjs --list` (run, 30 mutations) is entirely server-side — streak_always_one, streak_ignores_state, lookup_widens, lookup_takes_the_day, uncapped_mult… — and not one mutation names the sheet's advertised day.\n\nINCIDENT NAMED: b497, live prod on the QA account 2026-08-31 (documented at smoke-test.js:27290-27300 and src/data/rewards.js:99-118): the sheet advertised \"Claim Day 3 · 2,000 gold · 5 gems\" and the server paid Day 1's 500 gold, no gems. Sibling b475 was the under-promise (sheet Day 1 · 500 gold, claim credited Day 3 + 5 gems). Root cause both times: the sheet rendered `player_state.streak_days` (hr_apply §4c SETTLE streak, 2026-08-21-streak-state.sql:1259-1298, advanced by any delta carrying `accrued_to`) as if it were the CLAIM streak.\n\nCommands run: `grep -rln \"HearthriseDaily\" tests/*.mjs` (no output); `grep -rn \"progress_truncated\" tests/*.mjs src/features/smoke-test.js`; `grep -rn \"daily-reward\" tests/*.mjs`; `node tests/claim-intent.mjs --list`; `git log --oneline -1` → 00b927b1 (current main).",
  "proposed_test_ok": false,
  "test_spec": "NAME: tests/daily-sheet-parity.mjs — node guard, PGlite (tests/pglite-chain.mjs), NO live DB. Wired into tests/run-smoke.mjs's PGlite preflight beside tests/claim-intent.mjs. Plus one in-page companion, DAILY-SHEET-6, in src/features/smoke-test.js.\n\nPREREQUISITE IN THE SAME COMMIT (this is the class-kill, not scaffolding): extract the envelope→lookup projection out of the IIFE into src/data/rewards.js as `export function loginLookupFromEnvelope(env, atMs)` → `{prev, rows}|null`, implementing verbatim what daily-reward.js:123-152 + :163-196 do today (keep only kind==='daily'&&key==='login'; drop period ''; decline on non-array progress; decline on progress_truncated===true; dayN from Date.parse(env.now); decline when the asked-about day is outside [dayN, dayN+1]; prev = botd.utcDayKey((dayN-1)*86400000)). daily-reward.js then calls it. Reason: daily-reward.js is a classic IIFE on window (:24, :574-585) that Node cannot import and `priced` is not exported, so any node guard must otherwise RETYPE the projection — a second copy, which is the exact defect this class is. This is the same move b498 made for deriveLoginStreak (rewards.js:120-124: \"One implementation, read by both halves, is the only shape in which the question 'which day will I be paid?' has one answer\").\n\nFIXTURE (the divergence must be manufactured, or the test is vacuous): hr_create_character; then, using the owner-DML day-shifting claim-intent.mjs already uses (never a client-named day):\n  D-3 real claim via runClaimReward → daily/login row 'claimed', value 1\n  D-2 real claim → value 2\n  D-1 NO claim, but a real hr_apply delta carrying `accrued_to` so player_state.streak_days advances (2026-08-21-streak-state.sql:1270-1298) — the PLAYED-but-unclaimed day\n  D-0 the day under test.\nThis is the only shape in which the settle streak and the claim streak disagree, and it is the literal live b497 state (claimed two days ago, played yesterday, streak_days=3).\n\nASSERTIONS:\nA0 CONTROL (proves the fixture diverges): env = `select hr_state_of(uid,0)`; assert env.state.streak_days === 3 and env.progress_truncated === false.\nA1 THE INCIDENT: sheetPrice = priceDailyLogin(deriveLoginStreak(loginLookupFromEnvelope(env, Date.parse(env.now)))); then call runClaimReward as the user; assert granted.cycle_day === sheetPrice.cycleDay, granted.gold === sheetPrice.gold, (granted.gems||0) === (sheetPrice.gems||0), AND assert sheetPrice.cycleDay !== env.state.streak_days — the one assertion that goes RED on the pre-b498 code that read state.streak_days.\nA2 DEGENERACY CONTROL: same fixture with D-1 claimed → advertised day 3, paid day 3, gems equal (so the fix cannot become \"always Day 1\").\nA3 THE PROJECTION SEAM: insert >1000 dated player_progress rows so hr_state_of returns progress_truncated true and yesterday's daily/login row falls off `order by period_key, kind, key limit 1000` while hr_claim_lookup still sees it; assert env.progress_truncated === true, loginLookupFromEnvelope(...) === null (the client declines), and the server still pays the full day — the client may under-state, never over-state.\nA4 THE FALLBACK DOOR (expected RED on current main — this is a finding, not a fixture): no envelope; G.streak.count = 5, G.dailyReward.lastClaimDay = yesterday, server claim streak = 2; assert advertisedDay <= paidDay. daily-reward.js:216-224 returns the play-streak residue and advertises Day 5 against a Day 2 payout. Fix: the residue may only ever be used when it is 1 (or the sheet shows no day until an envelope lands); smoke-test.js:27382-27390's control must be re-ruled with it.\n\nMUTATION CONTROLS (--selftest, house pattern, each must be CAUGHT):\n  streak_from_settle — loginLookupFromEnvelope reads env.state.streak_days → A1 RED (b497 by name).\n  truncation_ignored — drop the progress_truncated decline → A3 RED.\n  state_ignored — deriveLoginStreak stops checking row.state → A1/A2 RED.\n  residue_uncapped — revert the A4 fix → A4 RED.\n\nIN-PAGE COMPANION (src/features/smoke-test.js, DAILY-SHEET-6): source assertion that window.HearthriseDaily routes through the shared `loginLookupFromEnvelope` and holds no local kind/period filter of its own, so the browser half cannot fork away from the guarded one; and convert DAILY-SHEET-3/4's module early-outs to assertions (class 11's vacuity finding).\n\nLIVE DB: not needed anywhere — PGlite + the real migration chain + the real runClaimReward bytes, exactly tests/claim-intent.mjs's shape.",
  "note": "WHY THE PROPOSED TEST FAILS THE INCIDENT TEST (it would NOT have caught b497):\n1. Its fixture makes the two streaks AGREE. \"Seed k consecutive claimed daily/login rows via the real claim verb\" means every claimed day is also the only activity — the exact condition under which the bug \"looked right for three weeks\" (src/data/rewards.js:115-118). Pre-b497 code reading state.streak_days would render the same day the server pays, and the test would be GREEN through the incident.\n2. Its \"missed day\" variant does not rescue it. streak_days only advances on a delta carrying `accrued_to` (2026-08-21-streak-state.sql:1270); a claim carries none, so in a claim-only fixture streak_days stays 0/1 and the wrong-quantity read still coincidentally matches. The fixture must PLAY the missed day, which the proposal never says.\n3. It is mechanically infeasible as written. src/features/daily-reward.js is a classic IIFE on window (:24) and `priced` is not on the export surface (:574-585); \"export `priced` … or shim window\" from a node guard means either retyping the row projection in the test (a second copy — the very defect) or a headless browser. The honest fix is the extraction in the spec above.\n4. Its named mutation (\"alter deriveLoginStreak's period spelling\") is already covered server-side by tests/claim-intent.mjs (streak_always_one, streak_ignores_state — observed via `node tests/claim-intent.mjs --list`), and it never touches the two real seams: progress_truncated / the 1000-row cap, and the pre-envelope fallback.\nSTANDING FINDING FOR THE BOARD (independent of any test): daily-reward.js:216-224 can still OVER-promise on current main whenever the client has no usable envelope and the claim history has an interior gap — same \"feels like theft\" shape as b497, through the fallback door, with smoke-test.js:27382-27390 currently asserting that behaviour as correct. READ-ONLY session: nothing was edited, committed or run beyond `node tests/claim-intent.mjs --list`."
 },
 {
  "gap_confirmed": true,
  "proposed_test_ok": false,
  "where": "Class (11) false-passing tests. Repo root R:\\the game\\the game @ 00b927b1 (main). Primary files: tests/run-smoke.mjs, .github/workflows/smoke.yml, src/features/smoke-test.js, tests/*.mjs (110 files).",
  "evidence": "GAP CONFIRMED — and it is WORSE than the reader stated. I re-derived every claim myself; the reader's flag census was roughly right but its conclusion was aimed at the wrong axis.\n\n1. NO META-GUARD EXISTS. Grep for guard-controls|controls-census|vacuity|assertCount across tests/ returns only prose comments inside individual guards (tests/delta-transport.mjs:805, tests/items-catalogue.mjs:100, tests/live-hash-drift.mjs:861, tests/native-dialog.mjs:180, tests/renown-kill-faucet.mjs:144, tests/run-smoke.mjs:1162, :1236, :3064, tests/slot-switch.mjs:41). No file reads tests/run-smoke.mjs as data; the single `readFile`-of-self is tests/run-smoke.mjs:632 `const SELF = normalize(join(ROOT,'tests','run-smoke.mjs'))`, used only to exempt secretGuard from matching itself (:659).\n\n2. THE REPO ADMITS THE GAP IN ITS OWN WORDS. tests/run-smoke.mjs:3854-3859: \"⚠ THIS IS A MARKER CHECK, NOT A CALL-GRAPH CHECK... What it cannot see is a guard that prints its success banner while asserting nothing; that is what each guard's own `--mutate` harness is for.\"\n\n3. THAT STATED DEFENSE IS NEVER EXECUTED BY THE GATE. Grep of tests/run-smoke.mjs for the literals '--selftest' / '--mutate' as argv: NO MATCHES. Every spawnSync in run-smoke.mjs (:1588, :1601, :1617, :1633, :1657, :1720, :1728, :1757, :1892, :1966, :3071) passes either no flag or '--check'. The 34 occurrences of \"selftest|mutate\" in run-smoke.mjs are all DOC COMMENTS telling a human to run it by hand (e.g. :2086 \"`node tests/perk-channel.mjs --mutate` plants six real defects\", :2171, :2602, :2680, :3504, :3535, :3764). MEASURED: 56 of 110 tests/*.mjs implement a --selftest/--mutate control; the local gate runs ZERO of them; .github/workflows/smoke.yml runs 10 (schema-drift :100-101, live-hash-drift :134-135, restore-census :193-194, renown-kill-faucet :154, conservation-fuzz :206, activity-intent :219, claim-intent :229, clan-journal-guard :239, anon-rate-gate :248, raid-band-denial :257) — and per fact (d) CI has been RED on every completed run since 2026-08-29, so nobody has seen those either.\n\n4. 13 GUARDS ARE RUN BY NOTHING. Computed (node script over tests/, run-smoke.mjs, smoke.yml): total 110, reachable from run-smoke.mjs 84, CI-only 11, orphan 15 — of which pglite-chain.mjs and schema-replay.mjs are helpers imported by other tests, leaving 13 TRUE ORPHANS executed by no gate and imported by no test: visual-qa.mjs, gold-intents.mjs, gold-intent-shape.mjs, character-bootstrap-guard.mjs, open-beta.mjs, enchant-intent.mjs, gem-daily-budget.mjs, leaderboard-lockdown-guard.mjs, market-offers-guard.mjs, market-offers-lockdown.mjs, raid-claim-exploit.mjs, run-sql-tests.mjs, worker-accrual.mjs. THE AUDIT'S OWN COVERAGE MAP CITES THREE OF THESE AS LIVE GUARDS: coverage.json class (5) cites \"tests/gold-intents.mjs (:890 balance reverts to server on refusal)\"; class (7) cites tests/character-bootstrap-guard.mjs and tests/open-beta.mjs; class (2) cites tests/worker-accrual.mjs. They protect nothing.\n\n5. IN-PAGE VACUITY IS REAL AND PERVASIVE. src/features/smoke-test.js:99 `const assert = (cond,msg) => { if(!cond) throw new Error(msg); }` — no counter. tryRun (:72-83) and tryRunAsync (:94-98) return pass(name) on any body that does not throw, including one that ran zero assertions. 1067 tryRun/tryRunAsync registrations; 185 have a bare `return;` within 320 chars of registration (upper bound on the early-out idiom, ~17%). Confirmed at the exact sites the reader named: smoke-test.js:2081 `if (!H || typeof H.upgradeProperty !== 'function') return;` (PROP-OK-1), :2171-2172 (FORGE-E2E-1), and I found a third the reader missed at :2121-2122 (ROOM-REFUSE-1) — all three are the b500/b501 forge-class guards, and all three PASS silently if src/features/homestead.js fails to load.\n\n6. NO FLOOR ON THE IN-PAGE TEST COUNT. `suite.total` is captured at tests/run-smoke.mjs:3811 and only PRINTED at :3821; grep for `.total` in run-smoke.mjs returns exactly those two lines. If the registration array truncated (a module-level throw), the run reports a smaller total and still exits 0.\n\n7. STATIC \"SOURCE CONTAINS STRING\" GUARDS CONFIRMED. tests/arm-homing-guard.mjs:365-366 `const MUST_CALL = ['reconcileInventory','reconcileBank','reconcileWorkers','reconcileCompanions','reconcileFarm','reconcileTraits']` — a HAND list regex-tested as `fn\\s*\\(\\s*G\\b` against record.js source (:369). I RAN IT (read-only, single standalone guard): `node tests/arm-homing-guard.mjs` → \"Arm-homing guard — all 75 G fields homed (71 scanned from src/, 9 record, 51 residue, 8 mechanism, 19 no-sync)\" — GREEN on the same main where fact (c)'s residue-ahead property deadlock is live for Paione. It has a real SELF-CHECK for its scanner (:194-224) but none for MUST_CALL.",
  "test_spec": "ORIGINAL INCIDENT OF THIS CLASS: b348 — documented in-repo at src/features/smoke-test.js:38-50: \"A SYNC RUNNER HANDED AN ASYNC BODY IS AN ALWAYS-GREEN TEST... MEASURED: I wrote two of these while fixing Xarn's reports and only found out because seven separate mutations — including restoring the exact bug — all came back GREEN.\" Live instance today: the b500/b501 forge guards (FORGE-E2E-1 :2165, PROP-OK-1 :2079, ROOM-REFUSE-1 :2120) are green while fact (c)'s property deadlock is live, and three guards the audit map cites as covering gold-refusal and the signup funnel are orphans no gate runs.\n\nVERDICT ON THE PROPOSAL: (a) guard-controls-census FAILS the incident test — it asks \"does the file HAVE a --mutate branch\", which is the wrong question: 56 files already have one and the gate runs NONE of them, and 13 files are not run at all. It would have gone green on b348 (in-page, not a tests/*.mjs guard). (b) smoke-vacuity-lint FAILS on b348 — those two tests had full assertion bodies; they never ran because the runner did not await. It is also the same static source-lint shape the gap_detail itself indicts. (c) the assertion counter is the ONLY one that catches b348, by construction — but as specified (\"count >= 1\") it is under-powered and has an async attribution hole. So: right in kind, wrong in scope. Replace with the following.\n\n═══ SPEC: \"no verdict without a demonstrated red\" ═══\n\n── G1 · tests/gate-census.mjs — node guard, NO live DB, wired as a run-smoke PREFLIGHT beside cacheBusterPreflight (tests/run-smoke.mjs:1984) so it runs before the browser and cannot be skipped; also its own CI step.\nFIXTURE: the filesystem — readdir('tests/*.mjs'); source text of tests/run-smoke.mjs and .github/workflows/smoke.yml; a new checked-in ledger tests/gate-census.ledger.json = { helpers:[], ciOnly:[{file,reason}], quarantined:[{file,reason,owner,expires}] }.\nASSERTIONS:\n A0 (vacuity) parse found >= 100 test files AND >= 50 run-smoke references; else FAIL \"the census is scanning nothing\".\n A1 EXECUTION: every tests/*.mjs is (i) reachable from run-smoke.mjs (static import, dynamic import, or spawnSync path), or (ii) invoked by a `node tests/<f>.mjs` line in smoke.yml AND listed in ledger.ciOnly with a reason, or (iii) imported by another test (helper, listed in ledger.helpers), or (iv) in ledger.quarantined with reason+owner+a future expiry. Else FAIL naming the file. → RED TODAY on the 13 orphans listed in evidence §4.\n A2 CONTROL EXECUTED: for every file whose source handles '--selftest' or '--mutate', that exact invocation must appear in run-smoke.mjs argv or in smoke.yml. Else FAIL \"<file> ships a mutation control that no gate runs\". → RED TODAY on 46 of 56.\n A3 GATE PARITY (mechanises fact (d)'s P0): every `node tests/…` step in smoke.yml must be reachable from run-smoke.mjs unless in ledger.ciOnly with a stated reason. → makes \"local green while CI is red\" a build failure instead of a discovery.\n A4 MARKER PARITY: every guard in run-smoke.mjs's REQUIRED_GUARD_MARKERS (:3860ff) must correspond to a file A1 says is executed.\n MUTATION CONTROL (`node tests/gate-census.mjs --selftest`): on a temp copy of run-smoke.mjs, (1) delete one `await import('./x.mjs')` → A1 must go RED; (2) delete one smoke.yml `--mutate` line → A2 must go RED; (3) add a `node tests/newthing.mjs` step to smoke.yml absent from run-smoke → A3 must go RED. All three must fail; a green selftest fails the guard.\n\n── G2 · in-page assertion ledger — src/features/smoke-test.js, no DB, enforced by run-smoke.mjs.\nCHANGE: smoke-test.js:99 → `let _ASSERTS = 0; const assert = (cond,msg) => { _ASSERTS++; if(!cond) throw new Error(msg); };`. tryRun (:72-83) snapshots `_ASSERTS` before `fn()` and compares immediately after; tryRunAsync (:94-98) compares in its resolve handler, so a stray later assert cannot be credited to it.\nEXACT ASSERTION: `if (status === 'PASS' && _ASSERTS === before) return fail(name, 'VACUOUS — this test returned PASS with 0 assertions executed (early-out on a missing module, or a body that never ran)')`. This is precisely what catches b348: an async body on the sync runner reaches pass() with the counter unmoved.\nPLUS A PINNED FLOOR: a checked-in tests/assert-floor.json mapping test name → assertion count, generated once from a green run. run-smoke fails when any test's count drops below its pin, when a registered test has no pin, or when `suite.total` is below the pinned registration count (closing evidence §6 — add the comparison at tests/run-smoke.mjs:3821). A floor of 1 is not enough: it would let a test that asserts only its own setup survive.\nMUTATION CONTROL: ship two planted vacuities behind `window.__HR_VACUITY_SELFTEST__` — one `async` body registered with tryRun, one `if (!window.__nope) return;` as the first statement of a real test — and have run-smoke assert both come back RED before trusting the run.\n\n── G3 · convert the module-missing early-outs (same commit, mechanical): `if (!H || typeof H.upgradeProperty !== 'function') return;` → `assert(H && typeof H.upgradeProperty === 'function', 'HearthriseHomestead did not load — this guard cannot run');` at smoke-test.js:2081, :2121-2122, :2171-2172 first, then sweep the remaining ~185 sites. G2 makes the un-converted ones fail anyway; conversion turns \"silently skipped\" into a named failure.\n\nLIVE DB: none. G1 is filesystem-only, G2/G3 run inside the existing headless page. G1 belongs in the preflight block (run-smoke.mjs:1984-1989); G2's enforcement belongs at run-smoke.mjs:3810-3827; both must also appear in smoke.yml so A3 holds for the census itself.",
  "note": "Two corrections to the phase-1 reader for the parent's ledger. (1) Its flag census (62/110 with a control, 48 without) is close but off: matching the literals '--selftest|--mutate' gives 56 with, 54 without — the reader's bash grep for bare \"mutate\" also matched prose like \"mutation\". The number does not change the verdict. (2) The reader framed the gap as \"guards lack controls\". The measured gap is one level up and more serious: the controls EXIST and are never executed (zero --selftest/--mutate invocations in run-smoke.mjs), 13 guards are executed by nothing at all, three of which this very coverage map cites as active coverage for classes (2), (5) and (7) — so the audit's own guard inventory is partly fictional. Any consumer of coverage.json should re-check its cited guards against the orphan list before trusting a \"covered\" verdict. Related but out of scope for this item and already dispatched per fact (d): the CI-green lane; G1/A3 is the mechanism that would keep that fix from silently rotting again. Also worth flagging to the parent: tests/visual-qa.mjs (fact (f)) is one of the 13 orphans — it works and found two P1s, and A1 is the assertion that would have made its absence from the gate a build failure."
 },
 {
  "gap_confirmed": true,
  "proposed_test_ok": false,
  "where": "Class 12 (session/tab concurrency). Code under test: R:\\the game\\the game\\src\\net\\sync.js:1017-1059 (claim gate ahead of the armed residue put), :1234-1330 (decideConcurrent / decideUploadAllowed / decideClaimAction), :1502-1504 (pauseSync); R:\\the game\\the game\\src\\net\\accrue.js:3146-3178 (decideSettle), :3231-3254 (start/stopSettleLoop); R:\\the game\\the game\\src\\net\\auth.js:1112-1144 (eviction gate + \"Bring it back here\"). Existing coverage: R:\\the game\\the game\\src\\features\\smoke-test.js:36661-36851 (CONCURRENT-1/2, CLAIM-CLOBBER, TAKEOVER-1/2). Proposed home for the new guard: R:\\the game\\the game\\tests\\run-smoke.mjs (browser section, beside saveSlotGuard at :1075) + a pure companion in smoke-test.js.",
  "test_spec": "NAME: sessionClaimHandoffGuard(browser, url) in tests/run-smoke.mjs (wired into the browser section beside saveSlotGuard, run-smoke.mjs:1075) + one in-page pure companion SETTLE-CLAIM-1 in src/features/smoke-test.js.\n\nLIVE DB: NO. Everything is page.route()-served. Requirement is only a served build (run-smoke already boots one) and two Playwright pages in ONE context — which run-smoke has never done (every newPage sits on its own fresh newContext: run-smoke.mjs:205, :815, :884, :1077, :1202, :2004).\n\nFIXTURE: one browser.newContext() (shared localStorage), two pages A and B (distinct sessionStorage -> distinct instance ids, which is exactly what the claim keys on, sync.js:69 INSTANCE_KEY / :107 MY_EPOCH_KEY). addInitScript stubs the Supabase SDK to a fixed authenticated session for u1 WITHOUT __HR_TEST_HARNESS__ (same technique as saveSlotGuard's SDK stub, run-smoke.mjs:1076-1100). page.route('**/rest/v1/session_claims*') serves ONE mutable in-memory row {user_id, device_id, heartbeat_at, claimed_at} with GET + upsert semantics and a test-controlled clock. page.route counters on '**/rest/v1/rpc/hr_put_client_state' and '**/functions/v1/hr-accrue*' returning canned ok bodies. Capstone left in its SHIPPED armed state (do NOT __setBlobRetired(false) — the point is the armed path).\n\nASSERTIONS, five blocks, each with its named mutation control:\n\nD1 DEPARTED WRITER IS NOT CONCURRENT — the original incident, reproduced. Boot A, let it claim, close A, leave its game_saves write recent but freeze heartbeat_at at now-(CLAIM_STALE_MS+1000). Boot B. ASSERT: B paints no concurrent-session warning (no #hr-evicted-gate, no \"another device\" toast, sessionStorage 'hr:concurrentWarnedFor' never written) AND the claim row's device_id becomes B's instance id. MUTATION: gate decideConcurrent on the save row's __device/__cloudSavedAt instead of hbMs -> RED.\n\nD2 LIVE OWNER STILL WINS, ON THE ARMED WRITE. Boot A (heartbeat beating), boot B. ASSERT: B shows the eviction gate, and across 3 forced snapshotIfDue(true,false) cadences B issues ZERO rpc/hr_put_client_state requests while A issues >=1. This is CLAIM-CLOBBER driven through the real function with claimEndpoint set and the capstone armed — the case nothing drives today. MUTATION: move the decideUploadAllowed gate (sync.js:1020) below the isBlobRetired() branch (sync.js:1033) -> RED. NOTE: this replaces the reader's proposed \"PUT-CLAIM-1 (capstone path calls the same ownership verdict as the blob path)\", which is ALREADY TRUE on main and would pass vacuously.\n\nD3 THE EVICTED TAB STOPS SETTLING — the open defect, EXPECTED RED ON MAIN. Same fixture as D2; keep B visible with an active activity. ASSERT: B fires ZERO hr-accrue settle requests after eviction. It fails today: decideSettle (accrue.js:3146-3178) takes only {enabled, configured, visible, kind, lastSettleAt, eventAt} — no ownership input — and stopSettleLoop (accrue.js:3245) is called only by resetSettleLoop (:3254), never by the eviction path (auth.js:1113 calls pauseSync only, which sets sync.js:113 `paused`, read by the write path alone). FIX SHAPE: add `owner:boolean` to the settle state and return {settle:false, reason:'not-owner'}, plus SETTLE-CLAIM-1 in smoke-test.js asserting that pure verdict so the rule is testable without a browser. Do NOT delete this block to go green.\n\nD4 THE RELOAD LEG / NO PING-PONG — the b374 half, which is why real pages are needed. Click \"Bring it back here\" in B's gate (auth.js:1140); B reloads and reclaims with a strictly newer claimed_at. ASSERT: after B's reload the claim row's device_id stays B's on A's next poll (A parks and shows its own gate, does not steal back), and B is not re-accused across its reload (the CONCURRENT-2 property exercised through a REAL location.reload() rather than a hand-read sessionStorage key). MUTATION: drop the epoch comparison from decideClaimAction -> RED.\n\nD5 NEVER A DEADLOCK. Freeze B's heartbeat past CLAIM_DEAD_MS, let A poll. ASSERT: A reclaims and resumes residue puts. MUTATION: park unconditionally on a newer epoch -> RED.",
  "evidence": "GAP CONFIRMED, verified myself, not from the reader's list.\n\n1) Zero node guards cover this class. `grep -ln \"session_claims|decideClaimAction|decideUploadAllowed|decideConcurrent|CLAIM_STALE\" tests/*.mjs` -> NO output (110 files). All 26 references live in src/features/smoke-test.js (`grep -c` -> 26).\n\n2) Every existing test is a PURE-FUNCTION unit test on hand-typed claim views. Read smoke-test.js:36661-36851: CONCURRENT-1 (:36661) calls S.decideConcurrent on literal objects; CLAIM-CLOBBER (:36730) calls S.decideUploadAllowed on literals and its only live-seam step (:36765-36772) just round-trips __setClaimView; TAKEOVER-1/2 (:36775/:36814) call S.decideClaimAction on literals. `grep \"__setClaimView|getClaimView\" smoke-test.js tests/*.mjs` -> 5 hits, ALL inside CLAIM-CLOBBER; none is combined with a snapshotIfDue drive.\n\n3) No test drives the claim gate through the real write path. `grep -n claimEndpoint smoke-test.js tests/*.mjs` -> only 3 fixtures: :31828 (b331 clock test, blob path), :32045 and :32191 both `claimEndpoint: null`. run-smoke.mjs's snapshotIfDue drivers (authResilienceGuard :882-992, saveSlotGuard :1075-1150) build a `base` config with NO claimEndpoint, so sync.js:1017 is skipped in every automated run.\n\n4) No two-tab fixture exists. `grep -n \"newContext|newPage|page2|ctx2\" tests/run-smoke.mjs` -> 6 pages, each on its own fresh newContext (:204/205, :814/815, :883/884, :1076/1077, :1198/1202, :2004). Nothing shares localStorage between two live pages.\n\nCORRECTION TO THE READER (load-bearing, changes the proposed test): the reader wrote that whether the residue put is claim-gated \"is not asserted anywhere I found ... grep 'claim' near putClientState in sync.js finds only header prose.\" The CODE gate exists — sync.js:1017-1021 runs `if (!decideUploadAllowed(claimView, getInstanceId(), Date.now())) return false;` BEFORE the `if (isBlobRetired())` residue branch at :1033-1059, so the armed put inherits it (the comment at :1029 says so: \"The throttle / auth / claim gates above still apply\"). What is missing is the TEST, not the gate. The reader's proposed PUT-CLAIM-1 (source-shape check that the capstone path calls the same verdict) would therefore pass vacuously on main.\n\nNEW FINDING THE READER MISSED (verified): the SETTLE loop has no ownership gate at all. accrue.js:3146-3178 decideSettle takes no claim/paused input; `grep -n \"paused|pauseSync\" src/net/accrue.js` -> one hit, :3452, a UI string. `grep -rn \"stopSettleLoop\" src/net/*.js src/legacy.js` -> defined accrue.js:3245, called only accrue.js:3254 (resetSettleLoop) and exported :3649; legacy.js:2259 starts it and nothing stops it. auth.js:1113 (showEvictedGate) calls only pauseSync, and sync.js:1502-1504 pauseSync sets the module flag at :113 which gates snapshotIfDue (:977) — the write path only. So an EVICTED tab keeps firing hr-accrue settles under BLOB_RETIRED. That is the live, unguarded half of class 12.\n\nWOULD THE PROPOSED TEST HAVE CAUGHT THE ORIGINAL INCIDENT? Incident named: the 2026-08-17 device handoff — Tyler verbatim, .claude/coordination/DECISIONS.md:551: \"We desperately need to fix this server / local back and forth bullshit... all I tried to do was move from my computer to my phone. Wildly terrible experience.\" Fixed b366 (CHANGELOG.md:653) and b374 (CHANGELOG.md:590). Root cause, per the guard's own header at smoke-test.js:36656-36659: liveness was inferred from a WRITE, not a heartbeat, so every clean handoff looked like a double session. ANSWER: only partly. (a) The incident's headline symptom is a FALSE POSITIVE — a departed writer accused as concurrent — and the proposed script only ever stages TRUE concurrency (A live -> B refused) then a takeover; it never boots B against a tab that WROTE recently and STOPPED beating, so the primary defect goes unasserted. That is D1 in my spec. (b) The b374 half (epoch ping-pong) and CONCURRENT-2 (re-accusation) both live across a location.reload() that \"Bring it back here\" performs (sync.js:98-105, auth.js:1140); the proposed script never reloads a page, so its \"A does not steal back\" step cannot reproduce them. That is D4. (c) Its \"PUT-CLAIM-1\" companion is vacuous on main (see correction above) — that is D2. (d) Its \"B sends no settle\" clause is correct as a target but nothing on main makes it true, so it must be filed as expected-RED-until-fixed with the fix shape named, or it will be \"unblocked\" by deletion — that is D3.",
  "note": "Ran read-only. Only command executed against the suite: `node tests/client-state.mjs` -> \"client-state: dormant no-regression (blob) + armed server-read + putClientState builds the RPC — all green\" — green, and it asserts nothing about claim ownership (it is absent from the claim-reference grep in item 1). Did not run run-smoke. No files edited. Two process notes for the parent: (1) the reader's `derived_not_handtyped:false` and `reload_exercised:false` flags for this class are both correct — every claim test is hand-typed literals and none survives a page reload; (2) D3 (evicted tab keeps settling) is a defect on live main, not merely a test gap, and belongs on the board independent of whether the guard gets built."
 }
]