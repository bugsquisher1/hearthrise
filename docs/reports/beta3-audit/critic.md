## COMPLETENESS CRITIC — follow-ups, ranked by risk to beta-3

**1. All three documents are stale against `main`: the SA-002 / C2-1 fix is already integrated, so their headline live-P1 evidence is now false.**
`git log --oneline -3` → `6f06d7f5 integrate: property rung is SERVER truth in both directions` and `b53c6f0a fix(property): the server's rung is the tier in BOTH directions (b502)`, two commits ahead of the `00b927b1` the docs were built on. `src/net/property-record.js:293-303` (`mergeRung`) no longer returns `max`: a **complete** projection SETS in both directions, a truncated one stays a floor. **Do:** re-verify every "RED on main today" claim against `6f06d7f5` before dispatching. Specifically, test-plan RT‑1b and backlog C2‑1/C2‑2 are specified as *expected-fail pinned to the open P1* — on current main they are **green**, and a lane that authors them as expected-fail will either wire an inverted assertion or delete them as "already passing." **Why:** the readiness verdict's Risk 2 and Tier 1.2 both rest on this row; shipping a beta-3 gate list whose second P0 is already closed miscosts the whole critical path. Also note `tests/property-gate-census.mjs` now exists and is imported at `tests/run-smoke.mjs:2393` — the test plan's §2 orphan census and the readiness "13 orphan guards" count both predate it and must be recounted.

**2. `node tests/run-smoke.mjs --ci` does not exist — the entire Tier-0 Definition of Done cites a flag with no implementation.**
`grep -n '\-\-ci\b' tests/run-smoke.mjs` → **no matches**. The only flags parsed are `--headed` and `--url` (`tests/run-smoke.mjs:101-104`). The readiness §3 Tier-0 DoD, the §5 Risk-1 gate, and the new CLAUDE.md release rule ("green only when local `--ci` AND the GitHub run are green") all name it as if it ships. **Do:** make building `--ci` (run every `node tests/…` step `smoke.yml` runs, in the same order) an explicit Tier-0.1 deliverable with its own row, paired with `tests/gate-census.mjs` A3 deriving the step list from `smoke.yml` rather than a hand-typed ledger. **Why:** the P0 process finding ("local gate is strictly weaker than CI") is stated as closable by a flag nobody has written; without the row it silently doesn't get built and beta-3 ships on the same weaker signal.

**3. UNCOVERED FAILURE CLASS — the 256 KiB residue cap versus three unbounded-growth residue fields. Zero tests.**
`RESIDUE_FIELDS` includes `chronicle` ("the permanent achievement log", `src/net/client-state.js:108`), `bestiary` (`{monsterId:{kills,firstKill}}`, `:190`) and `dropLog` (`:191`) — all grow monotonically with play — and all 51 fields share ONE server cap (`supabase/migrations/2026-08-22-client-state-denylist.sql:62`, `:86` → `patch_too_large` / `state_too_large`). The client handles it honestly (b486, `client-state.js:406-433`: console + telemetry + one visible warning) but **not recoverably**: past the cap every residue field stops persisting forever and the b466 bestiary-reset incident returns wholesale. `grep -rln "state_too_large\|262144" tests/` → only `schema-apply-order.json`; **no guard anywhere exercises it.** **Do:** add to the plan (a) a node guard that synthesises a plausible 6-month `G` (2,000 bestiary rows, full chronicle, full dropLog) and asserts `octet_length(buildResiduePatch(G))` is under a stated fraction of the cap, and (b) a design ruling on bounding or server-homing `bestiary`/`chronicle`/`dropLog` — a kill record is a server fact, not residue. **Why:** neither document mentions the cap. It is the one mechanism that makes A1's `tests/reload-truth.mjs` green forever and still loses every field in production.

**4. CONFIRM-DOOR-1 is a designed false-pass, and two live-only door failures are invisible to the map.**
Confirmed: `grep -rn "emailRedirectTo\|detectSessionInUrl\|exchangeCodeForSession\|auth.resend" src/ tests/` → **zero hits**, so the finding is real. But asserting "`signUp` was called with `options.emailRedirectTo`" (test plan C6‑1 / readiness 3.1) proves nothing about production: Supabase **silently falls back to Site URL** when the redirect is not in the Auth allowlist, so the guard is green while prod still dead-ends. Two further live-only classes nobody names: (a) **the itch.io iframe wrapper** (`fingerguns123.itch.io/hearthrise`) — a confirm link opened from mail lands on the bare origin in a new tab, and inside a partitioned third-party iframe the Supabase session in `localStorage` is a *different* storage bucket, so "lands you IN" can be true at the origin and false in the wrapper players actually use; (b) **built-in SMTP throughput** — a beta-3 wave signing up inside one hour can exhaust Supabase's default mail rate, producing exactly the "-11 at the email wall" signature with no client bug at all. **Do:** add three rows — verify the Auth redirect allowlist and custom-SMTP configuration as an explicit Tier-3 config artefact (screenshot/API read, not an assertion); a manual confirm-link walk **inside the itch iframe**; and a stated ruling on whether beta-3 keys go out staggered because of mail rate, not just abuse. **Why:** the readiness prices 73% of funnel loss on this item and certifies it with a guard that cannot see any of the three real failure modes.

**5. C1‑2 lets the CLIENT decide whether a span was attended — which preserves the bug it is meant to fix, and it contradicts C1‑4 in the same document.**
C1‑4 correctly says derive "attended" **server-side** (`supabase/functions/hr-accrue/accrual.js:1374` hard-codes `ctx.away=true`; the fix is a kill/xp-credit row inside the window). C1‑2 then re-keys the food gate on "is this span attended" evaluated in `src/legacy.js:8483` — a client predicate. A client that never claims attendance gets the server to debit its own re-sim meals while the player's watched meals return: the identical restock, one branch over. Second, unaddressed: the `eat` verb exists (`supabase/functions/hr-accrue/request.js:140`) and takes a client-named item; the client has *already applied the heal locally* during the attended fight, so a server-computed heal on the same meal double-heals or diverges `player_state` HP. **Do:** unify the two rows onto one server-side attendance derivation (same predicate, same commit), and add an explicit HP-reconciliation clause plus a security-review line for the eat-verb heal magnitude. **Why:** C1 is the highest-leverage item on the board; shipping it with two different definitions of "attended" reintroduces the reported bug on the path nobody tests.

**6. A1 (kill-count parity) passes vacuously at zero, and RT‑0 ships a self-widening escape hatch.**
A1 asserts `settle.summary.kills === credited`. If the fixture character dies, the window is mis-seeded, or `hr_credit_kills` is rate-refused, both are `0` and the test is green through the incident. RT‑0 asserts the sentinel survives **"or F is on NO_SYNC/derived with a stated reason"** — so any future field can be greened by adding it to NO_SYNC, which CLAUDE.md invariant 3 calls "silent cloud data loss — forbidden." **Do:** add to A1 `assert(credited >= 8, …)` and `assert(settle.summary.kills > 0)` before the equality; add to RT‑0 a pinned count of the NO_SYNC/derived exemption set so growing it is a reviewed change, and require each exemption to carry a named test proving the field is genuinely re-derived. **Why:** these are the two tests the readiness DoD leans on hardest for Tier 1; both are currently satisfiable by the failure they exist to detect.

**7. The rate gate is a live constraint on C1's fix, and no document checks it.**
`hr_rate_ok` gates the kill-credit path (`supabase/migrations/2026-08-30-bounty-kill-credit.sql:357`, with `hr_record_rejection … 'rate_limited'` on overflow). The kill/XP cadence is 60 s (`HR_KILL_CREDIT_MS` / `HR_COMBAT_XP_MS`, `src/legacy.js:4907`/`:4759`) = 60 calls/hour, and R4 already flags a 30/hour bucket elsewhere. C1‑1 moves loot into that same RPC and C1‑3 adds two more forced flushes per stop/switch. **Do:** measure the actual bucket + limit on the kill-credit path, and add a load-shaped assertion (a player switching targets every 20 s for an hour is not rate-refused) plus a `hr_rejections` `rate_limited` watch for wave day. **Why:** a silent rate refusal on the new loot path reproduces the exact under-credit symptom the fix is named for, and the only evidence would be a rejection row nobody is reading.

**8. Two backlog rows require an Edge redeploy that another backlog row forbids while `live-hash-drift` is red — the documents contradict each other.**
C11‑4 explicitly says "do NOT edit those; touching them changes the Edge bundle while live-hash-drift is red." C1‑1 (server loot roll), C1‑4 (attended derivation) and C5‑1 (`COMPANION_XP_SERVER_BACKED`, vendored into the hr-accrue bundle) all require a redeploy. **Do:** state the ordering as a hard dependency — no Edge redeploy until C12‑3's REVIEW placeholders are filled and `hr_record_rejection` / `hr_state_of` live≠replay is *explained* (not re-pinned) — and add a row for the live-hash re-pin ritual after each of the three. **Why:** redeploying while the repo already disagrees with prod destroys the one artefact that could tell you what changed, on the same day the economy changes.

**9. Every new node guard is pinned to slot 0 by the hero-slot create-gate workaround, so multi-character addressing gets no coverage — while R‑IP5 names it as a live bug.**
A1 (`tests/reload-truth.mjs`) explicitly says "not hand-rolled `hr_create_character` slot 1 — that is the §8 create-gate ripple"; W4 recommends `gem-daily-budget.mjs` partly because it "creates on slot 0 so it is immune"; R6 fixes conservation-fuzz by planting the slot entitlement. Meanwhile R‑IP5 documents that the residue PUT and the `hr_load` read can address *different* slots (`src/net/sync.js:1036/1048`, `src/net/record.js:1242`). The 2026-09-01 play-gate was slot 2; the wipe-day fresh-account playthrough will be slot 0. **Do:** require at least one node guard (reload-truth is the natural home) to run its whole matrix on **slot 1** using the R6 entitlement-plant, and add a slot-2 arm to the residue-ahead play-gate fixture. **Why:** the one class R‑IP5 proves is live is the one class every new guard is constructed to avoid.

**10. The SA-021 designer ruling is scheduled on data the SA-001 bug polluted.**
Readiness 1.3 asks the designer to rule on the 4-`wolf_pelt` Farmstead gate using a prod census taken while ~38% of attended drops evaporate. If pelts are attended-kill drops, the observed `{have:3, need:4}` × 6 players may be entirely SA-001, and a data-row rebalance would then over-correct into a faucet on a wiped economy. **Do:** before the ruling, compute `wolf_pelt`'s *authored* drop rate from `src/data/monsters.js` against server-credited pelts in `player_ledger`, and sequence 1.3 strictly after 1.1's measurement. **Why:** it is the difference between a balance change and an accounting bug, and the wipe makes the wrong answer permanent for beta-3's whole cohort.

**11. Nobody measured the residue-ahead blast radius, and the fix's "players self-heal on next load" is asserted without evidence.**
C2‑1 claims "Client-only, no data patch — players self-heal on next load"; the readiness claims "5 hard-locked characters" — neither cites a query. Post-b502, healing downward requires `progress_truncated === false` (`src/net/property-record.js:283`), so a player whose envelope never declares completeness stays stuck. **Do:** run a prod census — for all 36 characters, `client_state->'homestead'->>'tier'` vs `max` rung in `player_progress` `unlock:property.*` — to get the real count, and a follow-up census 48 h after b502 to prove the heal actually fired in the wild. **Blocker to name:** the Supabase MCP server currently reports "requires authentication" in this session, so this probe is gated on Tyler's connector re-auth (readiness 4.7) — that item is Tier 4 today and should be promoted, since it blocks verification of a Tier 1 fix.

**12. No rollback plan for the one change that rewrites the economy.**
SA-001 is an RPC + Edge redeploy + client change landing on the same tree as the wipe, and it is the only item that can mint items for every player at once. No document states what "wrong" looks like within an hour, what the revert is (Edge rollback is not a git revert), or whether it ships **before** the wipe so beta-3 starts on it. **Do:** add a row — SA-001 ships and bakes on the pre-wipe tree with a `player_ledger` rate-of-credit watch and a named revert artefact (previous Edge bundle hash), and the wipe ceremony is gated on it having been live for at least one full play-gate. **Why:** every other Tier-4 row has a rehearsal; the largest economy change has none.